{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 1 baseline: core window/app access only, no plugins. Phase 3G: the main window keeps picker-window creation/focus/close plus the typed parent-child event transport.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-create",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:webview:allow-create-webview-window",
    "core:event:allow-emit-to",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
