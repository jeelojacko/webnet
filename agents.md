# Agents Guide - WebNet (industry-standard-style LSA clone)

## Project Context

- Goal: browser-based clone of industry-standard software performing mixed-observation least-squares adjustment (TS distances/angles, GNSS baselines, leveling dH) with control points, error ellipses, residuals, and basic outlier cues.
- Current behavior also includes adjustment-report results-pane cleanup: summary-first ordering with `Solve Profile Diagnostics` directly below summary, `Adjusted Coordinates`/`Observations & Residuals`/`Residual Diagnostics`/`Setup Diagnostics`/`Suspect Impact Analysis` visual ordering, removal of redundant ranked suspect blocks, optional diagnostics visibility gates (feature ON + applicable data), cluster-revert action visibility tied to cluster workflow enablement, and local-mode coordinate-system labeling that no longer appends grid CRS text.
- Current behavior (TypeScript): parses industry-standard-style text blocks (instrument library with EDM const/ppm, HZ/VA precision, centering errors, control, D/A/G/L/B/V/M/BM/TB/T/TE/DB/DN/DM/DE/SS observations + inline options .UNITS/.COORD/.ORDER/.2D/.3D/.DELTA/.MAPMODE/.MAPSCALE/.CURVREF/.REFRACTION/.VRED/.LWEIGHT/.NORMALIZE/.END/.LONSIGN/.I/.EDM/.CENTERING/.ADDC/.DEBUG/.AMODE/.TSCORR/.ROBUST/.ALIAS/.PRISM/.ROTATION/.LOSTSTATIONS/.QFIX/.AUTOSIDESHOT/.GEOID/.GPS, C/E/CH/PH/EH records with per-component `!/*`fixity where lone`\*`remains legacy-fixed with warning, and std err tokens; inline`#`comments /`'Description`trimmed; P/PH lat/long projected to local EN via first P origin; D/M/BM/DV capture HI/HT and delta mode, DV/M in delta-mode emit distance+dH; DV/BM/M slope emit zeniths with face-2 weighting and derived stds; 2D M lines accept sigma tokens after distance without forced zenith; EDM mode supports additive/propagated; GNSS supports per-component sigmas with optional EN correlation; centering can be disabled via`.CENTERING OFF`and explicitly-entered sigmas only inflate when`.ADDC ON`; debug logging includes per-obs w in rad/deg plus a step check (`wnew ≈ w − A·dx`); 3D runs auto-hold heights for stations with no vertical-sensitive observations; non-sideshot observations auto-create missing station records with default approximate coordinates; `A`records support explicit`.AMODE`control and stricter AUTO classification;`.TSCORR`adds setup/set-scoped TS angular correlation weighting with configurable rho and diagnostics;`.ROBUST`adds iterative Huber downweighting with per-iteration stats and top-downweighted rows; `.ALIAS` supports explicit mappings and PREFIX/SUFFIX/ADDITIVE rules, then canonicalizes station/observation IDs before unknown building; `.PRISM` parses global/set prism state with unit-safe diagnostics, attaches scoped correction metadata used by distance modeling and zenith weighting paths, and now annotates correction source+magnitude in report/export/listing adjusted-observation rows; `.ROTATION` now parses cumulative plan-rotation state (DD/DMS compatible, wrap-normalized) and applies it to azimuth-bearing observations (`B`, `BM`, `A` in `DIR` mode, and `SS AZ=`) for downstream modeling, with plan-rotation diagnostics in report/export/processing-summary/industry-listing outputs; `.LOSTSTATIONS` now tags station IDs as lost, persists station metadata flags through solve results, surfaces them in run/report/listing diagnostics, and supports project-option visibility toggles to show/hide lost stations in map, listing, and export outputs; `.QFIX` now supports inline fixed-sigma overrides (`.QFIX` or `/QFIX`) for linear and angular `!` sigma tokens, drives weighting with configurable constants instead of hard-coded fixed sigmas, and reports active constants in solve-profile/report/export/listing outputs; .LWEIGHT applied for leveling weights with ft lengths converted to km; bearings/zeniths solved; DB/DN direction sets ingest raw circle readings, reduce face-paired shots by target (with reduced sigmas), track reduction-quality metrics (raw max residual, face-pair delta, per-face spread), emit structured direction reject diagnostics, and solve per-set orientation parameters with raw-vs-reduced diagnostics; map mode now supports grid scale reduction for horizontal distances; optional curvature/refraction vertical reduction is supported via`.CURVREF`/`.REFRACTION`+`.VRED CURVREF`; sideshots support optional explicit azimuth tokens (`AZ=`/`@`) and setup-based horizontal-angle tokens (`HZ=`/`HA=`/`ANG=`) tied to current backsight, are excluded from adjustment with occupy/backsight validation, and emitted in a post-adjust section with computed coordinate/precision (including azimuth source) where available; mixed-face traverse/direction shots rejected when .NORMALIZE OFF; TE closure legs log residuals/misclosure vectors/geometry when available), warns when `.ORDER` is missing (defaults to EN), normalizes to meters/radians, runs iterative adjustment (2D mode drops height parameters and skips vertical obs), logs basic network diagnostics for under-observed stations plus per-direction-set prefit/residual summaries, direction-target repeatability diagnostics (raw spread/raw-max residual/face-pair delta/per-face spread, face balance, local-test/MDB, suspect scoring), multi-set direction repeatability trends by occupy-target (cross-set range/RMS and suspect ranking), setup summaries with setup-level suspect metrics (RMS/Max standardized residuals, local-test fail counts, worst-observation traceability), and expanded traverse closure diagnostics (ratio/ppm/angular/vertical checks, threshold pass-warn flags, per-loop severity ranking and suspect cues), applies overrides/exclusions, applies weighted coordinate/elevation control constraints from station std errors, reports SEUW/DOF plus normal-matrix condition diagnostics, standardized residuals (Qvv), redundancy numbers, chi-square test with 95% critical bounds and variance-factor interval, local-test outcomes with MDB values, point precision (σN/σE/σH + ellipse azimuth), relative precision between unknown points, adjusted coordinates, error ellipses, residual diagnostics summaries (|t| bins/local-fail counts/redundancy weakness plus by-type screening), residual tables sorted by |StdRes| with source-line traceability, and a processing log. UI adds automated suspect-impact what-if exclusion scoring (dSEUW/dMax|t|/chi-square change/unknown shift + one-click exclude+rerun), report-header hover tooltips for technical fields (including SEUW/chi-square labels), hover-tooltips for settings controls in an industry-style tabbed Project Options modal, robust-vs-classical top-suspect comparison when robust mode is enabled, dedicated traverse loop/suspect tables, editable observation tables, parse-mode toggles (.DELTA/.MAPMODE/.NORMALIZE/.LWEIGHT/.COORD/.ORDER/.LONSIGN/.AMODE + TS correlation/robust controls) grouped across Project Options tabs (Adjustment/General/Instrument/Listing/Other Files/Special/GPS/Modeling), re-run with exclusions, auto-clears stale exclusions/overrides when input changes between runs (with log note), report navigation tabs for Adjustment Report + Processing Summary + Industry Standard Output + Map/Ellipses, map/ellipse view with wheel zoom + middle-drag pan + middle-double-click reset-to-extents + zoom-aware symbol scaling + full-height viewport usage + zoom-adaptive label readability, export results as text with selectable format (unit-correct WebNet report or industry-style listing), refresh-to-last-run, and seeded demo + TS-per-record example datasets. In parity workflows, angle centering inflation uses a geometry-aware correlated-ray model to better match industry-style stochastic behavior.
- Current behavior also includes `.ORDER` station-triplet token support (`ATFROMTO` / `FROMATTO`), `.UNITS` angle-unit token support (`DMS` / `DD`), direction-set processing mode support (reduced-by-target or raw per-shot), `.ALIAS` parse-state diagnostics (`aliasExplicitCount`, `aliasRuleCount`, `aliasExplicitMappings`, `aliasRuleSummaries`, `aliasTrace`) plus report/export alias-trace sections showing source alias -> canonical references with line/context traceability, run-pinned solve-profile diagnostics surfaced in report/export (profile, direction-set mode, stochastic/reduction switches, industry fallback, angle centering model), explicit default stochastic-model reporting (active instrument EDM/angle/centering defaults + centering/EDM modes), default-sigma usage counts by observation type, a-priori precision scaling fallback for zero/non-positive DOF so radial/non-redundant setups still report non-zero coordinate standard deviations, and Listing File project-options controls that directly configure industry-style output section visibility/sorting/row limits.
- Current behavior also includes industry-standard-style input-compatibility parity batch 1: directive normalization with legacy aliases/unique-prefix matching, `.DATA ON/OFF` parse gating, `.SEPARATOR` station-token parsing, `.3REDUCE` horizontal-equation handling for slope/zenith workflows, include pre-expansion with source-file traceability and include-scope state restore (child state does not leak to parent), hard-fail include diagnostics for missing/cycle/depth/relative-path errors (with parser regression coverage for cycle/depth source-file+line reporting and CLI regression coverage for missing/cycle/depth/relative-path deterministic hard-fail behavior, plus relative-path/nested-include-order coverage across parser bundle + CLI filesystem flows), CLI `--run-mode` support (`adjustment`/`preanalysis`/`data-check`/`blunder-detect`), run-mode-specific report/processing-summary/industry-listing output sections (data-check differences and blunder-detect warning/cycle notes), an explicit run-mode compatibility matrix with structured diagnostics/mode-gated overrides (including leveling-only `blunder-detect` hard-fail), and project schema v3 include bundles (`mainInput` + `includeFiles`) with v1/v2 migration.
- Current behavior also includes inline-option parity matrixing (`docs/INLINE_OPTION_APPLICATION_MATRIX.md`) and Phase 2 `.LWEIGHT` fallback propagation into non-`L` leveling-producing record paths (`V`/`DV`/`M`/`BM`/`T`/`DM`/`SS`) when vertical sigma tokens are omitted, with directive-level diagnostics in parser logs.
- Current behavior also includes an industry-style listing-output readability overhaul: explicit padded column rendering for coordinates/observation tables (no concatenated numeric fields), adjusted-observation sections split by family (Angles, Distances, Directions, Azimuths+Horizontal Distances with relative azimuth confidence), relationship-pair-driven azimuth/relative-ellipse rows (including fixed-to-adjusted station links), and expanded error-propagation output with station and relative 95% ellipse sections; processing-log lines are omitted from industry-style listing output.
- Current behavior also includes Phase 5 listing-format regression coverage (`tests/industry_listing_format.test.ts`) with fixture-locked heading order and row-format/spacing checks to prevent column-concatenation regressions in industry-style output.
- Current behavior also includes parity fixtures/tests for mixed `.ALIAS` scenarios across conventional + GNSS + leveling records (`tests/fixtures/alias_phase4_mixed.dat`) to keep canonical-ID remapping and alias traceability stable across multi-section inputs.
- Current behavior also includes a tiered computational parity harness rollout across reference projects (`tests/fixtures/computational_parity_suite.json` + `tests/computational_parity_harness.test.ts`) with gated summary/coordinate/residual tolerances for webnet and industry-parity solve paths.
- Current behavior also includes Legacy compatibility corpus gate Phase 1 curation (`tests/fixtures/legacy_compatibility_corpus_phase1.json`) with mixed conventional/GNSS/leveling/include fixture coverage, explicit run-mode outcome expectations per project, and fixture-lock validation in `tests/legacy_compatibility_corpus_phase1.test.ts`.
- Current behavior also includes Legacy compatibility corpus gate Phase 2 harnessing (`npm run corpus:legacy` via `src/legacyCorpusHarness.ts`) with include-aware corpus execution plus enforced checks for expected parse/run outcomes, required run-mode diagnostics, and no silent unknown/ambiguous inline-directive drops.
- Current behavior also includes Legacy compatibility corpus gate Phase 3 CI wiring: harness supports summary artifact outputs (`--summary-json`/`--summary-text`) and baseline comparison (`--baseline`), baseline snapshots are tracked in `tests/fixtures/legacy_compatibility_corpus_phase1_baseline.json`, and CI runs `npm run corpus:legacy:ci` with artifact upload from `artifacts/legacy-corpus/`.
- Current behavior also includes cluster-detection Phases 1-4 diagnostics/workflow: post-adjust cluster candidate detection with deterministic keys (`CL-<n>-<rep>`), linkage mode (`single` or `complete`), and coord-mode-aware tolerances (2D/3D), plus dual-pass solving when approved merges are supplied (`clusterApprovedMerges`: pass-1 detect, pass-2 apply merges) surfaced in report/processing summary/export outputs and run logs, a report-side review/override table where users can approve/reject candidate clusters and choose retained canonical points before rerun, cluster-outcome reporting for applied merges (with per-merge coordinate deltas from retained points) and rejected proposals, a Project Options -> Adjustment cluster-detection ON/OFF toggle, and a top-level report action to revert applied cluster merges.
- Current behavior also includes a full Project Options instrument editor workflow: users can create/select instruments in UI, edit EDM/angle/direction/azimuth/zenith/centering/elevation-difference parameters with unit-aware labels and 2D/3D field gating, and run solves with UI instruments taking precedence over inline `I` rows for matching codes. Precision defaults are now zero when no instrument/obs sigma is provided (no hardcoded 5"/0.005m/0.01m fallbacks).
- Current behavior startup defaults now set run profile to Industry Standard parity, cluster detection to OFF (user-toggle in Project Options -> Adjustment), and seed the project instrument library with `S9` (`industry standard S9 0.5"`) as the default-selected instrument baseline.
- Current behavior also includes Auto-Adjust Phases 1-4 workflow support in Project Options -> Adjustment (default OFF, adjacent to cluster detection toggle): configurable `|t|` threshold, max cycles, and max removals per cycle; candidate selection is local-test aware with redundancy guards and deterministic tie-breaks; per-cycle diagnostics and removed-observation line-trace listings are emitted in logs/report/listing output when enabled; parser/CLI-style parity supports `.AUTOADJUST` and `/AUTOADJUST` directive forms with fixed-fixture regression tests.
- Current behavior also includes automatic sideshot detection Phases 1-4 for non-redundant `M` records: post-adjust redundancy-aware candidate detection with fixed-control target safety exclusion, run-log summaries, report/processing-summary candidate tables, export/listing sections for candidate traceability, and adjusted-observation row annotations (`AUTO-SS` / `[auto-ss]`), plus Project Options/parse-state toggle support (`Auto-Sideshot` UI control and `.AUTOSIDESHOT`/`/AUTOSIDESHOT` parser directives, default ON), with parity regression fixture coverage in `tests/fixtures/auto_sideshot_phase4.dat`.
- Current behavior also includes expanded SS and GNSS-topo parsing for field workflows: `SS` now accepts legacy `SS from to ...` plus shorthand `SS at-to ...` and `SS at-from-to ...` station tokens (with deterministic unprefixed-angle defaults and optional trailing `HI/HT`), and parser state now accepts `GS` GNSS topo coordinate rows (`GS <point> <coord1> <coord2> [h] [sigma1 sigma2 [sigmaH]] [FROM=<station>]`) that are excluded from adjustment equations but emitted in post-adjust report/summary/listing/map outputs.
- Current behavior also includes description-consistency diagnostics/workflow for apostrophe-tagged station descriptions on `C/P/PH/CH/EH/E` records: parser stores per-line trace rows, groups by canonical station ID, supports `.DESC`/`/DESC` reconciliation policy (`FIRST` or `APPEND` with custom delimiter), reports repeated/conflicting descriptions in parse-state/log diagnostics, applies reconciled descriptions in report/export/industry-listing coordinate-style outputs, and emits dedicated report/listing description-summary sections with file-line conflict references.
- Current behavior also includes industry-style weighted statistical summaries computed from per-observation `vTPv` contributions by family (Angles/Directions/Distances/etc.) with industry-style group error-factor normalization (group RMS scaled by `sqrt(totalCount/dof)`), with chi-square bounds surfaced in both error-factor and variance-factor forms in Processing Summary and listing/export views.
- Current behavior also includes dense SPD normal-equation solving via scaled Cholesky factorization (replacing explicit `inv(N) * U` in the primary correction solve) plus diagonal-damped regularization for ill-conditioned normals, with undamped scaled covariance recovery performed through pivoted symmetric `LDLᵀ` solving (including 2x2 pivot blocks) for diagnostics and earlier explicit singular-after-damping failure reporting when stable corrections cannot be recovered.
- Current behavior also includes solver diagnostics that log pivoted symmetric `LDLᵀ` covariance recovery usage (including 2x2 pivot-block counts when present), plus regression coverage for a weak-but-recoverable regularized network alongside the singular fail-cleanly example path.
- Current behavior also includes effective-distance metric computation for angular observation families (`angle`, `bearing`/`dir`, `direction`, `zenith`) during the adjustment statistics pass, with `EffDist` output wired into adjusted-observation report/listing tables and a processing-summary effective-distance block, plus fixture-locked regression coverage in `tests/fixtures/effective_distance_phase3.dat`.
- Current behavior also includes an optional 3D map mode behind a feature flag (default OFF): shared scene/camera primitives (`src/engine/map3d.ts`), projected 3D station/connection rendering, wireframe confidence-ellipsoid rendering, orbit/pan/zoom interactions, and view-cube presets (ISO/TOP/FRONT/RIGHT) are implemented; Project Options -> General includes a `Map 3D` toggle, dense/mobile scenes auto-fallback to 2D for performance stability, and UI regression coverage is in `tests/map_view_3d_ui.test.tsx`.
- Current behavior also includes map/ellipse right-click context tools in the Map tab: `Points` lists visible adjusted stations with coordinate/std-dev values, `Inverse` supports typed IDs or dropdown selection and reports azimuth both directions plus horizontal distance, and `Angles Between` supports three-point (pivot + legs) selection and reports inside/outside angles.
- Current behavior also includes CLI/batch support via `npm run adjust:cli -- --input <file>`: headless parser+engine execution with profile/max-iterations/unit/coord-mode arguments, output modes (`summary`/`json`/`listing`) plus optional `--out` file writes, deterministic exit codes (`0` success, `1` solve failure, `2` usage/io), auto-adjust flags (`--autoadjust`, `--autoadjust-threshold`, `--autoadjust-cycles`, `--autoadjust-max-removals`), preanalysis toggle support (`--preanalysis on|off`), and coordinate-system GNSS frame controls (`--gnss-vector-frame`, `--gnss-frame-confirm`); CI includes CLI smoke runs using `tests/fixtures/cli_smoke.dat`.
- Current behavior also includes expanded input-editor context-menu tooling: multiline selections can still be block-commented line-wise with `Shift+3` (`#`), right-click now exposes `Undo`, `Redo`, `Cut`, `Copy`, `Paste`, `Delete`, and `Select All`, explicit `Block Comment` / `Block Uncomment` actions are kept at the bottom of the menu so existing `# ` comments can be nested and then restored one marker at a time, custom menu edits now preserve the textarea’s native undo/redo history, the custom `Paste` action fires directly from the app menu while still respecting any browser clipboard-permission prompt, and pressing `Tab` inside the input textarea inserts a literal tab character instead of moving focus.
- Current behavior startup now seeds the input editor with a convergent mixed regression network that includes TS, GNSS, and leveling observations, solves in a couple of iterations, and holds `SEUW` near `1.0` so it works as a reliable quick regression-check example during UI/solver changes.
- Current behavior also includes GPS/CRS Phases 1-3 scaffolding: parser/project state supports `.CRS` directives (`OFF`, `ON`, `LEGACY`, `ENU`, optional `LABEL`, optional `SCALE`, optional `CONVERGENCE`), geodetic `P/PH` projection routes through a dedicated projection abstraction (`legacy-equirectangular` or optional `local-enu`), optional grid-ground scale and convergence corrections can be enabled for modeling/inverse workflows, Project Options -> GPS keeps all CRS-related corrections default-OFF, and CRS projection/scale/convergence state is now surfaced in solve-profile diagnostics across report/processing-summary/export/listing outputs.
- Current behavior also includes GPS geoid/grid Phases 1-3 support: parser/project state supports default-OFF `.GEOID` directives (`ON`/`OFF`, `MODEL`, `INTERP`, `HEIGHT`), Project Options -> GPS includes optional geoid model/interpolation and height-datum conversion controls, engine/report/export/listing diagnostics surface active model metadata and conversion status/counts, and checkpoint regression coverage locks known geoid undulation + conversion expectations via `tests/fixtures/geoid_phase3_checkpoints.dat`.
- Current behavior also includes external geoid/grid source loading for GTX/BYN binaries: parser directives now accept `.GEOID SOURCE`/`.GEOID FILE` controls, engine/CLI wiring consumes source bytes with deterministic built-in fallback diagnostics, and regression coverage includes binary fixture paths (`tests/fixtures/mock_geoid.gtx`, `tests/fixtures/mock_geoid.byn`) plus CLI-option parse-state checks.
- Current behavior also includes browser geoid-file ingestion: Project Options -> GPS can load GTX/BYN source bytes directly (`Load GTX/BYN File`) so browser solves consume external models without Node filesystem access, and Geoid/Grid Model ID presets now include `NAD83-CSRS-DEMO` for common Canada-first workflows.
- Current behavior also includes GPS vector mode Phases 1-3 support: parser/project state supports `.GPS NETWORK` / `.GPS SIDESHOT` directives (including `/GPS` form), stores active GPS vector mode in parse state, tags `G` observation rows with `gpsMode`, excludes `SIDESHOT` vectors from adjustment equations, emits post-adjust GPS sideshot coordinate/precision rows, and now renders dedicated GPS sideshot sections in report/processing-summary/export/listing outputs with mixed-mode fixture coverage (`tests/fixtures/gps_network_sideshot_phase3.dat`).
- Current behavior also includes GPS AddHiHt Phases 1-3 support: parser/project state supports `.GPS AddHiHt` (including `/GPS AddHiHt`) with scoped ON/OFF defaults and optional HI/HT values (unit-aware validation warnings), keeps defaults OFF unless explicitly enabled, tags parsed GNSS vectors with per-record antenna HI/HT metadata, applies base/rover antenna-height correction during GNSS preprocessing when those per-record values are present, and surfaces AddHiHt correction diagnostics (vector counts, positive/negative/neutral/default-zero/missing-height buckets, scale min/max) in logs/report/listing outputs with fixture coverage (`tests/fixtures/gps_addhight_phase3.dat`).
- Current behavior also includes GPS rover-offset Phases 1-3 support: parser accepts supplemental `G4` rover-offset records on the preceding `G` vector, stores azimuth/slope/zenith plus derived dE/dN/dH metadata, applies the effective horizontal offset in GNSS network and GPS-sideshot modeling (including AddHiHt height geometry), and surfaces dedicated report/listing diagnostics with fixture coverage (`tests/fixtures/gps_offset_phase3.dat`).
- Current behavior also includes LandXML export Phases 1-3 support: solved networks can now be exported as deterministic LandXML 1.2 with adjusted coordinates, connection geometry, station precision metadata, virtual sideshot target points, and unit/profile annotations through both the browser export selector and CLI `--output landxml`, with regression coverage in `tests/landxml_export.test.ts` and `tests/cli_phase2_output.test.ts`, including round-trip consumer-style parsing checks for point/connection references and unit conversion.
- Current behavior also includes GPS loop-closure diagnostics Phases 1-3 support: parser/project state supports default-OFF `.GPS CHECK` loop diagnostics, engine detects GNSS loop candidates and computes closure vectors/magnitudes independent of adjustment residuals, applies tolerance checks/ranking (`0.0200m + 50ppm * loop distance`) with pass/warn status, processing/report views surface ranked loop diagnostics, and WebNet/industry-style exports include dedicated GPS loop diagnostic sections; result/log/export behavior is covered by `tests/fixtures/gps_loop_phase1.dat`, `tests/fixtures/gps_loop_phase2.dat`, `tests/fixtures/gps_loop_phase3_pass.dat`, and `tests/fixtures/gps_loop_phase3_fail.dat`.
- Current behavior also includes GPS/data external-import pipeline Phases 1-3 plus GPS OPUS import Phases 1-3 support: file-load import now runs through a generic importer registry (`src/engine/importers.ts`) with a normalized imported control/observation model and shared WebNet serializer so additional native field-data importers can plug into the same workflow; current first-party importers detect NGS OPUS/OPUS-RS text solution reports (`.txt`/`.sum`/`.rpt`), JobXML datasets (`.jxl`/`.jobxml`/JobXML-style XML) with reduced-point ingestion plus setup-context station/measurement conversion when occupy/backsight/target references are resolvable, industry software survey-report HTML fallback files (`.htm`/`.html`) with station/orientation/shot table parsing, core FieldGenius raw setup/shot files (`.raw` and matching record patterns), Carlson/TDS RW5-style raw files (including compact angle tokens, backsight context, and separate `V` output when only horizontal distance + delta-H are available), and DBX text/XML exports for local/geodetic points plus setup observations, convert them into editable WebNet `P/PH`/`C`/TS input text, preserve source-line/source-code comments ahead of generated records, and append import-trace warning/error comment sections so unsupported content remains visible for traceability in the imported text and banner summary.
- Current behavior also includes improved industry-software-style JobXML total-station import coverage: reduced-point `Reductions -> Point` blocks now seed approximate coordinates, `StationRecord` `StationName`/`TheodoliteHeight` and `BackBearingRecord` `BackSight`/face-circle/orientation tags are resolved for setup context, `ComputedGrid` supports target approximations, `MeanTurnedAngle` (`MTA`) observations are preferred over duplicate raw face shots, and deleted shots are skipped during JobXML conversion.
- Current behavior also includes a staged external-import review workflow: file imports from supported external sources now open in a modal review table before modifying the input editor, with controls grouped at the top, setup-style grouping heuristics for observations, per-row imported-data/source-type/source-line visibility, exclude toggles, editable group comment lines, and a warning/error section; final import writes clean grouped WebNet text into the editor without the raw trace-comment block.
- Current behavior also includes import-review workflow refinements: importer-aware grouping strategies now label setup-style groups more accurately across JobXML, FieldGenius, Carlson/TDS RW5, and DBX imports; staged rows are directly editable before import; and output-style presets (`Clean WebNet`, `Field Grouped`, `TS Direction Set`) can reshape final grouped import text, including optional `DB/DN/DM/DE`-style setup blocks for compatible conventional observation groups.
- Current behavior also includes import angle-mode and 2D conversion controls in the staged import workflow: selecting `.jxl/.jobxml/.htm/.html` for main input now prompts `Raw Angles` vs `Reduced Angles (BS = 0)` before parsing (reduced mode seeds direction-set shaping), and the import-review modal now includes a `Convert SD+Zenith to HD (2D)` action that converts eligible SD+zenith rows to HD-only, strips HI/HT, strips control Z/elevation output fields, and forces `.2D` on final imported text.
- Current behavior also includes deeper industry-software-style JobXML review fidelity: raw direct readings are preserved alongside `MeanTurnedAngle` (`MTA`) rows, setup/shot metadata (setup type, classification, face) now flows into grouped import-review source labels, normal station-setup backsight shots no longer mislabel as resections, and only true `StandardResection` setups are rendered as `DB/DN/DM/DE` direction-set style blocks in staged import presets.
- Current behavior also includes richer staged import-review editing controls: top-level bulk exclude toggles can exclude all `MTA` rows or all raw non-`MTA` rows at once, each staged setup header can now exclude all observation rows in that setup at once, individual staged rows can be duplicated or followed by inserted comment rows, staged rows can be moved between setup-style groups or spun into brand-new custom setup groups before import, setup-group labels can be renamed inline, empty setup groups can be staged before rows are moved into them, rows can be manually nudged up/down within a setup group, source-line display is simplified to line numbers only, imported rows now support per-row fixed toggles plus final-import type overrides (measurement, plain `D`, `DV`, `A`, `V`, `B`, and setup-aware `DN`/`DM` shaping), those fixed toggles now emit the correct number of `!` markers for the final row family (`C/P/PH` coordinate components, TS observation families, imported GNSS vectors, and vertical/level-style rows), imported zenith values serialize in DMS rather than rounded decimal degrees, imported `V` rows now emit hyphenated `from-to` tokens that match parser/adjustment handling, the `Field Grouped` preset now reorders setup observations by backsight/target with target-aware subheaders, and the `TS Direction Set` preset emits cleaner `DB/DN/DM/DE` resection blocks plus target-ordered `D/M/A` conventional setup rows for industry-software-style JobXML imports.
- Current behavior also includes richer import-review compare/reconcile support: while the staged import modal is open, users can load a second external file (for example JobXML plus survey-report HTML), switch between `Non-MTA Only` and `All Raw Rows` comparison modes without reloading files, review raw/compared counts plus setup/target/family delta tables side by side, and see staged rows highlighted directly when their bucket differs from the comparison dataset before deciding what to import.
- Current behavior also includes Phase 1 import-reconciliation batch 1: staged external imports now scan the current editor content before import and surface deterministic conflict rows for exact station-ID collisions, coordinate conflicts, description conflicts, control-state conflicts, and duplicate observation-family buckets by canonical endpoints; conflicting staged rows are highlighted in the import-review modal, and open import-review/conflict sessions now persist through browser-local draft recovery.
- Current behavior also includes Phase 1 import-reconciliation batch 2: the import-review modal now exposes deterministic per-conflict resolution controls (`keep existing`, `replace with incoming`, `rename incoming`, `keep both`), renamed-station resolutions cascade through the staged imported rows, `keep both` emits explicit `# KEEP BOTH` markers into the final merged text, reconciled imports preserve current include-file bundles instead of clearing them, and open resolution choices persist through browser-local draft recovery.
- Current behavior also includes Phase 1 import-reconciliation batch 3: compare-file loading now expands into a true multi-source reconciliation workspace where additional external files are added as staged reconcile sources instead of count-only side comparisons, imported rows/groups retain source-file metadata in the review grid, the reconcile summary now renders a source matrix for mismatched setup-target-family buckets across all loaded sources, final committed text emits deterministic `# SOURCE ...` separators when multiple reconcile sources are imported together, and the whole in-progress multi-source review state persists through browser-local draft recovery until the session is applied or cancelled.
- Current behavior also includes 2D total-station slope-distance handling that still reduces slope shots to horizontal distance when a matching zenith is available, while continuing to skip the vertical equation itself in `.2D` runs; if no zenith accompanies a slope-distance shot, the distance is treated as a horizontal proxy with existing diagnostics.
- Current behavior also includes dedicated differential leveling loop diagnostics independent of traverse closures: active `L` observations are scanned for loop candidates using station-path graph tracing, raw dH closures are computed and ranked by `mm/sqrt(km)` severity, parser/project options now support configurable level-loop tolerances via `.LEVELTOL` or Project Options (`base mm + k mm/sqrt(km)`), Project Options also exposes preset shortcuts for common tolerance models, each detected loop is evaluated with pass/warn status against that tolerance model, per-loop source-line/path metadata is retained, warn-loop totals are summarized, suspect segments are ranked heuristically across failed loops, and the processing summary, adjustment report, and industry-style listing export now include both ranked loop summaries and per-segment detail/suspect tables.
- Current behavior also includes Project Options cleanup across the main settings workflows: Project Options -> Adjustment now uses a cleaner card/grid layout for solver, geodetic, and leveling controls; level-loop tolerances support user-editable named custom presets in addition to the built-in preset list; Project Options -> Other Files now exposes real export/output controls instead of a placeholder panel; and the `GPS`, `General`, `Instrument`, `Special`, and `Modeling` tabs now use the same condensed label-left/control-right layout with inline enable/disable toggles and aligned numeric/text inputs.
- Current behavior also includes user-facing convergence controls in Project Options -> Adjustment: `Convergence Limit` (default `0.01`) now sits beside `Max Iterations`, solver convergence now uses iteration-to-iteration weighted residual objective delta (`vTPv`) against that limit, and industry-style listing output reports the active convergence-limit value instead of a hardcoded constant.
- Current behavior also includes focused Project Options modal regression coverage: server-render tests now lock the condensed tab layouts for `Adjustment`, `General`, `Instrument`, `GPS`, `Other Files`, and `Modeling` so the card/row structure and key controls remain stable during future UI cleanup.
- Current behavior also includes interaction-focused Project Options modal regression coverage: jsdom tests now verify tab switching behavior and `Apply`/`Cancel` draft-state persistence/discard flows so modal behavior remains stable beyond static layout checks.
- Current behavior also includes project-workspace persistence and adjusted-points deliverables: users can save/open `webnet-project` JSON files (`.wnproj/.wnproj.json/.json`) containing input text + project settings + parser settings + instrument library + custom level-loop presets + export preferences (without solved results), and can export adjusted stations via configurable adjusted-points output (preset/custom columns with max-6 ordering, comma/space/tab delimiters, optional LAT/LON blank-when-missing behavior, and `Z`/`EL` alias-height output) from both toolbar actions and Project Options -> Other Files. Adjusted-points export now also supports a post-adjust transform card with active rotation controls (enable, pivot station, angle, All/Selected scope + Select Points popup with OK/Cancel); rotation is export-only, uses positive-angle CCW convention, validates pivot presence in the current export set, and emits one output containing original rows, transform notes, and rotated rows.
- Current behavior also includes no-result-change solver optimization batch 1: `LSAEngine` now reuses active-observation collections within solve/statistics stages and applies per-iteration geometry caching for repeated azimuth/zenith lookups, reducing redundant computation without changing adjustment outcomes.
- Current behavior also includes no-result-change UI/report optimization batch 2: heavy result tabs (`ReportView`, `MapView`, `ProcessingSummaryView`, `IndustryOutputView`) are now lazy-loaded, and `ReportView` memoizes heavy derived arrays/maps so repeated tab rendering does less sort/filter recomputation without changing solve outputs.
- Current behavior also includes scalability foundation batch 1: browser adjustment runs now execute through a worker-backed pipeline with a typed message contract (`RunRequest`/`RunProgress`/`RunSuccess`/`RunFailure`/`RunCancel`), shared run-session orchestration (`src/engine/runSession.ts` + `src/engine/solveEngine.ts`) is used by the browser worker path and CLI solve wrapper, the app shell consumes that pipeline through `src/hooks/useAdjustmentRunner.ts`, toolbar status now shows queued/solving/finalizing phases with cancel support, and the last successful result remains visible during reruns.
- Current behavior also includes scalability batch 2: `ReportView` now keeps report filter/load-more state local, supports station/source-line text filtering plus observation type/exclusion filters, limits first paint for heavy adjusted-coordinate/observation/covariance/relative-precision tables with explicit `Show more` windows, applies the same pattern to selected ranked diagnostics (traverse/GPS/leveling suspects and direction rejects), and locks the workflow with jsdom regression coverage in `tests/report_filter_windowing.test.tsx`.
- Current behavior also includes scalability batch 3 operator-workflow improvements: source-line cells in report tables now jump back into the input editor through an imperative `InputPane` handle, collapsible detail sections can be pinned/unpinned and revisited from a report-side favorites bar, and the report header shows a compact current-vs-last-run settings diff before reruns; focused jsdom coverage lives in `tests/input_pane_source_jump.test.tsx` and `tests/report_operator_workflows.test.tsx`.
- Current behavior also includes a Section 5.6 parity closeout slice plus browser large-project coverage: per-component `*` markers on `C/P/CH/E/PH/EH` control records now explicitly release fixed/weighted control components while lone `*` still logs the legacy fixed-all warning, adjusted-coordinate report badges now distinguish weighted-control stations with component-status tooltips, industry-style listing output now includes a dedicated `Control Component Status` section for fixed/free/weighted traceability, report/listing observation tables now surface weighting-source traceability (`EXPLICIT`/`DEFAULT`/`FIXED`/`FLOAT`) including mixed GNSS component labels, directive-family semantics and solve-profile output are fixture-locked in `tests/inline_option_matrix_phase3.test.ts` + `tests/inline_option_profile_outputs.test.tsx`, and browser-sized regression coverage is fixture-backed via `tests/fixtures/browser_large_project_benchmark.json` + `tests/browser_large_project_benchmark.test.tsx` to guard the shared run-session path plus first-render coordinate windowing on a synthetic 120-point network.
- Current behavior also includes QA-focused review workflow batches: shared QA-derived models drive report/map comparison workflows, the browser keeps the last five successful runs in memory with baseline pinning and thresholded comparison summaries, report/map station-selection is synchronized with suspect next/previous navigation plus temporary pinned review rows, and deterministic QA bundle export presets can emit WebNet report + industry listing + adjusted points + optional LandXML + comparison summary from one action.
- Current behavior also includes comparison/export UX consolidation: the run comparison panel is collapsible and collapsed by default, refresh-to-last-run clears comparison history, the main export selector now defaults to `Adjusted points`, bundle exports flow through that same selector with descriptive tooltips instead of separate toolbar buttons, and report-side comparison actions continue to preserve the last successful run during reruns.
- Current behavior also includes browser-local workspace recovery for the interactive shell: input text, include bundles, parse/project settings, selected instrument, export settings, active tab/split/sidebar state, report view filters/windowing/collapse pins, report-map selection, comparison thresholds, pinned review rows, saved run snapshots, and loaded geoid source bytes are persisted in local storage with explicit `Recover draft`, `Discard`, and `Clear local draft` controls; recovered drafts intentionally do not restore stale solve results and instead prompt the user to rerun.
- Current behavior also includes Phase 3 batch 1 rerun-planning cache: `src/engine/scenarioRunService.ts` plus `src/engine/scenarioParsedModelCache.ts` now cache both parsed models and topology-aware solve-preparation metadata under the shared browser/CLI solve path, with planning reuse keyed by parsed input/settings fingerprints plus excluded-observation sets; unchanged-topology override reruns, grouped compare reruns, run-session repeats, and repeated cluster dual-pass passes now skip repeated equation-planning work, `src/engine/adjustmentPreprocessing.ts` owns the shared active-observation filtering plus cached-plan replay helpers, and large-project benchmark/regression coverage now asserts both parse-cache and planning-cache hit paths.
- Current behavior also includes Phase 4 core-seam extraction closeout: parser sigma/default-resolution helpers now live in `src/engine/parseSigmaResolution.ts`, parser alias/canonical-ID resolution plus `.ALIAS` directive handling now live in `src/engine/parseAliasPipeline.ts`, direction-set face/reduction/treatment orchestration now lives in `src/engine/parseDirectionSetWorkflow.ts`, parser inline-command dispatch/handler routing now lives in `src/engine/parseDirectiveRegistry.ts`, solver cluster dual-pass orchestration now lives in `src/engine/adjustmentClusterWorkflow.ts`, chi-square probability/quantile helpers plus summary shaping now live in `src/engine/adjustmentStatisticalMath.ts`, preanalysis weak-geometry median/severity/ranking helpers now live in `src/engine/adjustmentWeakGeometry.ts`, parsed/used-in-solve reduction-mode counting now lives in `src/engine/reductionUsageSummary.ts`, GPS and differential-level loop builders now live in `src/engine/adjustmentLoopDiagnostics.ts`, setup-summary and traverse-closure builders now live in `src/engine/adjustmentSetupTraverseDiagnostics.ts`, auto-sideshot and cluster candidate diagnostics now live in `src/engine/adjustmentReviewDiagnostics.ts`, `parse.ts`/`adjust.ts` delegate to those modules instead of carrying that logic inline, include-scope restoration now round-trips the extracted alias state, and focused regression coverage lives in `tests/parse_sigma_resolution.test.ts`, `tests/parse_alias_pipeline.test.ts`, `tests/parse_direction_set_workflow.test.ts`, `tests/parse_directive_registry.test.ts`, `tests/adjustment_cluster_workflow.test.ts`, `tests/adjustment_statistical_math.test.ts`, `tests/adjustment_weak_geometry.test.ts`, `tests/reduction_usage_summary.test.ts`, `tests/adjustment_loop_diagnostics.test.ts`, `tests/adjustment_setup_traverse_diagnostics.test.ts`, and `tests/adjustment_review_diagnostics.test.ts`.
- Current behavior also includes shared QA review quick actions inside the collapsible `Run Compare` workspace: previous/next suspect, jump-to-input, pin selected observation, and focus-report-filter actions now sit together within the compare panel, collapse with `Hide compare`, expose hover-help tooltips, and use the shared workspace review state so selection/filter context survives report remounts and draft recovery.
- Current behavior also includes Phase 2 saved-run artifact workflow batches 1-2: `Run Compare` still supports a manual `Save current run` path, now also exposes a dedicated `Saved Runs` surface for restore/compare/rename/notes/delete actions, saved snapshots store deterministic input/settings fingerprints plus summary metadata, override payloads, and persisted report/map reopen metadata, the saved list remains bounded per workspace/project, restored saved runs rehydrate active diagnostics/review/compare thresholds directly, and those saved artifacts persist through browser-local recovery plus project save/load without reviving stale active results automatically on draft/project load.
- Current behavior also includes Phase 2 batch 3 browser-first export deliverables: the shared export selector now exposes first-class `Adjusted points CSV`, `Observations & residuals CSV`, and `GeoJSON network` targets alongside the existing configurable adjusted-points export, text outputs, LandXML, and QA bundles; CSV/GeoJSON artifacts are generated through dedicated deterministic browser serializers and save/download routing, and the expanded export-format choices persist through project save/load and browser-local workspace recovery.
- Current behavior also includes dense-map review guards: `MapView` clips projected geometry to the viewport, suppresses labels by density for large networks, and renders active selection on a separate overlay layer so dense review sessions stay responsive without trimming underlying result data.
- Current behavior also includes next-cycle shell-hardening Phase 1 completion: the top toolbar now lives in `src/components/AppToolbar.tsx`, the draft-recovery prompt lives in `src/components/WorkspaceRecoveryBanner.tsx`, the right-hand results/tab shell now lives in `src/components/WorkspaceChrome.tsx`, both the import-review modal and the Project Options modal body are lazy-loaded off the browser first-paint path, the external importer registry/converters are loaded on demand from the file-import workflow instead of the initial bundle, the remaining direct in-process solve/orchestration path now lives in `src/engine/directRunPipeline.ts`, and project-load replacement coverage now locks that browser-local draft snapshots are replaced when a project load swaps the workspace state.
- Current behavior also includes next-cycle Phase 2 report decomposition progress: report-local filter/window/pin/collapse state plus shared report/map selection review state now flow through `src/hooks/useReportViewState.ts` and `src/hooks/useWorkspaceReviewState.ts`, shared report-section IDs/window constants live in `src/components/report/reportSectionRegistry.ts`, reusable report primitives now cover collapsible section headers plus load-more footers, and the report toolbar/filter panel/pinned panel/adjusted-coordinates table/observation tables/solve-profile diagnostics now render through dedicated `src/components/report/*` modules while preserving ordering, selectors, browser-recovery restore behavior, and focused regression coverage (`tests/report_view_state.test.tsx`, `tests/workspace_review_state.test.tsx`, existing report layout/windowing suites).
- Current behavior also includes Phase 2 decomposition regression locks for workspace review workflows: report ordering stays fixture-covered, report/map row selection remains stable through the shared review-state path, filter focus can be requested externally and restored after report remounts, and the shared `Run Compare` review-actions bar stays covered for collapse/tooltip behavior with focused integration coverage (`tests/workspace_review_actions.test.tsx`, `tests/run_comparison_panel.test.tsx`, existing report layout/selection suites).
- Current behavior also includes architecture follow-up Phase 1 progress in the app shell: shared app-facing state types now live in `src/appStateTypes.ts`, workspace/project artifacts and reset semantics live in `src/hooks/useWorkspaceProjectState.ts`, Project Options draft/apply orchestration lives in `src/hooks/useProjectOptionsState.ts`, Project Options modal change/select/apply orchestration now lives in `src/hooks/useProjectOptionsModalController.ts`, import-review prompt/review workflow state lives in `src/hooks/useImportReviewWorkflow.ts`, adjustment run/review orchestration now lives in `src/hooks/useAdjustmentWorkflow.ts`, project save/load orchestration now lives in `src/hooks/useProjectFileWorkflow.ts`, export dispatch/browser download orchestration now lives in `src/hooks/useExportWorkflow.ts`, run-profile/run-diagnostics composition now lives in `src/engine/runProfileBuilders.ts`, WebNet text report composition now lives in `src/engine/runResultsTextBuilder.ts`, listing/LandXML output wrapper composition now lives in `src/engine/runOutputBuilders.ts`, parser directive-state normalization/core state-directive handling now lives in `src/engine/parseDirectiveState.ts`, control/geodetic parser families (`C`, `P/PH`, `CH/EH`, `E`) now live in `src/engine/parseControlRecords.ts`, conventional observation families (`D`, `A`, `V`, `DV`, `B`, `BM`, `M`) now live in `src/engine/parseConventionalObservationRecords.ts`, traverse families (`TB`, `T`, `TE`) now live in `src/engine/parseTraverseRecords.ts`, direction-set families (`DB`, `DN`, `DM`, `DE`) now live in `src/engine/parseDirectionSetRecords.ts`, parser include expansion/path normalization/include-file-map resolution now lives in `src/engine/parseIncludes.ts`, parser include-scope snapshot/restore orchestration now lives in `src/engine/parseIncludeScope.ts`, parser post-parse canonicalization/traceability/final log shaping now lives in `src/engine/parsePostProcessing.ts`, solver preprocessing/control-constraint planning now lives in `src/engine/adjustmentPreprocessing.ts` + `src/engine/adjustmentConstraints.ts`, solve-loop equation assembly now lives in `src/engine/adjustmentEquationAssembly.ts`, per-iteration normal-equation control and correction application now lives in `src/engine/adjustmentIteration.ts`, solver statistics/residual summary builders now live in `src/engine/adjustmentStatisticsBuilders.ts`, final parse-state/result payload assembly now lives in `src/engine/adjustmentResultBuilder.ts`, and focused hook/module regression coverage now exists alongside the existing UI suites including `tests/project_options_modal_controller.test.tsx`.
- Current behavior also includes architecture follow-up Phase 1 progress in the app shell: shared app-facing state types now live in `src/appStateTypes.ts`, workspace/project artifacts and reset semantics live in `src/hooks/useWorkspaceProjectState.ts`, Project Options draft/apply orchestration lives in `src/hooks/useProjectOptionsState.ts`, Project Options modal change/select/apply orchestration now lives in `src/hooks/useProjectOptionsModalController.ts`, import-review prompt/review workflow state lives in `src/hooks/useImportReviewWorkflow.ts`, adjustment run/review orchestration now lives in `src/hooks/useAdjustmentWorkflow.ts`, project save/load orchestration now lives in `src/hooks/useProjectFileWorkflow.ts`, export dispatch/browser download orchestration now lives in `src/hooks/useExportWorkflow.ts`, run-profile/run-diagnostics composition now lives in `src/engine/runProfileBuilders.ts`, WebNet text report composition now lives in `src/engine/runResultsTextBuilder.ts`, listing/LandXML output wrapper composition now lives in `src/engine/runOutputBuilders.ts`, shared report/listing/processing-summary/map selector normalization now lives in `src/engine/resultDerivedModels.ts` including visible-station rows, dense-label priority scoring, pair-key normalization, and severity-based station/ellipse styling helpers, parser directive-state normalization/core state-directive handling now lives in `src/engine/parseDirectiveState.ts`, control/geodetic parser families (`C`, `P/PH`, `CH/EH`, `E`) now live in `src/engine/parseControlRecords.ts`, conventional observation families (`D`, `A`, `V`, `DV`, `B`, `BM`, `M`) now live in `src/engine/parseConventionalObservationRecords.ts`, field/GNSS/leveling parser families (`SS`, `GS`, `G`, `G4`, `L`) now live in `src/engine/parseFieldObservationRecords.ts`, traverse families (`TB`, `T`, `TE`) now live in `src/engine/parseTraverseRecords.ts`, direction-set families (`DB`, `DN`, `DM`, `DE`) now live in `src/engine/parseDirectionSetRecords.ts`, parser include expansion/path normalization/include-file-map resolution now lives in `src/engine/parseIncludes.ts`, parser include-scope snapshot/restore orchestration now lives in `src/engine/parseIncludeScope.ts`, parser post-parse canonicalization/traceability/final log shaping now lives in `src/engine/parsePostProcessing.ts`, solver preprocessing/control-constraint planning now lives in `src/engine/adjustmentPreprocessing.ts` + `src/engine/adjustmentConstraints.ts`, solve-loop equation assembly now lives in `src/engine/adjustmentEquationAssembly.ts`, per-iteration normal-equation control and correction application now lives in `src/engine/adjustmentIteration.ts`, solver statistics/residual summary builders now live in `src/engine/adjustmentStatisticsBuilders.ts`, final parse-state/result payload assembly now lives in `src/engine/adjustmentResultBuilder.ts`, and focused hook/module regression coverage now exists alongside the existing UI suites including `tests/project_options_modal_controller.test.tsx` and `tests/parse_field_observation_records.test.ts`.
- Current behavior also includes dev-environment caveat cleanup: the transitive `flatted` advisory is cleared through an explicit package override, `npm audit` now reports zero vulnerabilities on install, and Vite build warning thresholds are tuned to the current chunk topology so the prior oversized-engine warning no longer appears in normal production builds.
- Current behavior also includes no-result-change parser/import optimization batch 3: parser hot-loop directive tokenization now uses lower-allocation whitespace/comma helpers, and importer hot paths now avoid full-file line splits for first-line detection while using cached line-number resolvers for JobXML/survey-report/DBX source-line mapping on large external files.
- Current behavior also includes JobXML angle-only vertical preservation during external import, so angle-only total-station shots that carry vertical-circle values are imported as paired `A` + `V` observations rather than dropping the vertical component; this allows industry software JobXML and survey-report HTML imports such as `UNDERT2` to reconcile to the same comparable observation count once `MTA` rows are excluded.
- Current behavior also includes fixture-locked centering geometry regression coverage (`tests/fixtures/centering_geometry_reference.dat`) to keep slope-distance and zenith centering inflation stable under industry-standard geometry weighting.
- Current behavior also includes robust Huber reweighting via a small inner IRLS loop per outer solve iteration, using postfit residuals (`v = A·dx - L`) for reweighting while keeping diagonal updates O(m) and limiting correlated reweight scaling to active correlation blocks.
- Current behavior also includes preanalysis / network-planning Phases 1-5: Project Options -> Adjustment `Preanalysis` mode accepts planned `?` observation values plus omitted `D`/`A` measurement tokens for geometry-derived planning, resolves them from approximate geometry using the existing row-building path (`L = 0`), fixes `sigma0^2 = 1` for predicted covariance, exposes station covariance blocks plus connected-pair relative covariance/precision rows, renders weak-geometry cues while suppressing residual-based QC sections in report/map/output views, adds hover-help coverage across preanalysis report headers/labels, adds a separate locked planned-observation section for fixed-sigma planned rows, adds a what-if planning table for removing or re-adding planned observations with predicted precision deltas, excludes fixed-sigma planned observations from removable what-if candidates, and supports CLI/example workflows via `--preanalysis on` and `public/examples/preanalysis_network_plan.dat`.
- Current behavior also includes expanded hover-help coverage across non-preanalysis report summary cards/solve-profile labels and across the Project Options modal, including tab buttons, section headings, field labels, and instrument-editor controls in addition to existing control-level tooltips.
- Current behavior also includes Canada-first coordinate-system Phase 1 rollout: Local/Grid mode state, Canada NAD83(CSRS) UTM CRS catalog, parser directives (`.SCALE`, `.MEASURED`, `.GRID` including `DISTANCE=ELLIPSOIDAL`), mode-aware reduction modeling with per-station convergence/grid/elevation/combined factors, expanded GPS/CRS options UI, grid/geodetic diagnostics in report/listing, and matching CLI flags for coordinate-system batch control.
- Current behavior also includes Canada-first coordinate-system Phase 2 catalog start: CRS catalog expansion with NAD83(CSRS) MTM zones and priority provincial entries (including New Brunswick Stereographic Double Projection), plus CRS-ID normalization so parser/CLI `.CRS ID` and `--crs-id` accept either canonical catalog IDs or EPSG aliases.
- Current behavior also includes Canada-first coordinate-system accuracy upgrades: CRS definitions now expose projection-parameter rows, area-of-use bounds, and supported datum-operation metadata; parser state includes standardized `observationMode` alongside legacy grid mode fields; geodesy factor computation now uses projection-family formulas for TM and oblique stereographic systems with deterministic numerical-fallback diagnostics; solver captures coordinate-system diagnostic codes/warnings (`CRS_OUT_OF_AREA`, `CRS_DATUM_FALLBACK`, `GEOID_FALLBACK`, `FACTOR_APPROXIMATION_USED`) and surfaces datum-op + area-of-use status in parse/report/listing outputs; listings include per-station factor source; and Project Options -> GPS now includes CRS search filtering plus richer CRS details (datum operation + area bounds) and out-of-area warning visibility from the latest run.
- Current behavior also includes coordinate-system parity pass A upgrades: reduction behavior is now driven by an authoritative `ReductionContext` (industry-standard defaults), Grid-mode `.SCALE` now replaces combined-factor reduction for measured distances (distance-only), pre-solve input gates now block invalid local/grid/geodetic class mixes and unknown GNSS frames unless explicitly confirmed, datum sufficiency is evaluated pre-solve with hard-fail vs soft-warn reasons/suggestions, and run/report/listing/processing-summary outputs now surface the expanded diagnostic set (`CRS_INPUT_MIX_BLOCKED`, `GNSS_FRAME_UNCONFIRMED`, `DATUM_HARD_FAIL`, `DATUM_SOFT_WARN`, `SCALE_OVERRIDE_USED`, `FACTOR_FALLBACK_PROJ_USED`) alongside GNSS frame default/confirmation state.
- Current behavior also includes Project Options Adjustment-tab action cleanup: adjustment-action toggles are now limited to `Preanalysis`, `Auto-Sideshot`, `Cluster Detection`, and `Auto-Adjust`, while map-display toggles (`Map Show Lost Stations`, `Map 3D`) were moved under General -> Local/Grid Reduction.
- Current behavior also includes test-runner compatibility hardening by pinning `jsdom` to `26.1.0`, restoring stable Vitest jsdom interaction-worker startup on the current Node runtime.

## Tech Stack

- Runtime: Node 18+ (ESM). Bundler: Vite 6.
- UI: React 19.2 (hooks), TailwindCSS 3.4 (classes in TSX, base in src/index.css), Lucide icons.
- Language: TypeScript 5 (strict), TSX entrypoints.
- Tooling: ESLint 9 (flat config), Prettier 3 + lint-staged + Husky, Vitest 4, PostCSS + Autoprefixer.
- Dependencies: react, react-dom, lucide-react.
- Dev deps: Vite + @vitejs/plugin-react, ESLint plugins (react, react-hooks, react-refresh), Tailwind/PostCSS, Vitest, TypeScript.
- Optional improvements: Web Worker/WASM path for large networks; keep Vite unless routing/server needs appear.

## Code Style

- TypeScript/React with functional components and hooks; prefer pure helpers and small components.
- Naming: camelCase for vars/functions; UPPER_SNAKE for constants; React components PascalCase.
- Formatting: Prettier defaults (2-space, semicolons); Tailwind utility classes for styling (avoid inline styles).
- Lint: ESLint flat config + react-hooks/refresh; `no-unused-vars` allows leading `_` for intentionally unused args. Keep TS strict; avoid `any` - prefer shared types/helpers.
- Data/units: calculations normalize to meters/radians internally; unit conversions (ft <-> m) happen at parse/override/display boundaries. Angle conversions via `dmsToRad`/`radToDmsStr`; keep station/obs ids as strings.

## Commands

```bash
npm install      # install deps
npm run dev      # start Vite dev server
npm run lint     # ESLint over repo
npm run typecheck # TypeScript check (no emit)
npm run test     # Vitest (watch)
npm run test:run # Vitest (CI/one-shot)
npm run build    # production build
npm run preview  # preview built assets
npm run format   # Prettier write
npm run format:check # Prettier check
```

## Architecture

- Root configs: vite.config.js, tailwind.config.js, postcss.config.js, eslint.config.js, tsconfig.json.
- Entry: src/main.tsx renders <App /> with src/index.css (Tailwind base).
- Styles: src/index.css Tailwind directives + Vite defaults; src/App.css is legacy template (not imported).
- Assets: public/vite.svg, src/assets/react.svg (template).
- Core (TypeScript):
  - Types: src/types.ts for stations, observations, instruments, results, parse options, overrides.
- Math helpers: src/engine/matrix.ts (zeros/transpose/multiply/inv), src/engine/angles.ts (RAD/DEG/SEC, dms helpers).
- Parser: src/engine/parse.ts ingests industry-standard-style text with inline options (.UNITS/.COORD/.ORDER/.2D/.3D/.DELTA/.MAPMODE/.MAPSCALE/.CURVREF/.REFRACTION/.VRED/.END/.AMODE/.TSCORR/.ROBUST/.ALIAS), per-component fixity via `!/*`(lone`\*`legacy-fixed with warning), inline`#`comments /`'Description` trimming, C/E records (fixity/std errs, NE/EN order, ft<->m), D/A/G/L observations with instrument lib lookups (GNSS per-component/correlation support), and returns stations/unknowns/obs/logs. Direction sets are reduced by target (face-paired means + reduced sigmas) when normalization is enabled; alias canonicalization runs before unknown-building.
- Importers: src/engine/importers.ts registers external file importers, normalizes imported control/observation records, serializes imported datasets back to editable WebNet text, and currently hosts OPUS/OPUS-RS, JobXML, FieldGenius, Carlson RW5, TDS raw, and DBX text/XML export importers used by the file-load workflow; serializer output includes source-trace comments and an import-trace block for warning/error visibility.
- Engine: src/engine/adjust.ts (LSAEngine) builds A/L/P, normals N=(A^T P A), iterates corrections, applies overrides/exclusions and unit normalization, computes SEUW/DOF, residuals, ellipses, sH, conditioning/residual warnings, direction-set diagnostics, setup diagnostics, traverse closure ratios, and logs.
- UI: src/App.tsx (shell) manages input/settings/layout; components in src/components (InputPane for edits/upload; ReportView for tables/overrides/exclusions/logs; MapView for plan/ellipse view).
- Tests: Vitest specs in /tests (angles, matrix, parser, engine) with fixtures in /tests/fixtures.
- CI: GitHub Actions workflow (.github/workflows/ci.yml) runs lint, vitest (--runInBand), and build on pushes/PRs to main.
- Data flow: user edits textarea -> handleRun instantiates LSAEngine with settings -> solve() mutates stations/observations -> result stored in state -> ReportView renders tables.

## Suggested Next Steps

- Continue the new field-to-office roadmap in `TODO.md` with Phase 3 batch 1: cache unchanged-topology equation-planning metadata so exclusion/override/cluster/saved-run reruns reuse more than just parsed-model state.
- Keep the new multi-source reconciliation workspace, saved-run restore/manage path, and browser-first CSV/GeoJSON export routing stable while the next cycle adds deeper rerun-planning reuse.
- After that, Phase 3 batch 2 moves artifact generation off the main thread; keep App/browser recovery/project-file paths aligned with that roadmap.

## Todo

- See TODO.md for the current checklist.
- Documentation and Examples have been added (User Guide, Demo Data).

## Process Note

- Update TODO.md, README.md, and agents.md after every batch of updates.
- Before starting a batch, add planned tasks for that batch to TODO.md (as checklist items) so implementation scope is explicit.
- During a batch, update TODO.md continuously: mark tasks complete as soon as they are finished, and keep in-progress/remaining items accurate.
- Keep `manual/` local-only reference material. Do not commit `manual/` contents to git.
- When referencing competing or external software in code, docs, comments, tests, or examples, use generic wording such as `industry standard software` or `industry software` instead of product names unless a specific file format or interoperability feature requires the exact name.
- Before every commit/push, run a naming sweep across `agents.md`, `TODO.md`, `README.md`, and all code/docs/tests changed since the last commit: replace non-required product-name mentions (for example industry-standard software brand names) with generic wording (`industry standard software`) unless exact naming is required for file-format/interoperability behavior.
- Commit and push to GitHub after every completed batch of improvements.
- Always commit and push to GitHub immediately after each completed batch; do not leave finished batches unpushed.
- After each batch of updates, run:
  - `npm install`
  - `npm run lint`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`.
- If any command errors, fix the issues and rerun the full sequence until all commands succeed before pushing.
