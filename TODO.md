- [ ] Phase 18X explicit baked TIN surfaces and flatten workflow (branch feat/cad-surface-explicit-bake, baseline origin/main 3c3649f40a3cc5900d93c85b056bf042f3b790b1 = PR #114 merge, exact match, fetch-verified no advance). STATUS: ARCHITECTURE AUDIT DONE — docs/evidence/phase18x-explicit-tin-architecture.md (Strategy B: generalize importedTin, fail-closed predicate, frozen srev1:imported: prefix, kind webnet-bake provenance). BASELINE: lint 0err/2 pre-existing warns, typecheck clean. ENGINE DONE (predicate + discriminated provenance + shared materializer seam + digest + worker budget + transform + guard generalization + 9 topology tests). BAKE DONE (cadExplicitBake canonicalizer + SURFBAKE/SURFBAKECOPY + CURRENT/expectedRevision/worker/lock gates + one-undo + 12 tests). UI DONE (Baked Explicit TIN snapshot/text + editor + toolspace + ribbon Definition Tools + manager actions + bake warning prompt + 13 UI tests). HOOK FIX DONE (explicit→imported-style ref mapping in 4 hook files). BROWSER+PERF DONE 2026-09-26 (uncommitted): tests-browser/cad-surface-bake-18x.spec.ts (mission §101 A–M, 7/7 green headless, zero page/console errors) + docs/evidence/phase18x-bake-performance.md (measured §60–63: payload bytes 38KB/458KB/2.58MB/5.22MB @1k/10k/50k/100k; explicit rebuild 3.7/30.6/167/323ms vs native Delaunay 23/163/889/1646ms; bake transaction 3.5/40.7/203/425ms; WNCAD in-place delta 0.14/1.54/7.99/16.0MB; LandXML parse guarded at 100k by the 200k-element cap) + scripts/phase18xBakePerf.ts. TWO LIVE-APP DEFECTS FOUND+FIXED (browser QA caught both): (1) bake rejected every in-session surface because `deriveSurfaceStatus === 'CURRENT'` needs a persisted `cachedRevision` the app never writes (rebuilds stay out of history) — added optional `sessionCurrent` to SURFBAKE/SURFBAKECOPY, set from `surfaceBakeCapability`; absent = fail-closed, engine gate pins preserved + new engine pin; (2) LandXML civil surface export was unreachable (Export Center never passed `civilSources`; `landxmlCivilSource` gated on the same never-written `cachedRevision`) — Export Center now forwards session caches + a fresh cache hit at the current source revision counts as session CURRENT, persisted fallback intact (18L tests green). VALIDATION: typecheck clean, lint 0 errors (+2 pre-existing warnings), test:agent 5734 passed / 3 pre-existing study-desktop fails, test:wasm 74/74, build clean, portable-paths 0 violations, browser spec 7/7. NEXT: reviewer gate, PR (do NOT merge).
- [ ] Phase 18W native surface boundary/breakline source editing (branch feat/cad-surface-boundary-breakline-editing, baseline 8ea69d69 = PR #113 merge, no advance). STATUS: ARCHITECTURE AUDIT DONE — docs/evidence/phase18w-boundary-breakline-architecture.md (ownership A/B/C, revision semantics, EDIT_ENTITY reuse, parcel gate, imported-TIN command-guard gap, rebuild/replay/downstream reuse, no-schema-bump plan). BASELINE: lint 0 errors / 2 pre-existing warnings, typecheck clean. ENGINE DONE + UI DONE + QA DONE + REVIEWER GO-WITH-SURFACE-DEFINITION-RESTRICTIONS (15/15). PR #114 opened, NOT merged. HEAD b3113f2c. UI SLICE DONE 2026-09-25 (uncommitted) — engine consolidation split (cadTransactionsSurfaceBoundaryCommands.ts 280 lines, behavior-identical, 18w engine 25/25 green) + editor split (shell 148 + breakline/boundary sections + parts) + breakline tables/chain editor (status via engine codes, preflighted reorder/insert/replace/rename/reverse/convert, transient selection highlight) + boundary tables/vertex editor (parcel-disabled, shared-confirm, EDIT_ENTITY/GRIP commits, recreate path for count changes, outer-confirm) + 5 shell commands (SURFBREAKLINE*/SURFBOUNDARY* + alias) + ribbon/toolspace/properties + 5 new registry tests. FOLLOW-UP DONE 2026-09-25 (uncommitted, §76 one-transaction): EDIT_ENTITY polyline-vertices batch variant (single-vertex path byte-identical) + CREATE replaceVoidSourceEntityId void swap in one commit + vertex-editor single-Apply + 4 new single-history-entry pins (batch move/undo, bow-tie reject, void swap/undo, unknown-source reject). QA DONE 2026-09-26 (uncommitted): lifecycle file (13 tests: revision lifecycle, downstream profile/section/volume/analysis, shared-source, point-follow, polygon MOVE, GRIDGROUND commutativity, WNCAD roundtrip, undo/redo) + replay/export file (4 tests: 18S+18T survival, fail-closed SURFACE_EDIT_POINT_OUTSIDE_DOMAIN, LandXML Pnts/Faces, imported-TIN) + browser spec (7 flows, zero errors) + perf evidence docs/evidence/phase18w-performance.md. BUGS FOUND+FIXED (minimal): EDIT_ENTITY vertex edits rejected polygons (created boundary sources are polygons); boundary preflights mapped clean-null to PREFLIGHT_UNAVAILABLE (ring create + vertex Apply dead); findSurfaceBrokenRefs ignored station-id chain refs (bogus BROKEN after entity convert); chain editor advanced draft past rejected commits (index-remove deleted wrong point — now gated on commit success). Full validation: lint 0 errors (+2 pre-existing warnings), typecheck clean, agent 5664 passed / 3 pre-existing study-desktop fails (stash-proven), wasm 74/74, parity 25/25, build clean, portable-paths 0 violations, browser spec 7/7 green in-file.
- [ ] Phase 18Q professional CAD transform tools (branch feat/cad-professional-transform-tools, baseline origin/main 69ce1eafecad711e4099bdf05eb9a7d88f6002a9 = PR #106 merge, exact match, fetch-verified no advance). STATUS: BASELINE DONE 2026-09-20 — lint PASS, typecheck PASS, test:wasm 74/74 PASS, parity 25/25 PASS, build PASS, portable-paths PASS, test:agent 5253 pass + 3 pre-existing study-desktop fails (unrelated calibration/preflight). Scout audit DONE (MOVE/COPY/block/annotation/preview/WNCAD spans recorded). NEXT (prior workers DONE: kernel, entity seam, mirror representation, Helmert/GridGround engine): commands batch 1 (ROTATE/SCALE/MIRROR/ALIGN2D) DONE 2026-09-20 — engine keys + registry, UI sessions/picks/typed submits, transform-selection preview + MOVE ghost dim, pick-loop fix (command capture ahead of block-insert repeat), ribbon Modify entries, tests/cad_transform_commands_18q.test.ts 10/10; test:agent 5316 pass + same 3 pre-existing study-desktop fails. NEXT: Helmert/GridGround UI (owns cadHelmert2D/cadTransform2D/cadBlocks + Helmert/GridGround UI), browser QA, reviewer gate DONE 2026-09-20 (reviewer-18q GO 15/15, 0 fix rounds), PR #107 OPENED (do NOT merge). 2026-09-20 — HELMERT2D (HELMERT alias, explicit pairs, RIGID|SIMILARITY, compact panel + fit report + live ghost) + GRIDGROUND (direction + CSF + origin, explicit formula) engine keys + registry, sessions/picks/typed/panel/preview/prompts, Modify + Survey-Transform ribbon, tests/cad_helmert_gridground_commands_18q.test.ts 8/8 + tests/cad_transform_export_18q.test.ts 3/3, tests-browser/cad-transform-18q.spec.ts 5/5 flows A–N zero page errors, perf evidence docs/evidence/phase18q-transform-performance.md (ghost <2ms at 5k), plus two prior-batch bug fixes (empty-Enter MIRROR/ALIGN2D No-default commit; dock-typed submits via live session ref). NEXT: reviewer gate, PR (do NOT merge).
- [ ] Phase 18O professional CAD annotation (branch feat/cad-professional-annotation, baseline origin/main 24c648690d37554fe82b48f897850eb4e1b493a9 = PR #104 merge, exact match, fetch-verified no advance). STATUS: IMPLEMENTATION COMPLETE 2026-09-19 — semantic annotation (mtext/leader/dimension x5/bearing/curve), additive text-style sizing (legacy-screen/model/paper) + annotation scale (default 500), bounded anchors (fixed/survey-point/line-endpoint/arc/block-insertion; polyline vertex → fixed) with BROKEN_REFERENCE honesty, block-based arrowheads (Closed/Open/Dot/Tick, +X convention), one shared text-metrics resolver, one dimension geometry resolver, COGO/curve-helper reuse, WNCAD additive persistence, SVG/PDF full + DXF approximated + LandXML NOT_APPLICABLE, ANNOTATE ribbon + 4 style managers + Properties + workspace seam, Playwright 2/2 zero errors. VALIDATION: lint 0err, typecheck clean, test:agent 5220 + 3 pre-existing study-desktop fails (stash-proven on baseline), wasm 74/74, parity 25/25, build clean, portable 0 violations, CAD suites 919/919, perf evidence docs/evidence/phase18o-annotation-performance.md (export-scene/bounds O(n²) + no dirty tracking documented as follow-up, not gate). NEXT: reviewer gate, PR (do NOT merge).
- [ ] Phase 18O annotation workspace seam (branch feat/cad-professional-annotation). STATUS: SEAM DONE 2026-09-19 - src/hooks/surveyCad owns the seam: surveyCadAnnotationSnapshot.ts (style tables + scale + refcounts + arrow defs + selected detail via shared engine derivations), surveyCadAnnotationOps.ts (pure validated UiOp applier for 25 style ops + entity field edits + scale, stable reasons LAYER_LOCKED/STYLE_IN_USE/DUPLICATE_NAME/EMPTY_NAME/STYLE_NOT_FOUND/LAST_STYLE/UNCHANGED), useSurveyCadAnnotationSessions.ts (MTEXT/LEADER/DIMx7/BDLABEL/CURVELABEL pick flows with fixed anchors + Enter/Esc commit), framework cases (types/starters/session/prompt/help/preview/construction/consume/lifecycle-live-ref), useSurveyCadWorkspace exposes annotationSnapshot + runAnnotationOp + 10 starters. Minimal extras: ANNOTATION_COMMIT engine op (types + 1 def + registry line) for undoable style/entity commits; SurveyCadWorkspace.tsx starter/snapshot/action glue (14 lines); CadCommandDock forwards non-command text to the live session + submitSessionText action (typed MTEXT/LEADER entry); spec helper fixes (Annotate tab in addDimension, LAYER .first(), pretty-JSON mtext assertion, DXF-only export loop since SVG/PDF are sheet-gated and blank drawings have no sheet UI). VALIDATION: tsc clean, eslint clean, new tests/cad_annotation_workspace_seam.test.ts 6/6, ui+engine annotation suites 138/138 with shell panels, test:agent 5220 pass (3 pre-existing study-desktop fails, stash-proven), build clean, Playwright cad-annotation-18o 2/2 green with zero page errors. FOLLOW-UPS: associative anchors from snaps (survey-point/line-endpoint/arc/block-insertion); SVG/PDF e2e legs need sheet-creation UI or sample-file flow; CURRENT_BEHAVIOR/ARCHITECTURE annotation notes pending with shell docs.
- [ ] Phase 18N native CAD blocks and professional survey symbol library (branch feat/cad-blocks-survey-symbols, baseline origin/main 876911254df0b165315b4c183804e913a2c4e2e7 = PR #103 merge, exact match to mission creation SHA). STATUS: BASELINE FETCH DONE 2026-09-19 — origin/main 87691125, no advance; branch created. STATUS: IMPLEMENTATION DONE 2026-09-19 — engine b862e9d2 (core types/transform/bounds/validate, 29 tests) + persist 2b87cb39 (WNCAD/9 transactions/style markers/native DXF/snap, 20 tests) + UI 0d7e391d (manager/26 seeds/registry/toolspace/ribbon/properties/browser 1/1). VALIDATION: lint 0err/2 pre-existing warns, typecheck clean, test:agent 5101+3 pre-existing study-desktop, wasm 74/74, parity 25/25, build 9.68s, portable 4199/0, perf 100/1k/10k = 2/8/35ms linear. REVIEWER: GO-WITH-BLOCK-RESTRICTIONS (reviewer-18n 15/15, no NO-GO failures; fixes 85078f0c unified UI redefine base semantic + stale comments). PR #104 OPENED 2026-09-19, NOT merged. Do NOT merge without explicit instruction. ENGINE b862e9d2 LANDED (types/cadBlocks/sanitize/persistence/point-style marker ref). PERSIST+EXPORT SLICE DONE 2026-09-19 (uncommitted) — WNCAD trailing blockDefinitions + dangling-ref sanitize (cadBlockPersistence.ts), 9 undoable transactions (cadTransactionsBlockCommands.ts 394 + cadBlockSources.ts 107; BLOCK_SEED/BLOCK_EDIT converge the UI worker's interim seam), point-style block markers via the single renderer seam (cadRendererBlocks.ts 67; F2F style-only, points stay survey-point), native DXF BLOCK/INSERT in R12+R2000 (dxf/dxfBlockExport.ts 179; sanitized names; LandXML unchanged/fail-closed), snap/bounds/selection expand nearby refs only (cadSpatialBlockSnaps.ts 165), oracles in tests/cad_blocks_transform.test.ts + transactions in tests/cad_blocks_transactions.test.ts + persist/size in tests/cad_blocks_wncad.test.ts + coverage matrix extended (blockref/ptblock: SVG/PDF FULL, R12/R2000 FULL native, LandXML ref-omitted/point-FULL). FLAG FOR UI WORKER: cadBlockUiCommands.ts toLocalChild stores children base-shifted AND sets basePoint=base — double-subtraction under the normative engine transform world=insert+R·S·(local−basePoint); seeds (base 0,0) unaffected, but manager create/redefine instances will misplace until it stores drawing-coord children (engine BLOCK_CREATE convention). UI SLICE (worker-18n-ui) DONE 2026-09-19 — registry BLOCK/INSERT/EXPLODE/BLOCKS + Block Manager + 26-seed survey library (lazy-seed gate) + Toolspace/Ribbon/Properties/overlay/grip/pick-loop + browser spec 1/1 (~9s) + unit tests 5/5; fixed persist-flagged double-subtraction (drawing-coord children now); fixed drag-release click leak (didDrag resets on press). Seams for persist worker: cadBlockUiCommands interim adapter (BLOCK_* keys reserved), CadBlockViewportOverlay removed in favor of scene-level buildBlockOverlayPrimitives.
- [ ] Phase 18K sample lines, cross sections, and section views (branch feat/cad-cross-sections-sample-lines, baseline origin/main 5700cd80863e433e87569844751ed42985de799f = PR #100 merge, exact match to mission creation SHA). STATUS: BASELINE DONE 2026-09-19 — lint 0err/2 pre-existing warns, typecheck clean, portable-paths 4092/0 violations, test:agent 4898+3 pre-existing study-desktop fails, wasm 74/74, parity 25/25, build 9.48s, surface/profile 262/262 + alignment/stationing 59/59. ARCH DONE — docs/evidence/phase18k-cross-section-architecture.md. ENGINE+PERSIST DONE + UI DONE 2026-09-19 (browser A–F 6/6, agent 4968+3 pre-existing, wasm/parity/build/portable green). NEXT: PR #101 OPENED 2026-09-19, NOT merged (reviewer GO-WITH-SECTION-RESTRICTIONS). Do NOT merge without explicit instruction. ENGINE DONE 2026-09-19 — new src/engine/cad/sections/ (sectionTypes/Tangent/Direction/Extract/Area/Stationing/index, all <=260 lines): exact element-geometry tangent (AHEAD at internal boundary, BACK at end, sharp-PI documented, no finite-difference), D=N*cos+T*sin with |skew|<89 fail-closed + width validation + canonical -right..+left domain, extractTinAlongSegment seam in profileSampling.ts (additive-only, walkLine body untouched), per-linexsource SectionResult (multi-segment, voids never bridged), exact cut/fill overlay (common coverage only, zero-crossing split, Net=Fill-Cut units^2, no volume), raw-persist stationing (display via cadAlignmentDisplayStationToRawStation, physical-raw intervals, OUT_OF_RANGE no-clamp) + tests/cad_sections_engine.test.ts 26/26 (sign/skew/boundary oracles, flat/planar/ridge/void/independence, area oracles incl. exact halves + diff-triangulation + partial/void, equation XY-unchanged, 2e6/7e6 translation, shuffled determinism, fail-closed). VALIDATION: new 26/26, profile/surface 46/46, lint clean (touched), typecheck clean, agent ~4936 pass, only fails outside slice (sibling cadTypes sectionViews key-order pin + 3 pre-existing study-desktop data pins; none import sections). PERSIST DONE 2026-09-19 (branch working tree, uncommitted until this slice commits) — new cadSectionTypes (schema clone/backfill/seed Existing Ground/Proposed/Secondary/No Display + group/line/source/style/view CRUD + engine result seam), cadSectionRevision (secg1/secl1/secg1 view; alignment XY + raw station + widths + skew + source srev1; station-equation display mapping EXCLUDED), cadSectionStatus (9-state per line x source), sectionCache (line x source, keeper<=2, source-only discard), worker 'sections' op with GO-level batching (each source mesh ONCE + line array, real engine extractor wired via sections/index), surfaceSectionService (drawingId+groupId+groupRevision+requestId ownership, supersede, cross-drawing discard, late-never-CURRENT, failure/retry), section transactions (SAMPLE_GROUP/SAMPLE_LINE/SECTION_SOURCE/SECTION_STYLE/SECTION_VIEW, group delete keeps alignment+surfaces + views reference-safe), WNCAD additive sampleLineGroups/sectionStyles/sectionViews (clone/backfill/seed + legacy empty tables + save/reopen). Tests cad_section_revision_status (10), cad_section_worker (12 incl. real-TIN mesh-once + supersession + cross-drawing + failure/retry), cad_section_wncad (7 incl. 3 lines/2 sources/3 views round-trip + legacy + save-as isolation); cad_point_groups_18d key-order pin updated to sectionViews. VALIDATION: new 29/29, profile/WNCAD/cad+surface+volume 85 files 785 pass, typecheck clean, lint clean (touched), portable-paths 4100/0, build clean. NEXT: UI worker after both return, reviewer gate, PR (do NOT merge). UI SLICE DONE 2026-09-19 — workspace wiring (section service/cache/snapshot/layers/actions/batch stack/manager), toolspace/ribbon/registry/properties/preview-canvas, agent-tier section UI tests 10/10, triage+style decision, browser flows A–F (C–M sub-flows) 6/6 green zero page errors. Finisher fixes: batch out-of-range lines excluded from worker request instead of failing the whole group (per-line OUT_OF_RANGE honest); section views default cut/fill ON; LOCK gates sample-line/group-content edits at the transaction boundary; Create Section Views stacks new groups below ALL existing frames (cross-group non-overlap); derived sample-line/section-view layers stop intercepting canvas clicks while a command is capturing points. VALIDATION: lint 0 errors (2 pre-existing warns), typecheck clean, test:agent 4968 pass + 3 pre-existing study-desktop fails (stash-proven identical without these changes), test:wasm 74/74, parity 25/25, build 9.59s, portable-paths 4122/0, Playwright cad-sections-18k A–F 6/6. NEXT: reviewer gate, PR (do NOT merge).
- [ ] Phase 18J alignment surface profiles + profile view foundation (branch feat/cad-surface-alignment-profiles, baseline origin/main 8bc5e9b4d1237dfa62747d71d565d007ad521fc2 = PR #99 merge, exact match to mission creation SHA). STATUS: BASELINE DONE 2026-09-19 — fetch confirms origin/main at mission SHA (no advance); lint 0err, typecheck clean, test:agent 4837 + same 3 pre-existing study-desktop fails, test:wasm 74/74, parity 25/25, build clean, portable-paths 0 violations. Scout audit + docs/evidence/phase18j-profile-architecture.md DONE. ENGINE DONE (48bfa938: shared fnv1a, profile types/revision/status, topology-exact line + adaptive-arc extraction, inquiry/stats, scoped cache, view display layers, 32 tests). PERSIST DONE (e2b1226a: profile worker op/service/transactions/WNCAD, 17 tests). UI DONE (3ad52dd1/59a11283: Toolspace/manager/inquiry/ribbon/properties/preview, 11 tests). CLOSEOUT DONE (821d4bd2: PROFILE_VIEW_UPDATE, viewport parity, browser specs A-C, perf evidence). VALIDATION 2026-09-19 — lint 0err, typecheck clean, test:agent 4898 + 3 pre-existing study-desktop fails, test:wasm 74/74, parity 25/25, build clean, portable-paths 0 violations. Reviewer verdict reviewer-18j: GO-CAD-SURFACE-PROFILES 15/15, no must-fix. PR #100 opened 2026-09-19, NOT merged.
- [ ] Phase 18I exact TIN-to-TIN cut/fill volume surfaces (branch feat/cad-surface-cut-fill-volumes, baseline origin/main 7e26a547c4d3f146ec5fb4b911c670a0b3348426 = PR #98 merge, exact match to mission creation SHA). STATUS: ENGINE + PERSIST LANDED, UI DONE 2026-09-18 — worker-18i-engine DONE (6 pure volume modules, 17/17 analytic oracles incl. fan-integration bug found+fixed, lint/tsc clean); worker-18i-persist DONE (types/revision/status/cache/protocol/service/transactions/WNCAD, 42 new tests, test:agent 4821 + 3 pre-existing study-desktop fails). worker-18i-ui DONE 2026-09-18 — workspace wiring (per-drawing SurfaceVolumeService + cache, revision-diff notifyMeshBuilt, manual Calculate only) + Toolspace volume rows + manager volume section (create/definition/quantities/Calculate/Delete) + volume style CRUD UI + aggregated CUT/FILL display layers w/ OFF/FROZEN filter + difference inquiry (live-source, CUT/FILL/BALANCED) + Volume Summary CSV (CURRENT-only) + ribbon VOLUME group + SURFVOLUME/SURFVOLCALC/SURFDIFF/SURFVOLREPORT registry + shell snapshotsEqual volume comparison (without it volume-only transitions never republish — root-caused via browser QA) + tests/cad_volume_ui 10/10 + tests-browser/cad-surface-18i 3/3 (flows A-K). Validation: lint 0err, typecheck clean, related suites 80/80, test:agent 4837 + same 3 pre-existing study-desktop fails, test:wasm 74/74, build clean, portable-paths 0 violations. COMBINED TREE COMMITTED f46bf91e (engine+persist atop UI); worker-18i-perfval DONE 2026-09-18 (scripts/phase18iVolumePerf.ts + docs/evidence/phase18i-volume-performance.md committed 5fbc2d34: 1k/10k/50k/100k all ~10us/tri, qty/display bitwise identical, ingest microseconds). FULL VALIDATION: lint 0err, typecheck clean, test:agent 4837 + 3 pre-existing study-desktop fails, test:wasm 74/74, parity 25/25, build clean, portable-paths 0 violations, 69 volume tests + 178 cad/surface/WNCAD/layer suites green, Playwright 18i 3/3. REVIEWER GO-CAD-CUT-FILL-VOLUMES 15/15 2026-09-18 (reviewer-18i, 0 fix rounds). NEXT: PR (do NOT merge). Planned scope: §§1-111 — docs/evidence/phase18i-volume-architecture.md (§2; uniform-grid index, extend surface worker + new service, relationship-only persistence, derived status/revision/cache, delta-plane math core, UI/report plan, A/B/C work split). worker-18i-engine (pure volume math + analytic oracles) + worker-18i-persist (types/revision/status/cache/protocol/service/transactions/WNCAD) dispatched in parallel; UI worker (C) queued after both return. Planned scope: §§1-111 — first-class CadVolumeSurface (base+comparison refs, no persisted derived geometry), volume revision/status/cache, surface-analysis worker 'volume' op with ownership+supersession, uniform-grid candidate index, Sutherland-Hodgman triangle overlap, local-frame conditioning, delta-plane exact fan integration, delta=0 split, machine-epsilon zero policy, compensated summation, analytic oracles (flat/cut/crossing/diff-tin/partial/void/concave/vertex/edge/translation/order/sliver/edge-touch), volume styles + cut/fill SVG display + difference inquiry + Toolspace/manager/ribbon + Volume Summary CSV + WNCAD + browser QA; do NOT merge PR.
- [ ] Phase 18H surface contours, labels, inquiry, and analysis (branch feat/cad-surface-contours-analysis, baseline origin/main 2141d91795071b6ff0c6ebf6b54b1d46c342bc61 = PR #97 merge, exact match to mission creation SHA). STATUS: REVIEWER GO-WITH-CONTOUR-RESTRICTIONS 2026-09-18 (reviewer-18h, 15/15 PASS incl. all NO-GO gates; restrictions: no cross-path label collision engine, saddle split by design, no DXF/SVG contour export, re-screenshot A/B panels-collapsed as evidence hygiene). NEXT: PR (do NOT merge). INQUIRY/STATS/PERSISTENCE SLICE DONE 2026-09-18 — surfaceAnalysis.ts (exact face-plane slope/aspect, edge/vertex disclosure, face stats) + structured slope inquiry/SURFSLOPE/panel modes + build stats (3D area/weighted mean/slope ratios) + WNCAD round-trip/legacy + export-exclusion regression; tests/cad_surface_analysis 10/10 + tests/cad_surface_stats_wncad 6/6; 13 surface/contour/shell suites 175/175; lint 0err/typecheck clean/build clean.
- [ ] Phase 18G CAD surface production hardening (branch perf/cad-surface-production-hardening, baseline origin/main 6d446c70948384b7894c60d2b0b50218eec6b566 = PR #96 merge, exact match to mission creation SHA). Planned scope: §§1-83 — wire production rebuilds through surfaceWorker (one build service + typed client, request ownership requestId/surfaceId/revision/drawing, latest-generation + cross-drawing isolation, cancellation, serialized rebuild-all, real BUILDING/FAILED session state, bounded sync fallback or removal); exact constrained domain classification replacing centroid filter (boundary segments as topology constraints + flood/label, adjacency + constrained-edge flags + validateTinMesh, area/interpolation restricted to retained domain, concave/void/multi-void/touching/invalid oracles); bounded cache lifetime; transfer/memory/display-edge measurement; tests-browser/cad-surface-18g.spec.ts + manual QA at 3 resolutions; all 18F numerical oracles + parity 25/25 preserved; NO contours/volumes; do NOT merge PR. STATUS: BASELINE + RECON DONE 2026-09-18 — origin/main fetched (no advance), branch created, stray untracked phase9l dirt removed, docs/evidence/phase18g-surface-production-path.md written (sync path SurveyCadWorkspace.tsx:730-779 + ponytail marker :727, worker present-but-dead with no `new Worker` caller, BUILDING unreachable, cache unbounded, centroid filter tinDomainFilter.ts:32-40 with boundaries NOT constrained). DOMAIN-TOPO SLICE DONE 2026-09-18 (worker-domain-topo, §§19-35,62,66-71): decision docs/evidence/phase18g-surface-domain-classification.md (flood-from-exterior + equivalence proof) + seam docs/evidence/phase18g-contour-readiness.md; boundary rings as enforced constraints (merge + Z-interp + interior-vertex split), materialized adjacency + edge-kind flags (derived only), flood classification, area/grid over retained only, validateTinMesh, inclusive edge policy, grid wired through cache/worker (query ~1us vs ~40-170us scan), 18 new oracles in tests/cad_surface_domain.test.ts; 18F suites green, NO tolerance loosening, NOT committed. PARENT STACK-FIX + VALIDATION DONE 2026-09-18 — argument-spread overflow (tinBase loop min/max/scale + isCollinearWorld loop min; zero numeric change) had killed 50k worker builds and 100k engine builds; after fix: 50k Chromium production-worker ack→CURRENT 10.8s with 0 page errors, 100k Node 1.68s, 984 KiB small-stack probes pass at both scales, new 50k stack-regression test (domain suite now 19/19). Full battery: lint 0err/2 pre-existing warns, typecheck clean, test:agent 4709+3 pre-existing study-desktop, test:wasm 74/74, parity 25/25, build clean, portable-paths 0 violations, Playwright 18g 3/3. NEXT: reviewer gate, PR (do NOT merge). REVIEWER GO-CAD-SURFACE-PRODUCTION-HARDENING 15/15 2026-09-18 (reviewer-18g, on 9c1e6c2d; 2 non-blocking notes closed after: actual-route buildPath + manual-QA wording, re-validated 97/97 + lint + build). PR #97 OPENED, NOT merged. Decision: GO-CAD-SURFACE-PRODUCTION-HARDENING.

- [ ] Phase 18E drawing-owned F2F catalog + professional feature-code manager (branch feat/cad-f2f-catalog-manager, baseline 43223e16 = PR #94 merge). STATUS: ENGINE DONE 2026-09-18 — drawing-owned catalog + revision + WNCAD + starter + diff + link contract + tests/cad_f2f_18e_catalog.test.ts (18 tests) green; existing link/sync tests updated to hash revisions; lint clean; typecheck clean for touched files (pre-existing runSessionAsync errors from another batch's untracked files remain). UI DONE 2026-09-18 — workspace ownership flipped to drawing-owned catalog (activeCatalog derived from history project; edits via replaceFieldToFinishCatalog/updateFieldToFinishSettings history transactions, one full-replace undoable; starter fallback for new/legacy-no-content, MISSING_LEGACY surfaced); professional manager (FeatureCatalogManager.table/editor/aliases split: search/sort/selection + style dropdowns + attributes + alias validation + CRUD with provenance-count delete warning); 5-step review chain (configure/review/mappings/preview/commit) with full-column review table, unmapped create-definition handoff, explicit Preview→Apply regeneration (GENERATED-only removal, one transaction); file import w/ diff preview + explicit replace/cancel, file export; drawing control-token profile editor wired into review+generation; Toolspace F2F node + ribbon F2F group via openSurveyManager sections; generated-features summary + legacy attach/import UI; review-utils aliases threading (optional param, default {}); link-flow test updated to new step labels; new tests/cad_f2f_18e_manager(.tsx) 17/17; lint clean; tsc clean except pre-existing runSessionAsync dirt. NEXT: regen/QA worker, reviewer gate, PR (do NOT merge). Ownership map DONE (docs/evidence/phase18e-f2f-ownership-map.md): catalog is workspace React state (SurveyCadWorkspace.tsx:310), NOT in CadProject, never reset on new/open; link (metadata.fieldToFinishLink) persists id+version-string only, no content hash; CATALOG_CHANGED unreachable (version display-only, import zero callers); order-sensitive stringify false-dirties; style refs never validated in production. NEXT: engine worker (drawing-owned catalog + revision + WNCAD + starter + diff + link contract + tests), then UI worker (manager + review + import/export + toolspace/ribbon), then regen/QA worker, reviewer gate, PR (do NOT merge). IMPLEMENTATION DONE 2026-09-18 — engine 69134583 + UI 0e336b1d + fixes (ownership map, panel useMemo, stale-banner/key-order test updates, reviewer notes). VALIDATION: lint 0err, tsc clean (except untracked phase9l pollution), test:agent 4621 pass (11 fails all proven pre-existing/pollution: 7 phase9l sibling, 3 study-desktop calibration, 1 manifest-by-pollution), parity 25/25, wasm 74/74, build clean, portable 0 violations, browser F2F + 18C(12) + 17E green (18A standalone fails on pre-existing port mismatch). REVIEWER APPROVE (15/15, 0 blocking). PR OPEN, NOT merged.
- [ ] Phase 18C CAD drawing standards and layer semantics (branch feat/cad-drawing-standards, baseline 8a0107d6 = PR #92 merge). STATUS: BASELINE SCOUT 2026-09-17 — origin/main 8a0107d6 fetched; branch created clean. BASELINE VALIDATION: lint 0 errors (2 pre-existing warns), typecheck clean, test:agent 4451 pass + 1 skip + 3 pre-existing study-desktop fails (calibration/calibration_v5/preflight, unrelated), parity 25/25, wasm 74/74, build clean, portable-paths 0 violations. MODEL INVENTORY: CadLayer (cadTypes.ts:42-60) = id/name/color/lineTypeId?/defaultStyleId?/visible/locked/printable?/lineweightMm?/role — NO on/off vs visible distinction, NO frozen, NO transparency, NO current-layer, NO description; 6 default layers (all visible/locked-false/printable/0.25mm); 2 linetypes (continuous, dash-short, units undocumented); CadStyle = id/name/color?/strokeWidth?/textStyleId?/pointSymbolId?/lineTypeId? (no lineweight/transparency); entities carry NO per-entity color/width/linetype — appearance = styleId->CadStyle with layer fallback + #94a3b8 default; resolveEffectiveColor override param never passed by callers; dangling layer ids `planning` (alignments) + `preview` (COGO preview) hit silent fallback; F2F forces role='points', palette-hashed colors, fixed strokeWidth 1.2, no linetype. EXPORT INVENTORY: DXF ignores layer.visible/printable (only entity.visible; layer flags always 70=0; R12 no 370); SVG/PDF drop hidden+non-plot items upstream in cadExportScene (printable:false beats viewport re-show); LandXML ignores layers entirely (entity.visible only). UI INVENTORY: 18B Layer palette = visibility-toggle only + no-active-layer gap note; renderer keeps hidden-layer primitives intentionally (Option-A prefilter reverted — viewport visible=true re-show depends on it); no LAYER_COLOR/linetype/lineweight/SET_CURRENT transaction keys; WNCAD schema v2, no layer/style migration seam. NEXT: appearance map DONE (docs/evidence/phase18c-appearance-map.md) + normative spec DONE (docs/evidence/phase18c-spec.md). DISPATCHED 2026-09-17: w1-engine DONE (dc898136) + researcher DONE (1d7c3408). WAVE2 2026-09-17: w2-render (resolver viewport/linetype/lineweight/locks/creation defaults) DONE 2026-09-17 (commit Phase 18C render: resolver-driven viewport, linetypes, lineweights, locks, current-layer defaults). + w3-ui (manager/Properties/quick-layer/LAYER/LWT) + w4-export (DXF/SVG/PDF/LandXML/F2F/WNCAD/goldens) running in parallel. W3-UI WAVE 2026-09-17: Layer Manager (LayerPanel rewrite w/ guards), ribbon Layers group, LAYER registry command, LWT workspace toggle, Toolspace Settings Layers+Linetypes, Properties appearance rows (ByLayer-or-explicit + VARIES + LAYER_LOCKED), EDIT_ENTITY entity-layer/appearance kinds, shell actions via LAYER_* transactions (undo-wipe paths killed), currentLayerId guards + general fallback. W4-EXPORT WAVE 2026-09-17 DONE — DXF R12+R2000 retain-all policy (OFF=negative 62, frozen=70b1, locked=70b4, entity.visible=false=group 60; entryStyle via resolver incl. style.strokeWidth gap closure, entity 370 only on layer mismatch) + SVG opacity/PDF ExtGState (opaque scenes byte-identical) + export-scene frozen filter + explicit-appearance correction via resolver + LandXML policy pinned unchanged + §6 policy table in webnet-cad-layout-plotting.md + QA fixture (tests/fixtures/cadQaDrawing.ts: 9 layers, 4 weights, 4 linetypes, transparency/locked/noplot/off/frozen, explicit+ByLayer, text/parcel/point/ellipse) + tests cad_export_18c (6) / cad_wncad_18c (2) / cad_f2f_18c (2: real-model layers, ByLayer entities, rerun-preserves-edits). INTEGRATION 2026-09-17: all waves pushed (88bb0356). Full validation CLEAN: lint 0err, tsc clean, parity 25/25, wasm 74/74, build clean, portable 0 violations, test:agent 4506 pass + only 3 pre-existing study-desktop fails. phase9l sibling files restored untouched. PENDING: browser QA, reviewer gate, PR (do NOT merge). QA DONE (11/11 Playwright, D/E/G bug -> ByLayer-create fix bc7dc46f -> D/E/G/J PASS). REVIEWER REQUEST-CHANGES -> R1-R4 fixed (a828966c). PR #93 OPENED 2026-09-17, NOT merged. DECISION: GO-WITH-DRAWING-STANDARDS-RESTRICTIONS.
- [ ] Phase 18F surface persistence + transactions + worker (branch feat/cad-surface-tin-foundation, baseline c9be1fe7; engine worker owns cadTypes surface model + cadSurfaces triangulation). STATUS: PERSIST SLICE DONE 2026-09-18 — WNCAD persists surfaces/surfaceStyles (additive, still v2; legacy => empty + seed styles; meshes never persist; reopen clears cached revisions); 16 undoable surface commands + style CRUD on the history seam (layer-lock gated; builds never dirty); scoped surface cache (no global singleton) + surfaceWorker (request id/revision, cancel/supersede, latest-wins, stale discard, failure diagnostic); `surfaces` layer role + factory (no default-layer seeding); new tests/cad_surface_wncad (11) + tests/cad_surface_transactions_worker (7) green; key-order settling regression included (migrate/sanitize append surfaces/styles after catalog backfill to match cloneCadProject, else the JSON-signature sync guard ping-pongs and clobbers UI state — proven via worktree bisection against surveyCadWorkspace.25/.32/.33). VALIDATION: lint 0err, focused + neighboring suites green (drawing/wncad/appearance/persistence/f2f/layers/export/draft/history/cogo 200+), test:agent shows only other workers' in-flight breakage (phase9l runSessionAsync, unlisted phase9l evidence file, study-desktop) + load flake, build clean. NEXT: engine triangulation landing is the other worker's; UI wiring is a later slice; do NOT merge.
- [ ] Phase 18F surface UI display + Toolspace + manager + ribbon + inquiry (branch feat/cad-surface-tin-foundation, baseline c9be1fe7; engine + persist slices own triangulation/transactions/worker). STATUS: UI SLICE DONE 2026-09-18 — display adapter (src/engine/cad/cadSurfaceView.ts: derived TIN layers alongside CadDisplayScene, one multi-segment SVG path per component with deduped interior edges, boundary = use-once edges; OFF/FROZEN hides via viewport filter, exports untouched; stale NEEDS_REBUILD/FAILED/BROKEN keeps old mesh dashed + STALE badge, never CURRENT; meshes session-only so reopen has no stale mesh); Toolspace Surfaces tree (status as TEXT, Definition + Statistics children, right-click Properties/Rebuild/Delete); manager palette (create Name/Layer/Style auto-naming, list Name/Style/Layer/Status/Verts/Tris, properties rename/layer/style/rebuild/confirm-delete, definition editor for point-group/XYZ-points/chain+entity breaklines with Z gate + F2F-explicit checkbox/outer-void boundaries with closed-ring validation + outer replace-confirm, inquiry E/N + one-shot viewport pick, no geometry created); Properties SURFACE + STATISTICS + DEFINITION (counts only, no mesh dumps); ribbon SURFACE tab (Create/Definition/Build/Inquiry/Style, no contour/volume) + SURFACE/SURFACEMANAGER/SURFCREATE/SURFREBUILD/SURFELEV registry commands; rebuild runs the pure engine build synchronously into the session cache (never history/dirty; ponytail-marked worker upgrade path); viewport picks map to the surface object, entity selection untouched. Shape note: UI consumes the converged cadTypes shape (single pointSource, entity-ref breaklines/boundaries) — the approved cadSurfaceTypes repoint was NOT applied (siblings converged executors/cache/worker/triangulation on cadTypes mid-slice; repointing would break 16 registered commands); deviations from brief: auto-naming "Surface N" (not "Surface1"), no Description field (not in model), single point source with replace semantics (no multi-group attach). VALIDATION: lint 0err, tsc clean except 4 pre-existing phase9l runSessionAsync errors, tests/cadSurfaceUi 9/9, shell panels/registry mocks extended, test:agent 4667 pass (11 fails all pre-existing: 6 phase9l sibling dirt + 1 manifest-by-pollution + 3 study-desktop calibration + 1 phase9l evidence), build clean. do NOT merge.
- [ ] Phase 18Z scale exact Surface composition to large TINs (branch perf/cad-surface-composition-scaling, baseline origin/main fb33d293d965f22f0bd2d52ba78f3ebe72f1ba2d = PR #116 merge, exact match, fetch-verified no advance). STATUS: BASELINE FETCH DONE 2026-09-26 — origin/main fb33d293, no advance; branch created. Scout recon DONE (engine/shell/worker spans, 18Y oracles, perf harness, pipeline, hot spots, WNCAD/provenance, dry-run limit 25k). BASELINE VALIDATION DONE 2026-09-26 — lint PASS (0 err/2 pre-existing warns), typecheck PASS, test:agent 5779 pass + 3 pre-existing study-desktop frozen-calibration/preflight fails (carried), build PASS, portable-paths PASS (4485/0), 18Y compose suites 10/10 + 16/16 green. Indexed recovery prototype DONE (tinConstraintRecoveryIndexed, 12/12 + 900-case fuzz clean, ~8x). Fast paths DONE (73x overlay/953x disjoint @10k, 64/64). NEXT2 dispatched 2026-09-26: opt-in promotion onto compose path (flag + call-site, reference stays old-path), randomized parity corpus (~25 cases, new test only), 18Z perf harness + ACTUAL 50k/100k (evidence tier). Awaiting handoffs. strategy matrix, fast paths + component partition + seam-local compose, indexed recovery/legalization prototypes, randomized parity corpus, actual 50k/100k runs, browser QA, reviewer gate, PR (do NOT merge).
- [ ] Phase 18A standalone CAD application extraction (branch feat/cad-application-extraction, baseline 2a30427c = PR #90 merge). STATUS: IMPLEMENTATION DONE 2026-09-17 — `src/App.tsx` dispatches `/study` -> StudyApp, `/cad` -> route-lazy `src/cad-app/CadApp.tsx`, else AdjustmentApp; CadApp owns its drawing/dirty/save-target via `useCadAppController` (no parser/solver/QC, never instantiates useAppController; isolation proven by throwing-mock import tests both directions). Bridge `src/cad-app/cadSourceBridge.ts` (engine-free): versioned localStorage snapshots (`AdjustmentSourceSnapshot`: stations only + reused 17B AppliedRunIdentity, deterministic `cad-src:<projectId>:<resultFingerprint>` ids) + per-project latest registry + legacy migration slot; URL carries only `?source=<id>` / `?migrate=1`. Adjustment "Send to CAD" requires FRESH_SUCCESS (via existing adjustmentF2fSource gate), publishes + navigates, never mutates drawings; toolbar CAD button navigates to /cad; `survey-cad` tab + live linked-rerun sync (`applySuccessfulAdjustmentRunToDrawing`) removed; .wnproj stays CAD-free; legacy decoders kept (project.surveyCad loads dormant + banner stages it as migration candidate, never auto-current). 17E preserved: import stamps snapshot identity, chip compares against registry latest (A CURRENT on import / B STALE after newer publish / C CURRENT after refresh / D unresolvable never CURRENT / E manual independent). Titles: /cad "WebNet CAD", / "WebNet Adjustment". Bundle: CadApp 6.78kB lazy shell; index chunk has zero CAD markers (was: embedded prop wiring); survey-cad chunk lazy via /cad only; no report/QC in CAD chunks. Race-guard fix 2026-09-17 — browser E exposed a pre-existing adoption/persistence ping-pong (any external drawing update thrashed blank<->imported; `useSurveyCadWorkspacePersistence` now keys off internal changes only + skips when the project signature moved underneath; content-compare keeps the legacy persisted-state fallback working; regression test added). Tests: new tests/cad_app_bridge (14) + cad_app_controller (7) + cad_app_adjustment_isolation (1); browser tests-browser/cad-standalone-18a.spec.ts 6/6 Chromium zero page errors (direct load, reload, isolation+nav, invalid token, seeded import->CURRENT, dirty guard dismiss/accept); updated projectOptionsModalInteraction.01 toolbar test to assert /cad navigation. Reused: surveyCadWorkspace 141 + cad_drawing_file/cad_dependency_gates/cad_import_ownership/project_bundle suites. VALIDATION 2026-09-17 — lint 0 errors, typecheck clean, test:agent 4426 pass + 1 skip with only the 3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build clean, portable-paths 0 violations. Stage-2 deferrals: ribbon/Toolspace/docking/tabs/Layer-Manager/properties/statusbar redesign; DWG/DGN/cadcodec/renderer/solver/F2F work; workspace-recovery CAD ride-along untouched; NO GPL copy (inspiration only).
- [x] Phase 17E adjustment-cad-dependency-integrity (branch feat/adjustment-cad-dependency-integrity, baseline e4ebbd68 = PR #89 merge). STATUS: DONE 2026-09-17 — PR #90 opened, NOT merged. Decision: GO-CAD-DEPENDENCY-INTEGRITY-PRODUCTION. GATES-UI DONE 2026-09-17 — exportCenter depOpts opt-in gate (svg/pdf/dxf-r12/dxf-r2000/landxml block with [reason] + blockMessage; MANUAL_ONLY/CURRENT pass; wncad/catalog never block) + new cadDraftLabelDependency.ts (derived=has sourceEntityId, STALE on missing/broken source, MANUAL source stays CURRENT-capable; no text rewrite) + workspace resultDependencyIdentity prop, useMemo summary (draft-stale forces STALE), single text chip (CURRENT/STALE/NEEDS REVIEW/MANUAL-ONLY + cause + action hint) near file-status, gate data passed to ExportCenterPanel (new optional props) + import call-site updated for sibling identity threading (guard + button disable when identity null) + tests/cad_dependency_gates 15/15 (gate matrix, label gate, round-trip matrix, undo/redo coherence). VALIDATION: new 15/15, export/drawing/history suites 118/118, eslint clean, tsc clean except sibling in-progress breakage (cad_drawing_file.test 1 fail + tsc identity error from sibling cadAdjustedPointsImport change — files not touched, reported). IMPORT-REFRESH DONE 2026-09-17 — owner-safe import MODEL A (F2F-owned stations skipped + byte-preserved, link untouched, skippedF2fStationIds on record + report row; absent-station import entities kept; every created entity stamped; parcel refresh follows moved stations with recomputed metrics + stamp, orphans/manual untouched) + linked-rerun CURRENT-sync F2F stamping (fail-closed absent=no-stamp; conflict/drift stamps nothing) + identity threading useAppController→AppShell→AppWorkspaceLayout→SurveyCadWorkspace→import fn (null blocks with same message) + SuccessfulAdjustmentRunInfo.resultDependencyIdentity from apply-time identity. Cycle fix: cadAdjustedPointsImport uses a local F2F-provenance mirror instead of importing cadGeneration (which pulls cadLabelEngine early and trips the cadCogoParcel* star-export cycle — surveyCadWorkspace.26 proven via isolated worktree bisection). Tests: new tests/cad_import_ownership 8/8 (skip worst-case, negative control + rerun moves, stamp CURRENT, SOURCE_MISSING keep, new station, parcel refresh, sync stamp/no-stamp) + cad_drawing_file identity update. VALIDATION: focused 111/111 (9 files), eslint clean (touched), tsc clean; agent tier shows only pre-existing fails (3 study-desktop calibration/preflight) after the cycle fix.
- [x] Phase 17D crs-export-coordinate-integrity (branch feat/crs-export-coordinate-integrity, baseline 8e869f34 = PR #88 merge). STATUS: BASELINE SCOUT DONE 2026-09-17 — origin/main 8e869f34 (PR88 MERGED); branch created clean (untracked phase9l pollution removed, typecheck clean). CRS-CORE: 280-row catalog (57 Canada + 223 US SPCS), proj4-only no WKT, DEFAULT_CANADA_CRS_ID=CA_NAD83_CSRS_UTM_20N, resolveCrsDefinition silent fallback, no runtime CRS provenance (ExportIntegrityMetadata.crsProvenance unwired), crsId persists save/reopen while crsLabel/transform/model reset, result.stations = solve-frame metres (grid EN or local), lat/lon derived via inverseENToGeodetic, datum ops are labels only (no grid-shift files). EXPORT-INV: 15 paths; no CRS step in export path; GeoJSON emits [E,N,H] metres with no .crs (MUST FIX); LandXML adjustment omits CoordinateSystem; industry listing silent default CRS fallback; export UI shows no CRS/space/units; GNSS/CAD exports bypass 17B gate (CAD feed gated via DOWNSTREAM_GEOMETRY). VALIDATION BASELINE: lint 0err/2 pre-existing warns, typecheck clean, test:agent 3 pre-existing study-desktop fails (calibration/calibration_v5/preflight); parity/wasm/build green (25/25, 74/74, 8.7s). MATRICES DONE + REVIEWER APPROVE 2026-09-17 — 23/23 matrices (LOCAL/EXPLICIT/INVALID tables, NB cross-format oracle, save/reopen, 17B/17C, vertical, goldens, perf 10k 216ms/20k 236ms); reviewer 15/15 PASS, GO-CRS-EXPORT-INTEGRITY-PRODUCTION; full validation lint 0err/typecheck clean/agent 4353+3 pre-existing/parity 25/25/wasm 74/74/build clean; pushed. BROWSER GATE PASS 2026-09-17 — new crs-export-harness.html + src/dev/crsExportHarness.tsx + tests-browser/crs-export-17d.spec.ts (real NB LSAEngine solve in Chromium; A lon/lat≈-66.5/46.5 + EPSG:2953 LandXML; B local CSV ok + GeoJSON FORMAT_REQUIRES_GEOGRAPHIC; C EXPORT_CRS_INVALID no fallback; D CRS change STALE; E label-only FRESH; zero page/console errors; 1 passed). PR #89 opened, NOT merged. STATUS: CORE DONE 2026-09-17 — new src/engine/exportCoordinateContext.ts (derived provenance EXPLICIT|PROJECT_DEFAULT|LOCAL|UNKNOWN_LEGACY|INVALID, stable gate codes EXPORT_CRS_INVALID/CRS_UNKNOWN/CRS_TRANSFORM_UNAVAILABLE/FORMAT_REQUIRES_GEOGRAPHIC/AXIS_ORDER_UNRESOLVED, 17B-integrity-first ordering, integrityProvenanceForContext wiring; resultIntegrity crsProvenance union widened, crsLabel still out of math freshness); GeoJSON strict [lon,lat] no-Z via inverseGrid + range checks, natively-geodetic direct path, LOCAL/UNKNOWN/INVALID/unsupported/range-fail BLOCK (never [E,N], no .crs member); LandXML <CoordinateSystem> EXPLICIT-only from registry metadata, values exact, INVALID blocks; industry listing honest CRS display (LOCAL/UNKNOWN/INVALID(id), silent default kept for plumbing only); text report authoritative CRS/units/grid-ground header block; useExportWorkflow CRS gate after 17B verdict (webnet diagnostic path stays reviewable); ExportCoordinateSummary in Other Files + Adjusted Points cards (informational); transformed-CSV column-semantics audit notes (ids kept stable). Tests: new tests/phase17d_export_coordinate_integrity.test.ts 15/15 + intentional updates (browser_exports geojson [lon,lat], export_artifacts grid-CRS geojson, run_results_text header indices, exportWorkflowState.01 grid geojson + LOCAL-block pin, harness parseSettings override). VALIDATION 2026-09-17 — lint 0 errors, typecheck clean, agent 4330 pass + 1 skip + 3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build clean. Committed on branch, NO PR, NO merge. MATRICES DONE 2026-09-17 — new tests/phase17d_export_matrices.test.ts (490 lines, 23/23): LOCAL/EXPLICIT/INVALID policy tables, NB-stereo-double (EPSG 2953) cross-format oracle (CSV/LandXML/report exact project space, GeoJSON == inverseGrid <1e-9 deg), GeoJSON engine oracle (NB + UTM21 + native-geodetic, axis order lon~-66.5/lat~46.5, no crs member, metre-array range guard), projectFile round-trip (EXPLICIT identical, LOCAL stays LOCAL, legacy-without-id never EXPLICIT), 17B (crsId stales math, crsLabel-only stays fresh, STALE primary over CRS, rerun re-allows), 17C (ft sources normalize via linearToMeters, one coherent project space, ft display is pure scale), vertical (Z = solve-frame h, GeoJSON omits Z, geoid never changes values), inline GeoJSON/LandXML goldens (m + ft, CoordinateSystem EXPLICIT-only), perf 10k 216ms / 20k 236ms (<3x, agent-tier fast). VALIDATION 2026-09-17 — lint 0 errors (2 pre-existing warns), typecheck clean, 17D 15/15 + 23/23, agent 4353 pass + 1 skip + 3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build 8.70s. Committed on branch, NO PR, NO merge.
- [x] Phase 17C import-unit-reimport-integrity (branch feat/import-unit-reimport-integrity, baseline b60809d3 = PR #87 merge). STATUS: CORE DONE 2026-09-17 — new src/engine/importUnitProvenance.ts (units, exact factors, UNKNOWN set, confirm helper) + src/engine/importSourceIdentity.ts (fnv1a fingerprint, duplicate/revision classification, atomic replace, station conflicts, adjusted-points guard); ImportedDataset gains sourceUnits/needsUnitConfirmation/contentFingerprint/columnMapping; manifest + run files gain sourceUnits/contentFingerprint/sourceKey (legacy-optional); registry wrapper attaches unknown-legacy+BLOCKING UNIT_UNKNOWN to unit-less formats; terrestrial-CSV requires explicit units (default path BLOCKING), missing-Z -> 2D + HEIGHT_MISSING (no silent 0), mapping surfaced; F2F explicit-m confirmed; JobXML Deleted=true counted INFO; warning codes + StagedSourceSummary added. Tests: new tests/phase17c_import_integrity.test.ts 23/23 (m/ft solve oracle, ft-vs-us-ft 0.6mm/km, sigma scaling, conversion-once, CSV/identity/repeat/station/provenance pins); updated phase2 (4) + importReview.01 (1) trace expectations. Importer matrix: source-declared = native-DAT/GNSS-BL/LandXML; UNKNOWN = dbx/jobxml/rw5-tds/fieldgenius/survey-report/opus/gvx/gnss-csv/terrestrial-csv-default. VALIDATION 2026-09-17 — lint 0err/2 pre-existing warns, typecheck clean, agent 4295 pass + 1 skip + 3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build 8.78s. CLOSE-OUT 2026-09-17 — GAP1 ImportReviewSourceSummary wired (per-source rows, unit selector, BLOCKING commit gate, dup/revision consequence text; confirm reparses rawText + rescales once) + GAP2 C/E/CH/P redefinition warnings (component-defined tracking, C+E quiet) + review STATION_DEFINITION_CONFLICT on staged merge + GAP3 GNSS sourceUnits direct (BL source-declared, GVX/CSV unknown-BLOCKING, CSV unitsConfirmed opt-in) + tests/phase17c_closeout.test.ts 13/13 + 1 CSV assertion update. Validation: lint 0err/2 pre-existing warns, typecheck clean, agent 4308 pass + 1 skip + 3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build 8.74s. REVIEWER-FIX 2026-09-17 — M1 GNSS panel unit selector (m/mm/cm) + BLOCKING adjust gate (CSV confirm reparses retained text with unitsConfirmed; GVX confirm-metres, non-metre GVX via CSV path) + L1 legacy-snapshot fail-closed (never labelled confirmed, re-select required) + L2 removed dead landxml SOURCE_DECLARED entry + L3 HEIGHT_MISSING on non-numeric elevation + L4 reparse-failure message keeps commit blocked; new tests/importReviewUnitConfirm.test.tsx (L1/L4 fail-closed 3/3) + M1/L3 focused adds. Validation: lint 0err (2 pre-existing warns), typecheck errors only in untracked pre-existing phase9l file, focused 45/45, agent 4320 pass + 1 skip with only pre-existing fails (3 study-desktop + phase9l dirt, stash re-proven), parity 25/25, wasm 74/74, build 8.76s. PR #88 opened 2026-09-17, NOT merged. Reviewer verdict GO-WITH-IMPORTER-RESTRICTIONS (15/15 gates PASS; M1/L1-L4 fixed).
- [x] Phase 18B professional CAD shell (branch feat/cad-professional-shell, baseline be584c23 = 18A head). STATUS: DONE 2026-09-17 — thin `src/cad-app/shell/` (16 files, all ≤340 lines) over SurveyCadWorkspace (viewport only, engine untouched): CadApplicationShell (quick-access + menu/ribbon/tabs/docks/command dock/statusbar), ONE registry (56 session defs + 10 chrome actions; aliases L/PL/M/CO/TR/EX verified vs ActiveCommandKey) routing menu+ribbon+dock+context menu to existing starters, CadShellLink bridge (workspace publishes snapshot + registers actions; cursor on a separate channel), docks (left/right, resizable/closable/movable, persisted `webnet.cad.shell.v1` + Reset, panel error boundaries), Toolspace (Prospector/Survey/Settings over real data + empty states), Properties (none/single/multi + VARIES via existing edit path), Layers (existing LayerPanel + no-active-layer gap note), Start tab + single-doc tabs (multi-doc deferred w/ restriction notes), Model/layout tabs over real draft.sheets, dirty title + parked-close guard, WNCAD open/save unchanged. Workspace diff: +223 lines (shellLink/shellChrome props, starter map, publish/actions, chrome hiding), hook +1 return field (pointerWorldPoint), SheetWorkspace +initialView. Researcher doc reused + §9 adoption notes. Tests: new cad_shell_registry (8) + cad_shell_layout (3) + cad_shell_panels (14) = 25/25. VALIDATION — lint 0 errors, typecheck clean, agent 4451 pass + 1 skip (3 pre-existing study-desktop fails, stash-proven baseline), parity 25/25, wasm 74/74, build clean, portable-paths 0 violations, browser 5/6 (18A-C fails identically on baseline = pre-existing dev-server networkidle flake). Deferred 18C: per-tab workspace state (true multi-doc), auto-hide/floating docks, current-layer engine model + quick-layer dropdown, layout rename/reorder, transparent commands/keyword buttons, full entity index for Prospector. REVIEWER-18B FIXES 2026-09-17 — (1) Option B (Option A reverted: pre-filtering buildCadDisplayScene broke the documented export semantic — viewport visible=true re-show of project-hidden layers — and failed tests/cad_draft_deliverables; display scene intentionally keeps hidden-layer primitives for export): CadLayerPalette gap note now states hiding does not hide viewport entities yet (18C), LayerPanel gains visibilityRequestOnly prop rendering request-hide title/aria, shell palette passes it. (2) CadDrawingTabs header reworded to confirm-discard park behavior (no 3-way modal). (3) Phantom ribbon/status layer-control sentence removed. (4) Counts already 56+10 in entry (recount: 56 session() + 10 action() calls in cadCommandRegistry.ts). (5) Screenshots docs/evidence/phase18b/*.png untracked, zero citations anywhere, prior phases committed no PNGs — left uncommitted, nothing to drop. (6) snapshotsEqual now includes layerEntityCounts (Toolspace counts republish on cross-layer moves) + status-bar OSNAP-subset-vs-Settings-14 comment.
- [ ] Phase 16C structured-weight statistics operator (branch perf/structured-weight-statistics-operator, baseline 0b838a8a = PR #84 merge). STATUS: BASELINE SCOUT DONE 2026-09-17 — 16B policy (m>=128, densityUB<=0.02, non-Huber; oracle+telemetry+kill switch; iteration+recovery structured; stats still dense-P handling; weightAt closures exist; LOO/auto-adjust re-route per solve; Huber/preanalysis-exclusions + native/sparse untouched). STATS P-READ INVENTORY DONE 2026-09-17 — 12 consumers (C1 assembly write; C2/C3 Huber dense + C4 structured twin; C5 row-product packing accepts structured; C6 normal accumulation = hard blocker, structured twin exists in structuredWeightOracle.ts but unwired; C7 weightAt has structured branch; C8 MDB sensitivity coupled-column; C9 external QxxA'Pe_i coupled-column; C10-12 stochastic via weightAt). P-vs-Qll separated (redundancy/w/tau/chi2/SEUW/GPS/precision = Qxx+Qll, no P). Admitted cohort (no WASM row-products) = 1 dense P at adjustmentEquationAssembly.ts. WORKER DONE 2026-09-17 — GO (no KEEP-DENSE-STATS): one WeightAccess file (src/engine/structuredWeightAccess.ts) + read-site wiring + tests/phase16c_statistics_structured.test.ts (33/33); stats dense-P 1->0 admitted (total dense-P 1->0/session), full-result JSON parity ON-vs-kill-switch on chain-200/GPS-168/sparse-TS/dense-TS-fallback/Huber/small fixtures/LOO/auto-adjust; external vector parity + off-diag negative control; fault injection null-closed; telemetry statsDense/statsStructured/weightAt/coupled/fallback+reason; kill switch structuredStatisticsWeights; validation lint 0err/typecheck clean/agent 4237+3 pre-existing study-desktop/parity 25/25/wasm 74/74/build 8.69s; bench chain-80 x7 ratio 1.039 (noise), chain-200 218.6 vs 216.4ms; m=500 payload -2MB/session (8/32MB at m=1000/2000 by m^2*8). Commits local, NO merge/push. REVIEWER APPROVE 2026-09-17 — 15/15 PASS (0 blocking, 5 low/info notes; 1152-line file accepted as pre-existing over-cap, follow-up split suggested). FINALVAL 2026-09-17 on HEAD 68ac303e: lint 0 err (2 pre-existing warnings), typecheck clean, agent 4237 + same 3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build 8.72s, portable-paths 0 violations. PR opened, NOT merged.
- [ ] Phase 16B structured-weight solve transfer (branch perf/structured-weight-solve-transfer, baseline de6c0aa3 = PR #83 merge). STATUS: BASELINE DONE 2026-09-17 — lint 0 errors (2 pre-existing warnings), typecheck clean, test:agent 4171 pass + 3 pre-existing study-desktop fails, parity 25/25, test:wasm 74/74, build clean. SCOUT TRACE DONE — dense P allocated at exactly one site (adjustmentEquationAssembly.ts:82) + 2 structured->dense conversions (:357,:359); n+2 dense-P per solve; 2 provable dead densifications (preanalysis statistics never read; covariance-recovery P dead after row-major accumulation); only hard dense consumer = external reliability. Route B Level 1 first (structured transfer/storage, dense accumulators retained); Level 2 only if gates pass. WORKER A DONE 2026-09-16 (62f37ad0) — oracle + telemetry + kill switch + covariance structured (m>=128) + preanalysis dead-P; 4197 pass, parity/wasm/build clean. WORKER B DONE 2026-09-17 — DECISION: GO-WITH-HYBRID-THRESHOLD (Level 2 ships under policy; see docs/evidence/phase16b-level2-decision.md). Iteration loop + recovery route structured (m>=128, density<=0.02 pre-UB + exact post-gate, Huber dense, kill switch); gates A/B/C PASS (m=2000: 1.86x chain / 1.38x TS-250x8, N+rhs bitwise; 11 families exact; blocks coupled + negative control); vTPv 1-4 ULPs (<=1e-12); ts-setup dense-extreme routed dense (0% regression); full-result JSON identical (6 fixtures + m=200 + LOO primary/alternate + auto-adjust with real removal); BEFORE/AFTER m=200: dense-P 7->1 allocs, -85.7% bytes, attribution exact; wall no-regression + memory leg; exports pinned 23/23. Validation: lint 0 errors, typecheck clean, test:agent 4204 + 3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build clean. New: tests/phase16b_iteration_structured.test.ts (agent 7/7) + tests/evidence/phase16b_iteration_gates.test.ts (21/21) + tests/evidence/phase16b_full_parity.test.ts (12/12); bench updated to hybrid routing. REVIEWER APPROVE 2026-09-17 — 15/15 PASS (equivalence, families, GPS/GNSS/TS/controls, Huber-dense gate, solve-local lifetimes, no stale, native/sparse unchanged, stats identical P, full parity, exports, dense extreme, gain worth complexity; UB/backstop, no tolerance widening, vTPv ULP acceptable). Portable-paths clean. PR opened, NOT merged.
- [x] Phase 16A structured-weight-matrix evidence (branch feat/structured-weight-matrix-architecture-audit, baseline 4a733b7c). STATUS: DONE 2026-09-16 — EVIDENCE ONLY, production diff empty (src/ cpp/ bit-identical). New: tests/evidence/phase16aWeightEvidenceShared.ts + phase16a_weight_sparsity.test.ts + phase16a_structured_prototypes.test.ts (evidence-tier registered) + docs/evidence/phase16a-structured-weight-evidence.md. Findings: P density 0.008–0.25 (TS 8-wide groups locally dense, ~g^2/m); structured storage 1–25% of dense 8m^2; sparse assembly ~2x faster than dense at m~128 (18.1 vs 37.4 ms at m=2000); prototype N bit-identical on scalar/GPS/controls, <=5.89e-15 rel on TS/mixed (summation order), vTPv/SEUW match; m>=2000 stress passes. Validation: evidence 2/2, tier manifest 11/11, lint 0 errors, typecheck clean. REVIEWER ROUND DONE 2026-09-16 — (a) dense-ONLY vs sparse-ONLY re-timing reverses the small-m claim (sparse comparable/slower at m<=128, wins via alloc-avoidance at scale: 18.1 vs 37.4 ms at m=2000); (b) added GNSS-baseline 3x3, corrXY controls, setup-scope + angle TS producers; free-network datum noted as separate assembler; (c) 6 overdetermined solve-based SEUW rows (0 to 1.7e-16) + proto-accumulate timing column; (d) production structured robust path bit-identical (0/True/0) on 4 cases; Qvv/packing/recovery out-of-scope list in md §9. NOT merged, NOT pushed.
- [ ] Phase 14F statistical-QC integration audit (branch feat/statistical-qc-integration-audit, baseline 6ad54fbc = PR #76 merge). §§13-17 SETTINGS/LEGACY/AVAILABILITY/WARNINGS DONE 2026-09-16 — settings audit: defaults conservative+compatible (legacy-fixed local, legacy-3.29 MDB, robust OFF, suspect auto), no default changes; Special tab fixes (Robust k disabled when OFF, legacy compatibility notes, robust-approximation section note); new src/engine/qcAvailability.ts (§16 six-category taxonomy, missing-summary not-analyzed lines) + src/engine/qcRestrictionNotes.ts (§17 eight canonical notes); ReportRunSummarySections missing-summary branches categorized; tests/qcAvailabilityNotes 4/4; lint 0 errors, typecheck clean, parity 25/25, agent tier 4109 pass + same 3 pre-existing study-desktop fails. STATUS: BASELINE DONE 2026-09-16 — HEAD 6ad54fbc (=PR #76 merge); lint PASS, typecheck PASS, test:agent 4087+1skip with only 3 pre-existing study-desktop calibration fails, parity 25/25, wasm 74/74, build PASS. RECON DONE — QC inventory (§2) + terminology/filesize audit (§4) delivered. ROUND A DISPATCHED 2026-09-16 (14e-split / heuristic-labels / corpus). 14F §§18,32-37,46 SPECIAL/CONTRADICTION/WORDING AUDIT DONE 2026-09-16 — corpus stories: A chi-PASS+3 local FAILs, D chi-FAIL+0 local fails, B/C consistent, E GPS (mdb 1/3, coordEff 2/3, loo 0/0), F huber (local approx + reliability approx + stochastic unavailable-with-reason, coherent), G free (residual/local valid, coordEff 0/162 unavailable-with-reason, LOO shift fail-closed, no 0.0mm). Wording: no must-agree claims (local = suspect-never-proven; LOO = what-if comparison-only; data-check = approximate-only, no LS stats); GPS per-component E:N + aggregate OR/max documented, no scalar flattening; TS section descriptive-only, verdicts reflect correlated weights per STATISTICAL_TESTING.md. ONE FIX: §46 'bad observation equation count' -> 'invalid ...' (worker fail-closed guard, src/workers/adjustmentNativeFullQxxAutoRoute.ts). FINALVAL DONE 2026-09-16 — HEAD f285991d (branch feat/statistical-qc-integration-audit, 15 commits, tree clean); lint PASS (0 errors, 2 pre-existing warnings), typecheck PASS, test:agent 4126 pass + 1 skip with ONLY the 3 pre-existing study-desktop fails (calibration, calibration_v5, preflight; 0 study-desktop files touched by branch, no stash-check needed), parity:industry-reference 25/25 PASS, test:wasm 74/74 PASS, build PASS. REVIEWERS: stats REQUEST-CHANGES -> MORE-WORK -> microfixes done; UI MORE-WORK -> microfixes done. DEFERRED LOWs: keyboard row selection, mobile/narrow coverage detail. NOTE: 0b20fa17 content correct, message mislabeled (duplicates 258dc497 subject) — relabel at squash.
- [ ] Phase 14E statistical-QC systematic patterns (branch feat/statistical-qc-systematic-patterns, baseline b76df8cc = PR #75 merge). STATUS: SCOUT AUDIT DONE 2026-09-16 — full inventory (setup/direction/residual/typeSummary/14A-14D, 5 undocumented suspectScore/severity heuristics as bare Score columns, no per-equation Cvv on main route, facePairDelta absolute-only at parse, stdRes absolute except GPS components, sourceLine = doc order not time, no setup ID in engine). RESEARCHER DONE 2026-09-16 — Teunissen Testing Theory + NQC full-text: pattern test T_q = e'Qy-1C[C'Qy-1QvvQy-1C]-1C'Qy-1e ~ X2(q), never invert Qvv, identifiability pre-check c'Qy-1QvvQy-1c, orientation absorbs per-set constants, B-method multiplicity, naive IID runs/Pearson/OLS invalid. DECISION: GO-DESCRIPTIVE-DIAGNOSTICS-ONLY (no formal p-values; covariance-aware tests need Qvv exposure + dense oracle + Monte Carlo beyond this phase). ENGINE DONE 2026-09-16 (5db2e579) + UI/DOCS DONE (c5fb4105) + REVIEWER FIX ROUND (7292f17a). REVIEWER VERDICT 2026-09-16: GO-DESCRIPTIVE-DIAGNOSTICS-ONLY, 18/18 gates PASS, no must-fix. PR #76 opened, NOT merged. UI+DOCS DONE 2026-09-16 — SystematicPatternSection.tsx (summary + setup/distance/face/repeat/zenith/leveling/GNSS/sign-runs, DESCRIPTIVE badges, fact-only tooltips, source-line links, overflow-x bounded, hidden preanalysis/data-check; mounted ReportViewTopSections after StochasticDiagnosticsSection), suspectScore/severity relabeled Heuristic score/severity with tooltip + text-export (heuristic) header annotations (fields kept), STATISTICAL_TESTING.md 14E section + CURRENT_BEHAVIOR link, tests/reportSystematicPatterns 5/5 + layout update, lint/typecheck clean, test:agent green except 3 pre-existing study-desktop fails, parity 25/25, build clean.
- [ ] Phase 14D statistical-QC leave-one-out influence (branch feat/statistical-qc-leave-one-out, baseline 4fb36b24 = PR #74 head). STATUS: SCOUT AUDIT DONE 2026-09-16 — sole what-if re-solve path is suspect-impact (runSessionSuspectImpact.ts + divergent directRunDiagnostics.ts twin, unreachable); candidates = local-FAIL OR |StdRes|>=2, cap 3, stable-sort ties; observation-level excludeIds (GPS whole-vector, TS full rebuild, direction-set recompute, no singular guard); exact full re-solve via solveCore/solveEngine with cached parse; robust re-runs unlabeled; free-network raw unaligned max-3D shift (datum-unsafe); heuristic score -40·dSEUW-20·dMaxStd-15·shiftMeters ±20 chi (displayed, no statistical basis); aborted-but-returned alt solves score as ok-improvements (seuw=0); auto-skip >5s main solve only; mode auto/on/off default auto; no AbortSignal; saved runs persist diagnostics with string-only staleness. ENGINE WORKER 1 DONE 2026-09-16 (commit 0e814901, pushed) — shared builder suspectImpactShared.ts, transparent sort (score deprecated), expanded rows, aborted-alt fail-closed, free-network shift fail-closed, shift detail+top-5, robust flag, cache-key builder, 27 new tests, lint/typecheck clean, parity 25/25, test:agent 4047 + 3 pre-existing study-desktop fails (stash-proven). REVIEWER REQUEST-CHANGES / MORE-EVIDENCE-REQUIRED 2026-09-16 — gates 1+2 FAIL (circular exactness test bypasses production path; family-contract evidence gaps), advisory 12+14 FAIL (direct-path robust label; cancel race). FIX WORKER 3 DONE 2026-09-16 (commit 9424040d, pushed) — production-path session exactness test (exclusions+override+merge threading), cancel-race fix + tests, GPS/correlated-TS/direction-set family tests, direct-twin DELETED (directRunDiagnostics/Pipeline/Core + chunk ref + ARCHITECTURE line), score computation+field deleted, 11A perf timeout not reproduced (flake), parity 25/25, wasm 74/74. RE-REVIEWER APPROVE-WITH-NITS / GO-LOO-PRODUCTION 2026-09-16 — all 15 gates PASS. NITS FIXED (08c4c8df): comment dedupe, GPS-retention docs, determinism-pin comment; focused 52/52, lint/typecheck clean. PR #75 opened, NOT merged. UI WORKER 2 DONE 2026-09-16 — report section renamed LEAVE-ONE-OUT INFLUENCE + What-if subtitle (id/order/gate kept, Score dropped, SEUW/chi/fails b->a + shift+station + tooltips, robust note, free-network/failed fail-closed cells, table overflow-x bounded), selected-observation LOO detail row in ObservationTableSection (BASE reuse / WITHOUT / COORD CHANGE + top-5 details; threaded ReportView->band->tables), text export new header + per-candidate detail via runResultsTextLeaveOneOut.ts (no Score, !isPreanalysis gate, industry output untouched, result JSON already carries rows), STATISTICAL_TESTING.md leave-one-out section + CURRENT_BEHAVIOR link, tests/reportLeaveOneOut 9/9 + layout updates, bench ~870obs base 257ms/1LOO 0.80x/3LOO 1.26x, playwright 2/2 (1366x768+1440x900, no widening, mm sane, actions distinct), race-safe by construction (rows built engine-side in-run; cancel never applies outcome), no auto-exclusion (single call site behind confirm guard). MANUAL Analyze-exclusion NOT implemented (blocker: single-worker run() always applies outcome via applyRunOutcome; analysis-only needs parallel no-commit path + seq guards = new framework; AUTO table stays). Validation: lint 0 errors, typecheck clean, test:agent 4055 + same 3 pre-existing study-desktop fails (stash-proven), parity 25/25, build clean. FIX WORKER 3 DONE 2026-09-16 — all 5 reviewer findings closed: (1) new tests/suspectImpactSessionExactness.test.ts with production-path exactness via runAdjustmentSession (pre-existing exclusion + live override + approved merge threaded; row absolutes + shifts vs manual-exclusion oracle); legacy direct-helper suite relabeled determinism-only; (2) worker-backed success-after-cancel now rejects as cancelled + 2 hook tests (drop-late-success, pre-cancel control); (3) family contracts — GPS whole-vector DOF delta 2 in 2D (excluded obs retained-listed with nulled stats, documented), correlated-TS candidate-is-member via honest 3-member blunder fixture with pair/row contraction 3->1/3->2 (direct path, session parity-profile force-disables correlation, documented), one-target direction-set set-absent + orientation-gone via production manual exclusion (lone reading stdRes exactly 0, never a suspect, documented); legacy correlated-ts fixture fixed to the 3-member blunder; production failed-alternate unreachable in well-posed nets (dof<=2 saturation caps suspects, documented) with fail-closed classifier covered by existing unit tests; (4) dead direct twin REMOVED (directRunDiagnostics/Pipeline/Core, chunk pattern, ARCHITECTURE line) — zero importers, tsc+build+agent tier verify; (5) deprecated score computation + field deleted (zero readers verified). Validation: lint 0 errors (2 pre-existing warnings), typecheck clean, focused 42 green, test:agent 4061 + same 3 stash-proven fails, parity 25/25, wasm 74/74, 11A perf 10/10 (reviewer 5s timeout = flake), build clean.
- [ ] Phase 14C statistical-QC variance components (branch feat/statistical-qc-variance-components, baseline 48113b8e = PR #73 merge). STATUS: SCOUT AUDIT DONE 2026-09-16 — errorFactor = sqrt(Ω_group/n_group)·sqrt(N_total/dof) confirmed (adjustmentStatisticsBuilders.ts:46,63); NOT Ω_g/r_g, no group redundancy computed anywhere; grouped by UI row type not equation; constraints/orientations in dof+vtpv but not groups; high mistake-risk ("Error Factor" col beside chi-sq bounds); GPS fallback formula inconsistent; RESEARCHER DONE 2026-09-16 — LS-VCE/Helmert/Förstner sourced brief (Teunissen & Amiri-Simkooei 2008, Amiri-Simkooei 2007 thesis, Wang et al 2009, Li et al 2020, STAR*NET v10 manual full-text; Förstner79/Koch/Xu-body via citations, flagged). Key: Förstner s_k²=v_k'P_kv_k/r_k valid under disjunctive-group model (uncorrelated BETWEEN groups, correlated WITHIN ok incl. GNSS block); iterated = Helmert/MINQUE/BIQUE/REML at convergence; chi-sq CI only for global factor; AUE non-negative initializer; identifiability iff B'Q_kB independent. DECISION: GO-GROUP-DIAGNOSTICS-ONLY target (single-pass Förstner + redundancy; no iterative re-solve, no auto-reweight). WORKER 1 DONE 2026-09-16 — engine stochasticGroupDiagnostics.ts (391 lines) + 5/5 tests (dense oracle, known-scale, gates, GPS block≠diagonal, TS cross-group fail-closed); commit 169114ec pushed; parity 25/25 verified. WORKER 2 DONE 2026-09-16 — presentation-only: StochasticDiagnosticsSection strip+table in ReportRunSummarySections/ReportViewTopSections (neutral, no threshold colors, fail rows with reasons), chi-fail-only pointer line, text blocks in runResultsTextResidualSections + ProcessingSummaryView, STATISTICAL_TESTING.md section + CURRENT_BEHAVIOR link, 7/7 new tests, playwright 2/2 at 1366+1440, overhead 0.143ms/1k-eqns 0 extra solves, parity 25/25, agent tier clean except 3 pre-existing study-desktop calibration failures. REVIEWER REQUEST-CHANGES/NO-GO 2026-09-16 — 12 findings (4 high: TS R missing Qll_AB; unbiasedness overclaim; clamped Qvv in R; tests lack re-adjusted evidence + correlated-TS oracle). WORKER 3 FIX ROUND DONE 2026-09-16 — all 12 reviewer findings addressed: (1) TS R now uses full Qvv_AB=Qll_AB-aAQxxaB via qllCrossAt (capped rho, effectiveStdDev) + correlated-TS oracle test; (2) unbiasedness claim removed everywhere (empirical first-pass diagnostic scale; E[Om_k] mixing documented); (3) production R aggregates UNCLAMPED qvv with post-hoc finite/R>eps gates; (4) new true re-adjusted known-scale test (2x solveEngine runs, halved sigmas => s²~4x, R+v move) + correlated-TS oracle; (5) weighted-controls rationale fixed (finite-sigma pseudo-obs, reporting-policy exclusion); (6) single-equation always UNESTIMABLE + test; (7) contaminated groups withhold quadform/descriptive (null, rendered -) + test; (8) explicit finite Om/R/s²/scale gates + tests; (9) gnssBaseline counted as 3 equations; standalone GNSS route documented unavailable-by-contract + 3x3 GNSS block oracle test; (10) pointer lower-tail picks smallest scale + may-indicate wording + tests; (11) adaptive R formatting + indicative-only weak-redundancy rows + tests; (12) assembly errors record unavailable-with-reason + test. RE-REVIEW dispatching 2026-09-16 (stochastic 22/22 + parity 25/25 verified). RE-REVIEWER APPROVE 2026-09-16 — GO-GROUP-DIAGNOSTICS-ONLY; residual negative-quadform clamp fixed + committed 4093fd9d, pushed; agent 4022 + same 3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build clean. PR #74 opened, NOT merged.. Legacy/statisticalSummary/industry/CSV/parity bit-identical (x2.04 scale, 25/25 parity). Validation: lint 0 errors, typecheck clean, stochastic 22/22, test:agent 4021 pass + 3 pre-existing study-desktop fails (stash-proven), test:wasm 74/74, build clean.
- [ ] Phase 14B statistical-QC internal + external reliability (branch feat/statistical-qc-external-reliability, baseline cc3a9e6c = PR #72 merge). STATUS: WORKER 1 (engine internal-reliability) RUNNING 2026-09-15 — contracts fixed: legacy MDB=3.29·SEUW·σ/√r bit-identical default; separate ReliabilityPolicy {legacy-3.29|statistical, α=0.001, power=0.8}; δ0=z(1-α/2)+z(power) via existing Acklam quantile (4.132 @0.001/80%); statistical MDB a-priori no-SEUW; Pope flagged approximation; GPS component semantics; preanalysis available seuw=1; data-check unavailable. WORKER 1 DONE 2026-09-15 — engine core + unit tests: new src/engine/reliabilityPolicy.ts (ReliabilityPolicy/sanitizer/equality, solveNoncentrality via Acklam normalQuantile, deriveReliability with legacy/exact/approx provenance, statisticalMdb a-priori no-SEUW, mdbLinearMm) + policy plumbed ParseOptions/engine/settings/stats-context/result (JSON-plain; default legacy) + run-level reliabilitySummary + per-obs reliability {mdb legacy-identical, mdbStatistical, mdbStatisticalComponents, mdbLinearMm, method} + GPS min-aggregate + robust/preanalysis guards; legacy default path untouched. Validation: new 20/20, focused local-test/stats 42/42, lint 0 errors, typecheck clean, test:agent 3948 pass + 3 pre-existing study-desktop fails (stash-proven), parity:industry-reference 25/25. Workers 2/3 (external/UI/exports/persistence/docs) pending. WORKER 2 DONE 2026-09-15 — analytical external influence + brute-force verification: new src/engine/adjustExternalReliability.ts (computeExternalInfluences O(n.u) via B rows, true-P coupling groups, free-network/untestable/sparse fail-closed gates, first-order-linear provenance) + additive per-obs reliability.external/externalComponents wiring in adjustStatisticsStandardizedResiduals.ts (active-model MDB: statistical else legacy; robust frozen-weights flagged approximate; preanalysis absent) + tests/reliabilityExternal.test.ts 20/20 (sign verified + convention kept; dist/angle/lev/zenith/GPS/TS-correlation/weighted-control BF agreement 1e-6 in linear regime; +MDB/-MDB exact opposition on linear fixture; fixed stations exactly 0; magnitude defs; legacy-model propagation; ~970-eq perf 0.24 s). Validation: lint 0 errors, typecheck clean, reliabilityInternal+localTest 32/32, test:agent 3968 pass + same 3 pre-existing study-desktop fails. WORKER 3 DONE 2026-09-16 — reliability UI/exports/persistence/docs (no math changes): settings Reliability (MDB) card beside Local Test Policy (Legacy 3.29 radio default w/ bit-identical note vs Statistical, power presets 80/90/95 + custom, alpha preset + custom default 0.001, Detection Power label+tooltip, parity-locked) + ParseSettings/RunSessionParseSettings/ParseOptions plumbing; report: MDB header tooltip discloses model/alpha/power + linear-equiv note, ONE compact CoordEff column (mm, rich tooltip w/ station + shift vector; table scrolls horizontally so column kept), RELIABILITY strip under LOCAL TESTING (model/alpha/power/d0 + worst-internal per compatible group + worst coordinate influence), suspect detail via existing row tooltips/selection; exports: CSV 11 fields appended at absolute end (reliabilityModel/Alpha/Power/Delta0/mdb/mdbLinearMm/externalPrimaryMm/externalAffectedStation/externalDEmm/externalDNmm/externalDHmm, order-pin test) + text --- Reliability (MDB / External) --- block, industry parity paths untouched; persistence: attachReliabilitySettings (legacy -> undefined = legacy default), snapshot + Reliability stale diff, legacy-vs-statistical fingerprints differ (statistical-report inputs); docs: STATISTICAL_TESTING.md reliability section (legacy formula, Baarda derivation, d0 table incl. 4.132/2.802/3.417/4.572/4.935, separate single-alternative alpha-1 0.001 not multiplicity-corrected, power semantics, Pope honesty, robust/free-datum/sparse/preanalysis+datacheck gates, +MDB sign + first-order linearity, GPS max-component semantics, unit rules; MDB-limitation paragraph replaced) + CURRENT_BEHAVIOR link; tests: reliabilityPersistence 6/6 + reportReliability 8/8 + CSV order-pin updated + report_observation_display MDB assertions updated to required disclosure. Validation: new/updated 18 green; reliabilityInternal+reliabilityExternal 40/40; lint 0 errors (2 pre-existing warnings); typecheck clean; test:agent 3982 pass + same 3 pre-existing study-desktop fails (stash-proven); parity:industry-reference 25/25; test:wasm 73 pass + 1 pre-existing fail (stash-proven); build clean. MATH-FIX 2026-09-16 (engine/types/docs/tests only; UI/CSV/report owned elsewhere) — statistical MDB now delta0*sqrt(qvv)/|R| with R=(QvvP)_ii full-column sensitivity (TS+GPS; dense/sparse agree; legacy obs.mdb bit-identical); untestable |R|<=1e-12 => +Inf, no silent legacy fallback under statistical selection; power bounds 0.5<=p<1 fail-closed (no clamping), alpha finite [1e-12,0.5]; GPS mdbStatisticalComponents +mU, min-finite aggregate; docs corrected (no shared clamped redundancy); new hand-matrix/correlated-GPS/realistic-sigma(1e-3 curvature note)/3D-U tests. ROUND-3 2026-09-16 — Phase11A real-WASM parity (2.22886 @384) root-caused to pre-existing worst-station argmax instability (exact-magnitude tie U20/U21, last-ulp native-vs-TS flip; reproduced with math-fix stashed): production fix in adjustExternalReliability.summarizeShifts (decisive-lead 1e-9 tie-break, fail-closed gates untouched) + unit tests; Phase11A 4/4, test:wasm 74/74, reliability 52/52, typecheck/lint clean. REVIEWER APPROVE GO-WITH-RESTRICTIONS (6 findings fixed, commit 90145fe2); test:agent 3999 + 3 pre-existing study-desktop fails; parity 25/25. PR #73 opened 2026-09-15, NOT merged.
- [ ] Phase 14A statistical-QC local-test policy (branch feat/statistical-qc-local-tests, baseline cd2ef2f4). STATUS: WORKER 1 DONE 2026-09-15 — engine core + unit tests: new src/engine/localTestPolicy.ts (Acklam normal quantile, beta-CF Student-t, tauCritical, effectiveAlpha, deriveLocalTestCritical, testability helper) + policy plumbed ParseOptions/RunSessionParseSettings/ParseSettings/RunSettingsSnapshot/fingerprint (JSON-plain) + run-level localTestSummary on context/engine/result + per-obs w+tau with family-selected verdicts (pass null when unavailable) + MDB legacy-3.29 in all modes + robust-approximation flag; legacy default bit-identical for testable equations (sole intentional exception: zero-residual-variance rows, r_unclamped ≤ 1e-12, reported not-tested fail-closed rather than clamp-derived; existing suites unmodified green). Validation: new 18/18, focused corpus 33/33, lint 0 errors, typecheck clean, test:agent 3896 pass + 3 pre-existing study-desktop fails (stash-proven on baseline). WORKER 2 DONE 2026-09-15 — report UI (compact PASS/FAIL/- Local column with w/τ tooltips + focus, LOCAL TESTING strip under Adjustment Summary, LOCAL/MDB header tooltips), settings UI (Local Test Policy card: mode/alpha presets+custom/correction, parity-locked), persistence (attachLocalTestPolicySettings on project load; legacy loads as default; stale-run Local Test diff; legacy-vs-formal fingerprints differ), exports (text-summary Critical line carries policy label; CSV appends localTestStatistic/localTestStatisticFamily at end, legacy positions intact), docs/STATISTICAL_TESTING.md + CURRENT_BEHAVIOR link, tests/reportLocalTest.test.tsx (11) + tests/localTestPersistence.test.ts (6), perf 1049-obs run legacy 1518ms vs formal 1414ms with single run-level critical shared by all rows. QC FIX-UPS DONE 2026-09-15 — (1) by-type localFailCount strict ===false + null test; (2) CSV localTestStatistic/Family moved to absolute end with order-pin test; (3) log/Critical lines family-aware (|w|/|τ|, derived run critical) + plumbing via shared effectiveCritical; (4) null-semantics sweep (autoAdjust/directRunDiagnostics/suspectImpact/setupTraverse/observation-text: ===false, '-' rendering) + auto-adjust/suspect/setup tests; (5) strip shows not-tested when unavailable + test; (6) custom legacy critical honored (policy.critical into derive/summary, MDB stays 3.29) + tooltip/summary-line truthfulness (legacy line: fixed critical, no alpha claim) + tests; (7) wording-only: nominal-alpha semantics (per-test None / family-wise target), 3.29 ≈.001 rounded, robust classical-distributions wording, GPS aggregate OR/max not vector test, Baarda sigma0=1, Bonferroni/Šidák dependence caveat. Validation: focused suites green, lint 0 errors (2 pre-existing warnings), typecheck clean. Validation: focused 25 files/79 tests green, parity:industry-reference 25/25, lint 0 errors, typecheck clean. REVIEW FIX 2026-09-15 (engine, both HIGH): (1) testability uses UNCLAMPED redundancy (qll>0 AND r_unclamped>1e-12) — zero-redundancy rows excluded from m, pass=null/available=false, statistic still reported; (2) formal pope-tau with non-positive/non-finite SEUW -> unavailable seuw-not-positive (legacy ||1 fallback bit-identical, baarda unaffected); +8 regression tests (spur-direction, exact-fit SEUW=0). Validation: localTest suites 26/26, corpus spot 33/33, lint 0 errors, typecheck clean, test:agent 3928 pass + same 3 pre-existing study-desktop fails.
- [x] Cross-platform filenames fix (branch fix/cross-platform-filenames, baseline f8a82da90d7a5890217aa40c14796cbeec7b51dd). STATUS: DONE 2026-09-15 — git-mv tests/fixtures/gnssRaw/aux.06o -> station_aux.06o (byte-identical f936c7ff; sole reserved-name violation of 3715 tracked paths; auxbad/auxfail untouched) + 4 spec refs updated (auxfail intact) + new scripts/checkPortablePaths.mjs (+ .d.mts; pure validatePortablePaths + CLI over git ls-files -z/NUL (no trim; reviewer-caught trailing-space fix) + tests/portable_paths.test.ts (agent-tier, unit + git-CLI integration) + AGENTS.md naming bullet. Validation: check:portable-paths 3715 paths 0 violations, portable_paths 16/16, gnssRaw focused 22/22, lint 0 errors (2 pre-existing warnings in untouched files), typecheck clean, reviewer APPROVE. No app/math changes. NOT run: browser E2E (playwright), full agent suite.
- [x] Phase 13F reviewer must-fix round (branch feat/gnss-multifile-product-wiring). STATUS: DONE 2026-09-15 — (1) persistence wired: useGnssMultifileProject hydrates/writes per-named-project durable state (manifest gnss entries + embedded content + settings bag via src/engine/gnssMultifilePersistence.ts, localStorage-backed, projectId plumbs AppShell->modal->panel->hook; project switch re-hydrates) + REAL UI unmount/remount + isolation test (tests/gnssBaseline/gnssMultifilePersistenceUi.test.tsx); (2) stale provenance closed: snapshot freezes full review/export context at solve time, panel renders/exports exclusively frozen, display names ride the run fingerprint; (3) control-only CSV keeps controlStations provenance with FIXED attributed to the control file (numerics untouched); (4) 50k campaign moved to manual evidence tier (tests/evidence/gnssMultifileScalingEvidence.test.ts + manifest; agent tier keeps the fast 5-file leg via tests/gnssBaseline/gnssMultifilePerfSupport.ts); (5) behavior-neutral split: report/export builders -> src/engine/gnssMultifileExport.ts with re-export shim (gnssMultifileProject.ts 692->~620 lines); (6) Part A fixes kept (deterministic code-unit ordering, legacy zero-delta fail-closed COORDINATES_CHANGED). No math/tolerance changes.
- [x] Phase 13F B2 static-GNSS multifile review/provenance/report/perf/E2E (branch feat/gnss-multifile-product-wiring). STATUS: DONE 2026-09-15 — shared-results review (GnssMultifileReview[.utils]: baseline source filter review-only, station trace files+FIXED/FREE+composed, warnings/notes; no second renderer; ResultsPanel review/reportSuffix/jsonExtra props, additive only), concise multifile text/JSON export block (name/sources+hashes/order/warnings/datum — never contents), snapshot-always-frozen + STALE on any edit + seq late-result guard + documented sync-composition/non-cancellable boundary, perf splits (5/~1k full: parse ~15/compose ~14/solve ~566/report ~13 ms; parse/compose-only 10/~10k, 25/~25k, 50/~50k; 10k+ solves + pathological pair-piling evidence-tier, pre-existing dense/Q-cli characteristics), E2E U1-U9 production UI green (full spec 20/20 incl A-K), split-evidence PASS byte-identical, single-file parity re-verified, raw isolation 30/30 + no DIRECT_INGEST, F2F/fingerprint 38/38. Validation: lint 0 errors (2 pre-existing warnings), typecheck clean, agent 3838 pass + 3 pre-existing study-desktop fails (stash-proven), build clean. No math/tolerance/route changes.
- [x] Phase 13F B1 static-GNSS multifile production wiring, first slice (branch feat/gnss-multifile-product-wiring). STATUS: DONE 2026-09-15 — engine: buildGnssMultifileAdjustInput extracted (pure, no math change; runGnssMultifileProjectSolve reuses it), 'gnss' ProjectSourceFileKind, gnssMultifile settings-bag reload survival via attachGnssMultifileSettings, SettingsState.gnssMultifile key. UI: useGnssMultifileProject hook + GnssMultifileProjectPanel (add/remove/enable/rename/reorder, per-source READY/WARNING/ERROR/DISABLED status, composition preview from engine summary verbatim, fnv1a duplicate acknowledgement gate, frozen run snapshot + stale marking, portable/missing-source states, datum opt-in + control + setup threaded, solve via existing worker run, shared GnssResultsPanel + provenance), mounted as Multi-file project tab in GnssWorkspaceModal. Guard/parity: tests/gnssBaseline/gnssMultifileProductionWiring.test.ts (production import-graph guard, kind, attach, parseProjectFile reload, single-source == single-file coords/residuals/VF/DOF/route). Validation: 5 new + 70 multifile/panel + 26 raw-isolation/projectFile pass, lint 0 errors (2 pre-existing warnings), typecheck clean. No math/tolerance/route/threshold changes; no raw-RINEX shortcut; no second result renderer.
- [x] Phase 13E linked F2F sync + production export hardening (branch feat/f2f-linked-sync-export-hardening, baseline origin/main 4abfb0d0 = PR #67 merge). STATUS: DONE 2026-09-15 — PR #68 opened, reviewer APPROVE-WITH-NITS, validation GO (lint 0 errors, typecheck clean, test:agent 3813 + 3 pre-existing study-desktop fails, test:wasm 74/74, parity 25/25, build clean, E2E A-W Chromium green). Buckets: (A) linked F2F source model + authoritative revision + sync states + coordinate-only auto-sync + rerun hook + persistence (§§1-16,53-56); (B) production Export Center UI + warnings + result contract + no-silent-drop + coverage matrices (§§17-23,38-43); (C) color/lineweight/linetype/symbol/DXF hardening (§§24-37); (D) canonical adjustment→F2F→export goldens + E2E A-W + perf + docs/decision/PR (§§44-52,57-62). Rules: no LS/GNSS/COGO/CRS math changes, no tolerance changes, no FeatureCodeCatalog redesign, FLD evidence-only, coded curves deferred unless importer/catalog evidence. Bucket A2 (adjustment-rerun linked coordinate sync: regeneration.ts orchestrator + hook seam + tests/cad_f2f_sync.test.tsx 11/11) DONE 2026-09-15. Buckets C2+C3 (R12/R2000 DXF hardening + lineweight/linetype + point symbols + polygon/parcel/alignment/ellipse + LandXML policy §§28,29,31-37) DONE 2026-09-15: new dxf/dxfColorMap.ts (nearest-ACI + 420 true color + linetype catalog + 370 mapper) + dxfExportModel WithResult (shared-resolver colors, POINT_SYMBOL_APPROXIMATED, alignment expansion, 36-gon ellipse, closed parcel/polygon, no silent default) + R12 ACI/LTYPE/62/6 (+lineweight warn) + R2000 62+420/370/full-LTYPE/paper color + landxmlCad WithResult (ellipse NOT_APPLICABLE) + tests/cad_export_dxf.test.ts 12/12. Validation: new 12/12 + existing DXF/LandXML 88/88, lint 0 errors, no typecheck errors in touched files. Bucket E (docs §§22,23,47,48,49,57) DONE 2026-09-15: entity × format + F2F-object × format coverage matrices, Export Center scopes, color precedence, DXF approximation rules, no-silent-drop policy in docs/webnet-cad-layout-plotting.md; adjustment-linked sync section in docs/FIELD_TO_FINISH.md; verdicts FXL_SUPPORTED_SUBSET (fxlAdapter.ts unchanged on branch) / FLD_DEFERRED (no adapter) / CODED_CURVES_DEFERRED (no inference; manual COGO only).
- [x] Phase 13D browser E2E + final validation (branch feat/field-to-finish-coding). STATUS: DONE 2026-09-15 — new src/dev/surveyDraftingF2fSteps.ts (23 harness steps A-W: open/import/mapping/code-desc/catalog/unmapped/linework-preview/commit/layers/styles/labels/linework/manual-edit/source-mod/regen-preview/regen-apply/SVG/PDF/model-DXF/save/reopen/adjustment-freeze/summary) wired into surveyDraftingHarness.tsx (13C pattern) + tests-browser/survey-drafting-f2f.spec.ts (23/23 Chromium green, zero page errors) + wiring fix in f2fReviewUtils (preview splits combined CODE CONTROL cells against the catalog so review agrees with commit; UI-only, no engine/importer changes) + tests/cad_f2f_review.test.ts (2 tests pin 12-mapped/1-unmapped/3-chains agreement). Validation: 9 F2F focused files 51/51, importer specs 25/25 unmodified (JobXML/RW5/FieldGenius/DBX numerical outputs unchanged), 13B/13C E2E still green, lint 0 errors (2 pre-existing warnings), typecheck clean, test:agent 3721 pass + 3 pre-existing study-desktop calibration failures (stash-proven), build clean. No math/tolerance/engine/importer-numeric changes. E2E coverage A-W complete; no steps harness-unrepresentable (catalog CRUD UI gestures covered by unit goldens, not the stepwise harness).
- [x] Phase 13D F2F UI + goldens (branch feat/field-to-finish-coding). STATUS: DONE 2026-09-15 — new SurveyCadFieldToFinishPanel (Feature catalog / Import review / Preview & commit tabs, hosted as a Field-to-Finish tab in SurveyCadDraftingPanel) + SurveyCadFeatureCatalogEditor (code/definition/layer/symbol/point-line-label style/linework/implicit-continuation CRUD + duplicate/rename/alias + style preview + validateCatalog issues) + f2fReviewUtils (row review + mapped/unmapped/no-code/invalid-controls/chains/warn/fail summary over the shared linework engine) + one-transaction commit via new useSurveyCadWorkspace.commitFieldToFinishPayload (runFieldToFinishCommand; undoable) + additive cadProperties F2F rows (Generated Linework state, feature codes, source record; read-only) + tests/cad_f2f_golden.test.ts (13-pt CSV→CAD→sheet→SVG golden, DXF independent parse, LandXML code/desc + unmapped warn, 10k-pt/1k-chain smoke <30s) + tests/cad_f2f_adjustment_plan.test.ts (LSAEngine decorate, coords equal adjustment golden exactly, metadata survives) + tests/cad_f2f_regen_golden.test.ts (moved/code-changed/added/removed preview deltas, manual entity + manual label preserved). No engine/fieldToFinish, importer, or math changes (read-only use).
- [x] Phase 13D F2F CAD generation + regeneration (branch feat/field-to-finish-coding). STATUS: DONE 2026-09-15 — new src/engine/fieldToFinish/cadGeneration.ts (pure builder: reference-or-create survey points with description/featureCode/metadata provenance, first point-role code wins, linework via generateLinework to line/polyline entities, component auto-labels with deterministic deconfliction, stable-id layers/styles reused by name with conflict choices recorded, F2F-UNMAPPED preserved + warned, one F2F_GENERATE undoable transaction) + regeneration.ts (explicit previewed REGENERATE, stable import+station+record identity, GENERATED/MANUAL_OVERRIDE/DETACHED, confirmed-only generated deletion, adjustment-rerun coordinate updater, adjusted>sideshot>coordinate-only resolution, control-station/adjusted adapters) + additive CAD edits only (CadPointSymbol.shape + 6 bounded F2F default symbols, F2F_GENERATE command key/registry; .wncad stays v2, legacy opens unchanged); tests/cad_f2f_generation.test.ts + tests/cad_f2f_regen.test.ts (17/17 agent-tier). Validation: lint 0 errors (2 pre-existing warnings), typecheck clean, test:agent 3712 pass + 3 pre-existing study-desktop calibration failures (stash-proven), build clean. No adjustment/GNSS/COGO math, tolerance, or numerical-output changes; no importer/F2F-core edits.
- [x] Phase 13D importer code/description normalization + terrestrial CSV (branch feat/field-to-finish-coding). STATUS: DONE 2026-09-15 — ImportedRecordBase gains optional `feature` ({rawCodeText, codes[] single entry role 'both', description, sourceOrder}); shared appendImportedObservationBundle carries description/featureCode to every emitted record (observation serialization omits descriptions, so WebNet text/numerics unchanged); RW5/TDS + FieldGenius shots and control split CODE/FC vs DESC, DBX splits Code/FeatureCode vs Description, survey-report shots read Code only when present (never invented), JobXML splits Code/FeatureCode vs Description/Descriptor while keeping collapsed-code bookkeeping (pointCode/targetCode/targetContexts) intact + new src/engine/terrestrialCsvImport.ts (explicit units m/mm/cm/ft/usft, no magnitude inference; CRS recorded never transformed; no N/E swap; file/row/field bounds; Trimble Access preset + manual column mapping; registered as terrestrial-csv with meter default) + tests/cad_f2f_importers.test.ts (12 tests). Validation: 12 new + 18 existing importer specs pass unmodified, lint 0 errors (2 pre-existing warnings), typecheck clean, test:agent 3690 pass / 3 pre-existing study-desktop fails (stash-proven), build clean. No adjustment/GNSS/COGO math, tolerance, or numerical-output changes. F2F catalog/matcher owned by sibling worker (src/engine/fieldToFinish/* untouched); importer `feature` shape is the minimal agreed subset.
- [x] Phase 13C §§49-52,55-59 polish integration + docs (branch feat/drafting-interoperability-polish). STATUS: DONE 2026-09-14 — new tests/cad_draft_polish_integration.test.ts (11 tests: same-sheet scene/SVG/PDF parity incl leaders/title/north/scale + layout-DXF subset; E=2400000/N=7400000 through auto-labels+leaders+layout-DXF+LandXML; v2 save/reopen identity incl overrides/leaders/continued tables/custom template, no schema bump; 13B v2 safe defaults; undo/redo auto-place/reset/leader/continuation/titleblock + LandXML single-transaction stage; agent-tier stress 63.5 ms total — 1000-label auto-place 45.8 ms, 2000-row continuation 0.2 ms, 10-sheet PDF 1.1 ms, 3000-CgPoint import 14.9 ms; XXE/oversize rejects; adjustment/GNSS/COGO/parcel/CRS isolation + no-observation import) + docs/survey-drafting.md (continuation/templates, LandXML subset/units/CRS/geometry-vs-legal, polish section) + ARCHITECTURE.md drafting module map (13B gap closed) + CURRENT_BEHAVIOR.md 13C note. Validation: 11 new + 102 drafting/landxml/cogo battery pass, lint 0 errors (2 pre-existing warnings), typecheck clean.
- [x] Phase 13C §§22-30 table continuation + title-block templates (branch feat/drafting-interoperability-polish). STATUS: DONE 2026-09-14 — logical table owns rows, fragments reference deterministic row ranges (no copies; AUTO slices sequentially, MANUAL first-only; header repeat + Continued marker; no silent resize; source change recomputes all ranges; coverage validated) + visual title-block templates (paper-mm line/rect/static-text/token-text + align/font/lineweight; bounded 11-token set incl viewport SCALE; create/duplicate/rename/edit/delete-if-unused/assign; new TitleBlockTemplateEditor.tsx 259 lines) + one renderer (buildTitleBlockItems) for preview/SVG/PDF/layout-DXF + history/persist round-trip with stable ids + tests/cad_draft_tables_continued.test.ts (7 tests). Validation: 7 new + 80 drafting pass, lint 0 errors, typecheck clean (3 study-AI calibration failures pre-existing on clean tree).
- [x] Phase 13C §§12-21 label deconfliction + leaders (branch feat/drafting-interoperability-polish). STATUS: DONE 2026-09-14 — placement state AUTO/MANUAL on CadDraftLabel + DraftDocumentLabel (AUTO_GENERATED/MANUAL_OVERRIDE normalize back; manual edits set MANUAL; move/override helpers; resetLabelToAuto) + per-viewport overrides (dx/dy/rotation/visible, presentation-only) + leader model (enabled/elbowMm/lineweightMm + paper-mm anchor/landing refs) + new src/engine/cad/cadLabelAutoPlacement.ts (paper-mm, deterministic 8-candidate scored set, AUTO-only unless reset, 3 mm leader threshold, viewport clamp) + persistence (v1 additive; sanitize/clone round-trip placement/overrides/leaders/source refs; text refresh keeps manual placement; missing source → BROKEN_REFERENCE) + shared buildPaperLabelItems/draftLabelsToPlacements wiring SVG + PDF + layout DXF paper labels (document DXF subset) + tests/cad_draft_labels_auto.test.ts (9 tests: parcel, dense cluster, curve/road, rotated + dual viewports, determinism, manual-wins, leader policy, save/reopen, E=2400000 precision) + docs/survey-drafting.md note. Validation: 9 new + 64 drafting pass, lint 0 errors, typecheck clean.
- [x] Phase 13C §§6-10 dual DXF export (branch feat/drafting-interoperability-polish). STATUS: DONE 2026-09-14 — R12 path untouched (byte-identical; no serializer changes) + new src/engine/cad/dxf/dxfLayoutExport.ts (R2000 AC1015: HEADER $ACADVER+$HANDSEED, CLASSES, TABLES LTYPE/LAYER/STYLE/APPID/DIMSTYLE/BLOCK_RECORD, BLOCKS, per-space ENTITIES, OBJECTS root+ACAD_LAYOUT+LAYOUTs, VIEWPORT id 1+ per layout with center/view-height/twist) + dual contract buildDxfModelSpaceText (existing, byte-identical) vs buildDxfLayoutText (new; sheets→named LAYOUTs, paper mm bottom-left, title block BLOCK+INSERT, explicit warning list) + large-grid exactness (E=2400000/N=7400000, no rebasing) + tests/cad_draft_dxf_layout.test.ts (6 tests, @mlightcad independent parse) + docs/survey-drafting.md dual-contract note. Validation: 6 new + 60 drafting pass, lint 0 errors, typecheck clean.
- [x] Phase 13C §§31-48 bounded LandXML import + CAD export (branch feat/drafting-interoperability-polish). STATUS: DONE 2026-09-14 — adjustment export frozen (golden pin tests/landxml_export_golden.test.ts; foot = international foot documented) + src/engine/landxmlImport.ts (subset preview: CgPoints N-E order, lines/circular curves, geometric parcels, line+curve alignments with spiral warn+skip, m/ft/USft explicit, CRS metadata-only, reject-or-rename duplicates, fail-safe bounds) + src/engine/landxmlCad.ts (CAD→LandXML adapter reusing serializer helpers) + tests/landxml_interchange.test.ts (8 tests). Validation: 16 landxml + 60 drafting pass, lint/typecheck clean for touched files.
- [x] Phase 13B survey drafting + deliverable output MVP (branch feat/survey-drafting-deliverables, baseline origin/main 78060b02 = PR #64 merge). STATUS: DONE 2026-09-14 — .wncad v2 (draft model, layers+printable/lineweight, styles, mm paper, stable IDs, v1 migration) + label engine (7 types, provenance, overrides, BROKEN_REFERENCE) + tables (point/line/curve, deterministic, OVERFLOW) + sheets (12 sizes+CUSTOM, multi-sheet order) + viewports (explicit scales, viewport-only rotation, north-up convention) + north/scalebar/titleblock-tokens/notes + layer UI + production workspace wiring + SVG canonical + hand-rolled vector PDF + isolated R12 DXF (independent round-trip) + small-parcel/large-grid/curve/road/perf/isolation/legacy goldens + Chromium E2E A-S + sample + docs/survey-drafting.md. Review: 2 fix rounds, APPROVE GO-DRAFTING-PRODUCTION. Validation: lint 0 errors, typecheck clean, test:agent 3610 + 3 pre-existing study-desktop fails, test:wasm 74/74, build clean, E2E green. No adjustment/GNSS/COGO/parcel-legal math changes, no tolerance changes.
- [x] Phase 12J.7 medium-baseline GNSS covariance validation (branch feat/gnss-raw-medium-baseline-covariance-validation, baseline origin/main 47df4699 = PR #59 merge). STATUS: DONE 2026-09-14 FIX-ROUND — 210/210 processed (206 FIXED/4 FLOAT/0 failed; STAR-1h 60/60, brdc 30/30) + 5/5 strictly-independent loops (D BLOCKER-PROVEN: max 1/day x 5d) + matched-pair T (FIT n=18 med 700.6 / VAL n=12 med 914.3) + candidates S=17.2 (FIT-matched med 2.37 / VAL-matched med 3.09, underpowered) / ENU h10v32 / CL rejected + bias separated + graph SPANNING_TREE_SUFFICIENT_INITIAL (4-window independent check) + PAR2/PAR1 66s/131s + driver hardened (set -euo pipefail, fail-closed, content-checked skip, sorted timing) + inventory 75-file manifest + reports/gnss/phase12j7-medium-baseline-covariance-validation.md (decision REVIEW_ONLY, no certified scope, S=17.2 characterization-only, ANTEX subset retained, DIRECT_INGEST NO), no src/ diff. EVIDENCE ONLY. Stage 1 was: 20/20 RINEX + 4 sitelogs + 5 CODE-final SP3 + 5 broadcast-NAV + ANTEX subset (2.36MB/124blks/c0eb7a8b) + byte-identical full-vs-subset RTKLIB static check (2856 epochs) + scripts/gnss/gnss12j7{AntexSubset,CorpusInventory}.mjs.
- [x] Phase 12J.6 modern Wettzell GNSS covariance calibration (branch feat/gnss-raw-modern-covariance-calibration, baseline origin/main f6ac0390 = PR #58 merge). STATUS: DONE 2026-09-14 — decision REVIEW-ONLY + ANTEX_READY_WITH_SIZE_BOUND, DIRECT_INGEST NO.
- [x] Phase 12J.4 raw static-GNSS review/export worker MVP (branch feat/gnss-raw-worker-review-mvp, baseline PR56-head 82690563 = 12J.3 + origin/main f7803989; baseline validation: typecheck fails ONLY in untracked phase9l dirt src/engine/runSessionAsync.ts, test:agent 3430 pass / 11 pre-existing fails. STATUS: DONE 2026-09-14 — Track UI: useGnssRawBaseline hook (job lifecycle, caps, swap, preflight gate, job-token cancel) + GnssRawBaselinePanel/Modal/FileSlots/OptionsForm/Review (all <400 lines) + gnssRawExport (authoritative JSON, no BL) + AppShell "Process Raw Baseline" sibling entry + tests/gnssRaw export-roundtrip + no-adjustment isolation guard (32/32) + tests-browser/gnss-raw-review.spec.ts 2/2 Chromium (real Worker/WASM: FLOAT review, diagnostic JSON, swap sign, blocks, cancel, project-unchanged) + docs/gnss/raw-static-worker-mvp.md. Validation: lint clean, tsc clean except pre-existing phase9l dirt, test:agent 3462 pass / same 11 pre-existing fails. NO adjustment math changes, NO auto-ingest, NO vendor RINEX, ANTEX deferred with reason.
- [x] Phase 12J.3 raw GNSS antenna + stochastic-model closure (branch feat/gnss-raw-antenna-stochastic-closure, baseline origin/main f7803989 = PR #55 merge; EVIDENCE ONLY — no production raw worker, no RINEX UI, no gnssBaseline math changes, no empirical covariance scaling, no Default-SE adoption, no tolerance changes, no vendor commits). STATUS: DONE — Track A: TRM60158.00 NONE CALIBRATION_UNAVAILABLE (0 hits igs20.atx direct grep + indexes, NGS 404s, TBC-local negative; aliases refuted-or-different); base TRM29659.00 SCIT frozen (single robot entry, DAZI 5.0); base-only ladder re-bases coherently (S32 18.3→91.0 mm, cohort median 82.6/max 91.0/RMS 84.6, NOT collapsed); bias decomposition Up-common-mode 11–24 mm + parallel length +5–14 mm; sensitivities trop 34/iono 106-degenerate/satPCV 0.1/windup 0. Track B: V=round(σU) exact 5/5, H bounded ±1 mm; pre-adjustment GVX proof all 5 legs + session-estimated forensics; §17/§18 operator-blocked; defaults resemble nothing (descriptive); network 4-net VF 0.323 vs 14.487 (~45×), coords shift ~22 mm; CLI/WASM parity exact. Decision GO-RAW-WORKER-REVIEW-MVP. Report reports/gnss/phase12j3-antenna-stochastic-closure.md, acceptance A–R all PASS (I operator-blocked-accepted, O design-only). Validation: lint 0 errors, src diff empty. No push/PR (parent owns).
- [x] Phase 12J.2 TBC static-GNSS processing parity closure (branch feat/gnss-raw-tbc-parity-closure, baseline origin/main 7f0e5f90 = PR #54 merge; EVIDENCE ONLY — no production RINEX worker/UI, no gnssBaseline math changes, no empirical covariance scaling, no TBC-default-SE adoption, no tolerance relaxation, no vendor commits, no GPS+GLONASS MVP requirement). STATUS: DONE — S32 closest-equivalent 18.3 mm / length −14.3 mm (10° + true precise igs13793.sp3; mask closed 2.6 mm, precise 1.7 mm); rover TRM60158.00 NONE absent IGS/NGS → PCO/PCV BLOCKED; cohort median 19.8 / max 24.8 / RMS 21.0 mm, SYSTEMATIC dY +17…+21 mm, all lengths short (mean −9.0 mm); CLI↔WASM bit-identical incl. SP3; V = 1σ Up reproduced, H unresolved (16%); no scaling (src diff empty); Default SE excluded for processed vectors; H FLAG corrected to 0.035+1ppm; S32 PASS/PASS. Decisions: stochastic policy C (mission-§43: no automatic production ingest; review/export only), readiness MORE-EVIDENCE-REQUIRED for production ingestion (rover-PCV + systematic bias block it; worker chain SUPPORTED under 12J.1 restrictions). Report reports/gnss/phase12j2-tbc-processing-parity-closure.md, acceptance A–T (19 PASS + M OPEN-spec'd + G blocked-accepted). No src/ changes; no push/PR (parent owns).
- [x] Phase 12J.1 RTKLIB/WASM static baseline numerical proof (branch feat/gnss-raw-rtklib-wasm-proof, baseline origin/main c28cae52 = PR #53 merge; merged as PR #54 at 7f0e5f90. EVIDENCE ONLY — no production RINEX UI, no production raw worker, no internet downloads, no vendor RINEX commits, no gnssBaseline/R2B/free-network math changes, no T01/DAT support, no tolerance inflation). Scope: mission §§0-46 (S32 golden leg, RTKLIB v2.5.1 @62d4677 pin, options description, marker/ARP/phase-center contract, canonical MARKER-TO-MARKER convention, ANTEX matrix, H/E/N sign test, P041 sensitivity, frame contract + transform oracle, staged L0-L5 table, broadcast-vs-precise, time/decimation, signal contract, ambiguity result, FIXED covariance source audit, 3x3 extraction, SPD, TBC covariance comparison, scaling experiment, quality classes, P041↔0124 cohort, a.dat secondary experiment, rover/rover, GLONASS last, CLI determinism, WASM build + binding + CLI↔WASM parity + determinism + memory + perf, canonical adapter, frame-aligned adapter, provenance, end-to-end injection, network reconstruction experiment, MVP + readiness decisions, report). STATUS: DONE — S32 explained (raw 1.9765 m → L1 marker-ARP 22.6 mm; legs C/D PCV BLOCKED no ANTEX; SP3 leg was broadcast-by-dispatch, true precise open); covariance sourced (post-fix filter cov, no scalar maps RTKLIB↔TBC 69–525×); cohort 5/5 FIXED 21–29 mm; CLI↔WASM |Δpos|=0; end-to-end PASS; network A−B 20–25 mm; decision GO-RAW-WORKER-MVP-WITH-RESTRICTIONS (6 restrictions). Validation: production-diff PASS, lint 0 errors, build PASS, parity 25/25, test:agent 3430 pass (11 pre-existing fails: study-desktop + phase9l dirt), 5/5 repros exit 0. Report reports/gnss/phase12j1-rtklib-wasm-proof.md, acceptance A–S all PASS. No production changes; no vendor bytes.
- [x] Phase 12J.0 raw static-GNSS baseline processing architecture audit (branch feat/gnss-raw-baseline-processing-audit, baseline origin/main 2ea0d015 = PR #52 merge; EVIDENCE ONLY — no production raw processing, no RINEX UI, no PPP/RTK/kinematic, no gnssBaseline math/covariance/R2B/free-network changes, no third-party source commits, no vendor data commits). Scope: §§0-42 mission (local raw inventory, raw→TBC traceability, formats, product boundary, MVP scope, RTKLIB audit, architecture comparison, RINEX contracts, browser feasibility, pipeline/differencing/AR/cycle-slip/orbits/antennas, frame/epoch analysis, oracle run, TBC vector/covariance comparison, downstream injection, provenance, network workflow, CORS seam, WASM probe, worker/cancellation/determinism/perf/failure/QC/security/licensing, decision, MVP contract, report). STATUS (2026-09-13): DONE — baseline verified (lint 0 errors, parity 25/25, build clean; typecheck/test:agent failures confined to untracked phase9l noise + pre-existing study-desktop); scout inventory (sole open RINEX = single-station P041; all rover files proprietary .T01/.dat; NO RINEX pair) + researcher RTKLIB audit (rtklibexplorer v2.5.1, BSD-2-clause+extra) + evidence worker (TBC parser 51/51, TBC↔GVX ≤0.45mm 3-spot, injection 2/2, emcc lambda.c exit 0, sizing table) + report reports/gnss/phase12j0-raw-baseline-processing-audit.md + independent review APPROVE-WITH-NITS (3 nits fixed, re-verified). Decision GO-RAW-MVP-RTKLIB-WASM (gated: RINEX pair, CLI-vs-WASM bitwise, FIXED-cov semantics, frame adapter). Oracle session G BLOCKED w/ proof; H/I/N PARTIAL (see §40). AMENDMENT
(2026-09-14): converted RINEX corpus (raw-baselines/, ConvertToRINEX
3.15.0 — 20 RINEX 3.04 rover + P041 2.10 + 40 nav + 15 GLO + 16 SP3)
lifted the blocker. Traceability now PROVEN (5 rounds × C(5,2) = 50 =
Dataset B). S32/B32 oracle MEASURED (rtklibexplorer v2.5.1 rnx2rtkp in
/tmp: FIX 90/93 ratio 28.6, raw 3D 1.977 m → 26 mm after 2.0 m antenna-
height reduction, SP3≡broadcast, rerun byte-identical, 0.08 s/10 MB).
Report amended (§§1–3, 8, 14–16, 19–22, 26, 31–32, 37–40); decision
GO-RAW-MVP-RTKLIB-WASM strengthened; 12J.1 6-leg sequence defined. No
vendor bytes committed; no production changes.
- [x] Phase 12I.2 free-network static-GNSS product UI (branch feat/gnss-free-network-ui, baseline origin/main 4fdb7d2d = PR #51 merge; PRODUCT/UI ONLY — no math/routing/cap/tolerance changes). DONE: Datum Handling selector + session datumMode + datum-aware preflight + component summary + free/mixed banners + precision wording + error mapping + gauge audit + multifile summary/persistence + docs + focused UI tests + 13-leg browser E2E. Validation: focused 120/120, E2E 13/13 Chromium, Dataset B free DOF129 SEUW1.965038, A0 2.100053 / A 1.102511 DOF228, lint 0 errors, build clean, review APPROVE-WITH-NITS (docs nits fixed). Decision GO-FREE-PRODUCTION.
- [x] Phase 12I.1 free-network static-GNSS MVP (branch feat/gnss-free-network-mvp, baseline origin/main fe460326; PRODUCTION TS-only temporary-gauge + inner-constraint S-transform behind explicit datumMode: 'allow-free', default 'constrained' bit-identical; NO UI, no R2B-for-free, no native free routing, no math/tolerance changes, no vendor commits, no terrestrial free support). Scope: datumMode API + per-component classification after composition/overrides + deterministic anchors + setup-before-gauge + extra-rank-defect/size gates + rank/DOF/datumSummary + blockwise S-transform + Phase12D reuse + relative covariance + worker/R2B isolation + persistence + report/JSON + agent-tier suites + synthetic corpus + Dataset A/B manual proofs + perf/cap table. STATUS (2026-09-13): DONE — src/engine/gnssFreeNetwork.ts (367 lines: datumMode types, GNSS_FREE_NETWORK_MAX_STATIONS=250, classification, deterministic anchors, gauge working copy, blockwise O(p^2) S-transform, relative covariance) + gnssBaselineAdjust.ts dispatcher (constrained core renamed intact; free path: native-reject, partial/isolated gates, self-baseline audit order, damping scan, rank DOF, Phase12D reuse, redundancy-trace hard gate, datumSummary) + R2B route free gate (TS-only) + multifile datumMode (options/persisted/input/JSON-export, datum-aware preflight skip) + report datum section (constrained JSON unchanged) + tests/gnssBaseline/gnssFreeNetwork{,Routing,Multifile,TestSupport}.test.ts (48/48 agent, ~3s) + docs (MATH §11, WORKFLOW, CURRENT_BEHAVIOR). Manual proofs (local intake only): B free 8/24/21/150/129 DOF129 SEUW 1.965038; A free 16/48/45/273/228 DOF228 A0 2.100053/AC 1.297104/AH 1.988831/A 1.102511; same-anchor parity bitwise; cross-anchor worstV 4.66e-10, Qvv 6.8e-21. Perf rings: 10/9ms 50/21ms 100/87ms 250/891ms 500/8s (cap 250 for agent-tier viability). Validation: lint 0 errors, typecheck clean, test:agent 3394 + 3 pre-existing Study fails (stash-proven), test:wasm 54/54, parity 25/25, build clean. Decision GO-FREE-UI (bounded: cap 250, TS-only, no UI yet). README untouched (no user-facing setup change).
- [x] Phase 12I.0 free-network static-GNSS mathematical architecture audit (branch feat/gnss-free-network-architecture-audit, baseline origin/main edc1e413 = PR #49 merge; EVIDENCE ONLY — no production free-network enable, no preflight weakening, no constrained-output/math/tolerance/R2B-routing/UI changes, no partial constraints, no terrestrial free support, no datum transforms, no RINEX/PPP). Scope: §§1-2 datum-defect 3/3c rank-SVD proofs + §§3,18 rank-based DOF/redundancy + §§4-8 KKT/GINV/gauge-S candidates + gauge-invariance + §§9-11 translation-constraint/apriori-invariance/constrained-free parity + §§12-13 Dataset A/B commercial evidence (local-only vendor) + §§14-17 covariance datum semantics/S-transform/Qvv invariance + §19 loop QC + §20 setup uncertainty + §§21-24 mode/API/import/multifile semantics (design only) + §§25-28 R2B selected-block analysis + §§29-32 relative covariance/gauge selection/solver reuse/setup-at-anchor + §§33-36 reporting/preanalysis/failure/extra-defect + §37 synthetic corpus + §38 perf + §39 MVP + §41 report reports/gnss/phase12i0-free-network-architecture.md + §§42-45 acceptance/decision/PR. STATUS (2026-09-13): evidence complete — 6 new files + report, 35/35 focused, lint/typecheck/build clean, test:agent delta-free (3 pre-existing Study fails stash-proven), decision GO-FREE-MVP-B; independent review APPROVE-WITH-NITS (3 nits fixed), then commit/PR.
- [x] Phase 12H.1 multi-file static-GNSS production proof (branch feat/static-gnss-multifile-production-proof, baseline origin/main 97de6061; enforced DEFAULT-OFF gate (OFF throws fail-closed); no math/covariance/12D/R2B-eligibility/tolerance changes; no free-network/datum-transforms/epoch/RINEX/terrestrial-mixing). DONE: src/engine/gnssMultifileComposition.ts (frozen H.0 composer: exact frame/epoch/ellipsoid fail-closed naming both sides, 1e-9 m first-seen merge, FIXED-wins control, every baseline retained, datum stays downstream) + gnssMultifileDuplicates.ts (indexed ~O(n) LEGITIMATE/POSSIBLE/STRONG/REVERSED; STRONG across sources blocks naming both records, else warn+retain, never deletes) + gnssMultifileFlag.ts (DEFAULT OFF) + gnssMultifileProject.ts (independent per-source parse incl 2-pass CSV, mixed terrestrial+GNSS guard, post-composition overrides, one run-level setup, precomposition READY/BLOCKED summary, portable save gate, settings-bag persistence, provenance report/JSON export) + scripts/gnss/gnssMultifileSplitEvidence.ts (`gnss:multifile-split-evidence`, fail-closed SKIP when intake absent) + reports/gnss/phase12h1-commercial-split-evidence.md (NUMBERS ONLY: A 91v/16stn A0/AC/AH/A pins 2.100053/1.297104/1.988831/1.102511 DOF 228 + B 50v/8stn B0 1.965038 DOF 129; 25/25 split legs PASS incl odd-even/5-chunk/control-separated/reversed; coords bitwise, stats <=1e-12, loop counts equal, reports equal modulo provenance-order fields) + tests/gnssBaseline/gnssMultifile{Composer,Duplicates,Project}.test.ts (21/21 agent, <2s: frame fail-closed, A-F + boundary, control matrix, order invariance, datum-after-composition, 4 duplicate classes + 2000-row O(n), save/reload equality, save gate, mixed guard, overrides, enable/disable, single-file regression, stub R2B parity, cross-source Phase12D, 10/~1k + 50/~10k + 100/~50k composition perf). Validation: lint/typecheck/test:agent/test:wasm/build (see batch report). README untouched (flag OFF, no user-facing change).
- [x] Phase 12H.0 static-GNSS multifile composition audit (branch feat/static-gnss-multifile-architecture, baseline origin/main ae030912 = PR #46 merge; EVIDENCE ONLY — no src/ production edits, no math/tolerance/R2B-eligibility/solver/worker/file-format changes, production multi-file GNSS NOT enabled). DONE: tests/gnssBaseline/gnssMultifileComposition.test.ts (9/9 agent, ~300ms: test-local compose helper with fail-closed frame/epoch/ellipsoid match, 1e-9m rounding contract no-averaging, station merge A-F, control matrix, order invariance 12dp, enable/disable, cross-source loops/redundancy, perf parse10x100=6ms merge1000=0ms) + reports/gnss/phase12h0-multifile-composition-audit.md (mission §35: architecture reusable-vs-gaps, canonical model, compatibility matrix, merge rules, provenance, setup run-level scope, Dataset A/B split DEFERRED to manual evidence — vectors vendor-local-only ~/Downloads/webnet-gnss-12e/tbc-intake, never committed — R2B unchanged-gate assessment, serialization/UI design, verdict GO-MULTIFILE WITH-RESTRICTIONS, acceptance 18/19 PASS + P deferred). Validation: gnssBaseline 328/328, parse_project_run_files 2/2, lint 0 errors, typecheck clean. README untouched (no user-facing change).
- [x] Phase 12F.4 enable certified native static-GNSS R2B route default ON (branch perf/gnss-native-r2b-default-on, baseline origin/main a47dc9fb = PR #44 merge; SMALL enabling phase — flip kill-switch default OFF->ON for certified cohort only, freeze classifier/bounds/math/tolerances/F-BRIDGE, R1 stays OFF, no UI/file-format/vendor changes). DONE: one-boolean flip (`gnssNativeR2BEnabled` false->true + comments/reason string; no dispatcher refactor — no production caller needed it) + test rework (default-ON + kill-switch describe: pristine-import default, explicit-OFF zero-load, re-enable, below-floor bitwise) + NEW scripts/gnss/gnssR2BDefaultOnProof.ts (8/8 real-Worker/real-WASM production-dispatch legs, no overrides) + reruns: worker proof 11/11, datasets 8/8 (pins A0 2.100053/AC 1.297104/AH 1.988831/A 1.102511/B0 1.965038, bitwise), 60-leg perf ladder (zero plumbing regression; ring@750 9.51x, mesh@750 2.69x; known small-mesh sub-1.0 legs unchanged from 12F.3), browser zero-WASM smoke. Validation: gnssBaseline 328/328, R2B RealWasm 5/5, wasm 54/54, parity 25/25, lint 0 errors, typecheck clean, build clean, agent 3246 + 3 pre-existing study-desktop env failures. Decision GO-PRODUCTION. Reports: phase12f4-r2b-default-on (+ arch doc note). No math/tolerance/C++/UI/R1 changes.
- [x] Phase 12F.3 bounded R2B static-GNSS production proof (branch feat/gnss-native-r2b-production-proof, baseline origin/main d890c6a6 = PR #43 merge; PRODUCTION ROUTE but DEFAULT OFF). DONE: engine seam (provenance native-sparse-selected-qxx, nativeSelectedBlocksProvider, Dense/Selected union, block-statistics + identity gate, no dense Qxx) + R2B worker route (dedicated kill switch, fixed-order eligibility, pre-solve fill gate via correction factorNnz, 1 bridge call, clean-TS fallback, never R1/full-Qxx) + verifier (12F.2 thresholds) + 37 stub tests + 5-test RealWasm test + 60-leg perf ladder (crossover ring@75/mesh@250, mesh@750 37.2s→23.4s, fill ≤11.07) + 8/8 dataset legs (A pins + B0 1.965038 reproduce, coords bitwise) + 11/11 worker-thread proof + browser OFF smoke. Bounds kept: MIN 225 / MAX 750 stations / 2250 params / 4000 blocks / 1.5M nnz. Decision GO-ENABLE-R2B (12F.4 may flip certified cohort ON). Validation: lint 0 errors, typecheck clean, agent 3245 + 3 pre-existing study-desktop env failures (re-proven at pristine baseline), wasm 54/54, parity 25/25, build clean. Reports: phase12f3-r2b-production-proof, phase12f3-perf, phase12f3-datasets, phase12f3-worker-proof. No math/tolerance/C++/dispatcher changes; R1 stays OFF.
- [x] Phase 12F.2 §§8, 11-13, 16-19, 21, 23-24 R2B evidence runner + full parity/performance/fault evidence (branch feat/gnss-native-batched-selected-covariance; EVIDENCE ONLY — no production routing, no R1 default change, no math/tolerance changes, no dense Qxx inside R2B). DONE (this batch): src/engine/gnssBlockStatistics.ts (recoverGnssBaselineStatisticsFromBlocks: identical Phase 12D formulas, Qxx via block-store accessor, missing block throws, never silent full-Qxx fallback) + tests/gnssBaseline/gnssBlockStatistics.test.ts (4/4 agent: stub-backed block-vs-dense parity ≤2e-9, redundancy identity, missing-block fail-closed) + scripts/gnss/gnssR2BEvidence.ts (`gnss:r2b-evidence`: R0 vs old-R2 vs R2B per case, isolated children + timeout, R2B-first per-leg resilience, R2B-only mode ≥1000 non-chain, sparse-only assembly with no-dense-A/P asserts, 1 bridge call assert, fault matrix, bridge benchmark, dataset-B/setup-case hooks) + reports/gnss/phase12f2-r2b.json/.md (16 cases: 6 ok + 8 partial + 1 error + 1 timeout). Findings: mesh/ring/hub parity coords bitwise, vTPv ≤8.1e-15, Qvv/Cvv ≤1.1e-11, blockT ≤2.4e-10, what-if top1/top5 match, identity worst 4.0e-11 (gate 1e-6 PASS); R2B 1.8-15.7x vs R0 (mesh@750 69s→12s), ~1x vs R2; mesh@1000/1500 R2B-only PASS (30s/82s, identity PASS — sparse survives where dense OOMs); mesh@2000 MEMORY FAIL even sparse; chain 100-1500 R2B query ok everywhere but R0/R2/R2B statistics all blocked by pre-existing F-BRIDGE production PSD gate (R0 itself throws, orthogonal); chain@2000 TIMEOUT; fault matrix 9/10 store-internal (transposed-Q_AB structurally invisible + numerically identical here, oracle-parity noted); bridge chain@12: 1 vs 1 crossing, 222→21 requests, 1776→1512 bytes, solve 0.12→0.08ms; dataset-B NOT-RUN (no local intake). Decision GO-R2B-PRODUCTION-PROOF WITH-BOUNDS. Validation: new tests 4/4 + focused 28/28, lint 0 errors, typecheck clean, agent 3208 pass + 3 pre-existing Study calibration failures (stash re-proven).
- [x] Phase 12F.2 batched native selected-covariance architecture (branch feat/gnss-native-batched-selected-covariance, baseline origin/main c3730533 = PR #42 merge; ARCHITECTURE/EVIDENCE ONLY — no production routing, no R1 default change, no GNSS/Phase12D math/tolerance changes, no UI, no C++ parser logic, no setup-to-C++ move). DONE: §1 profiler (bridge exonerated — 1 call + 1 factorization; chain dominated by p triangular solves, mesh by TS pack + TS dense-assembly OOM @1500/2000) + §2 canonical block plan (28-50% scalar trim) + §§3-4/9/28 generic C++ solve_sparse_selected_covariance_blocks (one multi-RHS solve, streaming, transpose-memoized) + WASM binding + TS block store (no dense mirror) + block-store Phase12D (identical formulas, fail-closed) + R2B evidence runner (16 cases) + Dataset B R0-vs-R2B on A0/AC/AH/A (coords bitwise, SEUW rel <=5.7e-16). Findings: parity coords bitwise / Qvv <=1.1e-11 / identity 4.0e-11; R2B 1.8-15.7x vs TS; §7 = PARTIAL (still ~p solves — Takahashi deferred); chain stats blocked for ALL routes by pre-existing F-BRIDGE gate (R0 throws too); 2000 TIMEOUT/MEMORY-FAIL documented; faults 9/10. Decision GO-R2B-WITH-BOUNDS. Reports: phase12f2-profile, phase12f2-r2b, phase12f2-datasetb, phase12f2-batched-selected-covariance. R1 stays default OFF. §§1-2 DONE (this batch): scripts/gnss/gnssSelectedCovarianceProfile.ts (`gnss:selected-profile`, real-WASM R2, child-isolated OOM capture) + reports/gnss/phase12f2-profile.json/.md (14/16 ok; sparse-mesh@1500/2000 OOM in TS dense assembly) + src/engine/gnssSelectedBlockPlan.ts (pure canonical dedup) + tests/gnssBaseline/gnssSelectedBlockPlan.test.ts (5/5). Dominant cost: A (repeated triangular solves, uniqCols==params) on chain + mesh@1000, D (alloc-copy pack) on mesh ≤750; F duplication zero. Evidence only, no production/math/UI changes.
- [x] Phase 12F.1 bounded native static-GNSS R1 production proof (branch feat/gnss-native-r1-production-proof, baseline origin/main 406296de = PR #41 merge; PRODUCTION PROOF but DEFAULT OFF — R1 native sparse-correction + full-dense-Qxx worker route, TS setup/postprocessing, eligibility classifier + kill switch + provenance + fallback matrix + fault injection + boundaries + browser smoke + Dataset A/B manual proofs; no R2 routing, no math/tolerance/GVX/UI changes). DONE: src/engine/gnssBaselineAdjust.ts nativeRuntime seam (optional sparse-correction + native-Qxx provider, default bit-identical TS-dense, provenance typescript-dense|native-sparse-full-qxx) + src/workers/gnssBaselineNativeR1Route.ts (kill switch default OFF, worker-only ECEF-gnssBaseline-only eligibility, Tarjan bridge exclusion, bounds 150/750, S3 + inline C1/C2/C3 via 10I verifiers, clean-TS fallback) + tests/gnssBaseline/gnssBaselineNativeR1.test.ts (24/24 agent, stubs only) + reports/gnss/phase12f1-native-r1-production-proof.md. Findings: Dataset-B SEUW 1.965038 coords 0 qxx 6.9e-15; Dataset-A pins 2.100053/1.297104/1.988831/1.102511 reproduce + native coords bitwise qxx <=1.8e-12; §13 gates (F-BRIDGE/F-PSD) real-but-shared, recorded unchanged; solve-side speedup 1.29x@147/1.34x@747 but verification tax erases it (verified ≈ TS); heap GC-noisy, analytic p²·8B; browser zero-WASM by default; worker eligible/setup→native, fail/kill→TS. Decision NO-GO on enable (stays default OFF), bounds stand. Validation: lint 0 errors, typecheck clean, agent tier + 3 pre-existing Study failures (stash re-proven), build clean. Pushed same branch, no PR.
- [x] Phase 12F.2 §§3-4, 9-10, 28 batched native selected-covariance block API (branch feat/gnss-native-batched-selected-covariance; EXPERIMENTAL, evidence/test-harness only — no production routing, no R1 default change, no math/tolerance changes, all existing APIs intact). §§3-4, 9-10, 28 DONE (this batch): generic dimension-agnostic C++ `solve_sparse_selected_covariance_blocks` (single factorization reuse, canonical unique columns, ONE multi-RHS Eigen solve, stream-extract rows per column, identical (row,col) computed once, (B,A) answered as bitwise transpose, full bounds/finite validation, duplicates bitwise-identical, no dense Qxx) + `webnet_sparse_selected_covariance_blocks` WASM binding (factor nnz + phase timings + error buffer, native-test-linked) + wasmTypes decl + `queryBlocks` on WasmSparseSelectedCovariance (validate, single bridge call) + src/engine/gnssSelectedBlockQuery.ts (`queryGnssSelectedBlocks`: plan → block inputs → {diag, offDiag} flat-Float64Array store + transpose accessor, one bridge call) + cpp/tests/sparse_block_covariance_test.cpp (8/8: dense agreement, scalar agreement, dup/transpose/symmetry, determinism, OOB/blockSize-0/negative/count-0/NaN paths) + tests/gnssBaseline/gnssSelectedBlocks.test.ts (agent 6/6, stub-backed) + tests/gnssBaseline/gnssSelectedBlocksRealWasm.test.ts (wasm tier 4/4, real bundle; tier-registered). Numbers: chain-6 maxAbs 2.0e-19 maxRel 1.4e-13, repeated-edge-8 7.4e-19/2.2e-12, ring-12 2.2e-19/1.3e-12 (contract 1e-9 abs); unique columns 15/21/33 vs legacy R2 scalar lengths 111/207/291 (7-10x fewer solves); bridge calls 1; memory n×m solved only, no p² retention. Validation: cpp 8/8, gnssBaseline 286/286, lint 0 errors, typecheck clean, wasm:build clean, agent 3204 pass + 3 pre-existing Study calibration failures (stash re-proven).
- [x] Phase 12F.0 static GNSS native/WASM architecture & performance audit (branch feat/gnss-native-architecture-audit, baseline origin/main f2defd41 = PR #40 merge; EVIDENCE ONLY — no production routing/math/tolerance/GVX changes, TS oracle stays, no UI/RINEX; R0 oracle vs R1 native-full-Qxx vs R2 native-selected covariance; ECEF correction parity, 3x3 weights, Phase12D completeness, setup cases, Dataset B, corpus parity/perf/memory; GO/NO-GO + cohort recommendation in reports/gnss/phase12f0-native-architecture-audit.md). DONE: scripts/gnss/gnssNativeArchitectureAudit.ts + gnssNativeAuditCorpus.ts (deterministic ECEF/SPD corpus, fidelity-gated timed oracle, R1/R2 native runners reusing sparse-correction + selected-covariance APIs, parity comparators, intake loaders) + tests/gnssBaseline/gnssNativeArchitectureAudit.test.ts (13/13 agent: golden parity, translated-origin, fixed-endpoint hand-Qvv, trace identity, setup self-consistency, multi-component, F-BRIDGE/F-PSD pins) + tests/evidence/phase12f0_native_architecture_audit.test.ts (4/4 real-WASM manual, tier-registered + phase12f0 suite). Findings: R1/R2 coords bitwise, Qxx rel <=1.3e-9, trace 3.6e-12/1.3e-11 at 500/1000 (roundoff-scale, 80x+ under contract), full-vs-selected <=4.1e-20 (incl. hetero-mesh-25 lopsided-weighting probe); Dataset-A SEUW pins reproduced + native <=1.8e-12; Dataset-B SEUW 1.965038 + native <=2.2e-14; crossover ~100-150 params; R2 to 1000 stations (27.7 s), 2000 OOMs in TS dense assembly (documented, not in-suite); F-BRIDGE (bridges trip the TS eigen gate once dof>0 — solve legs bridgeless-only) + F-PSD (eigen gate ~12 orders stricter than diagonal floor). Decision GO-R1-THEN-R2 (R1 p<=750 bridgeless; R2 after R1; setup stays TS; GPS/G routes distinct; fallback designed not enabled). Validation: lint 0 errors (2 pre-existing warnings), typecheck clean, gnssBaseline 239/239, native-route focused 25/25, agent 3168 pass + 3 pre-existing Study calibration failures (re-proven via stash). No src/engine diffs, no tolerance changes. No PR (orchestrator opens).
- [x] Phase 12E.3 production GNSS endpoint setup uncertainty (branch feat/gnss-endpoint-setup-uncertainty, baseline origin/main 8cb1c17a = PR #39 merge). DONE: src/engine/gnssBaselineSetupUncertainty.ts (optional network-level setupUncertainty {horizontalCenteringSigma, antennaHeightSigma} m 1-sigma default zero; per-endpoint local ENU->ECEF from fixed a-priori coords, C_eff=C_raw+C_from+C_to, raw on rawCovariance, fail-closed sigma/ellipsoid/orientation/SPD) + adjust/report/import wiring (additive only) + tests/gnssBaseline/gnssBaselineSetupUncertainty.test.ts (17/17 synthetic: zero bit-identical, equator/lat45 hand goldens, independent cross-checks, endpoint sum, reversal, fixed-endpoint, trace, validation, Up-vs-naive, perturbation, full-solve AC/AH/A-style parity, routing tripwire, perf smoke) + reports/gnss/phase12e3-setup-uncertainty-production.md (manual A0/AC/AH/A: production SEUW == evidence 2.100053/1.297104/1.988831/1.102511, TBC intervals PASS, maxCov 2.7e-20, coords bitwise; perf 50/1.1ms 100/0.9ms 1000/3.8ms 10000/31.5ms). Text directive deferred programmatic-first. Gates A-I all PASS. No robust/mixed/native/sparse/WASM/UI/tolerance/CRS/GVX changes.
- [x] Phase 12E.2 real TBC commercial parity evidence (branch feat/static-gnss-tbc-parity-evidence, baseline origin/main 72009202 = PR #38 merge; EVIDENCE-FIRST — no production math/12D/tolerance/native/WASM/UI/CRS changes; vendor files read-only local-only, never committed). Delivered: scripts/gnss/tbcParityEvidence.ts via `npm run gnss:tbc-parity` (+ tbcAdjustmentReport.ts report parser, tbcXlsxReader.ts zero-dep xlsx reader, tbcParityModel.ts pure grouping/geodetic/setup-hypothesis helpers) + tests/evidence/tbcParityTooling.test.ts (7/7 synthetic-fixture, tier-registered) + reports/gnss/phase12e-tbc-commercial-parity.md. Outcome: gates A/B/C/C0/C2/D/E/H PASS, F/I FAIL (Model A SEUW 2.10 vs TBC 1.10; vTPv 1005.5 vs implied [273.4,278.4)), G/J/B-HYP NOTE; parity level 1/4. Model B setup hypothesis (0.005/0.002 ENU-rotated) closes the gap (SEUW 1.1025, max3D 78um) but is HYPOTHESIS, not TBC's formula. Brief correction recorded: ECEF table has 16 NAME-keyed rows, not 182. Residual/precision NOT COMPARABLE (Az/DeltaHt/EllipDist vs raw ECEF). 12E.3 recommendations in the report; docs/gnss/GVX_IMPORT.md untouched (no standards-relevant parser behavior found). Dataset B DONE (same branch, this entry): ProcessingGNSSBaselines 50v/8 stations zero-setup-error case via `npm run gnss:tbc-parity -- <dir> --dataset b` (new scripts/gnss/tbcParityDatasetB.ts + tbcBaselineSummary.ts + tbcGvxCompare.ts, report parser needed NO generalization — dataset-B HTML parses as-is) + reports/gnss/phase12e-tbc-dataset-b-parity.md (71-file manifest w/ SHA-256). Outcome: processing 50/50/0/0 all Fixed; reconciliations exact; pre/post GVX 50/50 serialization (max 9.3e-10 m, cov diff 0); MODEL B0 DOF GATE 150/21/129 EXACT, 2 iters, SEUW 1.965038 vs 1.97 display, coords sub-nm vs post-GVX (max3D 1.4e-9 m); parity level 3/4, CASE 1 — core LS validated for zero-setup-error case, dataset-A gap reclassified as setup-error stochastic (Model B stays HYPOTHESIS). Dataset A rerun still level 1 (SEUW 2.100053, max3D 4.334e-3). Validation: lint 0 errors, typecheck clean, evidence 11/11, gnssBaseline 209/209, agent 3138 pass + 3 pre-existing Study calibration failures. A0 zero-setup-error external-experiment recommendation recorded in report. Controlled setup-error experiment DONE (same branch): external A0/AC/AH bodies (manifest w/ SHA-256 in report) + original GVX; invariant gate PASS (only centering/height differ per expected matrix); CLI generalized (`--model m0|mc|mh|mch --report --out`, report-derived factor intervals, parser refFactorText + error columns, synthetic tests 14/14); four-run scorecard A0/M0 SEUW 2.100053 vs 2.10 PASS L3, AC/MC 1.297104 vs 1.30 PASS, AH/MH 1.988831 vs 1.99 PASS, A/MCH 1.102511 vs 1.10 PASS (all DOF 228, deltas + precision-deltas agree within display); verdicts causal PASS, independent-endpoint model SUPPORTED (centering + height), cross-baseline covariance NO detectable effect, Dataset-A revised level 3/4, 12E.3 production recommendation (optional setup sigmas default zero, tests vs A0/AC/AH/A). H2/H2-negative-control and S16 rotation analyses not triggered. Validation: lint 0, typecheck clean, evidence + gnssBaseline 209/209, agent tier, dataset-A/B reruns unchanged, no src/engine or vendor diffs.
- [ ] Phase 12E0 GVX 1.0 intake prep (SUPERSEDED BY 12E.2/12E.3/12G — deliverables ship, TBC checklist closed as evidence; see reports/roadmap/phase13a-todo-reconciliation.md §2) (branch prep/phase12e-gvx, local-only, DO NOT push/PR; vendor-neutral standards prep only — no TBC parity claim, no Trimble-specific code, no WASM routing, no adjustment-math/12D-statistics changes, no UI, no frame transforms, endpoints never FIXED). Deliverables: src/engine/gnssGvxXml.ts (zero-dep secure scanner, no XXE/entities) + gnssGvxSyntax.ts (raw document + frame resolution) + gnssGvxImport.ts (parseGvx/importGnssBaselineGvx, orientation b_obs=X_TO-X_FROM, m/m², Cxy=PXY·SDX·SDY, shared Cholesky no-repair) + tests/gnssBaseline/gvxImport.test.ts (order gate/orientation/version/security/frame/error policy + local-only official-sample determinism) + gvxParity.test.ts (original 3-station triangle GVX<->BL exact parity incl. TEST-ONLY artificial-datum adjustment) + scripts/gnss/gvxEvidence.ts via `npm run gnss:gvx-evidence` (manual-only, never CI) + docs/gnss/GVX_IMPORT.md + reports/gnss/phase12e0-gvx-prep.md. Samples (local-only ~/Downloads/webnet-gnss-12e/noaa/): 18v/20, 18v/21, 15v/21, 13v/15 marks; all cov valid, a-priori ~5e-9 m, parse 38-80 ms. Gates A-H recorded in report. TBC intake checklist 1-10 PENDING.
- [x] Phase 12D static GNSS baseline statistics/QC/reporting (branch feat/static-gnss-baseline-statistics, baseline 399e4793 = 12C over 12B over origin/main 9441897b; PRs #35/#36 unmerged at branch time, stacked locally). Validation: gnssBaseline 119/119 (17 files), lint 0 errors, typecheck clean, build clean, agent 3048 pass + 3 pre-existing Study calibration failures (same as 12B/12C), parity 25/25, native-route focused 35/35. All gates A-I PASS (12E GO). No auto-merge.
- [x] Phase 12C static GNSS baseline text import (branch feat/static-gnss-baseline-import, baseline 38d0f5e78 on feat/static-gnss-baseline-core = 12B over origin/main 9441897b): FILE/IMPORT BOUNDARY ONLY — WebNet-native BL text syntax (FRAME/UNITS/GX/BL/COV/SIGCORR) + generic delimited CSV importer + ENU shared-origin rotation + unit normalization + data-check diagnostics + canonicalize into Phase 12B gnssBaseline. No math/core/UI/native/WASM changes. Delivered gnssBaselineRotation + gnssBaselineNetworkImport + gnssBaselineCsvImport + 7 new test files + fixtures + examples/gnss/static-baseline-example.txt + docs/gnss/STATIC_GNSS_BASELINE_FORMAT.md + ARCHITECTURE 12C notes + reports/gnss/phase12c-baseline-import.md. All gates A-H PASS (12D GO). Validation: gnssBaseline 92/92 (13 files), lint 0 errors, typecheck clean, build clean, agent 3021 pass + 3 pre-existing Study calibration failures (re-proven via stash), parity 25/25, native-route focused 35/35. No auto-merge.
- [x] Phase 12G static GNSS product integration (2026-09-13, branch feat/static-gnss-product-integration, baseline 68a96f87; completed, validated, NOT committed/pushed per instruction): PRODUCT/WORKFLOW ONLY — zero math/covariance/R2B-eligibility/R1/tolerance changes (untouched: gnssBaseline*, gnssGvx*, R1/R2B routes, cpp, tolerances). New files: src/engine/gnssWorkspaceSession.ts (summarize/setStationFixed/buildGnssSessionInput/runGnssWorkspacePreflight) + gnssSampleNetwork.ts (1-fixed+5-free ring/diagonal/repeat, SYNTH tags) + src/hooks/useGnssBaselineWorker.ts (same worker bundle, direct-TS fallback) + src/components/gnss/{GnssWorkspacePanel,GnssResultsPanel,GnssStationTable,GnssWorkspaceModal}.tsx + tests/{gnssWorkspaceSession,gnssWorkerRoute,gnssWorkspacePanel} + tests-browser/gnss-static-workflow.spec.ts + docs/gnss/STATIC_GNSS_WORKFLOW.md. Additive wiring only: gnss-run/gnss-success/gnss-failure protocol + adjustmentWorker branch (runGnssBaselineWithNativeR2B isWorker:true), .gvx in IMPORT_FILE_ACCEPT, *.gvx intercept in useImportReviewFileSelection -> webnet:open-gnss event, AppShell toolbar-adjacent button + modal (no WorkspaceTabKey change), README one-liner + CURRENT_BEHAVIOR note. Validation: lint 0 errors, typecheck clean, new suites 20/20 agent-tier, gnssBaseline 29 files/328 green (numerics unchanged), real-browser E2E green, build green, test:agent 3268 pass + 3 pre-existing study-desktop calibration failures re-proven pre-existing via stash. Sample ring closes deliberately (degree-1 spurs trip backend Qvv PSD gate). No auto-merge.
- [x] Phase 12B core static GNSS baseline observation + 3x3 weighting (branch feat/static-gnss-baseline-core, baseline origin/main 9441897b): 12A contract implemented on TS dense path, GNSS-only ECEF internal mode, no BL parser. Delivered gnssBaseline type + covariance validate/invert + [-I +I] rows + datum/graph preflight + runGnssBaselineAdjustment + fixtures 1-8 + independent Gauss-Jordan golden + native-route tripwires + reports/gnss/phase12b-core-baseline-adjustment.md. All gates A-F PASS. Validation: 40/40 new, 12A 6/6, agent 2969 pass (3 pre-existing Study calibration failures re-proven via stash), parity 25/25, native-route focused 50/50, lint/typecheck/build clean. No auto-merge.
- [ ] Phase 12A static GNSS baseline network adjustment architecture (SUPERSEDED — contract consumed by 12B; see reports/roadmap/phase13a-todo-reconciliation.md §2) (branch feat/static-gnss-baseline-architecture, baseline origin/main 4afd157b): ARCHITECTURE + MATHEMATICAL CONTRACT ONLY — no production solver/parser/CRS/geoid/UI/native-routing changes. Deliverables docs/gnss/STATIC_GNSS_BASELINE_ARCHITECTURE.md + docs/gnss/STATIC_GNSS_BASELINE_MATH.md + reports/architecture/phase12a-static-gnss-baselines.md + tests/architecture/phase12a_gnss_baseline_math.test.ts (self-contained formula proofs). Decisions: MVP = ECEF Cartesian adjustment (single-origin local variant on same linear path), canonical 3x3 covariance m2, [−I +I] rows, min datum one fixed 3D station, GNSS-only first, independent blocks, TS path compatible / native routes need recertification, no CRS prerequisite. Phase 12B GO (bounded: core gnssBaseline observation + 3x3 weighting on TS dense path).
- [x] Phase 11A native 3D full-Qxx cohort widening certification + FINAL PRODUCTION batch (branch perf/3d-native-cohort-widening-certification, baseline 43d5a525): evidence certified GO to 768, then CAP-ONLY production change (NATIVE_FULL_QXX_MAX_PARAMS 384 -> 768 in src/workers/adjustmentNativeFullQxxAutoRoute.ts; eligibility/C1-C3/tolerances/correction-OFF/fallback frozen). Production proof: boundary 765/768 admitted + 771/900 rejected (no override), default native route + exact parity at 384/513/639/768, 768 fault matrix clean-TS, browser/memory/perf smoke, <=384 unregressed. Reports reports/performance/phase11a-3d-cohort-widening.{json,md}. PR perf/3d-native-cohort-widening-certification -> main (NO auto-merge).
  - STATUS (2026-09-12, DONE, uncommitted, no PR): diagnostic maxParams seam (default 384) + tests/evidence/phase11a_cohort_widening.test.ts (8/8 green) + scripts/phase11aBrowserEvidence.mjs (Chromium + Firefox green) + tier wiring + reports/performance/phase11a-3d-cohort-widening.{json,md}. Verdict GO to 768 (evidence only; production constant NOT changed). Validation: lint/typecheck/build clean; agent 2917 pass + 3 pre-existing Study calibration failures (proven pre-existing via stash); wasm 41/41; C++ 7/7; parity 25/25; release 4/4; evidence phase10i/phase10n/phase10m-toggle pass; unrelated regenerated reports restored.
  - STATUS (2026-09-12, FINAL PRODUCTION batch, DONE, uncommitted, no PR): CAP-ONLY change NATIVE_FULL_QXX_MAX_PARAMS 384->768 (src/workers/adjustmentNativeFullQxxAutoRoute.ts + cap-wording comments); everything else frozen, correction OFF. Boundary 765/768 admitted + 771/900 rejected (no override); default native route + exact parity (diff 0) at 384/513/639/768; 768 fault matrix (7) clean-TS; Chromium+Firefox 8/8 native/accepted/parity-0; memory 5x768 heap -9.7MB; perf ratios 0.62/0.69/0.66/0.49 (observation only); <=384 unregressed. New tests/phase11a_production_cap.test.ts (agent 6/6) + tests/phase11a_production_route_realwasm.test.ts (wasm 4/4, tier-registered); phase10m over-cap gate -> 257u/771; 11A evidence pins -> 768 + true-default routing; browser harness -> real default route; docs (CURRENT_BEHAVIOR/TEST_TIERS) + report §13 + JSON productionProof. Validation: lint 0 errors; typecheck clean; build clean; agent 2923 pass + same 3 pre-existing Study failures (re-proven pre-existing via stash); wasm 45/45; C++ 7/7; parity 25/25; release 4/4; evidence 10o/10p + 11A seam/regression selections green; unrelated regenerated phase8a7/8b/8b1 reports restored. Verdict READY TO MERGE (no auto-merge).
- [x] Phase 10P correction verification boundary study (2026-09-12, branch perf/3d-native-correction-verification-study, baseline 74cb10f7): STUDY ONLY — zero production/routing/default/cap/eligibility/tolerance/S3/C1-C3 changes, correction OFF. S3 A-F purposes + diagnostic cost split @384/system (factorize 8.2/59%, P-build 2.7/20%, accum 2.2/16%, condition 0.5/4%, solve 0.3/2%; bit-identical, >=95% attribution) + matrix-free residual candidate (exact weight semantics, derivation-pinned <1e-9) + shadow 52/52/0/2 with 2 PROVEN unexplained false accepts on weak geometry (dist-heavy: diff 2.17 vs residual 1.9e-16; weak-dir 1e-8 missed, ~100x blindness) + 20 fault forms (metadata gates load-bearing; S1 misses damping/condition faults) + no global tolerance separation + condition gate rejects 44/52 valids yet passes the bad case + S0-S5 all NO-GO + corpus 13/14 accepted (ladder + 8 10M families + altseed) + 2D read-only pass + floor ~94ms ESTIMATE (CLEAR WIN arithmetic, non-actionable) + 22/22 stale proofs + browser NOT-RUN. GATE D — keep S3, stop. Reports reports/performance/phase10p-correction-verification.{json,md}. Left uncommitted per mission (no PR).
- [x] Phase 10O study native factorization lifecycle (2026-09-12, branch perf/3d-native-factorization-lifecycle, baseline 8c2e3954): NO production/routing/default/cap/tolerance/S3/C1-C3 changes. Fingerprints N-only hash: iter L2 30/30 (0/30 RHS-only), last->final L3 8/10 + L2 2/10 (L3 = same-N at 1e-12 quant, not bitwise), Option-B B 8/10 + C 2/10; factorization reuse prize ~0.3ms (~0.2%) so prototype SKIPPED; single-family generator-ladder corpus (terrestrial-only/weak-chain/low-redundancy/uneven/compact NOT covered) A/C bit-identical + S3/C1-C3 accepted; 384 A/B/C C/B straddles 0.90 across runs (noise >> 0.28ms prize; committed 10N PARITY); S3 ~38% wall => robust >=10% impossible without S3 work => NO; GATE A unmet (reuse line stops), GATE B MET => bounded Phase 10P correction-verification boundary study (cut S3 cost WITHOUT weakening acceptance; correction stays OFF, no prototype). Reports reports/performance/phase10o-factorization-lifecycle.{json,md}. Browser NOT-RUN (no three-arm harness). Left uncommitted for review per mission. Trace nonlinear solve sequence + system fingerprints + reuse-level classification + C++/WASM lifecycle audit + symbolic-vs-numeric split + diagnostic factor-handle design (+prototype ONLY if safe, never production-wired) + same-system/iteration benchmarks + Option-B rebuild + augmentation audit + memory/ownership + handle safety + S3 lower-bound + full 10-case corpus + browser corroboration + GATE A/B/C/D decision. Reports reports/performance/phase10o-factorization-lifecycle.{json,md}. PR perf/3d-native-factorization-lifecycle -> main (NO auto-merge).
- [x] Phase 10N prove native 3D correction integration (2026-09-12, branch perf/3d-native-correction-integration, baseline c5e4cee0): separate default-OFF correction switch + dual-solver route with shared S3 + 10L C1/C2/C3 proof and whole-attempt clean-TS fallback; agent 12/12 (kill-switch + 12-case fallback matrix, A/B/C parity on gps-3d-cov-08/32); real-WASM evidence A/B/C on 32/64/128/128-altseed — C/B PARITY 0.906-0.952, C/A CLEAR WIN 0.66-0.67 at 384, correction diff 8e-16-5e-15, final parity BIT-IDENTICAL, S3 tax ~59ms at 384 consuming the kernel saving; browser NOT-RUN (no three-arm harness; deferred); Option B factor-reuse NOT valid, no handle API. Reports reports/performance/phase10n-native-correction.{json,md}. Cap 384, tolerances, C1/C2/C3, S3, 2D route, correction default OFF all unchanged. PR perf/3d-native-correction-integration -> main (NO auto-merge, NO default-ON).
- [x] Phase 10M certify 3D native production route (2026-09-12, branch perf/3d-native-production-certification, baseline 94cbfb1f): evidence-only certification — headline SURVIVES 5/5 processes (128: ~208->159, ratios 0.74-0.79); Chromium 128 ratio 0.83, Firefox 0.75; realistic corpus 7/7 pass + 1 excluded-with-reason; crossover first clear win 255 params, parity 144-192; 480-768 diagnostic-only (ratios 0.55-0.45, Qxx MB-scale); cap KEEP 384; kill-switch GO ON <=384 PROPOSED (not implemented); correction dominant recoverable stage (66.7 vs verify 29.5 at 384); solver 3D-capable, 2D gate policy-only; single-factorization needs new WASM export (~62-65 ms opportunity); fallback 6/6; corpus green except known 10K 2 ms noise gate (untouched); Phase 10N YES. Reports reports/performance/phase10m-production-certification.{json,md}. No production/routing/cap/kill-switch/tolerance changes.
- [x] Phase 10L reuse native verification evidence + fast physical validation (2026-09-12, branch perf/3d-native-verification-reuse, baseline 9b18161c): cached inline evidence + fail-closed finalizeNativeFullQxxVerification (16-test differential/fault matrix, 0 mismatches); packed-index dense C3 fast path with legacy fallback (16-test differential incl. holey-array fallback, 0 mismatches); evidence benchmark + reports/performance/phase10l-verification-reuse.{json,md} (gps-3d-128: new route ~156ms vs TS ~202ms, verdict A; finalizer ~1us; C3 74.85→2.57ms); kill switch default-OFF unchanged. Validation: lint/typecheck/build clean, agent 2893 pass + 3 pre-existing Study Desktop calibration failures, wasm 41/41, C++ 7/7, parity 25/25, 10I/10J evidence pass (10K trips known 2ms timing-noise assert, file untouched); independent review APPROVE with nits (unrelated report regenerations restored, hole guard added).
- [x] Phase 10K isolate 3D native verification overhead (2026-09-12, branch perf/3d-native-verification-boundary): diagnostic-only NativeFullQxxVerificationTiming collector (10 collector buckets + 8 dense-op counts; optional trailing params on verifyNativeFullQxxSystems + capture solver; production call sites pass nothing; on/off decisions bit-identical) + evidence-tier `tests/evidence/phase10k_native_verification_boundary.test.ts` (`phase10k` suite wiring). Route cohort gps-3d-cov-08/32/64/128 (1+5 runs): 2.0x double verification (1 inline + 1 route-level per system) proven; at 384 params verify ~113 ms/run (C3-physical 78.6 dominant, C2 7.0, probe/index/compare ~28, C1 0.1) of 262 ms native-vs-TS overhead; wall reconciles to session + re-verify + eligibility within ~1 ms. Cumulative A-G measured on pure stages; 768-param hypothetical ESTIMATE 1856 ms (diagnostic-only). Audits: duplicated-work, Qxx-consumer, C1/C2/C3 inventory (no redundancy conclusions). Reports `reports/performance/phase10k-verification-boundary.{json,md}`. GATE B (optimize verification implementation, bit-identical C1/C2/C3); GO for 10L scoped to GATE B only. Validation: 10K + 10J evidence pass, lint/typecheck/build green, agent 2862 passed with only the 3 pre-existing unrelated Study Desktop calibration failures. No production/routing/tolerance/numerical/C++/WASM changes.
- [x] Phase 10J 3D native performance decomposition (2026-09-11, uncommitted): measurement-only evidence campaign `tests/evidence/phase10j_3d_native_performance_decomposition.test.ts` (evidence tier + `phase10j` suite wiring) over the Phase 10I real-WASM route/bundle — production-route cohort gps-3d-32/64/128 + mixed gps-3d-cov-08 (1 warm-up + 5 measured runs/arm, median/p25/p75) plus a gps-3d-256 diagnostic-engine-only cohort (inline existing-generator input, seed 2401; route-ineligible above the 384-param cap, explicit boundary). Reports native SparsePhaseTimings/call counts/Qxx elements+bytes/factor metadata/verification+reuse per run, A/B/C/D attribution (C1/C2/C3 wrapper-only, no invented sub-buckets; otherMs unattributed), and unavailable-by-design controlled-variant verdicts. Committed `reports/performance/phase10j-3d-decomposition.{json,md}` (actual run values) + `phase10j-architecture-note.md` (A/B/C/D map, exact call graph). No production/C++ changes. Validation: evidence test pass, typecheck, lint, tier-manifest 11 pass.
- [ ] Study Phase 5 beta foundation — version/build identity, bundled official package and safe first-run/update UX, portable backup migration onboarding, release workflow/checksums, signing/updater decision docs, compatibility tests, and hosted validation. Preserve app identifier and Study domain behavior; do not touch Phase 9L files or adjustment/parser/solver/CRS/C++/WASM.
- [x] Study Phase 5C/5D/5E/5O-A/B/C/G bundled official-content boundary (2026-09-11, uncommitted): current package as read-only Tauri resource (`bundle.resources` at existing `study-content/packages/` file; fixed-file Rust status/id/read commands, no path crosses IPC, 64 MiB cap, fail-closed); platform-neutral frontend seam with browser fallback/test bridge reusing parse/validate/preview/import core; explicit first-run Install (refuses when official imports exist) via Get Started + Manage; bundled match/different preview with explicit Install Update only; mocked boundary/core tests incl. malformed fail-closed (9) + 4 Rust tests. Identifier/version, manual import, Study domain unchanged. Validation: Study 119 files / 1328 passed / 4 skipped; Rust bundled 4 passed, fmt/clippy clean; study-desktop typecheck/lint clean. No live GUI/packaged-resource validation yet.
- [x] Study Phase 4C final UX/correctness polish (2026-09-11): structural Complete Document renderer/header with exact-copy preservation; Tauri-only return-link cleanup; compact technical package diagnostics; clarified attempted/accurate/exam-ready drill metrics; neutral mock wording; conditional current-binding mock Home metric; focused/full Study validation. Learn intentionally unchanged. No adjustment/parser/solver/CRS/C++/WASM/numerical changes.
- [x] Study Phase 4C polish (2026-09-11, `e0bbfb93`): logical legal-clause reflow with hanging indent and `(a)`/`e)` display recognition; picker card boilerplate removal; all meaningful search-term highlighting; explicit provision `documentTitle` metadata with search schema/index version 2; current official package diagnostics and non-destructive preview visibility; lazy raw Manage controls; Learn CSS containment plus memoized grouping; DEV-only mount timing probes. Current package `nb-sit-statute-corpus-2026-09-11`: 61 documents / 3,756 components, SIT section `15(1)` has corrected registration/by-laws/surveyor-in-training wording and no subsections. Validation: Study 114 files / 1,290 passed / 4 skipped; Rust 29 passed; WASM 41 passed; industry parity 25 passed; hosted Study Desktop run `34617724272` Linux+Windows passed. Root agent/typecheck remain blocked only by unrelated untracked Phase 9L sibling files; local Tauri AppImage remains environment-blocked at `linuxdeploy`. No PDF normalization or adjustment/C++/WASM/parser/solver/content changes.
- [x] Study Phase 4 — desktop validation hardening (closed 2026-09-11 on `feat/study-desktop-tauri-port`, hosted run `34545688130`, commit `e76a6fe7`: standalone lint fixed by explicit local `tsx` dependency/Node invocation for Windows; Linux and Windows frontend lint/typecheck/tests/build, Rust fmt/check/tests, Tauri production builds, and bundle uploads all passed. Linux artifact `study-desktop-linux-bundles` and Windows artifact `study-desktop-windows-bundles` uploaded successfully. Local Linux `.deb`/`.rpm` passed; AppImage remains blocked at `linuxdeploy`. GUI smoke remains blocked on headless host (`DISPLAY`/`WAYLAND_DISPLAY` unset, `XDG_SESSION_TYPE=tty`), so no live GUI claim. Versions remain 0.0.0, legacy migration/versioning/branding/signing/update work deferred. No adjustment/C++/WASM/parser/solver/content changes.
- [ ] Study Phase 3 — native desktop interactions/windowing (3B–3E DONE 2026-09-10, uncommitted: `studyFileInteractions.ts` platform-neutral boundary; coupled Rust `study_backup_*` Open/Save dialogs via `tauri-plugin-dialog` with temp+rename export and 32 MiB import cap; Manage native buttons Tauri-only; browser file input/textarea/export-text unchanged; mocked boundary+hook tests + 6 Rust tests; no new frontend dialog/fs capability): 3A interaction inventory; 3B platform-neutral interaction boundary; 3C native backup Open dialog; 3D native Save dialog/atomic export; 3E parse-before-replace import (the dialog-read text is validated by `parseStudyImport` before `replaceAll`; no staging area or recovery journal — a failed import leaves existing data untouched and the previous export file remains the recovery path); 3F source picker; 3G–3J DONE 2026-09-10, uncommitted (platform-neutral `locateWindowBridge.ts`: browser keeps window.open + BroadcastChannel byte-for-byte, Tauri uses stable `study-locate-picker` WebviewWindow + typed pick/control events with session scoping, focus/reuse/close, cleanup, explicit errors; same Study picker route in child, no Rust domain logic; mocked bridge tests + least-privilege window/event capabilities; no live GUI validation yet); 3K capability audit; 3L browser parity; 3M integration tests; 3N final gate. Preserve browser interactions, portable backup format, Study content/behavior, and narrow Tauri permissions.
- [x] Study Phase 2 — native desktop persistence (2026-09-10): completed 2A–2J. Mapped 22 logical stores and atomic boundaries; added browser/native StudyStorage composition; Rust `rusqlite` bundled SQLite schema v1 with transactional migrations and bounded batch/CAS commands; native confined atomic asset files; Tauri native adapter for all 34 StudyStorage methods; desktop MiniSearch bootstrap/artifact persistence with fail-closed no-IndexedDB path; native asset and browser-format snapshot/export parity plus conformance/restart/rollback/traversal tests. Browser `webnet.study.v1` IndexedDB v10, OPFS, content/hashes, and Study behavior unchanged. Legacy Phase 1 IndexedDB→SQLite / OPFS→native migration deferred; DB metadata + file bytes are not one atomic unit. Validation: standalone 106 files / 1234 passed / 4 skipped, typecheck/lint/Vite build; Rust 23 passed/fmt/check; root lint/typecheck/build and agent tier 457 files / 2746 passed / 1 skipped; Tauri release deb/rpm compile/package. No adjustment/C++/WASM/content changes. Known limits: no live GUI/Windows validation, AppImage remains environment-blocked.
- [x] Phase 10I bounded native full-Qxx worker route (production routing, fail-closed): worker-only 3D single-solve `<=384`-param route (`src/workers/adjustmentNativeFullQxxAutoRoute.ts`, kill switch default-off) injecting ONLY the all-entry dense native covariance solver with verified provenance (`allowVerifiedNativeDenseQxxReuse` threaded EngineOptions/Runtime/statistics-context/10E gate) feeding Phase 10E statistics reuse; correction stays TS, no row-products/selected store/selected mode; 2D/preanalysis preserved on existing routes; every failure reruns clean TS. Agent contract `tests/phase10i_native_fullqxx_route.test.ts` (13/13: eligibility/preservation/provenance plus C1/C2/C3 verification faults) wired agent-required; latent int32-truncation stub bug fixed with neighbor suites green; report skeleton `reports/phase10i/`. Real-WASM parity/timing campaign NOT run (follow-up evidence required before widening). Validation (evidence): real-WASM campaign PASS (32/64/128: result diff 0, Qxx ~1e-20, C1 ~1e-20, C2 ~1e-15..1e-14, reuse on both arms, 6/6 fallback matrix clean-TS); native ~2x SLOWER than TS (verification-dominated) so route ships default-OFF. Agent 13/13, neighbors 58/58, lint/typecheck green. Report `reports/phase10i/phase10i-native-fullqxx-routing.md` carries actuals + GO-on-safety/NO-GO-on-perf decision. No commit.
- [x] Phase 10H fair native full-Qxx + statistics-reuse evidence: expose native phase timings through diagnostic WASM metadata, add explicit evidence-only native-Qxx reuse seam, compare contract-preserving native full-Qxx against production dense TS on genuine 3D corpus, and report fail-closed parity/performance. No production routing or contract changes.
- [x] Phase 10G final covariance architecture evidence (research only; no production routing/math/contract changes): added `tests/evidence/phase10g_final_covariance_architecture.test.ts` (dense baseline + analytic A/B/C demand + 4 native WASM legs with boundary walls/diagnostics/1e-6 equivalence, orientation synth, fail-closed/damping) wired in `scripts/testTiers.ts`, `runEvidence.mjs`, Evidence workflow, `docs/TEST_TIERS.md`; decision report `reports/phase10g/phase10g-final-covariance-architecture.md` (keep dense TS all-entry; natives equivalent not identical, selected-network omits all-pairs, phase timings unavailable via ABI). Validation: phase10g evidence PASS, lint/typecheck/tier manifest green.
- [x] Phase 10F post-reuse 3D performance reprofile (evidence only; no production behavior, mathematical/public contract, routing, or numerical changes): added `tests/evidence/phase10f_post_reuse_profile.test.ts` with clean/profiler medians, honest top-level wall reconciliation, full post-10E stage attribution, reuse route counts, all-pairs scaling, orientation-heavy synthetic 3D observation, and forced-legacy comparator; wired `scripts/testTiers.ts`, `runEvidence.mjs`, Evidence workflow, and `docs/TEST_TIERS.md`; report `reports/phase10f/phase10f-post-reuse-profile.md`, artifacts gitignored under `artifacts/evidence/phase10f/`. gps-3d-128 production median 212.28 ms; final dense covariance inversion 93.33 ms (44.0%) is new dominant stage, correction factor/solve 34.52 ms (16.3%) second, all-pairs precision 10.53 ms (5.0%) third; reuse active on all eligible ladder cases with statistics accumulation/inversion 0; forced-legacy parity BIT-IDENTICAL and 27.5% saved at 128. Decision: recommend Phase 10G final covariance architecture evidence; no implementation selected. Validation: phase10f evidence PASS, lint/typecheck/tier manifest green.
- [x] Phase 10E production Qxx reuse promotion (bounded to the Phase 10D cohort; no UI/protocol/formula/routing changes): eligibility gate moved to production module `src/engine/statisticsQxxReuse.ts` (`decideStatisticsQxxReuse`), probes stay separate in `src/engine/qxxReuseEvidence.ts`; automatic reuse only for normal converged 3D dense TS final Qxx with finite correct dimension (no preanalysis/robust/augmentation/damping/selected-covariance/sparse-row-products; TS correlation admissible; explicit convergence required; 2D legacy); test-only `forceLegacyStatisticsQxx` oracle, test-only fallback-matrix injections kept; agent contract `tests/phase10e_production_qxx_reuse.test.ts` (5/5: auto-reuse parity+call counts, force-legacy oracle, 2D legacy, robust fail-closed, TS-correlation reuse, gate matrix) + evidence-tier `phase10e` campaign (`tests/evidence/phase10e_production_qxx_reuse.test.ts`: 1 warm-up + 5 measured runs on gps-3d-32/64/128, N/Qxx max diff 0, BIT-IDENTICAL, reuse skips accumulation+inversion, 384-param wall 394.63→292.03 ms); report `reports/phase10e/phase10e-production-qxx-reuse.md`, artifacts gitignored under `artifacts/evidence/phase10e/`; tier wiring (`scripts/testTiers.ts`, `runEvidence.mjs` `phase10e` suite, Evidence workflow option, `docs/TEST_TIERS.md`, `docs/CURRENT_BEHAVIOR.md`); CI classifier `phase[4-9]`→`phase\d+` so phase10+ tests stay numerical-required. Validation: focused 35/35, evidence phase10e PASS, lint/typecheck green, agent 450 files 2688 passed/1 skipped. 10D opt-in flag retired (10D evidence migrated to the oracle); `reports/phase10d/` untouched.
- [x] Phase 10D evidence-only Qxx comparison/reuse seam (research/evidence only; no production default/formula/tolerance/contract changes): test-only EngineOptions `reuseFinalCovarianceInStatistics` (default legacy) letting standardized-residual statistics reuse the recovered final dense Qxx while still assembling equations, plus `qxxReuseProbe` instrumentation capturing final/statistics normals/Qxx and call counts; fail-closed on missing/dimension-mismatch/non-dense/robust/sparse-row-products/preanalysis; agent contract `tests/phase10d_qxx_reuse_contract.test.ts` (4/4) + evidence-tier `phase10d` campaign (`tests/evidence/phase10d_qxx_reuse_evidence.test.ts` PASS 4.1s): genuine 3D ladder shows final-vs-statistics N/Qxx max diff 0.00e+0 on all 6 fixtures, full-result BIT-IDENTICAL on all 5 admissible, reuse skips 1 accumulation + 1 inversion (384-param wall 374.80→265.91 ms), robust-Huber fails closed with parity; damped final recovery fails closed (`damped-final-recovery`, industry_demo now BIT-IDENTICAL incl. logs); covariance-augmentation gate added (unit-proven; never fires on the corpus — no 3D slope-dist case triggers synthetic float-zenith rows); TS correlation proven admissible by measurement (TSCORR case: N/Qxx diff 0, reuse taken, parity). Reports in `reports/phase10d/`, machine artifacts gitignored under `artifacts/evidence/phase10d/`. Validation: lint/typecheck green, agent 450 files 2685 passed/1 skipped, wasm 8/41, parity 25/25. Note: running `test:wasm` regenerates committed `reports/phase8a7|8b` with Phase-9B cap wording drift vs the committed text (restored; pre-existing generator/report skew, unrelated to 10D).
- [x] Phase 10C 3D covariance/statistics dense-tail evidence (research/evidence only; no production routing, numerical formulas, tolerances, public contract, or sync-session changes): confirmed separate final covariance and standardized-residual normal assembly/inversion; added opt-in granular statistics/precision profiler; ran 1-warm-up+3-run genuine 3D ladder; measured 94.78 ms final covariance inversion + 87.74 ms statistics Qxx inversion at 384 params and preserved 8,128 all-pairs relativePrecision rows; industry_demo remains inadmissible. Qxx equivalence/reuse and A/B/C candidate modes were explicitly not measured, so recommendation is MORE EVIDENCE REQUIRED before Phase 10D. Evidence test/report under `tests/evidence/phase10c_covariance_statistics_evidence.test.ts` and `reports/phase10c/`.
- [x] Phase 10B genuine-3D native correction evidence (evidence-only on `feat/3d-adjustment-substage-native-evidence`; no production routing/numerical/tolerance/protocol changes): opt-in internal detailed stage profiler (`src/engine/adjustDetailedSolveProfile.ts`: per-iteration assembly/accumulate/factor/state-update + covariance assembly/accumulate/invert + statistics/call counts, single-branch disabled overhead, no public result fields) and test-only per-iteration packed-system probe threaded through EngineOptions/nested scenarios; evidence-tier `phase10b` campaign on the genuine 3D corpus (industry_demo weak-case plus gps-3d-cov-08/16/32/64/128) with the existing real-WASM injected sparse correction seam — Level 1 identical packed systems (max corr diff 6.66e-16→4.00e-15, zero damping) and Level 2 full correction-loop shadow parity at 1e-6 m with zero fallbacks on all five admissible cases, industry_demo recorded inadmissible (weak/non-converged, TS damped vs native undamped) rather than invented; Amdahl ≤1.13x for factor-only offload up to 384 params, covariance+statistics dense tail dominates; agent contract `tests/phase10b_detailed_timing_contract.test.ts` (bit-identical results, sane counts, 3D eligibility preserved); reports in `reports/phase10b/`, machine artifacts gitignored under `artifacts/evidence/phase10b/`.
- [x] Phase 10A normal 3D adjustment performance audit (research/evidence only; no production routing or numerical changes): architecture audit, genuine 3D corpus, existing solve timing profile (opt-in evidence suite run), dense TypeScript scaling, native comparison only where representable, parity/blocker report, independent review APPROVE; Phase 10B decision MORE EVIDENCE REQUIRED; PR #15 merged as 23375236. (research/evidence only; no production routing or numerical changes): architecture audit, genuine 3D corpus, existing solve timing profile (opt-in evidence suite run), dense TypeScript scaling, native comparison only where representable, parity/blocker report, independent review.
- [x] Phase 9J path-priority hot-path optimization (edge-metric map + hoisted worst-pair key; no heap — profiled negligible; camp base 33.6→4.7 ms with legacy-mirror parity; agent + evidence green; no recommendation mathematics, solveEngine, C++, WASM, sparse routing, or worker pooling changes).
- [x] Phase 9I preanalysis metric/path-summary reuse (on `feat/preanalysis-recommendation-metric-reuse`; planning modules only, no solveEngine/C++/WASM/routing/tolerance/graph changes): pure `PreanalysisResultMetrics` builder (`src/engine/preanalysisResultMetrics.ts`: exact stationMajors/relativeMetrics/worst/median/weak counts + path summary, per-planning WeakMap identity cache) built once per planning invocation; `candidateScore`/`buildRecommendationEvaluation` consume metrics (trailing optional args, source-compatible), `resolveCandidateTemplates` accepts optional reused base path/metrics, threshold planner reuses current-step metrics; agent `preanalysisPlanning.05.metricReuse.spec.ts` (exactness, scoring/selection equality, exact 16-candidate score/order/result equality) + manual evidence `phase9i_metric_reuse_profile.test.ts` (`phase9i` suite in testTiers/runEvidence/TEST_TIERS; camp helper-loop summaries 32→16, rows bit-identical).
- [x] Phase 9H serial hot-path audit + lazy template-source elimination: empty-active preanalysis sessions derive templates from initial main result, removing one hidden solve while preserving active-addition legacy path; added agent equivalence/regression coverage and manual `phase9h` serial profile evidence. No numerical/C++/routing changes.
- [x] Phase 9E preanalysis scenario fast-path first stage (on `feat/preanalysis-scenario-fastpath`; dense 2D only, no C++/WASM/sparse/C1-C2-C3/inverse changes): skip the discarded preanalysis correction factorization/assembly when eligibility is proven (`src/engine/preanalysisCorrectionFastPath.ts` fail-closed helper, now also closed on degenerate sizes: numParams/numObsEquations must be positive since null covariance recovery belongs to the legacy failure contract; condition recorded from the bit-identical recovery N with unchanged log order; damped/non-regularizable recovery falls back to the legacy loop). Legacy loop retained as test oracle via test-only `preanalysisCorrectionFastPath: false` runtime override (default fast when eligible) threaded through AdjustmentRuntime/EngineOptions/nested scenarios, no persistence/UI. Phase 9D cache, candidate count/order/scoring, and all result/log contracts preserved; repeated-session parity moved to manual evidence tier (`tests/evidence/phase9e_preanalysis_correction_fastpath.test.ts`: 16 camp scenarios, recommendation list, forced-threshold cache, active-template, 3D integration fallback; `phase9e-fastpath` suite in runEvidence/evidence workflow/test tiers), agent tier keeps the bounded contract (`tests/phase9e_preanalysis_fastpath_eligibility.test.ts`: eligibility-unit matrix incl. degenerate sizes plus leveling-only 2D null-recovery failure parity, no false success). Audit evidence stays manual tier; audit test/report unchanged.
- [x] PR #10: Phase 9D solve-audit timing moved to evidence tier (test infrastructure only, zero production/numerical changes): repeated 4-run (1 warm-up + 3 measured) wall/stage timing campaign moved from `tests/preanalysisPlanning/preanalysisPlanning.04.solveAudit.spec.ts` (agent tier keeps functional memoization regression + integrity assertions) to `tests/evidence/phase9d_solve_audit_timing.test.ts` (manual evidence tier, timings recorded only, never gated, self-contained base-request setup with no audit-logic duplication); `scripts/testTiers.ts` EVIDENCE_TESTS + `scripts/runEvidence.mjs`/`evidence.yml` `phase9d` suite; docs/TEST_TIERS.md updated.
- [x] Phase 9D preanalysis scenario memo (session-local canonical scenario deduplication, audit coverage, camp contract/timing report; no sparse/numerical/C++ changes).
- [x] Phase 9C default preanalysis fast routing (closure: named >16 AND >0.25 preflight constants with fail-closed invalid handling, authoritative 128 cap before preflight, threshold-boundary/invalid/129/corpus tests, 86-effective vs 92-historical terminology): preserve Phase 9B.1 historical measurements, add conservative direction-heavy static preflight before WASM loading, prove exact camp direct-TS routing with unchanged contract, preserve exact-256/plain-GPS sparse cases, and document C2 conditioning research follow-up.
- [ ] Test Architecture V2 / evidence-tier migration (on `chore/test-evidence-tier`; test infrastructure only, zero production/numerical changes): five explicit tiers from one authoritative manifest (`scripts/testTiers.ts` + `TIER_MANIFESTS` symmetry map): `test:full` (unchanged), `test:agent` (full minus evidence/release/wasm), `test:wasm` (unchanged 7 files), `test:release` (now the fast automatic Phase 8B.1 verdict gate over committed reports, no workers/artifact), `test:evidence` (manual-only long campaigns moved to `tests/evidence/`: phase8b1 release, phase8a6 session, phase8a5 safety). New: `vitest.evidence.config.ts`, `npm run test:evidence` (via `scripts/runEvidence.mjs` selected suites all/phase8a5/phase8a6/phase8b1, unknown fails fast), `npm run test:certify` (local agent+wasm+release partition), evidence profile support, manual `Evidence` workflow (`.github/workflows/evidence.yml` with ref/suite inputs, native configure/build/CTest, selected runner, always-uploaded evidence reports); CI omits `test:full`/`test:evidence` by design. Manifest test enforces symmetry/disjointness/`tests/evidence/` name policy, reverse symmetry (manifest exactly equals the on-disk suite), plus suspicious campaign-like filename policy (stress/benchmark/soak/calibration under evidence or in explicit `EVIDENCE_NAME_EXCEPTIONS`); classifier treats `tests/evidence/` as numerical. AGENTS.md (root + tests + src/engine), docs/TEST_TIERS.md updated. Validation: manifest/config listing checks (no full/evidence execution); pre-existing full tests lost: 0.
- [x] CI test-tier routing / PR feedback optimization (2026-09-07, `chore/ci-test-tiering`; reviewer APPROVE): ordinary PRs run core only (`test:agent` + lint/typecheck/harness/parity/build/CLI/corpus) with a successful numerical no-op; numerical/unknown PRs run fresh WASM build + CTest + `test:wasm` + `test:release`; main/manual runs always certify. Local artifact-free core equivalent: 89.05s; numerical gates: fresh WASM 8.31s, CTest 7/7, WASM 40/40, release 11/11 in 648.79s; full 2687 passed/1 skipped in 670.70s; parity 25/25; tier union: full 450, agent 440, WASM 7, release 3, pre-existing full tests lost 0. Classifier is fail-closed; Phase 8B.1 remains in WASM and duplicate CI job removed. No production/numerical source changes.
- [x] Test execution tiering / agent feedback-loop optimization (COMPLETE 2026-09-07 on `chore/test-tiering`; test infrastructure only, zero production/numerical changes): four explicit Vitest tiers from one authoritative manifest (`scripts/testTiers.ts`): `test:full`/`test:run` (authoritative full suite, unchanged semantics; CI still runs `test:run`), `test:agent` (broad everyday regression minus release/evidence + real-WASM integration gauntlets), `test:wasm` (real-WASM/worker/native integration), `test:release` (expensive certification/evidence campaigns). New: `vitest.shared.ts` base + `vitest.{agent,wasm,release}.config.ts`, `scripts/testProfile.mjs` (`npm run test:profile [-- full|agent|wasm|release]` -> /tmp/webnet-test-profile, not committed), `tests/test_tier_manifest.test.ts` (cheap manifest regression, agent tier). Classification is semantic, never runtime-based; `run_session.test.ts` stays in agent (profile: 2 heavy tests 19.0s/29.4s = genuine startup-preanalysis computation, rest <2.3s; core behavior, not stress). Two artifact-requiring real-worker files (`phase7b5_candidate_corpus`, `phase7b_worker_sparse_proof`) moved agent->wasm tier because they fail (not skip) without the WASM artifact, making `test:agent` fully artifact-free. AGENTS.md (root + src/engine), README, docs/TEST_TIERS.md updated to the tiered workflow; CI comment corrected (120-session stress DOES run in CI full suite; only browser proof local-only); CI behavior unchanged. Measured on dev machine: pre-change full 660s (baseline 448 files/2668 tests); post-change FULL 674.4s (449 files/2674 tests = +1 file/+6 tests, the manifest test; **pre-existing tests lost: 0**); AGENT 40.0s (439 files/2623 tests, 2622 pass/1 skip, green with and without WASM artifact); WASM 27.8s (7 files/40 tests, fresh `rm -rf cpp/build-wasm && wasm:build` artifact, all pass; fails clearly when artifact absent); RELEASE 618.6s (3 files/11 tests, all pass); industry parity 25/25; CTest 7/7; build/lint/typecheck green; intentional-failure checks: failing ordinary test fails test:agent, failing wasm test fails test:wasm (temp mutations, reverted); release/full via config file-selection inspection (`vitest list`). Agent speedup: 660s -> 40.0s = 16.5x, 93.9% wall-time reduction (vs the 702.6s historical full profile: 17.6x). Independent reviewer verdict: APPROVE (20/20 points PASS, 0 fix rounds; minor M1 gitignore + M3 hasOwn guard applied, M2 committed-report timing noise noted as pre-existing/follow-up).
- [x] Phase 9 evidence execution/release separation (2026-09-08, test infrastructure only; zero production/numerical changes): split `tests/evidence/phase9a_cap_widening_evidence.test.ts` into independently executable `phase9a_cap_widening_scaling/faults/corpus` shards (no duplicate execution, every `it` body preserved verbatim; shared builders in `tests/evidence/phase9aEvidenceShared.ts`, no module-level shared evidence state) writing atomic raw fragments to `artifacts/evidence/phase9a/` (gitignored). Report assembly decoupled into pure `scripts/phase9a/phase9aReport.ts` (canonical key order, GO/NO-GO/NOT-EVALUATED verdicts, SHA fallback explicit env > GITHUB_SHA > `git rev-parse --verify HEAD` only > null with no origin fetch; `writePhase9aReports` rendering moved verbatim — committed `reports/phase9a/` Markdown re-renders byte-identically from its JSON). New fast gates: `tests/phase9a_report.test.ts` (agent tier, incl. committed-report render identity) and `tests/phase9a_release_verdict.test.ts` (release tier over committed reports). `scripts/testTiers.ts` (3 phase9a evidence shards + phase9a release verdict), `runEvidence.mjs` suites (`phase9a`, `phase9a-scaling/faults/corpus`), Evidence workflow (phase9a suites, separate fragment/report artifacts, dedicated `npm run phase9a:assemble-report` step), docs/TEST_TIERS.md updated. Committed `reports/phase9a/` files byte-identical; no production/C++/tolerance changes.
- [x] Phase 9A.1 sparse preanalysis cap-widening evidence (2026-09-08, evidence-only; reviewer APPROVE): production caps remain station 128 / parameter 128 / planning systems 64 / k=16 / query backstop 16384; no production numerical behavior or route defaults changed. Exact real-WASM coordinate-dominant ladder 128/160/192/256/384/512 passed with forced-TS contract equality, 1 warm-up + 5 measured runs (3 at 512), and false sparse authorities 0. Plain GPS passed at observed 258/514 dimensions; GPS covariance exclusion delegated 0 native calls; weak, ill-conditioned, direction-heavy, and all injected 256/512 faults failed closed with exact TS restart; max retained packed systems 1; mixed authorities 0. Final verdicts: PARAMETER CAP 256 GO; PARAMETER CAP 512 GO. Conservative recommendation: station-unknown cap 128, runtime parameter cap 256. Reports: `reports/phase9a/cap-widening-evidence.{json,md}` (HISTORICAL — byte-untouched by Phase 9B). Validation: fresh WASM/CTest 7/7, agent 2638/1 skip, WASM 40/40, release 27/1 skip, full 2705/1 skip, parity 25/25, lint/typecheck/build green.
- [x] Phase 9B production runtime parameter cap 256 (2026-09-08, on `feat/preanalysis-parameter-cap-256`; in progress): split authoritative caps — station unknowns 128 (static eligibility + whole-session policy, unchanged), runtime per-system parameters 256 (new `PREANALYSIS_SPARSE_PARAMETER_CAP` wired through correction pre-dispatch, covariance header/immediate-verify, and bounded-verification sentinel; runtime verifier helper defaults use the authoritative 256 parameter cap while historical 128 tests pass the explicit historical cap), planning systems 64, k=16, backstop 16384, captured 512 (all unchanged). Runtime boundary 255/256/257; station 127/128/129 unchanged. Camp direction-inflated count (~170) now fits pre-dispatch and falls back atomic via sentinel gates instead. Tests: new `tests/phase9b_preanalysis_parameter_cap.test.ts` (agent-required, fake-bundle), `tests/phase9b_preanalysis_parameter_cap_realwasm.test.ts` (wasm tier, one exact no-override chain-star session plus adjacent 257-parameter restart proof: 126 stations / 256 params sparse route + contract, offending 257 system not delegated — passed locally against the fresh real bundle in ~80s), `tests/phase9b_release_verdict.test.ts` (release tier over committed `reports/phase9b/`); updated `phase8a7` runtime boundary to 255/256/257 + camp fallback wording, `phase8b` bounds to default-256 verification with explicit-128 historical pin, `phase8b` real-WASM camp wording, evidence corpus `verifyCovarianceSystem` to the explicit historical 128 cap; `scripts/testTiers.ts` tiering; historical `reports/phase9a/` + `phase9a_release_verdict` untouched. Reports: `reports/phase9b/cap-256-rollout.{json,md}` (pins current caps + 9A sha256). No C++/UI/protocol/persistence/env controls, no 512 production, no evidence/full runs. Validation so far: lint/typecheck green, focused 7-file gate 34/34 green.
- [x] C++/WASM sparse preanalysis safety hotfix (2026-09-07, COMPLETE; on `hotfix/sparse-preanalysis-safety`; route re-enabled default-ON in a separate commit): (A) new shared `src/engine/effectiveProjectParse.ts` (`resolveEffectiveProjectParse`/`parseEffectiveProjectInput`: same profile resolution + includeFiles/projectRunFiles wiring as the direct solve) now used by `runSessionSolver` (pure extraction, no routing change) and by sparse eligibility, which judges effective UI settings plus parsed effective content (inline AUTOADJUST/ROBUST/TSCORR/GPS-covariance, incl. project run files; include-scoped directives stay scoped, matching the solver); (B) `evaluateSentinelC1` floor scaling fixed to existing constants (`allowed=max(ABS_FLOOR,tol*|expected|)`, relative over `max(|expected|,ABS_FLOOR)`) so tiny-covariance corruption rejects; (C,D) covariance gate extracted to `src/workers/preanalysisSparseCovarianceGate.ts` (shared abort/caps leaf `preanalysisSparseAutoRouteCaps.ts`): each system verified immediately with only compact verdicts retained (peak live packed systems <=1, instrumented and post-run guarded), `verifyCovarianceSystem` decomposed, atomic whole-session TypeScript fallback preserved. Tests: new `tests/phase8b2_sparse_preanalysis_safety_hotfix.test.ts` (13: include/projectRunFiles eligibility, tiny-C1, immediate-verdict/retention/corruption gate units, route-level corrupt fallback, source guards); updated `phase8a7` (UI-robust normalization + effective camp unknowns 46) and `phase8b` bounds guards for the gate file. Validation (final, route default ON): fresh local `wasm:build` (emcc 6.0.9) after `rm -rf cpp/build-wasm`; native CTest 7/7; all 9 WASM suites green; **final default-ON full suite 2667 passed / 1 skipped / 0 failed (448 files, 677s)**; lint/typecheck/build green; industry parity 25/25; focused hotfix + production 28/28; real-WASM corrupt covariance → atomic TypeScript restart identical; 120-session closure (60 sparse accepts / 60 fallbacks, false sparse authorities 0, bundle init 1); browser proofs at `/` and `/webnet/` with route naturally ON; Phase 8B.1 release proof 9/9, CI gate 5/5, verdict GO; **GitHub CI on `4481072`: build success + phase8b1-clean-runner-gate success**; independent reviewer APPROVE. **HOTFIX GO / READY TO MERGE** (not auto-merged; reports in `reports/preanalysis-hotfix/`). No routing/C++/tolerance/cap/k=16 changes.
- [x] C++/WASM engine Phase 7B.7 (closed 2026-09-05; test-only, no routing): automatic actual-worker corpus coverage and exact S0/S1/S2/S3 safety benchmarks completed. The 31-case corpus admitted 14 converged cases and ran all 14 through the real worker/WASM legacy-all-pairs path with full-contract parity, zero fallbacks, and zero false admits/rejects; metric-doped resection and perturbation boundaries were recorded. S3 (every correction-system oracle) is the conservative candidate policy with a 64-unknown cap; full benchmark evidence and worker timings are under `reports/phase7b7/`. Phase 7C has since closed (see Phase 7C entry below); worker-only fail-closed routing is enabled.
- [x] C++/WASM engine Phase 7B.7 actual-worker corpus closure (closed 2026-09-05; test-only, no routing): automatic admission (production eligibility + static preflight + reference convergence) over a deterministic 31-case corpus with metric-doped resection, start-coordinate and geometry perturbations; all 14 admitted cases run through the actual adjustment worker (real WASM, legacy-all-pairs compat, zero correction fallbacks) with full-contract agreement (coords/heights/status/iterations/DOF/SEUW/residuals/stdRes/redundancy/MDB/covariances/relativePrecision/order/condition) and zero false admits/rejects; deterministic report under `reports/phase7b7/`. Phase 7C parity fix (closed 2026-09-06): the sparse iteration branch now records result.condition on iteration 1 (native estimate preferred, TS-packed fallback), so the former bypass artifact is gone and the corpus report carries no condition notes. Tests 3 green; typecheck/lint pass.
- [x] C++/WASM engine Phase 7D (closed 2026-09-06; release hardening only, no cohort widening, no main merge): locked the bounded Phase 7C worker route contract — exact 63/64/65 unknown boundary, missing/corrupt WASM plus clean retry (no poisoned cache), every late S3 gate (correction/row-products/covariance fallbacks, NaN corrections, non-finite result, empty capture, 512-system capture bound) with byte-identical TypeScript restart, kill-switch block/re-arm, warn-only condition-threshold semantics, sparse-vs-TypeScript result.condition parity within 1e-9 relative, and session exclusions (preanalysis, inline .AUTOADJUST, GPS covariance). Tests 13 green in `tests/phase7d_release_hardening.test.ts`; deterministic 12-gate machine-readable report under `reports/phase7d/` via `npm run phase7d:release-report`; no routing/tolerance/C++/protocol/persisted-state/UI changes. No `verify:release` script added (report script + existing test/typecheck/lint commands cover it without duplication). Artifact audit: dist serving/base-relative glue path/CSP-free WASM instantiation unchanged; no versioning added.
- [x] C++/WASM engine Phase 8A (closed 2026-09-06; test/evidence only): audited the exact pre-analysis contract and compared a test-injected real-worker sparse planning path using real WASM correction plus legacy-all-pairs selected covariance, with exact planning-coordinate/iteration semantics, full covariance/precision contract checks, deterministic S0-S3 evidence, 4 admitted worker cases, 4 experimental variants, and zero production routing changes. Well-conditioned cases passed S0-S3; bounded camp planning geometry passed final S0 contract but failed closed at S1-S3 under ~1e51 conditioning. Evidence and audit are under `reports/phase8a/`; Phase 8B remains undecided pending condition-gated, bounded session-solve strategy work.
- [x] C++/WASM engine Phase 8B.2 (closed 2026-09-07 as GO; PR #1 open, no merge): after pre-enable CI passed on `5ab7028` and the isolated default flip, post-enable CI passed on `8a42c38` including the clean-runner gate; the bounded pre-analysis sparse route is enabled by default with the internal rollback switch retained. Post-enable local validation passed (2,654 tests, industry parity 25/25, lint/typecheck/build), browser production-worker proof passed at `/` and `/webnet/`, and the 120-session real-WASM evidence remains green. See `reports/phase8b2/`.
- [x] C++/WASM engine Phase 8B.1 (closed 2026-09-06 as GO; evidence only, no algorithm/routing/cap/cohort/adjustment changes, default stays OFF, no commit): exact-production-route real-WASM proof via test-only `scripts/phase8bPreanalysisWorkerBridge.ts` (+mem query) — 120 sequential mixed sessions on ONE reused worker (15 cycles x anchor/closure/smoke accepts + camp/campfull cap fallbacks + coldstream damping fallback + traverse adjustment + triangulation 7C-native interleave), 60 sparse/native accepts with contract maxCoordDiff 0, all repeats bit-identical, single bundle init, memory snapshots, cancel-then-healthy, terminate/restart, init-failure retry (9 green in `tests/phase8b1_preanalysis_release.test.ts`, `npm run phase8b1:release-proof`); browser proof at / and /webnet/ with real module worker + real dist webnet_core assets (`npm run phase8b1:browser-proof`): default-OFF control (anchor/adjust bit-identical cross-engine, camp core maxAbs 2.1e-11) PLUS enabled sparse acceptance through a test-only wrapper (real production worker module + harness-only switch, esbuild-bundled per base) with same-V8 native-path divergence on the anchor and bit-identical camp fallback; clean-runner CI job `phase8b1-clean-runner-gate` (wasm:build precedes the focused 5-test gate, `npm run phase8b1:ci-gate`); deterministic reports under `reports/phase8b1/` assembled by `npm run phase8b1:verdict` (closure JSON/MD with explicit browserEnabledSparseAcceptance/cleanRunnerCI fields, verdict GO, blockers none). Reviewer findings fixed (harness waiter timer cleanup). Full suite 445 files / 2648 passed, parity 25/25, native CTest 7/7, lint/typecheck/build green; prior timing churn restored.
- [x] C++/WASM engine Phase 8B (closed 2026-09-06; default-OFF release candidate, no main merge): recorded measured pre-sentinel costs (`reports/phase8b/pre-sentinel-cost.md`); refactored ONLY the production preanalysis route — C1 factors dense N once with selected column solves judging native-vs-TS agreement at every queried entry (no `invertSPDFromCholesky`/full Qxx), C2 native re-verification uses bounded deterministic complete columns with hard k = 16 (`PREANALYSIS_SPARSE_VERIFICATION_COLUMN_COUNT`; no `buildAllPairsQueries`/n^2 re-query, full columns only); kept C1+C2+physical C3, warn-only condition, correction non-authority, no dense P/full Qxx, adjustment route untouched; added source/no-n^2 guards plus focused tests (9 green in `tests/phase8b_preanalysis_bounds.test.ts`); proved exact production-worker dispatch through the real cpp/build-wasm bundle via test-only `scripts/phase8bPreanalysisWorkerBridge.ts` (no Phase 8A bridge, no fake bundle, no injected runtime) — default-OFF with zero WASM init, small-anchor sparse admission (16 correction + 32 covariance native calls, contract maxCoordDiff 0 / maxCovarianceDiff 4.3e-19), camp direction-inflation cap fallback (170 params > 128), corrupt/init-failure atomic restarts, 129-unknown and forced-C2 boundaries, 3-session interleave with one bundle init, terminate/restart recovery (11 green in `tests/phase8b_preanalysis_realwasm.test.ts`); deterministic release report under `reports/phase8b/`; Phase 8A-8A.7 suites green with timing churn restored. Default stays OFF; no commit.
- [x] C++/WASM engine Phase 8A.7 (closed 2026-09-06; production plumbing, default disabled): promoted the proven 8A.6 sentinel math into neutral production-safe `src/engine/preanalysisSparseCovarianceSentinel.ts` + `src/engine/preanalysisSparseSessionPolicy.ts` (no evidence/test/script imports) and added sibling `src/workers/preanalysisSparseAutoRoute.ts` (default-disabled kill switch; preanalysis/2D/plain eligibility with 128-unknown static gate; typed pre-dispatch fail-closed gates (`PreanalysisSparseCapError` before native delegation) enforcing per-system parameterCount<=128 and planning-system count<=64 with covariance pairing coverage; damping/fallback/C1/C2/C3/physical gates, bounded native all-pairs verification, condition warn-only, no correction authority, exactly-once atomic TS restart from the original request; test-only hooks: unknownCountOverride, systemCapOverride, forceC2Failure, forcePhysicalFailure); `adjustmentWorker` dispatches preanalysis to the new route and adjustment to the existing route (injected-runtime precedence and protocol unchanged); reused the existing adjustment WASM loader/cache untouched. Tests 15 green (`phase8a7_preanalysis_production`); phase8a6 suites green; reports under `reports/phase8a7/` (hook JSON/MD, audit MD). Default behavior unchanged (normal preanalysis stays TypeScript); Phase 8B stays NO-GO.
- [x] C++/WASM engine Phase 8A.6 (closed 2026-09-06; evidence-only covariance sentinel proof, no production routing/algorithm changes): pure test-only packed sparse normal accumulator (N = A'WA straight from packed design + packed upper-triangle weights, no dense P/full Qxx) with deterministic column probes (<=128 unknowns) reusing existing scale/factor/solve primitives, C1 dense-selected oracle, C2 inverse residual, C3 hybrid, and physical covariance validation (finite/positive-diagonal/symmetry/Cauchy-Schwarz); extended test-only Phase 8A bridge capturing actual packed selected-covariance inputs/results plus compact per-system sentinel metrics (production worker unchanged); deterministic fault injection (diagonal/off-diagonal/sign/swap/NaN/Infinity/missing/duplicate/packing) with two documented blind spots; production-shaped whole-session candidate with atomic all-or-TS fallback and clean restart; exact 127/128/129 unknown (exact-emergent via chain-star) and 63/64/65 planning-system (pure-policy) boundaries; 38-corpus re-run, 6 covariance-adversarial fixtures plus a synthetic near-singular damped fail-closed proof, 100 reused-worker mixed sessions bit-identical with zero fallbacks, cancel probe, C1/C2/C3 timing and all-pairs/selected scaling at 64/128. Every evidence criterion passed; Phase 8B automatic routing stays NO-GO (test-only by design: diagonal-only in-bridge metrics, pure-policy system counts, no production hooks). Reports under `reports/phase8a6/` (sentinel JSON, session-policy JSON, production-readiness MD, audit MD). Tests 8 green (`phase8a6_covariance_sentinel` 7 + `phase8a6_session_evidence` 1); typecheck/lint pass; no commit. Review fix (2026-09-06): C2 no longer re-solves a TS factor (tautological) — it judges captured native values with full-column coverage required, plus a bounded test-only all-pairs verification set through the same native solver; dedicated fault test proves C2 rejects a corrupted native value.
- [x] C++/WASM engine Phase 8A.5 (closed 2026-09-06; evidence-only numerical safety calibration): deterministic 38-case preanalysis corpus (4 Phase 8A anchors + 34 generated: size ladder 8/16/32/48/64/96/128/256 unknowns, GPS sizes, weight sweeps, weak-resection/collinear/angle-only/single-tie/narrow-brace condition family, redundant, rank experimental, 3D/GPS-cov/robust/TS-corr exclusions) with direct-TS per-case condition/physical-validity evidence, 6 bounded actual-worker runs (real WASM sparse bundle, legacy-all-pairs selected covariance, zero fallbacks, full final-contract agreement), P0-P4 ladder with explicit false admit/reject (P0 4 expected rank/underdetermined false admits; P1 absolute-1e12 gate rejects healthy cases so condition does not predict user-visible error; camp ~1e51 correction-unverifiable but result-correct), 50 sequential sessions on one reused worker bit-identical plus cancel-probe, timing/scaling recorded without gates, and deterministic reports under `reports/phase8a5/`. Caps proposed (unknown 128, session solves 64, condition warn-only) and Phase 8B NO-GO except a bounded S0+P3-sentinel path. Pure modules `src/engine/phase8a5PreanalysisSafetyCorpus.ts` / `src/engine/phase8a5SafetyStrategies.ts`, worker helper `tests/helpers/phase8a5WorkerEvidence.ts`, tests 11 green; no production routing/algorithm/tolerance/baseline/protocol/semantics changes.
- [x] C++/WASM engine Phase 7C automatic route (closed 2026-09-06; worker-only, fail-closed): ordinary single-solve 2D adjustment jobs with <=64 unknowns, no robust/TS-correlation/GPS-covariance/auto-adjust/suspect-impact/cluster-dual-pass, and clean static preflight are routed in the adjustment worker to the real WASM sparse bundle (every-iteration correction, row products, selected covariance with legacy all-pairs) with S3 verification of every captured system (dense-rebuild agreement 1e-9, undamped, finite condition, captured count equal to candidate iterations, result.condition agreement); an internal enabled-by-default kill switch, WASM init failure, sparse throw, engine fallback, oracle mismatch, damping, non-finite result, or missing condition cleanly reruns the original request in TypeScript. Worker protocol, persisted state, UI, C++, and tolerances unchanged. Tests 15 green in `tests/phase7c_sparse_auto_route.test.ts`; lint/typecheck/build pass.
- [x] C++/WASM engine Phase 7B.6 (closed 2026-09-05; test-only, no routing): pure static geometry preflight (fixed controls, unknowns, DOF, connectivity, metric/angular-only, direction setups) integrated fail-closed with production eligibility; three-layer first-system dense-vs-sparse correction handshake (captured first-system agreement, damping/fail-closed-condition-evidence gates, final+height agreement) with documented audit limits (first-system agreement and raw-N condition alone do not reject weak geometry); deterministic 32-case adversarial corpus with zero false admits/rejects; live in-process proof that weak resection is preflight+handshake rejected (1.48e+27 m divergence) while chain-16 handshakes clean (1.1e-15 m first-system, 7.11e-15 m coordinates); exact legacy precision preserved through fallback with clean TS restart. Tests 25 green (11 preflight + 10 handshake + 4 corpus); typecheck/lint pass.
- [x] C++/WASM engine Phase 7B.5 (closed 2026-09-05; no production routing): implemented exact legacy all-pairs compatibility through explicit Option-B sparse covariance demand, benchmarked against dense-Qxx demand, and simulated the candidate corpus in the actual sparse worker. `tests/phase7b5_precision_compat.test.ts` (17) proves row/order/field parity, fixed/orientation/3D/arbitrary-pair semantics and fail-closed reads; `tests/phase7b5_candidate_corpus.test.ts` covers 11 candidates, 5 eligible/6 rejected, 4 worker runs, weak-geometry divergence detection, and clean TS restart. Option-B demand reports at 25/50/100/128 use 0.510/0.505/0.502/0.502 of dense ordered demand. The Phase 7C NO-GO is resolved: the exact production-candidate corpus gate plus worker-only automatic routing has since been enabled (see Phase 7C entry above).
- [x] C++/WASM engine Phase 7A (closed 2026-09-05): restored/rebuilt the Emscripten 6.0.9 artifact; added deterministic 3D sparse-only evidence (`gps-3d-16/32/64/128`) with split horizontal/height truth checks and manageable TS-vs-sparse parity; audited the user-visible condition diagnostic and legacy all-pairs precision contract; added test-only fail-closed `evaluateSparseProductionEligibility`; reran fresh-artifact WASM/browser/worker gates; documented conservative dense-fallback and Phase 7B recommendations. Full ENU (EU/NU) covariance and sparse execution inside the adjustment worker were subsequently closed by Phase 7B; production routing remains unchanged.
- [x] C++/WASM engine Phase 7B numerical/precision batch (closed 2026-09-05; no routing/tolerance/baseline changes): (1) GPS covariance audit — the only field syntax carrying full 3D off-diagonals is G0/G1/G2/G3 into `gpsCovariance3d` (plain `G` carries only corrEN; no parser path sets corrEU/corrNU/stdDevU); in LOCAL mode the solve uses a diagonal proxy with an explicit geodetic warning, so full-ENU weights require `.CRS GRID` mode — proven by deterministic grid-mode fixtures (`src/engine/phase7bEnuFixtures.ts`: small manageable + 8-unknown scaling chain, all SPD blocks) with parser→inverse-weight (W·C=I) and dense-vs-structured quadratic parity; TS-vs-sparse agreement plus sparse-only scaling with zero fallbacks and repeat determinism; arbitrary-pair selected-covariance parity through the existing sparse API. (2) Sparse raw-N condition metadata — pure `estimateSparseNormalCondition` (`src/engine/sparseNormalCondition.ts`) bit-replicating dense/TS/C++ N assembly (ordered writes incl. the shared unguarded off-diagonal mirror-write rule), recorded only for the first correction iteration into additive test-only diagnostics; warning classification/wording mirrors production (`1e12`). Resolved: native C++ raw-N condition metadata is exported additively and consumed on the first correction iteration; an in-memory `AdjustmentRuntime` seam reaches LSAEngine and a test-only actual worker bridge executes the real sparse bundle with zero fallbacks. Remaining intentional limits: automatic routing, LOCAL-mode full-ENU weights (diagonal proxy by design), and persisted/UI backend choice.
- [x] C++/WASM engine Phase 6 (closed 2026-09-05): profiled and hardened the experimental sparse pipeline; measured packing/transfer/factorization costs, assessed structural reuse and sparse covariance alternatives, added shadow parity tooling, and recorded the production-readiness matrix plus explicit Phase 7 recommendation in `docs/CPP_WASM_ENGINE.md` — all without changing production routing. Phase 7 NOT started.
  - [x] Phase 6 measurement/shadow tooling (2026-09-05): added `bench:sparse-runtime` broad stage profiles, local `parity:sparse-shadow` TS-vs-selected-sparse reports, fail-closed comparison tests, and native sparse timing fields/tests separating assembly, equilibration, symbolic analysis, numeric factorization, and solve. Initial native measurements show symbolic analysis dominates small solves; no reuse/workspace optimization is adopted before finer WASM boundary measurements.
  - [x] Phase 6 developer tooling batch (2026-09-05): `npm run bench:sparse-runtime` (stage timing breakdown TS vs sparse selected-network over Phase 5 generated cases, coord tolerance 1e-6 m) and `npm run parity:sparse-shadow -- --input <file.dat>` (TS vs injected sparse selected-network deterministic JSON/Markdown summary with diagnostics + numeric maxima, fail-closed on divergence/fallbacks); pure `phase6SparseShadowCompare` helper with focused tests in `tests/phase6_sparse_tooling.test.ts`; no production routing changes, no symbolic reuse or persistent workspace yet. Quick runtime run: agreement 2.84e-14 m, zero fallbacks; industry-reference shadow run: PASS at 2.41e-12 m.
  - [x] Phase 6 covariance damping audit (2026-09-05): real-WASM probes classified full-rank SPD and near-singular full-rank covariance as undamped success, while rank-deficient systems required damping and returned unusable damped inverse magnitudes; existing selected-covariance damping rejection and dense TypeScript fallback were exercised and preserved. Eigen SimplicialLDLT is pivot-free and not equivalent to WebNet pivoted LDLT, so no sparse LDLT replacement was adopted.
    - [x] Phase 6 sparse-only large benchmark route (2026-09-05): test-only `npm run bench:adjust:sparse-large` (`scripts/benchmarks/benchmarkSparseLargeAdjustment.ts`) running actual `LSAEngine.solve` with only the injected full sparse selected-network bundle (no dense TS reference, no dense Qxx/all-pairs) over deterministic generated cases from `src/engine/phase6BenchmarkNetworks.ts` (chain 256/512/1000, GPS 128/256, `gps-2d-cov-08`, `chain-2d-robust-tscorr-16` via existing `.GPS WEIGHT COVARIANCE`/`.ROBUST`/`.TSCORR` syntax, and modest deterministic 3D `gps-3d-cov-08` with `.3D` + V-dh legs + GPS vectors + covariance weighting). Recording solver decorator captures equation rows/param counts/factor metadata (design/weight/normal/factor nnz, AMD/SimplicialLLT); fail-closed checks cover generated-truth (incl. height `h`), repeat determinism, zero all-pairs relativePrecision, selected queries << n^2, and zero fallbacks; size/wall-clock/RSS guards stop safely before OOM. Focused tests in `tests/phase6_sparse_large_benchmark.test.ts`. Full run 8/8 converged, zero fallbacks, bit-identical repeats; chain-1000 ~550 ms, 6996/4000000 queries, ~383 KB sparse vs ~183 MB estimated dense. No production routing/math changes.
- [x] Phase 6 medium-case evidence batch (2026-09-05): extended the deterministic Phase 5 generator from 6 to 9 full cases (`chain-2d-64`, `chain-2d-128`, `gps-2d-64`; quick mode unchanged, existing seeds/inputs byte-identical) and added a shared `BENCH_MAX_UNKNOWN_COUNT` size guard (default 256) that skips every route including the TS reference with an explicit reason. Full `bench:adjust:sparse-full` run: 9/9 cases converged on all four routes, zero fallbacks, max diff 5.68e-14 m; TS medians 25/108/34 ms vs selected-network 13/27/22 ms (chain-64/chain-128/gps-64); selected queries 0.01 of n^2 at chain-128. Limitations: no gps-128+ coverage, TS dense/all-pairs unmeasured beyond 128 unknowns, no reuse/routing changes adopted. No production routing or solver-math changes. pure `src/engine/sparseStructureFingerprint.ts` (parameter/equation counts, design rowOffsets/columns, weight rows/columns; values and identity excluded; dual-FNV fingerprint plus collision-safe array-confirming equality, fail-closed validation) used only in `tests/phase6_sparse_structure_fingerprint.test.ts` (6 tests: same-structure/different-values, changed columns, changed offsets/weights/counts, determinism, invalid lengths, repeated Phase 5 generated-solve stability). Conclusion: pattern stable across repeated Phase 5 generated solves with identical topology (rescaled values keep the fingerprint; chain-2d-04/gps-2d-08 repeats converge identically), sensitive to any structural change — preliminary stability gate for future reuse studies; no caches, reuse, or production routing added.
- [x] C++/WASM engine Phase 5 (2026-09-05): completed the test-only shared sparse numerical bundle, nested route diagnostics, selected-network covariance store/mode, deterministic generated-network benchmarks, selected-query scaling assertions, and worker/RSS/heap stress proofs; preserved production TypeScript and the legacy all-entry/all-pairs precision contract. Quick benchmark routes matched TS within 2.84e-14 m with zero sparse fallbacks; selected mode reduced covariance queries to 28/64 and 60/256; the 25-iteration worker stress passed with one bundle initialization, zero fallbacks, bit-identical repeat output, and stable post-init RSS. Phase 6 recommendation: keep observation models and production routing in TypeScript; first pursue only typed-array transfer/factorization-reuse experiments justified by larger-network measurements.
  - [x] Phase 5 bounded infrastructure (2026-09-05): test-only `ExperimentalSparseNumericalBundle` factory (one shared WASM module, all three sparse solvers), `buildExperimentalSparseEngineOptions` injection helper, and additive `ExperimentalSparseRouteDiagnostics` counters/reasons wired through the existing experimental correction/row-products/covariance paths with nested-solve propagation; production defaults unchanged. Focused tests in `tests/phase5_sparse_bundle_diagnostics.test.ts`; lint/typecheck pass.
  - [x] Phase 5 selected-network covariance mode/accessor (2026-09-05): test-only `experimentalSelectedCovarianceMode` flag plus pure deterministic `SelectedCovarianceStore` (fail-closed reads, canonical symmetric keys, deduped plan queries). Selected recovery queries `covarianceQueryPlan` station+connected/requested entries only (no dense Qxx reconstruction); precision propagation reads through the store accessor and skips only legacy all-pairs relativePrecision; default all-entry dense contract and nested propagation preserved. Parity tests in `tests/phase5_selected_covariance_mode.test.ts` (station/relative/REL/PTOL, query count 36 << 64, missing-entry failure); lint/typecheck/build pass.
  - [x] Phase 5 worker-compatible bundle proof (2026-09-05): test-only `node --import tsx` worker_threads script `scripts/wasmSparseBundleWorkerProof.ts` (`npm run wasm:sparse:bundle:worker-proof`) that initializes the bundle once per worker, runs one generated 12-param/30-equation case through all three solvers, returns route diagnostics, repeats 25x with heap/RSS observations (heap drift ~0MiB, RSS one-time init plateau ~+10MiB), and verifies repeat-seed determinism; lint/typecheck pass.
- [x] C++/WASM engine Phase 4 (2026-09-05): eliminated dense observation-equation P from successful experimental sparse correction, standardized-residual, and selected-covariance paths using structured symmetric weights, dense/reference and sparse writers, direct packed transfer, robust/TS/GPS/constraint parity, and memory benchmarks; preserved TypeScript production behavior and all-pairs relativePrecision. Full suite, industry parity, native/WASM CTest, WASM smoke/parity, builds, and benchmarks passed. Observation models and production backends remain unchanged.
- [x] C++/WASM engine Phase 3 (2026-09-05): audited Qxx consumers and added experimental selected covariance entries, batched row products, deterministic query planning, safe WASM wrappers, and injected standardized-residual routing while preserving dense TypeScript covariance/statistics and all-pairs relativePrecision behavior. Native/WASM covariance tests, wrapper tests, full suite, 25/25 industry parity, build, and sparse parity passed; no production backend switch, user-facing precision-scope change, parser/equation-model migration, SIMD, or threads.
- [x] C++/WASM engine Phase 2 (2026-09-05): added the experimental, explicitly injected sparse equation-row/P-upper-triangle boundary using pinned Eigen 5.0.1 sparse Cholesky; preserved Phase 1 dense/reference and TypeScript production paths. Native/WASM sparse tests, ABI checks, packing tests, robust/GPS/full-adjustment parity, browser smoke, and sparse benchmarks passed. Maximum full-adjustment coordinate difference was 2.41e-12; production remains TypeScript-only. No covariance, statistics, parser/equation-model migration, SIMD, threads, or Phase 3 work.
- [x] C++/WASM engine Phase 1 (2026-09-05): ported correction-only dense scaled/damped Cholesky solving with native CTest and real WASM ABI/wrapper parity; CMake 4.4.3, CTest 4.4.3, Emscripten 6.0.9, Ninja 1.13.2, Node 26.8.1. Differential n=1/2/5/10/25/50/100 max correction difference 3.39e-14; production remains TypeScript-only. `npm run wasm:solver:smoke` and `npm run wasm:solver:parity` passed. No sparse assembly, covariance, SIMD, threads, or Phase 2 work.
- [x] C++/WASM engine Phase 0 (2026-09-05): documented the current TS solve/matrix architecture and measured bottlenecks; added reproducible direct `LSAEngine` benchmarks and labeled TypeScript baseline artifacts; added the portable C++20/CMake core, native CTest smoke test, optional single-threaded Embind/WASM target, scoped C++ guidance, lazy TS backend/WASM loader seam, package commands, and focused sanity tests. Production remains TypeScript-only and no solver was ported. Native direct GCC smoke passed; CMake and Emscripten smoke checks were unavailable because `cmake`/`emcc` are not installed on this machine. Full tests/lint/typecheck/parity/build passed; committed and pushed on `feat/cpp-wasm-engine`.
- [x] Locate picker live-use follow-up (2026-09-05; schema stays v10 with exactly five Exam Prep stores; frozen pools 133/57/317/452/24 and curriculum/corpus hashes unchanged): (1) **idempotent picker-context adoption** — duplicate `picker-context` for the same sprintId+token is ignored completely in the Library (no search/debounced/results/waiting/notice/index reset; control channel subscribed once per sprint id via ref guard); a new token resets search/debounced/results/waiting/notice/index and updates the banner; (2) **primary same-tab Locate picker overlay** — `Open Locate Picker` no longer calls `window.open`; it renders a near-fullscreen overlay (`ExamPrepLocatePickerOverlay`, keyed per item) hosting the reusable StudyLibrary picker presentation/search/actions with the current prompt and `Close Picker`; direct picks call back into the sprint, close the overlay, freeze selection-vs-expected feedback, and persist only on Continue; overlay document/provision navigation opens in a new tab so queue/index/timer/found state stays alive; closing without a pick preserves the item; reopening after an advance starts fresh; the persistent BroadcastChannel picker remains as explicit `Open Picker in New Tab`, plus `Open Statute Library` and `Check Answer`. No curriculum/tasks/scoring/attempts/DB/schema/store changes; same search service, no second algorithm. Tests: new idempotency (duplicate-ignored/new-token-reset) + overlay (no-window.open, pick→feedback→Continue, close-preserves, reopen-fresh) regressions; existing new-tab flow/persistent tests retargeted to `Open Picker in New Tab` (8 files / 36 tests green); typecheck green.
- [x] Exam Prep Recall authoring review bundle (2026-09-04, baseline `36498a208a554a56bdadc991e4eb5e7bcebeb0eb`, independently reviewed and validated): pure deterministic 57-record authoring bundle with current card/unit/source/lookup/relationship context, mechanical flags, and null human-authoring fields; generated supplemental JSON/Markdown reports with one section per card and derived multi-card appendix; old `recall-quality-audit.md` remained byte-identical; no runtime import or curriculum/persistence changes. Focused authoring/curriculum/QA tests 121 passed, full suite 2294 passed (1 skipped), lint/typecheck/build green. Commit/push completed below. (2026-09-04, baseline `36498a208a554a56bdadc991e4eb5e7bcebeb0eb`): pure, deterministic, source-grounded HUMAN REVIEW preparation for all 57 frozen Recall cards (audit only). DO NOT rewrite curriculum/mustRecall/prompts/IDs/FSRS/queues/DB/schema/stores/Mock; do not change frozen hashes; keep `recall-quality-audit.md` byte-identical. New pure helper `src/study/examPrep/qa/examPrepRecallAuthoringReview.ts` (one record per card in canonical order: current prompt/answer + char/word counts, owning-unit exam goal/depths/core understanding/recognition cues, source documents + official titles, every owning-unit source anchor with title/label/role, same-unit mustLocate targets, related ids/unit-entry counts/siblings, mechanical flags genericPrompt/multiRecallUnit/fragmentLike/>240/>320/numericDetail/exceptionLanguage/multipleConcepts/answerOverlapsLocate — all deterministic, existing QA thresholds reused: fragment 10, >240 5; confirmed counts genericPrompt 57, multiRecallUnit 18, veryLong 0, numericDetail 6, exceptionLanguage 11, multipleConcepts 27, answerOverlapsLocate 5); `qa/examPrepRecallAuthoringReport.ts` renders byte-deterministic Markdown (group summary with counts + affected ids, one section per 57 cards with HUMAN REVIEW template, derived Multi-card units appendix incl. A-NBLS-01/A-SURV-03). Every JSON record carries null authoring fields (decision/proposedPrompt/proposedAnswer/proposedAction/reviewNotes). `npm run study:exam-prep:recall-authoring-review` regenerates ONLY `reports/exam-prep/recall-authoring-review.json` + `.md` (metadata frozen at baselineCommit/curriculumId/hashes, no wall clock). No runtime import. Tests: `tests/study/exam_prep_recall_authoring_review.test.ts` (10). Docs: STUDY_MODULE, CURRENT_BEHAVIOR. Completed: full validation, independent review, commit, and push.
- [x] Retrieval workflow corrective patch (2026-09-04, baseline `5b2aa95a713c30440820d63ceafddf9cea6c6a03`, independently reviewed and validated): Locate picker now uses token-scoped BroadcastChannel (not `window.opener`) with noopener/noreferrer preserved, stale/duplicate rejection and cleanup; objective picks freeze timer, show selection/expected feedback, and persist/advance only on Continue with retry-safe failures; popup null handles no longer produce false errors; normal/NAV/Mock Recognition wording is task-aware and shared; deterministic Recognition QA flags/report expanded. Frozen pools, hashes, DB/schema v10, and five stores unchanged. Full suite, lint, typecheck, build, and focused regressions passed; committed and pushed below. (crash-safe plan, baseline `5b2aa95a713c30440820d63ceafddf9cea6c6a03`): replace Locate picker opener messaging with token-scoped BroadcastChannel while preserving noopener/noreferrer; add feedback-before-Continue objective Locate flow with manual fallback and failure retry; fix popup-null false errors; unify task-aware normal/NAV/Mock Recognition wording; extend deterministic Recognition QA flags/report; add protocol, picker-feedback, wording, leak, and active-session regressions; preserve frozen pools/hashes and v10/five-store persistence; independently review, validate, commit, and push. Do not begin Recall curriculum rewrite. Implementation status (2026-09-04): src migration done — token-scoped BroadcastChannel picker (`webnet-study-locate-picker:<token>`) replacing `window.opener.postMessage` with channel cleanup + unsupported fallback; Locate objective pick pauses on selection-vs-expected feedback and persists/advances only on Continue (save failure keeps feedback; manual Check Answer fallback intact); `openStudyUrlNewTab` returns `StudyNewTabResult` and never reports failure from a null WindowProxy; task-aware Recognition wording/expected labels (normal vs NAV) shared across sprint + Mock prompt/grading/review; Recognition QA audit extended (short NAV ≤3 words, exact generic legal nouns, cross-unit duplicates) with report regenerated (Recall report byte-unchanged); focused tests added/updated (BroadcastChannel transport + no-opener, picker feedback, save retry, library picker actions over channel, new-tab null semantics, wording/expected labels, extended QA counts); docs updated (STUDY_MODULE, CURRENT_BEHAVIOR). Completed: focused/full validation, independent review, commit, and push.
- [x] Study Exam Prep learner QA / retrieval workflow patch (2026-09-04, baseline `693f9bb1b0a9a6a44836ea3ee5aa98315bc7d801`, completed and validated): share Study DB name/version config (`src/study/studyDbConfig.ts` — `STUDY_DB_NAME`/`STUDY_DB_VERSION`/`STUDY_SCHEMA_VERSION` = 10 — consumed by `studyStorage.ts` (re-export preserved) and the search worker opener; search snapshot v10-shaped + metadata `dbVersion` drift guard so search can never silently run on an older open layout; Library search error/unavailable banner); per-document in-memory legal-component cache (`studyDocumentCache.ts`) with in-flight dedupe invalidated at the `legalComponents` rewrite seams (`replaceAll` incl. delete-all, `importOfficialContentPackage`); centralize safe new-tab opening (`studyWindow.ts`, `noopener,noreferrer`, popup-block `null`) and route active Locate/Drill/Mock source links through it (`EXAM_PREP_OPEN_SOURCE_BUTTON newTab`, Locate/Mock Open Statute Library) so active sprint/drill/mock state never unmounts; ephemeral Locate picker (`examPrepLocatePicker.ts` — `?studyPicker=locate&prompt&token` URL, same-origin `study-locate-pick` postMessage, objective documentId + exact-pinned-sourceKey matching, token guard, no persistence/leakage; picker-mode Library + Document page `Select this provision` affordances; manual Check Answer fallback preserved); related-unit title chips + stable `exam-unit-<id>` Learn anchors with filter-clearing hash scroll (mount + hashchange), drill reveal Related chips navigate `/study/learn#exam-unit-…`; shared Recognition wording constants (`EXAM_PREP_RECOGNITION_ASK`/`_PRACTICE_COPY`) across sprint/Mock prompt/Mock grading/Home; Recall reveal persistence — prompt + typed answer stay visible with expected answer + FSRS ratings until rated and across unrelated parent rerenders; deterministic QA reports — `npm run study:exam-prep:qa-report` writes byte-stable `reports/exam-prep/recall-quality-audit.md` (57 cards) + `recognition-quality-audit.md` (317 cues) via pure `src/study/examPrep/qa/examPrepQaAudit.ts` with mechanical flags + A-SURV-03 human-review note (fragmented entries documented verbatim, zero recognition cues by design); no curriculum/schema/persistence changes (v10/five stores/57/317/452/24/133 intact). Tests added: `study_db_config.test.ts`, `study_document_cache.test.ts`, `exam_prep_qa_audit.test.ts`, `exam_prep_locate_picker.test.ts`, `exam_prep_locate_picker_flow.test.tsx`, `exam_prep_window_open.test.tsx`, plus recall-reveal-persistence test and wording assertion updates. Focused `tests/study` suite 88 files / 1112 tests green; lint, typecheck green. Docs updated: STUDY_MODULE, CURRENT_BEHAVIOR, ARCHITECTURE, README, this file. Generated `reports/exam-prep/*-quality-audit.md`. **REMAINING: full-suite run + independent review then commit/push (NOT authorized in this batch).**
- [x] Study source-link UX + global Return navigation (2026-09-04, baseline `971d2dcee1039dbd7b37db91111bc73ed4d4cbe2`, validated and independently reviewed): preserved frozen curriculum, mustLocate/sourceAnchors semantics, queues, FSRS, and v10/five-store persistence; Learn now labels LOOK HERE as exam lookup targets (`mustLocate`), SUPPORTING SOURCES as `sourceAnchors`, explains the distinction, and previews 12 anchors with component-local Show all/Show fewer controls while keeping every exact link openable and duplicates intact; `useStudyNavigation` stamps `__webnetStudy` history metadata, preserves caller state/returnTo and full path/query/hash, captures/restores the nested Study main scroll root after popstate, and exposes a global header Return that hides on direct loads and active in-progress mocks; focused 76/76 and full 2236 passed (1 skipped), lint/typecheck/build green, independent reviewer ready-to-commit. Commit/push completed below.
- [x] Exam Prep Phase 3 corrective patch (2026-09-04, baseline `1846f9f`, completed and validated): fix reviewer-found defects in the provisional Mock Exam engine while preserving v10/five stores, the frozen curriculum (all seven hashes, 57/317/452/24/133), the mock profile format, normal FSRS/attempts/readiness, `temp/**`, and the pre-existing package-lock change. (1) **Autosave revision race** (`ExamPrepMockActive.tsx`): the debounced CAS-serialized drain unconditionally reset `dirtyRef` and re-applied the (older) persisted copy to the draft after each save, so a revision typed while a save was in flight could be silently discarded while the indicator said Saved. The drain now re-checks the latest local revision after every successful save and keeps saving while a newer revision exists — it only clears the dirty flag and repaints the draft when the persisted copy matches the newest local revision; the indicator now sets Saving… while a save is actually running and returns to Saved only after the newest revision lands (previously the Saving… state never displayed). Regression test holds the first autosave open with a gate, types a second revision, releases the save, and asserts both revisions persist in order, the textarea keeps the newest text, and Saving… → Saved transitions. (2) **Transactional one-active-current-mock creation lock** (`studyStorage.ts`): `saveExamPrepMockSession` with `absent` expectation + `in_progress` status now scans the mock store inside the SAME readwrite transaction and rejects when any other current-binding `in_progress` session exists („A mock exam is already in progress…”), so two tabs or two rapid Start clicks can never both create a current mock; updates to an existing session id keep the ordinary CAS guard; archived-hash in-progress records and history (submitted/graded/abandoned) never block a current create; existing `selectDuplicateActiveMockSessions` still handles imported/legacy duplicates. Storage tests cover the reject path, submit→next-create cycle, own-session autosave, archived co-existence, and current-binding scope. (3) **Profile resources honored**: the answering view renders `[ Open Statute Library ]` only when the session's own `profileSnapshot.resources.builtInStatuteLibrary` is true (default profile keeps it; a synthetic library-disabled profile snapshot hides it — behavior test). (4) **Time used**: new pure `mockTimeUsedSeconds` (results module) bases reported time on `submittedAt`/`abandonedAt` clamped to the persisted hard-stop deadline — never on `gradedAt`, so self-grading done days later cannot inflate the exam clock and a late submission cannot exceed the profile duration; ResultsView and the graded history row both use it (the history row previously used `gradedAt`). Session tests cover submit-based, abandon-based, deadline-clamped, and in-progress-zero cases. (5) **Fail-closed validation**: `validateExamPrepMockProfile`/`assertValidExamPrepMockProfile` in `examPrepMockProfiles.ts` (structural checks + per-kind `questionMix` bucket keys and mix sums equal to the kind's question count + resource-flag consistency + positive duration/points/version, registry and provisional V1 pinned clean) run before paper generation, and `assertExamPrepMockPaperPostconditions` in `examPrepMockPaper.ts` asserts the built paper's question total, point total, per-kind quotas, sequential unique `qNN` ids, unique `sourceTaskId`s, and per-question `pointsPossible` — a malformed mix now throws instead of silently emitting a 26-question paper. Paper tests cover clean registry/provisional, mix-sum, unknown bucket, unknown kind, zero-total/negative/closed-book-library, over-quota-pool, and explicit-empty-drill-mix profiles. (6) **Drill completion only at 3/3**: `buildMockTypeBreakdown` now counts a drill as correct/complete only at its full self-assessed score (its `pointsPossible`, 3/3 for the provisional profile) — 1/3 and 2/3 stay partial — matching the drill-readiness rules; session test grades 3/3 + 2/3 + 1/3 drills and asserts correct = 1. Validation: focused Exam Prep/mock suites green (mock files 90 tests; Exam Prep + curriculum + storage-contracts scope 32 files / 408 tests), full suite **381 files / 2220 passed (1 skipped)**, lint, typecheck, and production build green; frozen manifest/projection hashes, v10 schema/five stores, export/import round-trips, profile snapshot format, and normal FSRS/attempts/readiness untouched; no new dependencies; `temp/**` and the pre-existing package-lock change preserved. Docs updated: CURRENT_BEHAVIOR (Phase 3 bullet), STUDY_MODULE (Phase 3 bullets), ARCHITECTURE (examPrep module bullet), this file. **REMAINING: independent review then commit/push (NOT authorized in this batch).**
- [x] Exam Prep Phase 3 Configurable Provisional Mock Exam (2026-09-04, baseline `1c7a740`, completed and validated): add a versioned provisional mock-exam engine, session persistence (schema v10), and the focused Mock Exam learner flow — with no AI grading/generation, no pass/fail, no changes to normal attempts/FSRS, and the frozen curriculum untouched (all seven hashes and 57/317/452/24/133 counts unchanged). (1) **Profiles/types** (`src/study/examPrep/mock/examPrepMockProfiles.ts`, `examPrepMockTypes.ts`): `ExamPrepMockProfile` registry with `EXAM_PREP_PROVISIONAL_MOCK_V1` (`nb-statute-provisional-v1`, version 1, status provisional, 150 minutes, 30 questions = Recall 6×1 + Recognition 8×1 + Locate 10×1 + Drill 6×3 → 42 self-assessed points, resources openBook + builtInStatuteLibrary, `hard_stop`, `passMarkPercent: null`), optional per-kind `questionMix` quotas, assumptions copy that labels everything provisional (“registrar has not yet confirmed … No official pass mark is configured”); sessions store `profileId`/`profileVersion` + a full `profileSnapshot`, seed, question refs (`q01..` with `kind`/`sourceTaskId`/`unitId`/`pointsPossible`), responses, grading, and status (`in_progress`/`submitted`/`graded`/`abandoned`). (2) **Deterministic history-blind paper** (`examPrepMockPaper.ts`, `examPrepMockRandom.ts`): `buildExamPrepMockPaper({profile, seed})` selects only from the frozen pools (57 Recall tasks, 317 Recognition cues, 452 Locate targets, 24 DRILL units) with a two-pass breadth rule (each curriculum unit preferred once across the non-drill portion), profile tier mix (Recall A2/B2/NAV1/C1, Recognition A3/B2/NAV2/CD1, Locate A4/B3/NAV2/CD1, Drills direct/routing/cross_document 2/2/2; deterministic cycling fallback for profiles without `questionMix`), seeded Fisher–Yates shuffles (mulberry32 + xmur3 hash; `Math.random()` never inside the builder) and a final seeded mix into session-local `q01..` ids; same profile + same seed → byte-identical paper for every learner; no learner attempts/due state/readiness inputs. Question content resolves post-submission from the frozen pools (`examPrepMockQuestionContent.ts`); prompts only render pre-submission (Recall fixed prompt, Recognition cue + “Which law or legal topic applies?”, Locate prompt, Drill fact pattern + task). (3) **Storage v10 + CAS** (`examPrepStorageConfig.ts`, `studyStorage.ts`, `studyTypes.ts`, `studySeed.ts`, `studySearchWorker.ts`): `STUDY_DB_VERSION`/`STUDY_SCHEMA_VERSION` 9 → 10 with exactly one new Exam Prep store `examPrepMockSessions` (key `id`, indexes byCurriculumId/byCurriculumContentHash/byStatus/byStartedAt/byProfileId); v9 DBs and v9 JSON snapshots migrate additively with `examPrepMockSessions = []` and every existing store byte-preserved (no fabricated history); `StudyDataSnapshot`/seed/loadAll/`loadAuthoritativeSnapshot`/replaceAll/reset/export/import carry the new array; `StudyStorage.saveExamPrepMockSession({session, expectation})` is CAS-guarded (absent + existing → reject, existing(A) + stored(B) → reject, missing record → reject) and writes ONLY the mock store. (4) **Pure session/selectors/results** (`examPrepMockSession.ts`, `examPrepMockSelectors.ts`, `examPrepMockResults.ts`, `examPrepFormat.ts`): immutable builders/updaters (create/setMockAnswer/setMockFlagged/markMockVisited/setMockCurrentIndex/submitMock/gradeMockQuestion/finalizeMock/abandonMock, none mutate question refs/profile snapshot), current-binding selectors (active = latest in-progress, duplicate-active warning, submitted/graded/abandoned, recent results with limit), and scoring helpers (expiry/remaining/elapsed from the persisted deadline, answered/visited/flagged/graded counts, fully-graded gate, `buildMockScore` (34/42 → 81%), per-kind breakdown, H:MM:SS clock). (5) **Hook/UI/routes**: `useStudyExamPrep.saveExamPrepMockSession` (persist first, then snapshot upsert), `ExamPrepViewKind` + `EXAM_PREP_ROUTES`/`decodeExamPrepView` `/study/mock-exam` → mock (legacy `/study/exam-curriculum` → learn unchanged), Mock Exam tab (ClipboardCheck), and focused components `ExamPrepMockExam` (landing with provisional numbers/assumptions + history with graded “34 / 42 · 81%”, “Grading incomplete · N of 30 graded” [Continue Grading], and a separate Abandoned list; Archived-hash sessions never become current), `ExamPrepMockActive` (deadline-derived countdown — reload cannot reset it; free-text answers; flag/visited palette; hard-stop expiry locks responses; Open Statute Library in a new tab; submit confirmation with answered/unanswered/flagged counts; Abandon confirmation keeps responses in history; debounced CAS-serialized autosave with Saved/Saving…/Save-failed states and immediate flush on navigation/flag/submit), `ExamPrepMockGrading` (per-question reveal of the frozen expected content: Recall rule, Recognition unit id/title + Likely sources, Locate Act/provision or the honest document-level note with Open-exact-provision links only after submission, Drills required lookups/answer points/trap + the three 1-point checkboxes; grading persists/resumes; Finalize Results gated on all 30 graded), and `ExamPrepMockResultsView` (**Practice score** 34/42 · 81%, type breakdown, time used, full review with wrong/partial highlighting; provisional profiles say “No official pass mark is configured for this provisional profile.” and never Passed/Failed/Exam grade; abandoned sessions render read-only responses without a score). The Exam Prep page hides the ordinary tabs while a mock is `in_progress` (focused mode) and shows “Exit / Save for later”. Home gains Mock Exam as step 6 of the Suggested study flow (“Combine the full workflow under one timed exam simulation.”) plus a compact clearly-provisional CTA (150 min · 30 questions · format awaiting registrar confirmation) that is NOT part of Recommended Now or the five readiness dimensions. (6) **Tests**: new `exam_prep_mock_paper.test.ts` (profile totals/provisional labels/mix, allocation fallback cycling, 30-question/42-point mixed paper, exact tier + difficulty mix, no duplicate sourceTaskId, breadth across non-drill units, DRILL-only from the frozen 24, same-seed determinism, different-seed validity, history-blind purity, every source maps to the exact frozen Recall/Recognition/Locate/Drill content, synthetic 30-min 20-question library-disabled profile), `exam_prep_mock_session.test.ts` (creation/deadline, immutable updates, transitions, results: expiry/remaining/elapsed, counts, 34/42→81% breakdown, ungraded zero scoring; selectors: current-binding only, latest-active + duplicate warning, submitted/graded/abandoned splits, recent ordering/limit, profile provenance), `exam_prep_mock_storage.test.ts` (v10 versions + five stores, v9→v10 additive migration preserving records, v9 snapshot import defaults `[]`, CAS create/duplicate/update/stale/missing, mock-save-only contamination regression, export/import round-trip of active/submitted/graded/abandoned/archived with profile snapshots + current-selector exclusion), `exam_prep_mock_views.test.tsx` (route decode incl. legacy, landing provisional labelling + numbers + mix rows, focused mode with tabs hidden, DOM answer-leak regression for all kinds while active, archived in-progress never becomes current, Home step 6 + provisional CTA), `exam_prep_mock_behavior.test.tsx` (typed-answer autosave + reload restore, flag persist/restore, current-index persist/restore, hard-stop expiry lock blocking edits, submit confirmation counts then submitted with answers frozen, abandon confirmation), and `exam_prep_mock_grading.test.tsx` (finalize gate disabled until all 30 graded, expected-answer reveals + your answer, finalize transition, resumed grading state, results 34/42·81% with no pass/fail wording, abandoned read-only, history rows incl. Continue Grading → self-grade, archived excluded). Updated storage tests to v10 semantics. Validation: focused Exam Prep/mock suite 96 tests green, all 22 Exam Prep/Curriculum view/queue/selector files 287 tests green, full suite **380 files / 2195 passed (1 skipped)**, lint, typecheck, production build green; frozen manifest contentHash `434703f0…` and A/B/C/D/NAV/DRILL projections untouched; no new dependencies; `temp/**` and the pre-existing package-lock change preserved. Docs updated: README, STUDY_MODULE (store list, version-10 note, Exam Prep Phase 3 section), CURRENT_BEHAVIOR (Phase 3 bullet), ARCHITECTURE (examPrep module bullet incl. mock boundary), this file. **REMAINING: independent review then commit/push (NOT authorized in this batch).**
- [x] Exam Prep Phase 2.5 learner workflow/readiness UX (2026-09-04, baseline `602dd69`, completed and validated): a focused learner-experience pass with no curriculum/content/queue/FSRS/storage/schema changes (schema v9, exactly four Exam Prep stores, no migrations, no new persisted settings). (1) **Pure helpers**: `examPrepToday.ts` (`buildExamPrepTodayActivity`) derives a five-count per-local-date summary strictly from current-binding immutable records (studied units `studiedAt`, recall reviews `reviewedAt`, recognition/locate `completedAt`, drill attempts by persisted browser-local `practiceDate` — never UTC slicing, never a daily store); `examPrepRecommendations.ts` (`selectExamPrepRecommendedAction` + `selectRecommendedExamPrepDrill`) implements the fixed advisory chain due Recall → unstudied Learn → missed Recognition → missed Locate → Developing drill → Unattempted drill → Accurate drill → generic Recognition practice, and drill status Developing → Unattempted → Accurate (never Exam-ready) then review weight high → medium → low then canonical DRILL order; both deterministic and advisory, with latest-per-task misses and current-hash-only participation (archived/other-curriculum attempts excluded); `examPrepDrillFilters.ts` exposes canonical DRILL rows + status/difficulty filters (`All`/`Needs Work`/`Unattempted`/`Developing`/`Accurate`/`Exam-ready`, `All`/`Direct`/`Routing`/`Cross-document`, Needs Work = Developing + Accurate exactly) with summary counts and concise readiness reasons. (2) **Home**: compact Suggested study flow (five steps, no progress attached), Recommended Now block with neutral copy and mode-routing CTA, five independent readiness dimensions (Learn X/133, Recall due + introduced X/57, Recognition coverage X/317 + latest-result accuracy, Locate coverage X/452 + latest accuracy, Drills exam-ready X/24 + attempted X/24 — no combined mastery score), Today's Activity panel (all-zero empty state message), all-studied Curriculum coverage note (never “mastered”), and a “What these measure” help section with honest labels (Studied ≠ mastered; accuracy ≠ coverage; exam-ready ≠ exam success). (3) **Lookup Drills**: top readiness summary (Exam-ready/Accurate/Developing/Unattempted + attempted) with the “Exam-ready = 3/3 within the target time on two different practice dates.” note, a Recommended drill block (compact summary + Go to drill that resets any hiding filters then scrolls the card into view), canonical filtered list via component-local state, and per-card readiness reasons (Developing “Latest attempt: X / 3”, Accurate one-day “1 qualifying practice day · needs another successful day for Exam-ready.” or “Correct, but not yet within the target time.”). (4) **Honest copy/identity**: Locate sprint intro says “find the correct statute or controlling provision” (390 pinned vs 62 document-level targets preserved — document-level reveal shows the statute title + “Document-level target — no single provision is pinned in the curriculum.” and never an Open-exact-provision link); Recognition reveal shows the curriculum unit id + title (never the internal `recognition:…` task id, which still persists unchanged in attempts); both sprint starts explain they sample the full exam curriculum; drill header target badges use M:SS (150 s → 2:30) via `formatExamDrillTime`; Exam Prep page subtitle updated. (5) **Tests**: new `exam_prep_today.test.ts` (5: mixed current-hash fixture 2/3/10/10/1, practiceDate-vs-timestamp, all-zero, local-day start/end boundaries, archived/other-curriculum exclusion), `exam_prep_recommendations.test.ts` (16: full priority chain incl. all-studied fixtures, archived-only exclusion, latest-result-wins, determinism, drill status/weight/canonical ordering, exam-ready never recommended), `exam_prep_drill_filters.test.ts` (7: rows/statuses/counts, Needs Work, exact Exam-ready two-distinct-date semantics, status + difficulty + combined filters, All reachability, archived ignored), `exam_prep_phase25_views.test.tsx` (14 jsdom: Home flow/help/dimensions/all-studied/Today panel/Recommended-Now routing, Recognition+Locate start copy, pinned s.15(1) vs document-level Locate reveal, 390/62 split, Drills summary/filters/M:SS/recommended block + Go-to-drill reset); updated `exam_prep_practice_views.test.tsx` (recognition reveal identity = unit id; saved attempt keeps internal task id) and `exam_prep_ui.test.tsx` (Recommended Now + all-studied coverage note). Fixed during implementation: `hasLatestMissed` now filters current-hash attempts (archived Recognition/Locate misses previously could steer Recommended Now). Focused suite 24 files / 305 tests green; full suite **374 files / 2130 passed (1 skipped)**; lint, typecheck, production build green; frozen manifest + all seven hashes and 57/317/452/24 task counts untouched; no Mock Exam/Phase 3; docs: README, STUDY_MODULE, CURRENT_BEHAVIOR, this file. Unrelated untracked `temp/**` QC files preserved (incl. probe/verify scratch restored byte-for-byte). Independent review completed; committed and pushed at `1c7a740` (“Polish Exam Prep learner workflow”).
- [x] Exam Prep Phase 2 retrieval practice (2026-09-04, baseline `3bf244c`, completed and validated): add deterministic Recognition and Locate task pools/sprints plus persistent self-assessed Lookup Drill attempts using the existing schema-v9 four-store model and discriminated `examPrepAttempts`; preserve the frozen 157-unit curriculum, all seven hashes, 57 FSRS Recall cards, 452 unscheduled LOOK HERE targets, and protected Study AI artifacts. Implement current-hash selectors, immutable generic attempt writes, explicit Recall CAS expectations, frozen 10-item 4/3/2/1 Recognition/Locate queues, local-date drill readiness, answer-safe UI/routes, export/import union support, focused regressions, docs, full validation, independent review, then commit/push. Do not start Mock Exam, AI grading/generation, or Phase 3. **Crash-safe implementation scope recorded here before code changes.**
- [x] Exam Prep recall-session fixes (2026-09-04, baseline `d594146`, NOT committed/pushed per instruction): deterministic frozen recall sessions over the same frozen 57-card deck / schema-v9 / four-store Exam Prep layer; no frozen-curriculum changes (manifest contentHash `434703f0a6de470095a5d0ad9fc6d1bb9534b15d58b1ab2b47460bba8a88952a` + projections A `07ae52f1…`/B `149f7253…`/C `6f9cd74e…`/D `f118e362…`/NAV `184c04c0…`/DRILL `30b75f6c…` byte-for-byte; 57 task ids/count and 452 mustLocate unchanged), storage/schema v9, drills, and protected pipelines untouched. (1) **Session freeze** (`ExamPrepRecall.tsx`): explicit idle → Start Recall Session → frozen session → completion state machine; the queue candidates (due earliest-first then priority new cards, capped by new/max limits) are frozen exactly once at Start; successful ratings persist via the existing parent action (immutable attempt + progress, stale-`updatedAt` protected) and advance through the frozen queue — ratings never refill/reshape the running session; failed/stale persistence keeps the revealed card + typed answer and does not advance; after the final frozen item the view shows Session complete + an explicit Start Another Session that freezes the next batch from the updated snapshot (hidden when no candidates remain). (2) **New-card priority**: shared `examPrepPriority.ts` (`examPrepPriorityGroup` + `compareExamPrepRecallTaskPriority`) with rank exactly high A=0, high B=1, high NAV=2, other A=3, other B=4, other NAV=5, C=6, D=7 (DRILL 8, never ranked); equal-rank ties by `curriculumIndex`, then `index`, then task id; wired into `buildExamPrepRecallQueue` fresh ordering; real-deck observed rank run 18×0/16×1/5×2/11×4/1×5/6×6 pinned by test. (3) **Derived metadata**: `ExamPrepRecallTask` gains `reviewWeight` (unit weight) + `curriculumIndex` (0-based manifest position) in `deriveExamPrepRecallTasks` without changing task ids/order/count; any earlier record shape untouched. (4) **Home recommendation**: deterministic recommended unstudied Learn unit (`selectRecommendedLearnUnit`) using the same rank philosophy, shown on Home with tier/weight/recall/lookup chips + Open Learn; purely advisory, never auto-marks studied (rank-ladder selector tests + UI test). (5) **Regressions**: new `tests/study/exam_prep_recall_session.test.tsx` (2 jsdom component/integration tests modeling parent-snapshot updates — 57-new/new=8/max=20 freezes 8, completes at 8, Start Another Session freezes the next batch tasks[8..15]; 12 due + 20+ new/new=8/max=15 freezes 15 = 12 due + 3 new and rates all 15 despite injected mid-session mutations on tasks[40]/[50]); new `tests/study/exam_prep_priority.test.ts` (4 explicit ordering tests); derivation metadata test; selector rank-ladder tests; updated UI tests for the Start gate. Focused Exam Prep suite 10 files / 67 tests; curriculum + storage/migration regressions (9 files / 183 tests) green; full suite **365 files / 2043 passed (1 skipped)**; lint, typecheck, production build green. Docs updated: README, STUDY_MODULE, CURRENT_BEHAVIOR, this file. Unrelated untracked `temp/**` QC files untouched. **REMAINING: independent review then commit/push (NOT authorized in this batch).**
- [x] Exam Prep Phase 1 (2026-09-04, baseline `ffe3736`, NOT committed/pushed per instruction): persistent learner state over the frozen 157-unit Exam Curriculum. (1) **Namespace/stores**: dedicated `src/study/examPrep/` (types, constants, manifest binding, recall-task derivation, selectors, queue, review transaction, settings, state updates, formatting, route decoder, tabbed page + Home/Learn/Recall/Drills views); DB/Study schema **8 → 9** with exactly four new stores — `examPrepUnitProgress`, `examPrepRecallProgress`, `examPrepAttempts`, `examPrepSettings`, all keyed by `id` with hash-composite unit/task ids (`${curriculumContentHash}::${unitId|taskId}`), `curriculumContentHash` as settings id, and byCurriculumId/byCurriculumContentHash/byTaskId/byCurriculumAndTask/byUnitId/byKind indexes — integrated into snapshot/migration/load/seed/replaceAll/export/import; v8 databases and snapshots migrate preserving every existing Study/legal/AI/settings/FSRS record (no fabrication or deletion). (2) **Derivation**: exactly **57 recall tasks** from `mustRecall` in canonical manifest order (A 18 / B 27 / C 6 / D 0 / NAV 6 / DRILL 0), ids `recall:{unitId}:{index}`, fixed prompt "State the key rule you should remember for this curriculum unit.", verbatim frozen expected answers; Learn = exactly 133 A-D/NAV units; mustLocate (452) / anchors / cues / drill answers never create cards. (3) **Binding**: every persistent record binds `curriculumId` + `curriculumContentHash` from one helper importing the bundled manifest; selectors filter both fields; archived same-curriculum/different-hash state stays byte-preserved/exportable/unscheduled and excluded from current metrics; old-hash imports remain exportable. (4) **FSRS recall**: reuses Study FSRS config/scheduler; deterministic queue = due earliest-first then prioritized (canonical) new cards; respects new/max session limits (defaults 8/20, bounds 0-57/1-57, new<=max); excludes future-due and archived records; uninitialized treated as new; pure previews + rated attempt/immutable attempt builder; atomic attempt+progress save with stale `updatedAt` protection in `studyStorage`; no undo/drafts/drill persistence/Study phase transitions. (5) **UI/routes**: sidebar item renamed **Exam Prep** (`/study/exam-prep`); tabs Learn `/study/learn`, Recall `/study/review`, Lookup Drills `/study/drills`; legacy `/study/exam-curriculum` renders Learn (routes decoded via `examPrepRoutes.ts`); Home = studied X/133, recall due X, introduced X/57, cards 57; Recall hides expected text/ratings until Reveal; drills = shared 24-card session-only UI with M:SS timer (150s -> 2:30), answer-leak-free; legacy curriculum page removed in favor of single-sourced Learn/Drill cards. (6) **Tests/docs**: 8 focused files (+1 storage support): derivation/distribution/exact answers, selectors+archive, settings bounds, state-update immutability + unit independence, queue ordering/limits/hash filtering, FSRS first/subsequent ratings + previews, v9 migration/upgrade/transaction/stale/export-import round-trips, UI (Home/Learn/Recall/drills/legacy route/sidebar/2:30 timer/leakage) — full suite **363 files / 2029 passed (1 skipped)**; lint, typecheck, production build green; frozen manifest untouched (contentHash `434703f0a6de470095a5d0ad9fc6d1bb9534b15d58b1ab2b47460bba8a88952a`; projections A `07ae52f1…` B `149f7253…` C `6f9cd74e…` D `f118e362…` NAV `184c04c0…` DRILL `30b75f6c…` byte-for-byte). Docs: README, STUDY_MODULE (schema v9 + Exam Prep section), CURRENT_BEHAVIOR, TODO. Frozen Study Map / V4-V5 / calibration-80 / remediation1 / holdout-96 / prepared 4,251-job artifacts untouched; unrelated untracked `temp/**` QC files preserved. **REMAINING: independent review then commit/push (NOT authorized in this batch).**
- [x] Exam Curriculum V1 Lookup Drill expansion (2026-09-04): deterministic, human-curated addition of exactly 24 lookup drills (DRILL-01..DRILL-24: 8 direct, 8 routing, 8 cross_document) after the frozen A–D + Navigation catalog at `397908e` — no model inference; frozen Study Map / V4-V5 / calibration-80 / remediation1 / holdout-96 untouched. (1) **Types/architecture**: `ExamCurriculumTier` extended to `'DRILL'`; `ExamCurriculumAnchorRole` extended with `'drill_answer'`; new `ExamLookupDrillDifficulty` (`'direct' | 'routing' | 'cross_document'`), `ExamLookupDrillAnswerKeySpec`, `ExamLookupDrillAnswerKey`, `ExamLookupDrillPayload`, `ExamLookupDrillSpec` types; `ExamCurriculumUnit.drill?: ExamLookupDrillPayload` optional field; `ExamCurriculumCatalogSpec` union extended with `ExamLookupDrillSpec`. (2) **Catalog**: `examCurriculumCatalogDrills.ts` — exactly 24 specs DRILL-01..DRILL-24 in canonical order (8 direct DRILL-01..08, 8 routing DRILL-09..16, 8 cross_document DRILL-17..24); `timeTargetSeconds` 60–90 direct, 120–150 routing, 180–240 cross_document; hidden answer-key lookup totals 8/17/28 = 53; each drill has `mustRecall=[]`, `mustLocate=[]`, `unitType='lookup_drill'`, `tier='DRILL'`, `relatedUnitIds` linking to A-D-NAV units. (3) **Resolver**: dedicated `buildExamCurriculumDrillUnitFromSpec` + updated dispatcher — per-source corpus resolution combining/deduping by `documentId::sourceKey`, every anchor `role: 'drill_answer'`, answerKey lookups resolved to `ExamCurriculumLookupTarget[]` via `buildExamCrossDocumentLookupTarget`, `mustRecall`/`mustLocate` kept empty. (4) **Validator**: `EXAM_CURRICULUM_TIERS` includes `'DRILL'`; `validateLookupDrillUnit` enforces tier DRILL, valid difficulty, ≥30s time target, non-empty factPattern/task/answerPoints/lookups, empty mustRecall/mustLocate, valid review weight, ≥1 relatedUnitId, every lookup resolves to a valid sourceKey. (5) **Composition/build**: `examCurriculumCatalogAll.ts` = 157 specs in A→B→C→D→NAV→DRILL order (DRILL indexes 133-156); `EXAM_CURRICULUM_TOTAL = 157`, `EXAM_CURRICULUM_DRILL_TOTAL = 24`; metrics + report extended with DRILL tier, `totalAnswerLookups` metric, DRILL projection hash, drill fact/task/answer key/trap summary in Markdown; `scripts/studyExamCurriculumBuild.ts` asserts 24 drill specs, 8/8/8 difficulty split, 8/17/28 answer lookups, correct time targets. (6) **UI**: `ExamCurriculumPage.tsx` gains `LookupDrillCard` component with Start fact/task/textarea/timer (fact pattern, task, required answer points, required lookups with deep-link source buttons, trap explanation), DRILL filter button, `drill` summary in `UnitCard`, `DRILL` tier badge and group heading. (7) **Outcome**: 157 units (51 A / 43 B / 21 C / 6 D / 12 NAV / 24 DRILL); unit types 57 `document_orientation` / 64 `core_concept` / 12 `cross_document_navigation` / 24 `lookup_drill`; anchors 2,613 (core 2,505 + drill 108); hidden answer-key lookups 53; mustRecall 57 / mustLocate 452 (unchanged from frozen); 0 validation errors; frozen projections byte-for-byte unchanged: A `07ae52f1…`, B `149f7253…`, C `6f9cd74e…`, D `f118e362…`, NAV `184c04c0…`; DRILL projection `30b75f6cde3fd9c1ec70a5244f23ed9b3a5a848abcfff361aa060fdb9f4ca608`; manifest contentHash `434703f0a6de470095a5d0ad9fc6d1bb9534b15d58b1ab2b47460bba8a88952a` (deterministic across reruns). (8) **Tests**: new `tests/study/exam_curriculum_drills.test.ts` (41 cases: catalog shape, canonical order/IDs, difficulty distribution, time targets, answer-lookup totals, composition/indexes, structural contract, every drill source anchor role/dedupe/lookup resolution, semantic facts for DRILL-01..DRILL-24, validator contract, build metrics); tier_b/cd/navigation tests updated for the 157-unit aggregator. Validation: lint, typecheck, 355 files / 1976 passed (+1 skipped), production build, and two curriculum builds with byte-stable frozen + DRILL hashes all green. Docs updated: CURRENT_BEHAVIOR, ARCHITECTURE, README, this file.
- [x] Exam Curriculum V1 cross-document Navigation expansion (2026-09-04): deterministic, human-curated addition of exactly 12 cross-document Navigation units (tier/group NAV, displayed as Navigation; `cross_document_navigation`) on top of the frozen Tier A–D curriculum at `8543185` — no model inference; frozen Study Map / Unit Authoring V4-V5 / calibration-80 / remediation1 / holdout-96 / prepared 4,251-job run untouched. (1) **Types/architecture**: `ExamCurriculumTier` extended to `'NAV'`; dedicated `ExamCrossDocumentSourceSpec` (documentId + ranges + optional schedules/forms), `ExamCrossDocumentLookupSpec` (prompt + explicit documentId + optional pins) and `ExamCrossDocumentNavigationSpec` (multiple sources, learning depths, examGoal, cues, core understanding, recall/locate targets, related IDs, review weight); `ExamCurriculumUnitSpec` itself unchanged; catalog union `ExamCurriculumCatalogSpec`. (2) **Catalog**: `examCurriculumCatalogNavigation.ts` — exactly 12 specs NAV-01..NAV-12 (titles/IDs/sources/depths/goals/cues/core/recall/lookup pins/related/weights verbatim per the user blueprint; corpus truth wins and deviations documented in the build report: NAV-12 Mining Act uses the reviewed B-MIN-01 administration bands + 90-94, EUB mirrors C-EUB-01 decision-maker/jurisdiction provisions minus the s.103 repeal-history; Parks mirrors the reviewed C-PARK-01 live scope; Public Health stays on live ss.23-24.1 with repealed s.22 never anchored/looked-up). Weights high 9 (NAV-01/02/03/04/05/07/08/11/12) / medium 3 (NAV-06/09/10); recall-bearing exactly NAV-01/02/05/07/09/11 with one compact rule each (6 total); mustLocate per unit 8/8/8/9/6/7/8/8/8/7/8/9 = 94. (3) **Resolver**: dedicated `buildExamCrossDocumentNavigationUnitFromSpec` + dispatcher `buildExamCurriculumUnitFromCatalogSpec` — per-source corpus resolution, combine/dedupe by `documentId::sourceKey`, spec source order then corpus order, `sourceDocumentIds` in explicit source order, every NAV anchor `role: navigation`, every mustLocate resolved only against its explicit documentId to a valid sourceKey; A-D path unchanged. (4) **Validator**: known tier NAV; NAV contract (tier/type, ≥2 source documents, recognize/understand/retrieve depths, non-empty examGoal/cues/core, ≥3 mustLocate across ≥2 documents with every pin resolved, ≥2 related units, valid review weight, recall-depth consistency, ≤1 compact recall); breadth warning NAV-aware via a separate 80-anchor NAV threshold (A-D keep 40, behavior unchanged). (5) **Composition/build**: new `examCurriculumCatalogAll.ts` = 133 specs in A→B→C→D→NAV order (NAV indexes 121-132); report metrics/reports extended to Navigation (per-tier breakdown, Navigation projection hash, `sourceCoverageByDocument` multi-source metric alongside legacy `unitsByDocument`); Markdown NAV sections list Documents instead of a single Document; script asserts 12 NAV specs + corpus presence. (6) **UI**: `/study/exam-curriculum` gains All (133) / Tier A (51) / Tier B (43) / Tier C (21) / Tier D (6) / Navigation (12) filter; Navigation heading/type badge (`cross_document_navigation`); NAV cards list every source document with per-document anchor counts and make LOOK HERE destinations document-obvious; >12-anchor collapse shows the document span; existing A-D rendering/REMEMBER/LOOK HERE/deep-links preserved. (7) **Outcome**: 133 units (51 A / 43 B / 21 C / 6 D / 12 Navigation); unit types 57 `document_orientation` / 64 `core_concept` / 12 `cross_document_navigation` / 0 `lookup_drill`; anchors 2,505 (A 742 / B 716 / C 263 / D 47 / NAV 737); mustRecall 57 (18 A / 27 B / 6 C / 0 D / 6 NAV); mustLocate 452 (112 A / 140 B / 94 C / 12 D / 94 NAV); 0 validation errors; 12 advisories = the 9 pre-existing Tier-A/B advisories unchanged + 3 documented Navigation advisories (NAV-10/NAV-12 deliberate breadth, NAV-11 curated 247-char recall). Frozen projections byte-for-byte unchanged: A `07ae52f1…`, B `149f7253…`, C `6f9cd74e…`, D `f118e362…`; NAV projection `184c04c01f767eb4f5175d56c8a6fcde8bf36458ced0702d62cdb829a3a3329b`; manifest contentHash `149cdff4ad0f401c610212b68e112632d5250e39a34e5f623b4c258b722853d2` (deterministic across reruns, double-build verified). (8) **Tests**: new `tests/study/exam_curriculum_navigation.test.ts` (32 cases: catalog shape, canonical order/indexes, type totals, recall/locate totals, resolved per-unit anchor pins, navigation roles, dedupe, related IDs, frozen + NAV + content hashes, validator contract incl. NAV-aware breadth threshold, and 12 semantic-regression groups); tier_a/b/cd tests updated for the 133-unit aggregator. Validation: lint, typecheck, 354 files / 1935 passed (+1 skipped), production build, and two curriculum builds with byte-stable content/NAV hashes all green. Docs updated: CURRENT_BEHAVIOR, ARCHITECTURE, README, this file. NOT committed/pushed (per worker instruction); generated manifest + `reports/exam-curriculum/build-20260904.{json,md}` regenerated.
- [x] Exam Curriculum V1 Tier-C/D expansion (2026-09-03): source-grounded, deterministic addition of exactly 27 open-book orientation units — 21 Tier-C (`document_orientation`, one per canonical document: Aquaculture, Archives, Bituminous Shale, Clean Environment, Crown Grant Restrictions, Devolution of Estates, Electronic Transactions, Escheats and Forfeitures, Executors and Trustees, Gas Distribution 1999, Marital Property, Metric Conversion, Occupational Health and Safety, Ownership of Minerals, Parks, Probate Court, Public Health, Energy and Utilities Board, Service New Brunswick, Underground Storage, Wills) and 6 Tier-D awareness units (Assignments and Preferences, Municipalities, Official Languages, Partnerships and Business Names Registration, Public Records, Residential Property Tax Relief) — on top of the frozen Tier-A (51) + Tier-B (43) baseline at `efdcaa3`; no model inference; frozen Study Map / V4-V5 / calibration-80 / remediation1 / holdout-96 / prepared 4,251-job run untouched; cumulative 121 units (57 `document_orientation` / 64 `core_concept`). (1) **Catalogs**: `examCurriculumCatalogTierC.ts` (21 units), `examCurriculumCatalogTierD.ts` (6 units), glue in `examCurriculumCatalogTierCD.ts` (families, expected counts, doc IDs/titles); `examCurriculumCatalogTierB.ts` now composes `examCurriculumAllSpecs` = 121 (A→B→C→D order) and `EXAM_CURRICULUM_TOTAL` = 121. Exactly six compact `mustRecall` entries total (C-CGR-01, C-ETA-01, C-METRIC-01, C-OHS-01, C-OMIN-01, C-UGS-01); every other Tier-C/D unit has zero recall. Tier-C default depths recognize/understand/retrieve (+recall for the six); Tier-D recognize/retrieve (D-MUNI-01 adds understand). (2) **Repeal handling (corpus truth)**: C-PH-01 excludes repealed Public Health Act ss.20/22 from its anchors and states former s.22 subdivision assessment is repealed (live land relevance = on-site sewage design/location approvals ss.23–24.1, s.26 emergency real-property appropriation, s.43 entry/inspection); C-EUB-01 keeps s.103 (Public Utilities Act repeal) as a historical/navigation anchor only; C-MAR-01 presents s.50 dower abolition, not a live dower right; C-ARCH-01/C-PROB-01 partial-subsection repeals handled by anchoring the live provisions. (3) **Validator**: empty `examGoal` allowed for tiers B/C/D; Tier-C orientation requires recognition cues + core understanding with recognize/understand depths; Tier-D orientation requires recognize + payload; A/B contracts unchanged. (4) **Builder/reports**: metrics + projection hashes for tiers C and D; Markdown/JSON reports and build script now cover A→D (assert 21/6 families and corpus presence for all 49 families). (5) **UI**: `/study/exam-curriculum` filter extended to All (121) / Tier A (51) / Tier B (43) / Tier C (21) / Tier D (6); document title map merged for A–D; rendering/REMEMBER/LOOK HERE/deep-links/>12-anchor collapse preserved. (6) **Tests**: 25 new tests in `tests/study/exam_curriculum_tier_cd.test.ts` — catalog shape, 0 validation errors, exact per-unit resolved anchor counts, all mustLocate resolved, per-tier metrics (C anchors 263 / recall 6 / locate 94; D anchors 47 / recall 0 / locate 12), frozen Tier-A hash `07ae52f1…` + Tier-B hash `149f7253…` byte-for-byte unchanged, pinned Tier-C projection hash `6f9cd74e46cd66c01d6434117d0489951017f0aae4f6e423a39476cf68bbfad7` and Tier-D projection hash `f118e3624d653ba9964f126534bf5a6a360028e5ac867974e5a1094058046712`, content-hash stability across `createdAt`, plus 13 semantic-regression groups (C-PH-01 repealed s.22 never taught live and excluded from anchors; D-MUNI-01 explicit legacy/repealed; C-CGR-01 release/waiver; C-OMIN-01 minerals separate from soil; C-UGS-01 separate-property vesting + s.4 oil-and-gas survey-system description; C-CEA-01 designated-land/environmental restriction + parcel identifiers; C-ARCH-01 archival inspection/certified copies vs D-PRA-01 custody; C-SNB-01 geographic information; C-ETA-01 electronic legal-effect; C-EUB-01 s.103 historical; C-MAR-01 dower abolition; D-RPTR-01 no hard-coded amounts; no-unit-infers-live-rule-from-repealed sweep). Anchor counts use corpus-resolved truth: per-unit deltas vs the earlier numeric draft matrix (C-CEA 17 vs 16, C-OHS 13 vs 11, C-PARK 14 vs 13, C-PH 10 vs 11 (repealed ss.20/22 excluded), C-EUB 16 vs 15, D-OLA 7 vs 5, D-PBNR 20 vs 5, D-RPTR 10 vs 8) arise because New Brunswick consolidations insert dotted provisions (e.g. `1.1`, `5.201`, `12.01`) that the document-order resolver anchors together with their parent; reconciliation documented in `temp/exam-curriculum-tier-cd-spec.md`. **Outcome: 121 units (51 A / 43 B / 21 C / 6 D), 57 orientation / 64 core_concept; anchors 1,768 (A 742 / B 716 / C 263 / D 47); mustRecall 51 (A 18 / B 27 / C 6 / D 0); mustLocate 358 (A 112 / B 140 / C 94 / D 12); 0 validation errors; 9 pre-existing Tier-A/B advisories unchanged; manifest contentHash `3ccef81091b90b2ef816f0e4bc8ff74d07af6436b8504dab6af589ba9822b693` (deterministic across reruns).** Validation: lint, typecheck, 353 files / 1903 tests (+1 skipped), production build, and two curriculum builds with byte-stable content hash all green. Docs updated: CURRENT_BEHAVIOR, ARCHITECTURE, README, this file. NOT committed (per worker instruction); generated manifest + `reports/exam-curriculum/build-20260903.{json,md}` regenerated.

- [x] Exam Curriculum V1 Tier-B expansion (2026-09-03): source-grounded, deterministic addition of exactly 43 Tier-B units across the 22 canonical Tier-B statute documents to the frozen Tier-A layer — baseline `c321251`, no model inference, no changes to the frozen Study Map / Unit Authoring V4-V5 / calibration-80 / remediation1 / holdout-96 / prepared 4,251-job run, and no Tier C/D, cross-document navigation units, lookup drills, or polished lessons. (1) **Catalog**: four new catalog files by subject area — `examCurriculumCatalogTierBLand.ts` (12 units: AGRI 2, AIR 2, ASMT 2, CWA 2, CONDO 3, CE 1), `examCurriculumCatalogTierBProperty.ts` (17: CLF 3, EASE 2, EVID 2, EXPR 3, LIM 2, PROP 2, TDA 1, TRSP 2), `examCurriculumCatalogTierBResources.ts` (7: MIN 3, ONG 2, QS 2), `examCurriculumCatalogTierBPublic.ts` (7: HWY 2, PNA 1, PW 2, RPTT 1, SFC 1) — combined in `examCurriculumCatalogTierB.ts` (`examCurriculumTierBSpecs` = 43 in canonical family order; `examCurriculumAllSpecs` = 94 with Tier A first; expected-count map; Tier-B document IDs/titles). Specs carry an optional `tier` field defaulting to `'A'` (`examCurriculumTypes.ts`); the resolver emits `spec.tier ?? 'A'` (`examCurriculumResolve.ts`) so the frozen Tier-A catalog is untouched. (2) **Validator**: lighter Tier-B educational contract — `examGoal` may be empty for Tier-B (41 of 43 units omit it by curation); Tier-B `document_orientation` units require at least one of recognition cues / core understanding / `mustRecall` / `mustLocate`; Tier-A keeps the strict contract unchanged. (3) **Builder/reports**: per-tier metrics and per-tier projection hashes (`examCurriculumTierProjectionHash` = sha256 over canonicalJson of a tier's units) in `examCurriculumBuild.ts`; Markdown report gains per-tier projection-hash lines + a tier breakdown section and omits empty exam-goal lines; JSON report carries `tierProjectionHashes` + per-tier metrics; `scripts/studyExamCurriculumBuild.ts` asserts 22 families / 43 Tier-B specs / 94 cumulative / all Tier-B corpus documents present. (4) **UI**: `/study/exam-curriculum` gains an All (94) / Tier A (51) / Tier B (43) filter with cumulative counts, merged Tier-A+Tier-B document title map, and empty exam-goal suppression; Tier-A rendering, REMEMBER/LOOK HERE panels, deep links, and the >12-anchor collapse are preserved. (5) **Tests**: 27 new tests in `tests/study/exam_curriculum_tier_b.test.ts` — catalog shape (43 specs / 22 families / family counts / ID uniqueness / tier field / document presence), cumulative 94 with Tier-A-first ordering and global ID uniqueness, per-document Tier-B breakdown, **frozen Tier-A projection hash `07ae52f1e5e6276dd74a52172bb98cc54cdd136fdc5422ae27fbe3452c4af4a5` invariant**, zero validation errors, exact per-unit anchor counts for all 43 Tier-B units (716 total), mustLocate pin resolution (only B-PNA-01 intentionally carries one prose pin without a sourceKey), per-tier metrics, Tier-B projection + content-hash stability across `createdAt`, byte-identical manifests for identical inputs, and 16 semantic-regression assertions (B-AGRI-02 conditional drainage + joint maintenance; B-AIR-01/02 surveyor certificate/seal vs development-officer/Director approvals; B-CWA-02 plans + permit unless exempted/waived; B-CONDO-02 bare-land coordinate-monument boundaries + surveyor certification; B-CLF-02 s.11 Crown-borders plan-submission trigger; B-EASE-02 general 20/40-year recall vs profit/benefit s.1 pin; B-EVID-02 records-of-survey evidence + s.36; B-EXPR-02 authorized entry + resulting-damage compensation; B-HWY-02 deemed centre-line on doubt/dispute; B-LIM-02 15-year general vs 60-year Crown + s.8.1 expiry extinguishment; B-MIN-02 NB Mineral and Petroleum Grid / NAD83(CSRS) UTM / vertical extents; B-MIN-03 NB-qualified surveyor / Surveys Act coordinates / CGVD2013; B-ONG-01 orientation-only; B-ONG-02 statutory special-order private entry; B-QS-02 surveyed Crown-land lease boundaries; B-RPTT-01 no hard-coded transfer-tax percentage; B-TRSP-02 statutory-authority exclusion). **Outcome: 94 units (51 Tier-A + 43 Tier-B), 30 document_orientation / 64 core_concept; anchors 1,458 (A 742 / B 716); mustRecall 45 (A 18 / B 27); mustLocate 252 (A 112 / B 140); 0 validation errors; 9 advisories** — Tier-A `excessive-source-breadth` A-BYL-04/A-BYL-06/A-CPA-01 (pre-existing) plus B-AIR-01 `recall-target-only-in-mustlocate` (curated orientation with recall depth and zero `mustRecall` by spec), `recall-entry-suspiciously-long` B-AIR-02/B-CWA-02/B-EASE-02/B-EXPR-02 (243–281 chars), and `excessive-source-breadth` B-MIN-02 (41 anchors) — all retained as warnings, not split. **Hashes**: manifest contentHash `5df443fac55e9bf8cbddfe0d5540816fab0ed43de2b73594f14150a472852ec4`; Tier-A projection `07ae52f1e5e6276dd74a52172bb98cc54cdd136fdc5422ae27fbe3452c4af4a5` (byte-for-byte unchanged from baseline); Tier-B projection `149f725394a0cae0a91759370a62cc70463893d5a7cecc18cdf32c431b121d8a`. Corpus binding unchanged: `nb-sit-statute-corpus-2026-08-29` / `6f0442fc…`. Reports regenerated at `reports/exam-curriculum/build-20260903.{json,md}` (per-tier breakdown + projection hashes). Docs updated: CURRENT_BEHAVIOR, ARCHITECTURE, README, this file.
- [x] Exam Curriculum V1 Tier-A human-QC closeout (2026-09-03): fixed the two human-review semantic curation defects and resolved all eight recall-depth advisory warnings against the committed Tier-A layer. (1) **A-BYL-06** `mustRecall` direction corrected — was "plans are to be based on monuments established in the field" (reversed); now "When a survey results in monuments being placed, a Survey Plan must generally be prepared in accordance with the Standards Manual; the Bylaws provide an exception for monumentation complying with qualifying existing coordinated plans." (2) **A-LTR-03** schedule mapping corrected to A=prescribed Land Titles forms, B=prescribed fees, C=statutory mortgage covenants, D=statutory lease covenants; "additional schedules" removed. (3) Recall-depth advisories: A-NBLS-03 / A-BCAR-01 / A-CPA-04 / A-CPA-05 / A-LTA-06 gained concise conceptual `mustRecall` entries (recall kept); A-REG-02 gained the compact s.34 registration-effect `mustRecall` ("A duly registered conveyance has statutory effect to transfer the land described according to the intent of the conveyance (Registry Act s.34)"), a `s.34` `mustLocate` pin, and its core-understanding reworded away from priority-memorization; A-BYL-07 and A-SURV-04 dropped to `understand`+`retrieve`. (4) UI: anchor lists over 12 chips already collapse; collapsed text now reads `Sources: N provisions resolved (full list in canonical manifest)` (A-BYL-04 49 / A-BYL-06 51 / A-CPA-01 59 no longer render as 40–60 visible chips; manifest retains all 742 anchors). (5) Six new semantic-regression tests in `tests/study/exam_curriculum_tier_a.test.ts`: A-BYL-06 directionality (reversed-proposition negative + verbatim curated fact), A-LTR-03 exact prompt↔schedule-letter pairing via resolved `sourceKey` (key-strict; fails on any wrong pairing), A-REG-03 witnessing prohibitions, A-SURV-03 integrated-area tying categories, recall-depth removals (A-BYL-07/A-SURV-04), and the advisory-set recall+mustRecall sweep. Outcome: **51 units (count unchanged), 0 errors, warnings 11 → 3** (only `excessive-source-breadth` A-BYL-04/A-BYL-06/A-CPA-01 remain — intentionally not split to silence), **mustRecall 12 → 18, mustLocate 111 → 112**, 742 anchors unchanged, manifest contentHash `9733a1ad…` → `bcb92089b0d5d6fc21df73df4c4c62c03d6bffcdbd30695fd4a65f0887aa38f0` (regenerated manifest + `reports/exam-curriculum/build-20260903.{json,md}`). Validation: lint, typecheck, 351 files / 1851 tests (incl. 26/26 exam-curriculum tests), build all green. No Qwen/model inference; frozen Study Map / V4-V5 / calibration / remediation / holdout / 4,251-job run untouched. Tier B intentionally NOT started.
- [x] Exam Curriculum V1 Tier-A layer (2026-09-03): deterministic, separate open-book statute-law exam curriculum for the ANBLS B-5.2 exam — no model inference, no changes to the frozen Study Map, Unit Authoring V4/V5, calibration-80/remediation1/holdout-96, or the prepared 4,251-job run. (1) New `src/study/examCurriculum/` module: `examCurriculumTypes.ts` (standalone `exam-curriculum-v1` schema v1: tiers A-D, unit types `document_orientation`/`core_concept`/`cross_document_navigation`/`lookup_drill`, depths recognize/understand/recall/retrieve, units/manifest/specs/corpus-view/validation types), `examCurriculumCatalog*.ts` (curated Tier-A: 51 units — NBLS Act 6, ANBLS Bylaws 7, Surveys Act 5, Reg 84-76 2, Boundaries Confirmation Act 4, Reg 95-166 2, Community Planning Act 6, Reg 80-159 2, Registry Act 6, Reg 84-190 1, Land Titles Act 7, Reg 83-130 3), `examCurriculumResolve.ts` (corpus view from the authoritative package keyed on `corpusMetadata.corpusContentHash`; document-order section-range resolution with exact / dotted-descendant / parenthesized-subpart endpoint grouping and subsection-expanded-label fallback; schedule/form lookup), `examCurriculumValidate.ts` (identity, corpus/document/sourceKey/anchor integrity, related-unit integrity, educational completeness; advisory recall-vs-retrieve and source-breadth warnings), `examCurriculumBuild.ts` (canonical manifest + SHA-256 content hash excluding `createdAt`, metrics, Markdown/JSON reports), and a bundled read-only `ExamCurriculumPage.tsx` at `/study/exam-curriculum` (sidebar entry; REMEMBER vs LOOK HERE panels; statute-reader deep links via the existing `onOpenProvision` path). (2) `scripts/studyExamCurriculumBuild.ts` (`npm run study:exam-curriculum:build`) loads `study-content/packages/nb-sit-statute-corpus.content-package.json`, asserts the 51 specs / 12 families, builds + validates, and writes `study-content/exam-curriculum/nb-sit-exam-curriculum-v1.json` plus `reports/exam-curriculum/build-20260903.{json,md}`. Outcome: **51/51 units resolved** (742 source anchors, 12 mustRecall entries, 111 mustLocate entries; 8 document_orientation / 43 core_concept; reviewWeight high 34 / medium 16 / low 1), **0 validation errors / 11 advisory warnings** (recall-target-only-in-mustlocate: A-NBLS-03, A-BYL-07, A-SURV-04, A-BCAR-01, A-CPA-04, A-CPA-05, A-REG-02, A-LTA-06; excessive-source-breadth: A-BYL-04 49, A-BYL-06 51, A-CPA-01 59 anchors), bound to corpus `nb-sit-statute-corpus-2026-08-29` (content hash `6f0442fc…`), manifest contentHash `9733a1ad7325bfbcc531abb374a5b5f932065835d6e17f2a04e79a28023aad1c`. (3) Determinism: consecutive CLI reruns produce canonical bytes identical except the explicitly separated `createdAt` (contentHash stable); identical-`createdAt` manifests are byte-identical. (4) 20 focused tests in `tests/study/exam_curriculum_tier_a.test.ts` (endpoint grouping incl. NBLS subsection fallback, schedule/form lookup, validator error/warning classes, spec-to-unit construction, full 51-unit counts/family breakdown, corpus binding, hash + byte determinism). Curation notes: A-REGR-01 curated as `document_orientation` (recognize/understand/retrieve — formatting rules are lookup material, not memorization); A-BYL-07 references the Standards Manual, which is outside the corpus, so its anchors are bylaws-only; ambiguous corpus labels (Bylaws `1.6.3`/`1.6.4`/`1.6.10`/`1986`/`19.0` artifacts, NBLS subsection-expanded labels like `15(1)`) resolve correctly via document-order grouping. Validation: lint, typecheck, 351 files / 1845 tests, build all green.
- [x] Study Unit Authoring V5 blind holdout-96 preparation (2026-09-03, PREPARED ONLY — NOT EXECUTED): deterministic, no-inference selection of 96 jobs from the prepared 4,251-job `ai-units-2026-09-02-frozen-map-v5-preflight` run (verified status prepared, completedCount 0 — no inference ever run, so the holdout is blind) for a production-calibration holdout. Excluded: the 80 calibration-80 jobIds (`ai-units-2026-09-02-frozen-map-cal80-v5`, incl. all remediation1 units). Stratified by risk × feature = `frozenMapPriority` × `mapDisposition` over the 4,171 eligible jobs: min-1 floor per non-empty stratum (11 cells) + largest-remainder proportional for the remaining 85 seats; within-stratum rank `sha256(seed + ":" + jobId)` asc, seed `webnet-v5-blind-holdout96-20260903`. Allocation P1|split 7 / P1|standalone 3 / P2|combine 1 / P2|split 25 / P2|standalone 22 / P3|combine 1 / P3|split 9 / P3|standalone 19 / P4|combine 1 / P4|split 2 / P4|standalone 6 → priority P1 10 / P2 48 / P3 29 / P4 9, disposition standalone 50 / split 43 / combine 3, 35 distinct documents. Rewritten via `rewriteUnitJobForRun` and written through `writeUnitAuthoringRun` as prepared run `ai-units-2026-09-02-frozen-map-holdout96-v5` (96 jobs, 12×8 batches, providerKind local-openai-compatible, gitignored run dir). Frozen selection manifest `reports/unit-holdout96-selection-freeze-20260903.json` (seed, strata, allocation, all 96 jobIds + post-rewrite inputHashes, exclusion list, blind statement; SHA-256 `9261f8e1f2c9959bc0af8a6c63a7b017a46a545fc967e5b0b74549051dbaec78`, selection SHA-256 `f0d5ce6b7bd6e2a4daf2b728deea090c0e33db5106de5f733ab0bcdb3c388e52`); builder `temp/prepare-holdout96.ts` verified deterministic (two runs, byte-identical manifest). Holdout inference NOT started — execution will use the same local provider configuration as calibration-80 (Qwen3.8-27B-UD-IQ4_XS @ 127.0.0.1:8080/v1, concurrency 1, strict-json-schema) and requires explicit go-ahead.
- [x] Study Unit Authoring V5 fidelity-gate regression hardening (2026-09-03): false-positive fixes in `src/study/ai/studyAiUnitV5Fidelity.ts` for faithful restatements — conditional-consequence "no … shall" (Land Titles s.80(9)-style "if no provision is made, then payments shall …") no longer flagged as POLARITY_REVERSAL, disjunctive separate duties ("do no damage … or shall make good …"), verbatim-quoted answer sentences exempt, duty-words carrying their own negation ("shall do no damage") preserved; approved-focus childLabels (e.g. schedule clause labels) now referenceable in summary/note leakage checks; EVIDENCE_NOT_EXACT_VERBATIM issues now carry a first-divergence code-point diagnostic (binary-search alignment, names both source and evidence code points, e.g. U+2019 vs U+0027); retry instructions updated (typographic Unicode rules + replace-only-the-named-character repair guidance). 8 new regression tests (25 total in `tests/study/study_ai_unit_v5_fidelity.test.ts`). Validation: lint, typecheck, 349 files / 1807 tests, build all green.
- [x] Study Unit Authoring V5 post-human-QC hardening (2026-09-03): V5-only fidelity gate `src/study/ai/studyAiUnitV5Fidelity.ts` (exact verbatim evidence, strict selected-label sourceCoverage, polarity reversal incl. sentence-level faithful-negation handling, conditional/discretionary legal modality, unsupported legal effects, context-reference leakage, summary actor overreach, study-note source + grounding) wired into `src/study/ai/studyAiValidation.ts` for `promptSpecVersion == unit-authoring-v5` only (V4 behavior byte-identical), retry instructions for the new codes in `scripts/studyAiLocalMapAuthorRetry.ts`, 17 tests in `tests/study/study_ai_unit_v5_fidelity.test.ts` (all 7 named human-QC defects + clean variants). Deterministic no-inference revalidation of the 80 accepted V5 units: **37/80 invalid** under the new gate (SOURCE_COVERAGE_EXTRA_LABEL x63, EVIDENCE_NOT_EXACT_VERBATIM x37, SOURCE_COVERAGE_MISSING_SELECTED_LABEL x16, POLARITY_REVERSAL x4, CONTEXT_REF_LEAKAGE x4, SOURCE_DERIVED_NOTE_UNGROUNDED x4, SUMMARY_ACTOR_OVERREACH x3, UNSUPPORTED_LEGAL_EFFECT x3, STUDY_NOTE_OUTSIDE_APPROVED_SOURCE x2, LEGAL_MODALITY_REVERSAL x1); all 7 named human-QC regression fixtures caught. Artifacts: `reports/unit-v5-post-human-qc-revalidation-20260902.{json,md}`, `reports/unit-v5-rejected-attempt-analysis-20260902.{json,md}` (69 rejected attempts / 58 jobs: GENERATED_UNIT_HAS_REVISION_SUGGESTION x69, APPROVED_FOCUS_NOT_COVERED x14, ANSWER_APPEARS_TRUNCATED x13, POSSIBLE_MODALITY_MISMATCH x11, UNSUPPORTED_NUMERIC_OR_REFERENCE x9, UNCOVERED_SUBSTANTIVE_SOURCE x8, EVIDENCE_NOT_FOUND x2, OUTSIDE_APPROVED_FOCUS x2, DEFINITION_ANSWER_MISSING_TERM_MEANING x1), `reports/unit-v5-production-gate-20260902.{json,md}` (**productionAuthorized=false**, studyMapReopenRecommended=false). Validation: lint, typecheck, 1799 tests, build all green. STATUS (final, 2026-09-03): remediation1 `ai-units-2026-09-02-frozen-map-cal80-v5-remediation1` **closed 37/37** — 36 local accepted + 1 human adjudication, `unit-762098496a0f61e2` (Land Titles Act s.80 accounting/indemnity, frozen P1): after 9 rejected attempts the model repeatedly omitted selected sourceCoverage labels 80(8)/80(9); the fail-closed `scripts/studyAiHumanUnitAdjudication.ts` promoted attempt 9 from the human correction file (only `sourceCoverage` changed — 80(7)→obj-accounting-duty, 80(8)→obj-primary-indemnity-funding, 80(9)→obj-consolidated-fund-fallback; modelInferenceUsed=false; post-correction V5 validation 0 errors / 0 warnings; accepted result row + human provenance sidecar written; duplicate-result guard in place). Delta: `reports/unit-v5-remediation1-human-delta-20260902.{json,md}` (37/37 repaired-valid, 0 still-invalid, 89 rejected attempts audited). Effective calibration-80 rebuilt (`temp/verify-effective-cal80.ts`): 43 originally valid + 37 remediation replacements = **80/80 accepted, 0 hard V5 fidelity errors, 0 unresolved** — `reports/unit-calibration-80-effective-v5-20260903.{json,md}`. Gates: lint, typecheck, 1825 tests, build all green (incl. 18 tests in `tests/study/study_ai_human_unit_adjudication.test.ts`). **productionAuthorized=false and studyMapReopenRecommended=false remain in force** until ChatGPT completes the human delta review. Full 4,251-job production inference NOT authorized.
- [ ] Study Unit Authoring V5 contract + same-cohort calibration (2026-09-02, IN PROGRESS): the completed V4 calibration exposed a status-contract ambiguity (35/80 needs-map-revision, incl. suggestions reasoned from unselected siblings) and a verbatim-OCR evidence miss. Work items: (1) new spec `study-content/ai/specs/unit-authoring-v5.md` (v4 md immutable; V4 sha `ecbb9730…`, V5 sha `8987b7da…`): `generated` is default even for long/splittable parents; `needs-map-revision` exceptional, exclusive triplet (MAP_GROUP_TOO_BROAD warning + ≥2-group mapRevisionSuggestion + non-contradictory reason); generated+suggestion/warning invalid; suggestion may only partition approved focusSelections/childLabels/definedTerms; exact verbatim OCR evidence; concision targets main ≤180 / guided ≤160 (non-hard); (2) deterministic audit of the 86 V4 coverage warnings (49 APPROVED_FOCUS_NOT_COVERED + 37 UNCOVERED_SUBSTANTIVE_SOURCE) → `reports/unit-v4-coverage-warning-audit-20260902.{json,md}` with provable A(genuine gap)/B(false positive)/C(duplicate) classification, no AI, before any severity change; (3) version-gated V5 validator contract (V4 behavior byte-identical); (4) V5 preflight `ai-units-2026-09-02-frozen-map-v5-preflight` = same 4,251 jobs, 0 invalid/duplicate/orphan; (5) same-cohort sibling run `ai-units-2026-09-02-frozen-map-cal80-v5` matched by (sourceMapProposalId, groupId), crosswalk `reports/unit-calibration-v4-v5-crosswalk-20260902.{json,md}` (v5 jobIds differ by design); (6) runner V5 support (v4 prompt bytes unchanged); (7) execute the same 80 via Qwen3.8-27B-UD-IQ4_XS @ 127.0.0.1:8080/v1, concurrency 1, strict-json-schema — batch 001 staged, validated, then remaining 72; (8) deterministic comparison `reports/unit-calibration-v4-v5-comparison-20260902.{json,md}` + side-by-side human review `reports/unit-calibration-v4-v5-human-review-20260902.{json,md}` (risk-ordered: V5 failures → needs-revision → regressions → warning-heavy → 7 anchors / 20 final-QC → remainder); (9) mock-provider tests (revision contract, contradiction, focus boundary, coverage, OCR, V4 compat, exact-80 crosswalk, 4,251 preflight) + lint/typecheck/test/build + docs/TODO; then commit/push. Do NOT force needs-map-revision to zero; frozen Study Map and V4 artifacts untouched; human V4 semantic review still pending; **full 4,251-job production inference NOT authorized**.

  STATUS (2026-09-02): steps 1-6 + the canonical V5 run are DONE — `ai-units-2026-09-02-frozen-map-cal80-v5` executed 80/80 accepted across an interrupted run + `--resume` (69 rejected semantic attempts, worst `unit-332a2ef669d3f464`), all 80 proposals `generated` (35 V4 needs-map-revision rows resolved under the V5 contract). Step 8 is DONE (WV5 deterministic batch, no inference): `src/study/ai/studyAiUnitCalibrationCompare.{types,utils,load,markdown,ts}` + `scripts/studyAiCompareUnitCalibrations.ts` produce the comparison + human-review reports, byte-identical on rerun (sha256 verified over two CLI runs; V4 86-warning coverage reconciliation recomputed 49/37 and matched). Headline: matrix generated→generated 45 / needs-map-revision→generated 35; V5 revision-consistency buckets all zero; v4/v5 warning-severity totals 171/62; over-target main>240 22→0, guided>220 18→0; OCR triple true on both artifact strings (`by - laws` → v5 obj-18-2-authorization, `Registr ar` → obj-18-2-evidence-entitlement); human-review tiers T3 status-change 35 / T5 named 17 / T6 remainder 28 (T1/T2/T4 0). Focused tests added (`tests/study/study_ai_unit_calibration_compare.test.ts`); lint/typecheck/test/build all pass. REMAINING: human V4/V5 semantic review of the risk-ordered pack, then commit/push (NOT authorized in this batch).
- [x] Study Unit Authoring: local-Qwen calibration run (2026-09-02): ran sibling run `ai-units-2026-09-02-frozen-map-cal80-v4` (80 jobs, 10×8 batches) through the local-Qwen `local-openai-compatible` provider per `reports/unit-calibration-80-launch-manifest-20260902.json` using the new fail-closed runner `scripts/studyAiLocalUnitAuthor.ts` (`study:ai:local-unit`; Qwen3.8-27B-UD-IQ4_XS @ `http://127.0.0.1:8080/v1`, concurrency 1, strict-json-schema): runner-stamped identity + `frozenMapPriority` (model can never set identity or priority), map-run rejection, canonical `AiStudyUnitProposal` validation against corpus components, bounded validator-grounded retries, provider recovery, resume-safe atomic writes, per-attempt failure artifacts. Outcome: **80/80 accepted, 0 semantic failures, 0 provider-incomplete**; 15 rejected semantic attempts, all recovered within the attempt budget (worst case `unit-07fa6fc1208594ca`, Land Surveyors Act s.18(2): 4 rejections because the model normalized the corpus OCR artifacts `by - laws`/`Registr ar` inside quoted evidence; the accepted attempt quoted them verbatim). Distributions match the frozen selection exactly: P1 24 / P2 28 / P3 20 / P4 8; statuses 45 generated / 35 needs-map-revision; canonical re-validation 28 valid / 52 warning / 0 invalid (171 warning-severity issues, 0 errors). Deterministic audit + human-review bundle (`study:ai:audit-unit-calibration`): `reports/unit-calibration-80-result-audit-20260902.{json,md}`, `-human-review-20260902.{json,md}` (risk-ordered, all 80 jobs), `-regression-anchors-20260902.md` (7/7 resolved and accepted, incl. stacked OH&S s.9), `-final-qc-20260902.md` (20 units under the 8 correction parents); byte-identical reruns, SHAs recorded in the reports. Findings classified for human judgment (model verbatim-evidence miss on corrupted source; 27 advisory `mapRevisionSuggestion` on generated status; 21 needs-map-revision without the broad-group warning) — **Unit Authoring v4 prompt/spec NOT changed, validator NOT loosened, frozen Study Map NOT modified, full-corpus unit generation NOT started, full preflight regeneration NOT needed** (prompt version unchanged). **Human semantic review PENDING; full production NOT authorized.**
- [x] Study Unit Bridge: calibration-80 selection, sibling run, launch pack (W5, 2026-09-02): deterministic no-inference `calibrate` bridge command selecting exactly 80 jobs from the prepared preflight job set (seed/tag `20260902`, no RNG, sorted passes): hard pins = the 20 unit jobs of the eight final-map grouping corrections (tag `final-map-grouping-adjudication`; Service NB Act s.56 skip contributes 0, noted), ≥1 representative per resolvable retry-nine target (9/9, tag `map-retry-history`), the 7 regression anchors (tag `unit-v4-regression-anchor`; OH&S s.9 stacks retry+anchor on one job, counted once), then deterministic fill; inherited `frozenMapPriority` never re-decided — actual priority exactly P1 24 / P2 28 / P3 20 / P4 8 and domain exactly core 34 / cadastral 28 / adjacent 18. Sibling run `ai-units-2026-09-02-frozen-map-cal80-v4` via `rewriteUnitJobForRun` (jobIds preserved, inputHash recomputed, all 80 revalidated 0 issues), `prepared` 0/0, batch 8 → 10 files, fixed generatedAt. Reports: `reports/unit-calibration-80-20260902.json` + `.md`, `reports/unit-calibration-80-review-pack-20260902.md` (one row per job, no raw source text), `reports/unit-calibration-80-launch-manifest-20260902.json` (SHA-256s of batches/run.json/job-report/selection report/review pack, intended provider `local-openai-compatible`, no model invoked). Byte-identical reruns; gitignored run dir. Docs: `docs/STUDY_AI_AUTHORING.md`.
- [x] Study Unit Bridge: frozen-map unit-authoring preflight (W4, 2026-09-02): deterministic no-inference `preflight` bridge command over the frozen Study Map. `src/study/ai/studyAiUnitJobValidation.ts` — `validateAiUnitAuthoringJob` verifies one frozen job against the corpus package + its frozen proposal: schema/run/prompt identity (runId, jobType `unit-authoring`, providerKind `local-openai-compatible`, promptSpec `unit-authoring-v4`), locked jobId + inputHash recomputation, frozen source identity (`sourceMapRunId == FROZEN_MAP_RUN_ID`, proposalId), group fidelity (approvedGroup/group deep-equal `proposal.proposedGroups[groupIndex]`; disposition/reason/learning-goal/document/context echoes), authoritative source resolution (every group sourceKey resolves; sourceHashes/statuses/contentFlags/exact+operative text recompute-consistent; no dropped combine sources), focus validity (sourceKey in group; structural child-label membership incl. paragraph compounds + whole-section markers; defined-term verb-pattern recognition; evidence = normalized-substring of the exact source union), zero-source protection, frozenMapPriority == proposal.suggestedPriority. `src/study/ai/studyAiUnitPreflight.ts` — `runFrozenUnitPreflight` runs gate → equivalence audit → frozen eligibility (skip/reference-only 0 jobs; validationStatus not valid/warnings, rejected states, unresolvable sources abort; freeze-final-authority bypasses recorded: 149 needs-review + 10 MAP_CONFLICT warning-level rows) → build all jobs via the shared builder (runId `ai-units-2026-09-02-frozen-map-v4-preflight`, batch 20, generatedAt fixed 2026-09-02T00:00:00.000Z) → cardinality hard assertions (expected = Σ groups = 4,251; unique jobIds; exactly one job per (proposalId, groupIndex); none from non-eligible proposals; zero zero-group jobs) → validate all 4,251 (0 issues) → write prepared run. `src/study/ai/studyAiUnitPreflightReport.ts` — deterministic distributions/layout/report content. CLI `preflight` subcommand (extend-only) writes `reports/unit-authoring-preflight-20260902.json` + `.md`; real run = 3,692 results / 2,971 grouped / 721 zero-group / 4,251 groups = 4,251 jobs, 213 batches, priority P1 382 / P2 2,236 / P3 1,312 / P4 321; byte-identical reruns verified (scratch re-run deleted). Run dir `study-content/ai/runs/ai-units-2026-09-02-*/` gitignored. Docs: `docs/STUDY_AI_AUTHORING.md`.

- [x] Study Unit Bridge: frozen-map freeze gate + map/proposal/job equivalence audit + frozen group inventory (2026-09-02): new deterministic, no-inference bridge tooling for authoring Study Units from the frozen Study Map, with pure logic in `src/study/ai/` and a trivial dispatcher in `scripts/studyUnitBridge.ts`. (1) `studyAiMapFreezeGate.ts` — `verifyFrozenStudyMap` fails closed on any mismatch against the tracked freeze report `reports/study-map-final-freeze-20260901.json`: runId == the canonical run `ai-map-2026-08-29T12-23-57-891Z-local-qwen-full-20260829-181342`, `finalState.resultRows === 3692`, `adjudication.verification.resultRowsInvalid === 0`, `adjudication.failed === 0`, `pinnedAnchors.allFound === true`, recomputed SHA-256 of the FINAL-CANDIDATE decision file === `inputs.decisionFileSha256` (`f31f8012…`), recomputed SHA-256 of `<runDir>/reports/map-proposals.json` === the freeze report's recorded value (`b2726e8e…`), exact recount of the priority distribution (P1 175 / P2 1,458 / P3 1,054 / P4 284 / null 721), `groupingCorrections` = the nine frozen jobIds, and schema-presence checks over all 3,692 canonical rows (0 invalid) — plus the computed canonical-results SHA-256 (`c38147c5…`) carried for downstream consumers. `auditMapProposalEquivalence` joins 3,692 results ↔ 3,692 proposals (`id = "<runId>:<jobId>"`) ↔ 3,692 prepared jobs with runId/jobId/corpusContentHash/document/target sourceKeys/sectionLabels identity and, for grouped rows, full deep equality of disposition/suggestedPriority/reason/proposedGroups (zero-group rows keep empty groups + null priority); 0 mismatches on the real run. (2) `studyAiUnitInventory.ts` — sidecar provenance classifier (explicit, unit-tested over synthetic sidecars) grounded in the observed values (`post-qc-final-human-grouping-adjudication`, `post-qc-final-human-priority-adjudication`, `final-production-tail-human-adjudication`, retry/promote source run `…-production-retry9-20260831-084717`, recovery blocks) with classes `final-QC-adjudicated` 9 / `human-adjudicated` 140 / `retry-promoted` 6 / `recovered` 14 / `original` 3,523; `buildFrozenMapGroupInventory` emits one deterministic row per (grouped result, proposedGroup) — 2,971 grouped / 721 zero-group / 4,251 group rows ordered corpus-document → prepared-job section → groupId, with priority (P1 382 / P2 2,236 / P3 1,312 / P4 321), provenance (final-QC 20 / human 414 / retry-promoted 22 / recovered 14 / original 3,781) and parentKind (standalone 2,309 / split 1,929 / combine 13; no anomalies) histograms; `finalGroupingAdjudicationRows` = 20. Writes `reports/frozen-map-unit-group-inventory-20260902.json` + `.md` (deterministic, no wall-clock, byte-identical reruns). Commands: `verify-freeze` (exit 1 on failure) and `inventory`. Tests: 28 cases across `tests/study/study_ai_map_freeze_gate.test.ts` (synthetic fail-closed fixtures + real-data gate/audit, skipped gracefully when run data is absent) and `tests/study/study_ai_unit_inventory.test.ts` (classifier 5 classes + precedence, inventory cardinality/sort/summary, real-data inventory). Real-run checks: `verify-freeze` ok with 0 issues / 0 mismatches; inventory CLI writes both reports. Full lint/typecheck/340 test files 1,680 passed (1 skipped)/build pass. Docs: `docs/STUDY_AI_AUTHORING.md`.

- [x] Study AI final post-QC human adjudication and freeze (2026-09-01): apply the verified final human review decisions to the frozen canonical run `ai-map-2026-08-29T12-23-57-891Z-local-qwen-full-20260829-181342` with no LLM inference, model-free and fail-closed. (1) Extended the review-decision `groupingDecision` vocabulary with `standalone` (the corrected artifact carries exactly one proposed group) in `src/study/ai/studyAiReviewDecision.ts`, the tracked decision template, docs, and tests; classifier unchanged (any grouping change → requires-corrected-map-result). (2) Authored the nine grouping decisions as full human-corrected Map result artifacts under `temp/study-ai-final-map-review/corrections/<jobId>.json`, each validated through the ordinary `validateLocalResult` path with zero issues: Registry Act s.71 (split P3, 2 groups), Clean Water Act s.40 (split P3, 3 groups), Clean Water Act s.13 (split P3, 4 groups, repealed s.13(1) excluded), Trespass Act s.1 (split P2, 2 groups), Crown Lands and Forests Act s.95 (split P3, 3 groups), Registry Act s.66 (standalone P3, repealed s.66(1) excluded), Public Health Act s.68 (split P4, 3 groups), Aquaculture Act s.90 (split P3, 2 groups), Service New Brunswick Act s.56 (skip, priority null, 0 groups). (3) New deterministic batch `scripts/studyAiFinalMapQcAdjudications.ts` (`npm run study:ai:final-map-qc`), dry-run by default with `--execute` required: verifies the decision file SHA-256 `f31f8012d941871e745932e6501f7cd4a3192bca25f7c6178db65e2a94b946d8` (211 decisions: 67 no-change, 135 priority-only, 9 requires-corrected, 0 invalid), fail-closed parses/classifies it, snapshots the 144 pre-adjudication result rows + provenance under the run's `reports/final-map-qc-snapshot-20260901/`, builds the labeled synthetic source run `final-map-qc-adjudications-20260901-src` (production jobs reused verbatim; reconstructed `local-failures` attempt artifacts with recorded `rawHash` and explicit reconstruction metadata), treats priority-only payloads as the accepted row minus runner-owned identity fields with `suggestedPriority` replaced and grouping payloads as the correction artifacts, adjudicates every job through the ordinary `adjudicateMapResult` path with reason `post-qc-final-human-priority-adjudication` or `post-qc-final-human-grouping-adjudication`, and verifies 3,692 total rows, exactly one row per affected job at the expected final priority, and byte-identical unaffected rows. `scripts/studyAiAdjudicateMapResult.ts` gained optional `adjudicationReason`/`decisionFileSha256` provenance fields (CLI defaults unchanged, decision SHA recorded in provenance). (4) Replayed after adjudication: `study:ai:validate-results` = 3,692 valid / 0 invalid; post-QC audit regenerated (20260831 report family) — 25 zero-group suspects, P1 175 (90 core / 74 cadastral / 11 adjacent), 31 broad standalone suspects, 138 large splits, all four pinned anchors found, with `map-19c48590a1b233de` (Clean Water Act s.40) and `map-d1fadd2dfd0ce395` (Aquaculture Act s.90) repinned from expected-detected to the new `resolved-grouped` expectation (grouped and no longer detected); adjacent-P1 and broad-group review bundles regenerated. Final priority distribution: P1 175 / P2 1,458 / P3 1,054 / P4 284 / null 721 of 3,692. Tracked freeze report: `reports/study-map-final-freeze-20260901.json` + `.md`. Full lint/typecheck/337 files 1,646 passed (1 skipped)/build pass; run artifacts and `temp/study-ai-final-map-review/` untracked. Docs: `docs/STUDY_AI_AUTHORING.md`.

- [x] Study AI whole-source repeal-metadata integrity audit, repair, and result rebase (2026-09-01): the pre-2026-09-01 source classifier used a lax start-anchored `Repealed` regex that marked mixed live/repealed provisions as whole repeals, corrupting `sourceStatus`/`contentFlags` on prepared jobs. (1) Replaced it with a strict line-based repeal classifier in `scripts/studyAiAuthoring.ts` (`classifyRepealOnly` + `isRepealStubLine`): a line is a repeal stub only when it is a bare/labeled `Repealed:`/`Repealed.` marker with an empty, citation/history, or date tail (e.g. `Repealed January 2006`); any substantive line makes the provision mixed; ambiguous-without-substantive is fail-closed. Regression tests: labeled mixed subsections (Partnerships Act s.2 — the bug), date-tail stubs, all-repealed children, live repeal-referencing/transitional text, headed variants. (2) New deterministic, model-free, fail-closed audit/repair tool `scripts/studyAiRepealIntegrityAudit.ts` (report by default; `--repair` for atomic in-place job-row rewrites): verified hash/fingerprint invariants on all 3,692 jobs (0 violations), reclassified the 303 previously repeal-flagged jobs — 298 confirmed whole-repeal, 5 mixed live/repealed, 0 ambiguous — plus 2 previously unflagged bylaw targets confirmed whole-repeal. `--repair` flipped metadata on exactly 7 jobs (5 repealed→current + `containsRepealedSubprovision`; 2 current→repealed), recomputed each job's `inputHash` and `authoringInputFingerprint`, rewrote 7 of 162 batch files, left the other 3,685 rows byte-identical, and verified by read-back (0 errors). Report: `reports/repeal-metadata-integrity-20260901.json` + `.md`. (3) New `scripts/studyAiRepealRebaseResults.ts` (dry-run by default, `--execute` required): reconstructed the 7 stale accepted result payloads (old row minus runner-owned identity fields), built synthetic source run `repeal-rebase-20260901-src` with explicitly labeled reconstructed `local-failures` attempt artifacts, snapshotted the 7 old result rows + provenance under the run's `reports/repeal-rebase-snapshot-20260901/`, removed them, and re-adjudicated all 7 through the ordinary `adjudicateMapResult` path (0 issues, 0 warnings each). Final state: 3,692 result rows, 3,692 unique job IDs, 3,685 rows byte-identical to pre-rebase, 7 rows with repaired identity and unchanged semantic payloads, `study:ai:validate-results` clean (0 invalid). (4) Post-QC audit regenerated on the repaired run: `repealMetadataExcluded=300` (was 301), 28 suspects (26 + Partnerships Act s.2 and Clean Water Act s.13, both operative-scope), P1 total 311 (90/74/147) and 36 broad suspects unchanged, all pinned anchors found — the `map-11fc0137f38dd967` anchor flipped from expected-excluded to expected-detected (operative-scope). Review bundles regenerated byte-identical (adjacent-P1 SHA-256 `5aacf4cb7ee6…`, broad-group `c2cd8db7a6cb…`). Provisional ChatGPT decision file was not present locally (template only), so no decision preview was run. Docs: `docs/STUDY_AI_AUTHORING.md`; tests in `tests/study/study_ai_authoring.test.ts` (+2 cases) and `tests/study/study_ai_post_qc_semantic_audit.test.ts`.

- [x] Study AI post-QC semantic audit + human review preparation (2026-08-31): prepare the frozen canonical run `ai-map-2026-08-29T12-23-57-891Z-local-qwen-full-20260829-181342` (3,692/3,692 accepted) for final human semantic review with no LLM inference and no canonical-result mutation. (1) Post-QC audit (`study:ai:post-qc-audit`) gained the **canonical repeal-metadata guard** — zero-group targets with `sourceStatus: "repealed"` or `contentFlags.repealOnly: true` are excluded from the suspect scan via authoritative job metadata (placed after static-geographic exclusion, before text masking), while live sections containing repealed children and non-repealed transitional provisions stay scannable; pinned-anchor mechanism extended so `map-11fc0137f38dd967` (Partnerships Act s.2) is expected **excluded-by-repeal-metadata** and validated like the three detected anchors (`map-19c48590a1b233de`, `map-21050fd5c7830508`, `map-d1fadd2dfd0ce395`). Regenerated audit: `repealMetadataExcluded=301` (was folded into `fullyRepealedExcluded=294`; residual text-only `fullyRepealedExcluded=1`), suspects 28→26, `scannedZeroGroupTotal=724`, all pinned anchors found, retry accounting unchanged. (2) P1 classification: `doc-new-brunswick-land-surveyors-bylaws` (15 rows) moved into `CORE_SURVEYING_DOCS` → buckets 90 core / 74 cadastral / 147 adjacent (total 311 unchanged; `suggestedPriority` untouched). (3) New deterministic review bundles (`study:ai:post-qc-bundles`): adjacent-P1 (147 rows, 29 documents, SHA-256 `5aacf4cb7ee6…`) and broad-group (36 rows, 6 high-risk incl. five pinned cases, SHA-256 `c2cd8db7a6cb…`) as JSON + Markdown with complete source/focus/provenance/retry context, byte-identical reruns, self-hashing. (4) New human review-decision schema (`src/study/ai/studyAiReviewDecision.ts` + tracked template) and preview-only CLI (`study:ai:review-map-post-qc`): validates job IDs/duplicates/priority-grouping consistency against the accepted run, classifies keep/keep → no-change, priority-only → adjudicable via `study:ai:adjudicate-result`, any grouping change → requires-corrected-map-result (full corrected Map artifact via the ordinary adjudication path), exits 1 with no write on issues, never touches canonical results. Tests: 45 focused cases across `tests/study/study_ai_post_qc_semantic_audit.test.ts` (repeal-metadata guards, live/transitional scannability, expected-excluded anchor, NB-bylaws P1 bucket), `study_ai_review_decision.test.ts` (parsing/preview errors, classification), `study_ai_post_qc_review_bundles.test.ts` (determinism, self-hash, high-risk pins). Full lint/typecheck/337 files 1,644 passed + build pass; run artifacts untracked. Docs: `docs/STUDY_AI_AUTHORING.md`.

- [x] Study AI final production tail — four-job human adjudication (2026-08-31): closed the last four permanent failures of canonical run `ai-map-2026-08-29T12-23-57-891Z-local-qwen-full-20260829-181342` by explicit human adjudication, no model calls. New deterministic utility `scripts/studyAiAdjudicateMapResult.ts` (`npm run study:ai:adjudicate-result`) takes a human-corrected plain model-output JSON plus the source-run saved attempt it derives from; it verifies production/source job identity and matching `authoringInputFingerprint`, verifies the referenced `local-failures` raw/validation artifacts and recorded raw hash, validates the corrected output through the ordinary `validateLocalResult` path (runner identity injected canonically), refuses already-accepted jobs, appends atomically (no duplicate rows), and writes per-job provenance with `adjudication: { humanAdjudicated: true, sourceRun, sourceAttempt, sourceRawHash, correctedOutputHash, resultRowHash, preAdjudicationIssues, adjudicationReason: final-production-tail-human-adjudication, adjudicatedVia: study:ai:adjudicate-result, adjudicatedAt }`; historical failure artifacts untouched, `--dry-run` supported, 10 hermetic unit tests in `tests/study/study_ai_adjudicate_map_result.test.ts`. Adjudicated jobs (from retry run `...-production-retry9-20260831-084717`, focus-metadata corrections only — substance preserved per human review): `map-1c37c940368bec16` Municipalities Act s.100 (attempt 2; licence-fee group made evidence-scoped so authoritative 100(1) stays in one group; P2), `map-1fa5a6239a1f7144` Community Planning Act s.1 (attempt 3; 1(2) kept only for the local-government interpretation rule; P1), `map-208559fbf2dbeffa` Mining Act s.68 (attempt 3; boundary-survey group made evidence-scoped spanning 68(1)(e)+68(3); P1), `map-56bae66370b899b1` Community Planning Act s.75 (attempt 3; public-purpose-land group made evidence-scoped spanning 75(1)(f)/(g)+75(2); P1). Dry-run validated all four with zero issues before writing. End state: production `results/local-map.results.jsonl` = 3,692 rows / 3,692 provenance files; `study:ai:validate-results` = 3,692 valid, 0 invalid; regenerated full-corpus audit = 3,692/3,692 accepted, 0 permanent failures, 0 provider-incomplete (acceptance rate 1.0). Full lint, typecheck, focused tests (10), full suite (334 files / 1,599 passed / 1 skipped), and production build pass. Run artifacts and `temp/study-ai-adjudication/` left untracked. Docs: `docs/STUDY_AI_AUTHORING.md`.

- [x] Study AI local full-corpus post-production cleanup (2026-08-31): fix the narrow focus-grounding false negative — model-supplied `childLabels` no longer narrow `evidenceText` grounding when the authoritative `sourceFocusOptions` entry has an empty child-label list (evidence then checks against the complete authoritative source text), while non-empty authoritative child lists keep strict child-scoped grounding and per-label membership/usability checks always apply — with regressions covering ad-hoc-label parent lead-in acceptance (Mining Act s.34 pattern) and preserved genuine wrong-child rejections (Community Planning Act s.1/s.75, Gas Distribution Act s.52, OHS Act s.9). Recovered all 15 affected production jobs deterministically from saved attempt 3 via `study:ai:recover-result` (model-free; dry-run verified first), regenerated the full-corpus audit at 3,683 accepted / 9 permanent semantic failures / 0 provider-incomplete of 3,692, and built three new deterministic no-inference utilities with hermetic tests: `scripts/studyAiBuildFinalRetrySet.ts` (emits runner-consumable `reports/final-production-retry-9-20260831.json`, SHA-256 `04bcefa4daa275abd8c019d42c47b17f8fc70dae942e8ffa042b06cdc2f13cb9`), `scripts/studyAiPromoteMapResult.ts` (provenance-safe cross-run promotion of accepted results: dual fingerprint/identity verification, no overwrite, no duplicate rows, preserved failure history, atomic append, dry-run; 13 unit tests), and `scripts/studyAiBuildBalancedReviewSet.ts` (deterministic 250-entry balanced production review bundle across 13 fixed strata incl. core NB surveying/licensing laws, clean controls, P1/P2, medium confidence, skip/reference-only/combine, large splits, broad-focus warnings, and a capped retry stratum, plus a document-diversity top-up; emits `reports/balanced-review-set-20260831.json` + `.md`, SHA-256 `c716d32cac86641c4f826e9fd6c1b9c46c86ef0e98c55fe9cf8a1b13c3febd1c`, byte-identical reruns; 6 unit tests). No Qwen/local inference, no promotion executed, no historical-artifact mutation, run data left untracked. Full lint, typecheck, 333 test files / 1,589 tests, and production build pass. Docs: `docs/STUDY_AI_AUTHORING.md`.

- [x] Study AI pre-production hardening batch (2026-08-29): block canonical V3 schema field names leaked into Study Map prose fields with a hard `STRUCTURED_SCHEMA_LEAKAGE` validation error (advisory `STRUCTURED_FIELD_LABEL_LEAKAGE` warning preserved), add a split-tolerant OCR-whitespace grounding fallback with omission-marker rejection, source-word boundary anchoring, and exact source-span reconstruction, give review-bundle entries their own per-record risk-stratum tier labels, make repeal-only source status heading-aware (heading stripped, optional numeric label + `Repealed:`/`Repealed.` only), add the `staticGeographicBoundaryDescription` advisory content flag with reference-only-by-default spec guidance plus NB land-surveyor registration/licensing/practice P1 calibration, re-prepare canonical run `ai-map-2026-08-29T12-23-57-891Z` in place (3,692 stable job IDs, 31 adjudicated status/flag corrections, Gate B reports restored), and add a deterministic 15-job final regression set builder (14 pinned coverage cases + lexicographically first unseen consequential-amendment job `map-01bf07d193d7714f`, byte-identical reruns) with hermetic tests; validated with lint, typecheck, 331 test files / 1,564 tests, and the production build, with no inference, no Gate B rerun, and no historical-artifact mutation.

- [x] Gate B surveying-heavy Study Map evaluation set (100 jobs, August 2026): new deterministic, fail-closed, no-inference builder `scripts/studyAiBuildGateBSample.ts` builds an exactly 100-job Gate B set from base run `ai-map-2026-08-29T12-23-57-891Z` (seed `20260829`) — fixed 23-document quota table (Tier A surveying-focused 60; Tier B cadastral/property/registration/planning/limitation 30; Tier C property-adjacent controls 10 across condominium/marital property), ranks `sha256(seed:baseRunId:jobId)`, unseen-first with explicit reused marking (97 unseen / 3 reused), and three deterministic calibration reservations: large Territorial Division Act boundary-description, a `consequentialAmendment` job, and a definitions job. Exclusions (Gate A 200-set, both 11-job post-Gate-A regression sets) resolve by jobId then document+sourceKey identity, 0 unresolved; `authoringInputFingerprint` recomputed and verified for every selected job (fail-closed). Emits `reports/gate-b-surveying-100-seed-20260829.json` + concise Markdown (SHA-256 `fd2dabecdbf3ce68…`); re-running produces a byte-identical sample. Run artifacts remain untracked; no Qwen/local inference, no Gate B execution. Full lint/typecheck/test (1532 passed, 1 skipped)/build pass.
- [x] Study local Qwen Study Map V3 final deterministic cleanup (August 2026): (1) unify statutory definition recognition in `src/study/ai/studyAiDefinitions.ts` — job construction (`sourceFocusOptions.definedTerms`) and grounding validation (`DEFINED_TERM_NOT_IN_FOCUS_SOURCE`) now share `DEFINITION_VERBS` (`means|includes`), `DEFINITION_TERM_PATTERN`, `extractDefinedTerms`, and `definedTermPattern`, so `includes`-style terms (Condominium Property Act s.1 `claim`/`land`, Highway Act s.44.1 `highway`) validate exactly as offered while a nonexistent term is still rejected; (2) fix `selectReviewBundle` to capture each tier's budget and total once, so the semantic review bundle includes every eligible comparison-set entry exactly once when `--review-size` is at least that size, preserving risk-tier priority for smaller budgets (deep same-document queues no longer truncate silently); (3) source-discipline regressions (test-only — no grouping/priority/confidence/disposition/policy change) pinning the spec's exact phrasing that `"Lieutenant-Governor in Council"` is never shortened to `"L-G in C"` and that the Devolution s.21 clause `"applies only in cases of death after May 1, 1951"` states a temporal boundary, plus corpus anchors (Ownership of Minerals s.3 actor wording, Devolution s.21 date boundary). No Qwen/local inference, no Gate B, no full-corpus execution. Docs: `docs/STUDY_AI_AUTHORING.md`. Full lint/typecheck/test (1532 passed, 1 skipped)/build pass.
- [x] Study local Qwen Study Map V3 grounding/schema/source-discipline hardening (August 2026): five targeted, generic fixes across the strict schema, grounding normalization, defined-term extraction, an advisory content flag, and spec source-discipline — no P1-P4 meaning, split-threshold, confidence, or general source-discipline changes; (1) `suggestedPriority` is now a **required nullable** field in `STUDY_MAP_V3_RESULT_SCHEMA` (`P1`-`P4` or `null`, `required` includes it) so the local strict grammar must always emit it (llama.cpp build 10566 `json_schema`→grammar supports the `null` alternative), `P1`-`P4` when `proposedGroups` is non-empty and exactly `null` when empty, never inferred — canonical committed schema regenerated + focused unit test on the generated object; (2) grounding `splitGluedClauseNumbers` splits a leading structural clause number glued to the first word (`7The`, `13.1(4)The`, `11(2)(b)Of`) symmetrically in source and evidence before whole-token matching (identifiers like `Section7`/`form3` untouched; no fuzzy matching), re-validating the saved REG 83-130 s.7 attempt-1 evidence — new `tests/study/study_ai_grounding.test.ts`; (3) `definitionTerms` recognizes a quoted term introduced by `means` **or** `includes` (Highway Act s.44.1 now yields `exemption`, `highway`, `person`, `roadway`, `usage agreement`); (4) new advisory `contentFlags.consequentialAmendment` flag (`isConsequentialAmendmentText`) marking amendment/repeal/substitution machinery targeting another instrument, steering Limitation of Actions s.33 toward skip/reference-only — always-present boolean (changes `authoringInputFingerprint` for all jobs, not jobId); (5) Limitation s.33 child-label investigation documented (parser exposes only `33(1)`/`33(2)`/`33(3)`; inline `33(3)(a/b/c)` and amended-Act labels `8(3.1)`/`8(4)`/`5(4)` are **not** structural children, validation not loosened); (6) Devolution s.21 neutral historic-applicability source-discipline (a historical date/applicability clause alone never supports obsolescence/non-operation/factual-impossibility; established `HISTORIC_TEMPORAL_APPLICABILITY` warning code; replaced "obsolete transitional material" wording) + spec-drift test; (7) statutory-actor exact-naming guardrail (no hyphenated single-letter shorthand such as `L-G in C` for `Lieutenant-Governor in Council`) + spec-drift test. No local Qwen invoked, no Gate B, no full-corpus inference; base full-corpus run `ai-map-2026-08-29T12-23-57-891Z` re-prepared in place (same 11 jobIds, all fingerprints updated by the always-present flag) and the 11-job post-Gate-A regression comparison set rebuilt against it via `studyAiBuildRegressionSet.ts` (deterministic, no inference; execution deferred). Docs: `docs/STUDY_AI_AUTHORING.md`. Full lint/typecheck/test (1528 passed, 1 skipped)/build pass.
- [x] NB-law normalizer supplemental-placement truncation fix (August 2026): `src/study/content/nbLawNormalize.ts` carried supplemental-component (schedule/form) positions through a first-occurrence `mainText.indexOf(component.text)` lookup, so one-line label components such as `Form 3` re-anchored at an earlier inline section mention and `normalizeComponents` truncated section text at that bogus boundary — Regulation 83-130 s.7 lost `…Certificate of Title in Form 3.` (and s.10/11/19.1/19.2/20/21.1 were similarly clipped). Fix: keep the exact supplemental start index captured at collection time (`SupplementalComponentPlacement`), use those indices for section trim boundaries and component ordering, and stop the label-based `Map`/`indexOf` re-derivation (which also overwrote duplicate labels). Synthetic regression test locks inline-reference-vs-schedule-entry behavior; both normalized corpora (nb-law-pilot, nb-sit) and the statute content package + reports regenerated (only substantive change: document `reg-land-titles-83-130` in both corpora, plus pilot `fetchDate` metadata drift). Content-addressed job-identity impact: the repaired s.7 section now prepares as job `map-196dd23bfe5fb2b8` (was `map-6425c3270b73132a`); full-corpus job set re-prepared as `ai-map-2026-08-29T12-23-57-891Z` and the 11-job post-Gate-A regression set rebuilt from it (builder jobId list updated; the other ten job IDs are unchanged); corpus-level `sourceHash`/content hash unchanged (raw-fetch based). Docs: `docs/STUDY_CONTENT_PIPELINE.md`, `docs/CURRENT_BEHAVIOR.md`, `study-content/ai/LOCAL_MAP_AUTHORING.md`; full lint/typecheck/test/build pass.
- [x] Deterministic Study Map result recovery CLI (August 2026): `scripts/studyAiRecoverMapResult.ts` (`npm run study:ai:recover-result`) is a model-free, deterministic recovery path that revalidates saved `local-failures/` attempt artifacts against the canonical validator and prepared jobs (identity, `authoringInputFingerprint`, raw hash), selects the highest-numbered validating attempt (or an explicit `--attempt`), refuses already-accepted jobs, supports `--dry-run`, appends recovered results to canonical `results/` JSONL with per-job recovery provenance, and preserves all historical failure artifacts. Used to recover the two Gate A validator false-negative jobs (`map-3f89b1579eed6e71`, `map-562652e92d734a71`) from saved attempt 6 after the grounding fix — no model calls; regenerated Gate A audit shows 198/200 accepted, 2 genuine permanent failures, 0 provider-incomplete. Focused unit tests in `tests/study/study_ai_recover_map_result.test.ts` (9 cases: valid-attempt selection, dry-run, no-valid-attempt failure, accepted refusal, pinned attempt, rawHash mismatch, unknown job, fingerprint mismatch, CLI parsing). Batch job loading in `scripts/studyAiLocalMapAuthor.ts` gained an injectable `runsDir` (default `RUNS_DIR`) so recovery/tests can target alternate run roots.
- [x] Study local Qwen Gate A findings batch (August 2026): after the stratified 200-job Gate A run (196/200 accepted, 4 permanent semantic failures, 0 provider-incomplete) — fix false-negative `FOCUS_EVIDENCE_NOT_IN_SOURCE` / `DEFINED_TERM_NOT_IN_FOCUS_SOURCE` grounding caused by bounded context excerpts clobbering the full target `operativeSourceText` when a context entry reuses the target `sourceKey` (target-owned keys always win; regression-tested); evidence/defined-term containment now normalizes (curly-to-straight quotes, non-letter/number to spaces, trim/lowercase) and matches contiguous whole-token sequences (typography/whitespace tolerant, mid-token matches, paraphrases, and omissions still fail); canonical `suggestedPriority` serialization (P1–P4 for grouped results, explicit `null` for zero-group, writer fills missing zero-group `null`, new `SUGGESTED_PRIORITY_FORBIDDEN_WITHOUT_GROUPS`, readers treat `null` like historical omissions); audit reliability split into semantic vs provider recovery (`acceptedJobs`, `firstSemanticAttemptAccepted`, `acceptedAfterSemanticRetry`, `acceptedAfterProviderRecovery`, `semanticRetryJobs`, `semanticRecoveryRate`; provider attempts excluded from per-error-code and retry-signature metrics); review bundle retiered semantically (permanent-failure, semantic-retry, low-confidence, needs-human-review, final-warning, priority-p1, multi-group, configured risk strata, provider-recovery, clean) with `reviewTier` + `reasonSelectedForReview` and semantic/provider attempt counts on every entry; audit now also writes `reports/v1-comparable-results.jsonl` with full comparable V1 result rows; deterministic post-Gate-A regression set builder (`scripts/studyAiBuildRegressionSet.ts`; 11 named jobs incl. the four Gate A failures, coverage/wording/truncation/acronym probes; builder performs no inference, execution deferred to an explicit run); docs updated; Gate A audit regenerated (review-size 80). Deterministic re-validation of saved attempts: Condominium s.1 and Highway s.44.1 clear under the fixed validator; Minerals s.3 (genuine citation error) and Quarriable s.13 (genuine structural error) remain failures requiring fresh generation.
- [x] Study local Qwen Study Map runner provider-recovery + audit separation batch (August 2026): separate provider-side failures from semantic validation failures in the local Study Map V3 authoring runner — shared provider-failure taxonomy (`scripts/studyAiProviderFailures.ts`) and bounded `GET /models` health-wait recovery (`scripts/studyAiProviderRecovery.ts`); provider failures are retried within the same semantic attempt (defaults: 3 provider attempts, 300000 ms recovery budget, 5000 ms poll), never consume semantic retries, are recorded as numbered `transport/provider` failure artifacts plus `reports/provider-events.jsonl`, and abort the run (exit 1, accepted results preserved, resumable with `--resume`) on recovery timeout, provider-attempt exhaustion, or structured-output rejection; fail-closed run identity via `reports/local-run-metadata.json` (model, comparison-set path/hash, job-file hashes) and accepted-result integrity checks (duplicate job IDs, `runId`, `authoringInputFingerprint`); BOM-safe JSONL parsing across runner/auditor/sampler; `--help` for all three CLIs; audit split into semantic vs provider metrics (`semanticPermanentFailures`, `providerFailureJobs`, `providerIncompleteJobs`), provider events embedded in `map-run-audit.json`, and pure provider-incomplete jobs excluded from the semantic review bundle; mocked-only tests (no inference). No model run; the interrupted 200-job run resumes per `study-content/ai/LOCAL_MAP_AUTHORING.md`.
- [x] Local map runner resume-identity fix (August 2026): `--resume` validation now compares each accepted result's `runId` against its job file's prepared `runId` (the canonical result-validator contract) instead of the requested run-directory name, so warm-started runs (jobs prepared under a different run) resume without rewriting stored results; regression test, docs, and validated resume of the interrupted stratified 200-job run (35/200 accepted at relaunch, run continues detached).
- [x] Study local Qwen 200-job stratified validation sample + run audit tooling (August 2026): build a deterministic stratified 200-job sampler (`scripts/studyAiBuildStratifiedMapSample.ts`) over `ai-map-4c12-full-corpus-v2` reusing the existing complexity taxonomy, a local-run audit tool (`scripts/studyAiAuditMapRun.ts`) producing reliability/structure/concision/hygiene/V1-comparator metrics plus a semantic review bundle, focused deterministic tests, and docs. No Study Map prompt/policy/validator changes, no local-model authoring run.
  - [x] Centralize the multi-label category/structural strata taxonomy in `scripts/studyAiMapStrata.ts` (reused by V2 artifacts, the sampler, and the auditor) and move V2 artifacts to it.
  - [x] Add the seeded stratified sampler with auto per-document quotas, shortfall redistribution, two-pass coverage-first per-document selection, V1 known-good mapping, and SHA-256 sample fingerprint (JSON + MD output under the base run's reports/).
  - [x] Add the local-run auditor (reliability incl. retry/recovery per error code, per-stratum tables, structure/final-validation, concision thresholds, output-hygiene patterns, descriptive V1 comparison, and a tiered semantic review bundle JSONL) with integrity checks (malformed/duplicate/unexpected jobs, base-job gaps, fingerprint mismatch).
  - [x] Add focused tests (`tests/study/study_ai_stratified_sample.test.ts`, `tests/study/study_ai_audit_map_run.test.ts`); sampler verified deterministic (seed-stable, 200/200 V1 mapped, all 16 categories and 8 structural strata covered) and auditor verified against the latest 24-job local run.
- [x] Fix 8 pre-existing `tests/workspaceRecovery/` failures (August 2026): Node >=26 ships experimental native `localStorage`/`sessionStorage` getters on `globalThis` (undefined without `--localstorage-file`), and Vitest 4's jsdom environment does not override globals it sees as pre-existing, so Node's built-in shadowed jsdom's working Storage objects and every `window.localStorage` access in tests resolved to `undefined`. Added `tests/vitest.setup.ts` (wired via `setupFiles`), which under the jsdom environment re-points `localStorage`/`sessionStorage` at the current jsdom window and restores Node's original descriptors in `afterAll`. Full suite: 1461 passed / 0 failed.
- [x] Study local Qwen Study Map V3 retry-correction and output-hygiene batch (August 2026): after the third 24-job calibration run (23/24 accepted; every failed attempt was `SUGGESTED_PRIORITY_REQUIRED`), make validation retries corrective rather than regenerative — retry prompts now carry the canonical spec, the original job, a bounded JSON-only echo of the previous invalid response, exact issue codes/messages, concise per-code fixes, and mandatory restatement on repeated errors; add non-blocking `STRUCTURED_FIELD_LABEL_LEAKAGE` warning for structured labels embedded in prose fields; add V3 spec output-hygiene and no-invented-abbreviation guidance; keep canonical schema, `suggestedPriority` runtime conditionality, max-retry behavior, and grouping calibration unchanged; no new model run.
- [x] Study local Qwen Study Map V3 grouping-calibration spec update (August 2026): canonical V3 spec restore/add source-discipline section (supplied-corpus scope, no browsing/outside legal knowledge, operativeSourceText vs exactSourceText, metadata/generated artifacts not operative law), fix disposition list to include skip, restore P1-P4 priority and high/medium/low confidence rubrics, add over-combining guard (one statutory mechanism can contain multiple independently recallable StudyUnits; smallest-useful-set goal; actor/modality cues; abstract calibration examples), choose non-blocking concision targets (~40/30 words), advisory 1-3 evidence guidance; extended spec contract test and docs; no deterministic validator changes; no new model run.
- [x] Study local Qwen 24-job findings repair (August 2026): send the canonical Study Map V3 spec in the local runner prompt (no duplicate), enforce disposition/group-count consistency, block overlapping split focus, require suggestedPriority for substantive groups, reject opaque warning codes, add curriculum-mapping/conciseness/grouping/evidence guidance, feed exact validation issue codes into retries, add regression tests and docs; no new model run.
- [ ] Study local Qwen Study Map V3 prompt calibration (August 2026): make exactly one targeted, generic model-facing instruction update for over-skipping, exact sourceKey/childLabel use, context/evidence boundaries, and skip/group consistency; add focused prompt contract tests/docs; run focused validation and review; do not change schema, validation, grounding semantics, Unit Authoring, or run the 24-job benchmark yet.
- [x] Study local Map comparison batch-mode tooling (August 2026): extend `scripts/studyAiLocalMapCompare.ts` with deterministic 24-job batch comparison, help/missing-argument handling, focused infrastructure tests, and required focused validation. No model calls, no Study Map authoring, no schema/validator/grounding semantics changes, and no commit.
  - [x] Preserve single-job CLI mode and report shape; add `--help` and missing-argument usage errors without reading `undefined` paths.
  - [x] Add deterministic batch mode over the 24-job comparison set with V2 job lookup, V1 known-good lookup by `v1KnownGoodResultLocation` + `jobId`, local lookup by `jobId`, per-job facts/flags, and aggregate counts/rates.
  - [x] Add focused tests in `tests/study/study_ai_local_map_compare.test.ts` and document batch usage in `study-content/ai/LOCAL_MAP_AUTHORING.md`.
- [x] Study Phase 4C.1.3 Study Map infrastructure repair (August 2026): repair child-label preparation, fail-closed V3 validation/schema, deterministic V2 preparation/reuse/regeneration artifacts, local OpenAI-compatible runner scaffolding, local comparison tooling/docs, focused tests, required validation, commit, push, and stop before any local or external authoring.
  - [x] Replace prose-derived child labels with parsed structural child labels only.
  - [x] Add canonical Study Map V3 result schema and fail-closed validation before grounding.
  - [x] Prepare `ai-map-4c12-full-corpus-v2` deterministically with audit, reuse, regeneration, status, and validation artifacts.
  - [x] Add local OpenAI-compatible runner with strict structured output, retry/resume/provenance, mock tests, and no real model calls.
  - [x] Add the local-model comparison manifest/report tooling and local Qwen workflow docs.
- [ ] Study Phase 4C.1.2 full-corpus Study Map V3 external execution (August 2026): execute all 3,692 prepared `ai-map-4c1-full-corpus-v1` jobs through genuine external Codex reasoning using disjoint sub-agent batch ownership, validate/report the full run, generate review queues and clean sample for the human semantic audit gate, run required checks, commit, push, and stop before Unit Authoring.
  - [x] Execute canary batches 001-010 and verify mechanical integrity before continuing.
  - [ ] Execute remaining batches 011-220 with disjoint batch ownership and resumability.
  - [ ] Run Study AI status, validation, reporting, coverage, conflict, queue, and sample generation for the complete result set.
  - [ ] Run required focused/full validation checks and commit/push the completed batch.
- [ ] Study Phase 4C.1.1 full-corpus Study Map eligibility repair (August 2026): fix schedule/form component eligibility before external authoring, add direct section-to-schedule/form references, report ANBLS Bylaws as a bylaw audit class without changing source identity, rebuild the unexecuted `ai-map-4c1-full-corpus-v1` preparation because it has zero result lines, add focused regression coverage, regenerate inventory/jobs/batch/status/coverage reports, run validation, commit, push, and stop before Unit Authoring.
  - [x] Replace blanket schedule/form exclusion with substantive deterministic eligibility and specific exclusion reasons.
  - [x] Regenerate `ai-map-4c1-full-corpus-v1` full-corpus Study Map V3 preparation and verify target/exclusion accounting.
  - [ ] Complete genuine external Codex Study Map V3 result authoring only if an external per-job authoring executor is available; do not fabricate deterministic Map proposals.
- [x] Study Phase 4C.1 full-corpus Study Map V3 pipeline support and prepared run scaffold (August 2026): preserve all Phase 4B artifacts, derive full-corpus Map jobs from the official NB SIT corpus instead of pilot selection files, add size-bounded batching/resumability/status validation, generate inventory/coverage/review queue/sample reports, update focused tests/docs, run required checks, commit, push, and stop before Unit Authoring.
  - [x] Prepare `ai-map-4c1-full-corpus-v1` from the official package with 3,692 eligible `study-map-v3` jobs in 220 size-bounded external-Codex batches after Phase 4C.1.1 schedule/form eligibility repair.
  - [ ] Complete genuine external Codex Study Map V3 result authoring for `ai-map-4c1-full-corpus-v1/results/*.jsonl`, then rerun validation/review reports for the human semantic audit gate. Do not use deterministic prose or start Unit Authoring.
- [x] Study Phase 4B.2.2 V4 Unit Authoring validator calibration (August 2026): preserve the genuine `ai-units-4b21-expanded-s48-v4fix` 48-result corpus, reduce deterministic false-positive truncation, reference, and evidence-support warnings, keep true legal-fidelity warnings and broad-group map-revision routing, regenerate validator reports only, update focused tests/docs, run required checks, commit, push, and stop before full-corpus generation.
- [x] Study Phase 4B.2.1 V4 Unit Authoring remediation (August 2026): preserve `ai-units-4b2-expanded-s48-v4` artifacts, audit the deterministic Phase 4B.2 generation path, remove deterministic/template prose authoring from the production external-Codex path, harden Unit Authoring validation for generic questions, truncation, definition responsiveness, duplicate/nonresponsive answers, unsupported-reference false positives, and map-revision grounding, rerun the same 48 approved Map groups under a new V4-fix run ID without full-corpus generation, generate remediation comparison artifacts, update docs/tests, validate, commit, and stop for human review.
  - [x] Audit root cause: Phase 4B.2 result JSONL was produced by deterministic local helper authoring in `scripts/studyPhase4b2Pilot.ts`, including generic templates and fixed-length clipping.
  - [x] Disable deterministic local Unit prose authoring and prepare same-48 V4-fix jobs under `ai-units-4b21-expanded-s48-v4fix`.
  - [x] Add validator diagnostics and regression tests for generic questions, truncation, definition responsiveness, duplicate/nonresponsive answers, unsupported map-revision concepts, and the Marital Property Act 30(3) reference false positive.
  - [x] Revalidate the preserved V4 run into separate Phase 4B.2.1 artifacts without replacing original Phase 4B.2 reports.
  - [x] Complete genuine external per-job Codex authoring for `ai-units-4b21-expanded-s48-v4fix/results/*.jsonl`, then generate old-vs-remediated before/after comparison totals for human review. Do not use deterministic templates or start full-corpus generation.
    - [x] Execute six disjoint external Codex batch authoring jobs into `results/batch-001.results.jsonl` through `results/batch-006.results.jsonl`.
    - [x] Import/validate remediated result JSONL through the normal Unit Proposal pipeline and generate proposal/report artifacts.
    - [x] Generate Phase 4B.2.1 old-vs-remediated diagnostics, V3 control comparison, same-focus consistency notes, and completion report for human educational-quality review.
    - [x] Run focused AI validation tests plus required lint, typecheck, test, and build checks; commit/push if repository changes are needed.
- [x] Study Phase 4B.2 expanded V4 Unit Authoring pilot (August 2026): preserve all prior Phase 4B.1 artifacts, select exactly 48 diverse approved Map groups where available, prepare a new `unit-authoring-v4` run, author fresh V4 unit proposals without production approval, validate, generate expanded selection/review/comparison/completion reports, update docs/tests as needed, run focused and required validation, commit, and push. Do not begin full-corpus generation.
- [x] Study Phase 4B.1.4 Unit Authoring validation calibration and map-revision feedback (August 2026): preserve `ai-units-4b13-pilot-s16-v3`, add `unit-authoring-v4`, separate full-approved-focus answer support from evidence completeness, normalize statutory references including decimal sections and relative subsections, reduce modality false positives while preserving true-positive gates, add broad-group map revision suggestions, revalidate the existing 16 raw proposals read-only, update docs/tests, validate, commit, and push. Do not mass-generate StudyUnits.
- [x] Study Phase 4B.1.3 Unit Authoring v3 pilot (August 2026): preserve existing Map runs, add `unit-authoring-v3`, prepare exactly 16 approved-map Unit Authoring jobs from corrected V3/V3-grounded groups, author/validate proposals without approving StudyUnits, generate raw results, validation, deterministic comparison, and human-review artifacts, update docs/tests, validate, commit, and push.
- [x] Study Phase 4B.1.2 target-source grounding gate (August 2026): preserve existing Phase 4B.1/4B.1.1 runs, enforce authoring-source vs context-source grounding for Study Map focus evidence, child labels, defined terms, and group topics, fix citation-only metadata, revalidate the existing 24 V3 proposals, run a small corrected Map pass for failing cases plus controls, update docs/tests, validate, commit, and push. Do not begin Unit Authoring.
- [x] Study Phase 4B.1.1 Study Map v3 remediation (August 2026): preserve `ai-map-4b1-pilot-s100-seed42`, audit the original 100-result provenance, add v3 non-templated Map guidance, focusSelections/source-status/content-flag/schema validation hardening, focus-aware split conflicts, targeted 24-provision pilot artifacts, docs, focused tests, validation, commit, and push. Do not begin Unit Authoring.
- [x] Study Phase 4B.1 real Study Map pilot execution (August 2026): prepare `ai-map-4b1-pilot-s100-seed42`, verify 100-job sampling with required golden cases and regulation top-up, process only Study Map v2 JSONL results, validate/generate Map proposals and pilot review artifacts, report pilot statistics, and stop before Unit Authoring.
- [x] Study Phase 4B.1 first real AI authoring pilot support batch (August 2026): add required Study Map pilot sampling includes, pilot-only human evaluation metadata, Study Authoring review controls, pilot metrics/audit reports, docs, focused tests, validation, commit, and push. Do not process the full corpus or mass-approve AI StudyUnits.
- [x] Study Phase 4B.0.1 AI authoring hardening batch (August 2026): add v2 prompt specs, source-cleaned/bounded AI job context, prompt-versioned job identity, approved-map-only unit prep, stratified representative sampling reports with manual includes, robust JSONL status/validation, fidelity/coverage/overlap warnings, minimal Study Map review lifecycle, Unit Proposal source/coverage/editor improvements, docs, tests, validation, commit, and push. Do not process AI jobs or start the 4B.1 pilot.
- [x] Study Phase 4B.0 AI-assisted authoring architecture batch (August 2026): add provider-neutral AI study-map/unit-proposal schemas, external Codex JSONL job workflow, deterministic validation/reconciliation/reporting, proposal IndexedDB storage/import/export, authoring review UI, focused tests, docs, validation, commit, and push.
  - [x] Add schema/types for AI authoring runs, study-map jobs/results, source groups, unit proposals, learning objectives, grounding evidence, notes, review/validation statuses, and generation metadata.
  - [x] Add Node CLI tooling for preparing resumable study-map batches, status checks, validating JSONL results, reconciling map conflicts, creating unit-authoring jobs from map groups, and unit-proposal validation.
  - [x] Add IndexedDB stores and repository methods for authoring runs, study-map proposals, and unit proposals without creating FSRS state before approval.
  - [x] Add Study Authoring UI for runs, map proposals, unit proposals, editable learning objectives, grounding display, stale/source-hash checks, and proposal approval into normal StudyUnits.
  - [x] Extend Study export/import and docs for AI authoring artifacts and the Codex/Plus external workflow.
  - [x] Add focused schema, grounding, reconciliation, lifecycle, approval mapping, and import/export tests; run required validation.
- [x] Study Phase 4A.1 NB SIT import persistence repair (August 2026): repair malformed legacy `legalComponents` IndexedDB key paths that collapsed full-corpus imports, make Manage preview compare against authoritative IndexedDB legal components instead of the lean React snapshot, invalidate derived search after official import, update docs/tests, validate, commit, and push.
- [x] Study Phase 4A.1 search quality, diagnostics, and Phase 4B readiness follow-up (August 2026): restore exact-phrase official-provision snippets/highlights, improve worker ranking/filtering, add search diagnostics and bulk Study index API seams, update docs/tests, validate, commit, and push.
  - [x] Add worker-side matched snippets so official-provision results for phrases such as `Director of Surveys` display highlighted matching text.
  - [x] Improve search ranking so exact phrase, citation/title, heading, and AND-term matches outrank incidental full-text OR matches.
  - [x] Add focused golden search-quality tests for official provisions, section searches, and snippets.
  - [x] Add Phase 4B bulk Study search indexing API seam and diagnostics reporting for artifact sizes/build/load/search timings.
  - [x] Run focused tests and required validation.
- [x] Study Phase 4A.1 scalable storage/search batch (August 2026): stop loading all legal component text into the React snapshot, add IndexedDB legal lookup indexes and repository APIs, add persistent MiniSearch-derived official/study search indexes behind a worker/service boundary, clear raw official package text after successful import, add focused storage/search tests, update Study architecture/current-behavior docs, validate, commit, and push.
  - [x] Lock `minisearch@7.2.0` after confirming TypeScript/browser packaging and `toJSON`/`loadJSONAsync` serialization support.
  - [x] Upgrade Study IndexedDB from v5 to v6 with legal component, unit, prompt, rubric, concept, and attempt lookup indexes plus derived search artifact stores.
  - [x] Add on-demand legal-component repository methods and remove legal component `getAll()` from normal Study startup.
  - [x] Add `src/study/search/` adapter, MiniSearch record builders, persistence, worker protocol, worker implementation, and service wrapper.
  - [x] Rewire document reader, Study Unit editor, preview, source-review acknowledgement, missing-content generation, and Library document counts to use on-demand legal content.
  - [x] Rewire Library search to the worker service with scope filters and compact result summaries.
  - [x] Clear official package raw text after successful import.
  - [x] Run focused tests and required validation.
- [x] Study content Phase 4A NB SIT statute-corpus ingestion foundation (August 2026): complete full normalize/build/import once ANBLS PDF English-only extraction is resolved.
  - [x] Add the 57-Act NB SIT manual-scope manifest, preserve the ten pilot documents, and keep the five pilot regulations as required.
  - [x] Add corpus inventory/fetch/normalize/validate/build/refresh commands plus inventory, candidate-regulation, fetch-status, integrity, coverage, source-change, package-diagnostic, and source-review report outputs.
  - [x] Add focused manifest/inventory/hash tests and documentation for the Phase 4A source authority and command workflow.
  - [x] Resolve legacy blockers by mapping Ecological Reserves and Health to existing current successors without duplicate content, replacing Official Languages and Public Utilities with current successor Acts, and fetching the ANBLS Act/Bylaws PDFs as professional-body sources.
  - [x] Add reliable English-only normalization for the bilingual ANBLS Act and Bylaws PDFs using the left-side English text columns, regenerate the normalized ANBLS Act/Bylaws artifacts, and clear corpus validate/build integrity errors.
  - [x] Run required-corpus import dry run, pilot reimport/idempotency test, full validation, commit, and push after the ANBLS PDF normalization blocker is cleared.
- [x] Study module Phase 3 acceptance verification and final polish (August 2026): confirm observed FSRS behavior, add empty-session next-review messaging, add nearest-due wake-up, expose persisted per-unit undo after reload, document safe source-review testing and FSRS settings/export/practice/preview acceptance status, run focused and standard validation, commit, and push.
- [x] Study module Phase 3 FSRS scheduling implementation (August 2026): replace the provisional Study scheduler with a `ts-fsrs` based per-StudyUnit scheduler while preserving StudyPhase behavior, local/offline persistence, source-review flags, preview/practice non-mutation rules, import/export compatibility, undo, docs, validation, commit, and push.
  - [x] Batch 3A - required pre-FSRS cleanup and scheduler foundation:
    - [x] Treat standalone `N.B. This Act/Regulation is consolidated to ...` lines as source metadata only: preserve exact official text, exclude from operative fact extraction, rubric/concept generation, actor/modality extraction, and add focused regressions.
    - [x] Extend existing trailing structural-heading cleanup to cover `COMMENCEMENT` leakage while preserving document structure, and add focused regressions.
    - [x] Inspect local Node/npm versions, install and lock the current `ts-fsrs` package, verify its installed TypeScript API/types, and document the locked version.
    - [x] Add a focused Study FSRS adapter/serialization/config foundation under `src/study/fsrs/` so React components do not call `ts-fsrs` directly.
    - [x] Add fixed/system Study clock helpers and deterministic adapter tests with fuzz disabled.
  - [x] Batch 3B - Study data model and migration:
    - [x] Extend `StudyProgress`, `StudyAttempt`, settings, snapshot, IndexedDB schema, export/import, and migrations for serialized FSRS scheduling state and attempt scheduling metadata without data loss.
    - [x] Preserve ambiguous legacy due dates as `legacyDueAt` instead of fabricating FSRS history; add replay predicates and idempotent migration tests.
    - [x] Add old-schema IndexedDB fixture coverage and any safe historical replay/reschedule path needed after real counted scheduling attempts exist.
  - [x] Batch 3C - pure queue builder:
    - [x] Build a pure Study queue model with source-review-required first, due Learning/Relearning, due/overdue Review, limited New units, and optional surprise practice outside normal scheduling.
    - [x] Add queue ordering, due-boundary, new-unit-limit, and deterministic tie-break tests.
  - [x] Batch 3D - session rating persistence:
    - [x] Wire Again/Hard/Good/Easy into atomic progress plus attempt writes with idempotency/double-click guards, while keeping StudyPhase transition logic independent for the scheduled Study session path.
    - [x] Add rating previews to the scheduled Study session path using the same timestamp as the final rating operation.
    - [x] Switch the live scheduled Study session source to the FSRS queue builder with source-review items guarded from memory-rating submission.
    - [x] Keep Preview and source acknowledgement non-mutating for FSRS; add focused session/editor tests.
    - [x] Add default Manual Practice and default Surprise Practice non-counted controls/tests, plus an explicit counted-practice override that schedules through FSRS.
  - [x] Batch 3E - UI integration:
    - [x] Add dashboard scheduling counts, Library scheduling badges/filters/sort, and unit-editor read-only scheduling panel.
    - [x] Add session completion summary and dev-only scheduler diagnostics.
  - [x] Batch 3F - undo, docs, full validation:
    - [x] Add immediate undo of the latest eligible scheduling rating using stored card/phase snapshots.
    - [x] Update `README.md`, `docs/ARCHITECTURE.md`, `docs/CURRENT_BEHAVIOR.md`, and `docs/STUDY_MODULE.md`; run required validation including full lint/typecheck/test/build and parity where applicable; commit and push.
- [x] Study generation Phase 2E.7 final generator cleanup batch (August 2026): add conservative final question normalization, citation-title handling for regulation section 1, inanimate/passive legal-object prompt cleanup and audit warnings, trailing structural-heading stripping for generated study text, rubric-answer prefix cleanup, focused regression tests, pilot audit rerun, validation, commit, and push.
- [x] Study generation Phase 2E.6 final heading-question polish batch (August 2026): add narrow strong-heading main-question templates, reject strong-heading topic mismatches, normalize duplicated heading wording, rerun pilot audit, validate, commit, and push.
- [x] Study generation Phase 2E.5 semantic consistency gate batch (August 2026): represent legal fact modality with operative actor/source clause, bind modal questions to extracted facts, add actor/modality semantic audit warnings and Tier A semantic downgrades, improve definition chunk estimates, mark section-level paragraph chunk granularity limits, lock regression cases, regenerate audit reports, validate, commit, and push.
- [x] Study generation Phase 2E.4 final generator polish batch (August 2026): improve natural legal question realization and Tier A quality downgrades, reduce genuine/false topic mismatch warnings, merge and subdivide chunk suggestions into source-key study units, clean low-quality concepts, update audit diagnostics/reports/docs, run focused and full validation, commit, and push.
- [x] Dependency vulnerability fix batch (August 2026): update transitive overrides/lockfile for high-severity `js-yaml` and `nanoid` advisories, verify `npm audit` is clean, run validation, commit, and push.
- [x] Study generation Phase 2E.3 reliability batch (August 2026): require positive source evidence before specialized main-question templates, add unsupported-topic QA, record Tier A/B/C question tiers for main questions and rubric prompts, add audit chunk suggestions, lock golden section expectations, update Study docs, validate, commit, and push.
- [x] Study generation Phase 2E.2 correctness batch (August 2026): scope regression examples by complete official-source identity, add generator grounding/provenance audit checks, preprocess amendment history before reference/rubric generation, prevent same-section cross-document contamination, add focused regression tests, update Study docs, validate, commit, and push.
- [x] Study generation audit harness batch (August 2026): add a Node-only `npm run study:audit-generation` command that audits every pilot section through the production Study title/question/reference/rubric/concept generators, writes JSON/Markdown/warnings/CSV reports plus optional baseline diffs, adds deterministic QA warning checks and focused tests, updates Study docs, validates, commits, and pushes.
- [x] Study module Phase 2E.1 guided recall and rubric quality batch (August 2026): render guided recall with per-rubric required answer fields and response/reference pairing, hide optional prompts behind expansion, improve deterministic rubric generation for Surveys Act section 14, Boundaries Confirmation Act section 16, and Community Planning Act section 83, generate main questions from finished rubric categories, add dev rubric diagnostics, keep manual rubric rows intact, update focused tests/docs, validate, commit, and push.
- [x] Study Manage reset confirmation fix batch (August 2026): replace browser confirm with visible in-app delete confirmation panel, make reset action execute directly, update tests, validate, commit, and push.
- [x] Study reset persistence fix batch (August 2026): prevent clean-slate resets from being auto-seeded on reload, add storage regression coverage, validate, commit, and push.
- [x] Study Manage testing reset batch (August 2026): add confirmed Delete All Data action that replaces Study IndexedDB with an empty valid snapshot, clears transient session/import state, updates docs/tests, validates, commits, and pushes.
- [x] Study module Phase 2E UX correction batch (August 2026): fix Library search highlight offsets, widen expanded sidebar label fit, move rubric question prompt to its own row with tooltip, make Preview close return to Study Units tab, add focused tests, validate, commit, and push.
- [x] Study module Phase 2E completion batch (August 2026): add compact persistent sidebar collapse, unified categorized Library search, optimized official-provision navigation shared by search/hash links, non-mutating study-unit preview, focused tests, docs, validation, commit, and push.
- [x] Study module Phase 2E starter batch (August 2026): introduce structured answer rubrics, deterministic section rubric generation, response-mode session rendering, coverage persistence, migration/export preservation, focused tests, and docs while preserving existing concepts and avoiding FSRS.
- [x] Study module Phase 2D navigation/library/custom-concepts batch (August 2026): implement one-click legal component navigation, editable/reorderable concept authoring with generated/manual preservation, separated Documents vs Study Units library views, custom unlinked study units, deterministic concept suggestion improvements, regeneration controls, focused tests, docs, and validation.
- [x] Study module Phase 2C automatic drafting batch (August 2026): add deterministic source-linked study-unit title/question/reference-answer drafting, citation/concept suggestions, generated-vs-user-edited field state, dedicated unit authoring route, existing-unit missing-content generation safeguards, focused tests, docs, and validation.
- [x] Study module Phase 2A.1 pipeline hardening batch (August 2026): replace the incorrect Community Planning 2019-29 regulation with 80-159, extract and validate regulation enabling Acts, prevent laws.gnb.ca interface contamination, improve section/subsection source keys, split schedules/forms into ordered legal components, fix consolidation-date extraction, normalize citation metadata, add integrity reports, regenerate pilot artifacts, and keep Phase 2B blocked until these checks pass.
- [x] Study module Phase 2A official NB law content pipeline batch (August 2026): add a versioned laws.gnb.ca pilot manifest for the five seeded Acts plus five key regulations, Node-only fetch/normalize/package scripts, structured exact-text parser models, saved parser fixtures, focused tests, generated pilot raw/normalized/package artifacts, and study docs while preserving existing browser Study data and leaving package import/reader section-selection UI for the next batch.
- [x] Study module Phase 2B official content browser import batch: extend Study IndexedDB migrations/import preview, document reader source display, section search/selection, create-unit-from-sections, source-hash update preservation, and source-review-required flags without overwriting user-authored summaries, prompts, concepts, progress, attempts, or drafts.
- [x] Study module initial implementation batch (August 2026): add isolated `/study` local-first law study module with IndexedDB metadata/progress storage, OPFS source-file paths, seeded NB statute-law sample content, recall/session workflow, export/import, focused storage/scheduler/autosave tests, and architecture/current-behavior docs without touching adjustment, network, CAD, parser, solver, or parity behavior.
- [x] Standalone Survey CAD drawing file batch (August 2026): separate Survey CAD persistence from adjustment project settings by adding a native `.wncad` drawing document, decoupling CAD workspace initialization from adjustment input/result, adding adjusted-point import into the active drawing, and preserving legacy embedded CAD import compatibility.
- [x] Project options modal interaction split batch 1 (July 2026): split oversized `tests/project_options_modal_interaction.test.tsx` into shared modal interaction support plus bounded workspace/general, adjustment/export, instrument/transform, and CRS/GPS spec chunks while preserving modal tab, apply/cancel, transform, instrument, CRS, and GNSS coverage.
- [x] Preanalysis planning spec split batch 1 (July 2026): split oversized `tests/preanalysis_planning.test.ts` into shared planning fixtures plus bounded diagnostics, advisory, and transform spec chunks while preserving recommendation, path, threshold, transform, and obstacle coverage.
- [x] Project file spec split batch 1 (July 2026): split oversized `tests/project_file.spec.ts` into shared project-file test support plus bounded schema, migration, and settings spec chunks while preserving serialization, defaulting, include-bundle, CRS, and residual-sort coverage.
- [x] Computational parity harness split batch 1 (July 2026): split oversized `tests/computational_parity_harness.test.ts` into parity fixture support and industry-reference assertion modules while preserving all summary, coordinate, residual, and detailed-reference gates.
- [x] Import review workflow state split batch 1 (July 2026): split oversized `tests/import_review_workflow_state.test.tsx` into shared workflow-state test support plus bounded spec chunks while preserving prompt, append, restore, industry-style, and associated-settings coverage.
- [x] Canadian CRS harness spec split batch 1 (July 2026): split oversized `tests/canadian_crs_harness.spec.ts` into shared CRS harness support plus bounded spec chunks while preserving catalog metadata, transform, Monte Carlo, edge, and summary coverage.
- [x] Import review spec split batch 1 (July 2026): split oversized `tests/import_review.spec.ts` into shared import-review test support plus bounded spec chunks while preserving JobXML grouping, face handling, SD/zenith conversion, row actions, and comparison coverage.
- [x] Map view interaction quality spec split batch 1 (July 2026): split oversized `tests/map_view_interaction_quality.test.tsx` into shared map-view test support plus bounded spec chunks while preserving wheel/pan, planning selection, tool input, labels, and obstacle-fetch coverage.
- [x] Report adjustment layout spec split batch 1 (July 2026): split oversized `tests/report_adjustment_layout.test.tsx` into shared report test support plus bounded spec chunks while preserving section order, collapsed diagnostics, and summary-card rendering coverage.
- [x] CAD spatial index spec split batch 1 (July 2026): split oversized `tests/cad_spatial_index.test.ts` into shared spatial-index test support plus bounded spec chunks while preserving snap, display primitive, arc, curve, and parcel indexing coverage.
- [x] Industry listing format spec split batch 1 (July 2026): split oversized `tests/industry_listing_format.test.ts` into shared listing test support plus bounded spec chunks while preserving listing wording, formatting, diagnostics, and fixture assertions.
- [x] Industry multi-case parity spec split batch 1 (July 2026): split oversized `tests/industry_multi_case_parity.test.ts` into bounded parity spec chunks plus focused support modules while preserving stored-reference comparisons and parity diagnostics coverage.
- [x] Parse input spec split batch 1 (July 2026): split oversized `tests/parse.spec.ts` into shared parse test support plus bounded spec chunks while preserving parser fixture, directive, observation, import, and parity case coverage.
- [x] CAD COGO helper test split batch 1 (July 2026): split oversized `tests/cad_cogo.test.ts` into shared COGO test support plus bounded spec chunks while preserving inverse, curve, parcel, alignment, and auto-layout coverage.
- [x] Project file workflow state test split batch 1 (July 2026): split oversized `tests/project_file_workflow_state.test.tsx` into shared workflow test support plus bounded spec chunks while preserving portable project import/export, session restore, source-file, settings, and saved-run coverage.
- [x] Adjustment engine spec split batch 1 (July 2026): split oversized `tests/adjust.spec.ts` into shared adjustment test support plus bounded spec chunks while preserving solver, CRS, preanalysis, residual, and diagnostics coverage.
- [x] CAD command history test split batch 1 (July 2026): split oversized `tests/cad_command_history.test.ts` into shared command-history test support plus bounded spec chunks while preserving command replay, undo/redo, COGO, parcel, fillet, and alignment coverage.
- [x] Survey CAD workspace test split batch 1 (July 2026): split oversized `tests/survey_cad_workspace.test.tsx` into shared test support plus bounded spec chunks while preserving CAD workspace coverage, assertions, fixture inputs, and test behavior.
- [x] Parse traverse records contract split batch 1 (July 2026): split oversized `src/engine/parseTraverseRecords.ts` by extracting traverse parser contracts while preserving T/TB/TE parsing, sigma resolution, station context updates, logs, and deterministic observation order.
- [x] CAD display type split batch 1 (July 2026): split oversized `src/engine/cad/cadTypes.ts` by extracting display primitive and scene contracts while preserving public type exports and renderer/import compatibility.
- [x] Map canvas basemap split batch 1 (July 2026): split oversized `src/components/mapView/mapViewCanvas2d.ts` by extracting basemap warp drawing while preserving canvas render options, tile mesh rendering, and 2D map canvas behavior.
- [x] CAD renderer style split batch 1 (July 2026): split oversized `src/engine/cad/cadRenderer.ts` by extracting renderer style helpers while preserving display primitives, labels, and scene output.
- [x] CAD frontage corner infill ranking split batch 1 (July 2026): split oversized `src/engine/cad/cadCogoParcelFrontageReferenceCornerInfill.ts` by extracting corner-draft ranking helpers while preserving lot selection, overlap/remainder ordering, and corner trim behavior.
- [x] CAD COGO curve metric split batch 1 (July 2026): split oversized `src/engine/cad/cadCogoCurveMath.ts` by extracting curve metric summary/solver helpers while preserving public curve math exports and curve submit behavior.
- [x] CAD alignment station transaction split batch 1 (July 2026): split oversized `src/engine/cad/cadTransactionsAlignmentStationCommands.ts` by extracting station-equation transaction command while preserving COGO reports, metadata, selection, and command registry exports.
- [x] FieldGenius importer parsing split batch 1 (July 2026): split oversized `src/engine/importers/fieldGeniusImporter.ts` by extracting raw line detection, field picking, and control-station parsing while preserving importer detection, trace wording, normalized observations, and registry exports.
- [x] Survey CAD command parsing split batch 1 (July 2026): split oversized `src/hooks/surveyCad/useSurveyCadCommandParsing.ts` by extracting point/label parsing helpers while preserving all public parser exports and command submit behavior.
- [x] Survey CAD traverse draft actions split batch 1 (July 2026): split oversized `src/hooks/surveyCad/useSurveyCadTraverseDraftActions.ts` by extracting traverse action contracts and shared draft helpers while preserving row editing, sideshots, adjustments, and loop-close behavior.
- [x] Adjustment loop graph split batch 1 (July 2026): split oversized `src/engine/adjustmentLoopDiagnostics.ts` by extracting shared graph traversal/path helpers while preserving GPS and leveling loop closure ranking, source-line ordering, and suspect segment diagnostics.
- [x] Result derived model contract split batch 1 (July 2026): split oversized `src/engine/resultDerivedModels.ts` by extracting public derived-result row/model contracts while preserving helper exports, traceability models, map-link models, and statistical summary behavior.
- [x] Survey CAD command hook contract split batch 1 (July 2026): split oversized `src/hooks/surveyCad/useSurveyCadCommands.ts` by extracting hook args/result contracts while preserving command wiring, preview state exports, and workspace command controller compatibility.
- [x] Run profile context split batch 1 (July 2026): split oversized `src/engine/runProfileBuilders.ts` by extracting profile context resolution while preserving solve-profile normalization, parity defaults, instrument fallback, and run diagnostics behavior.
- [x] Parse post-processing alias split batch 1 (July 2026): split oversized `src/engine/parsePostProcessing.ts` by extracting alias remapping/merge post-processing while preserving observation/station canonicalization, trace entries, fixity merging, and warnings.
- [x] CAD properties model split batch 1 (July 2026): split oversized `src/engine/cad/cadProperties.ts` by extracting property panel contracts, entity labels, and row formatting helpers while preserving property rows, edit-field types, selection grouping, and public import compatibility.
- [x] Parse directive state split batch 1 (July 2026): split oversized `src/engine/parseDirectiveState.ts` by extracting directive transition finalization while preserving no-effect warning reasons, effective line ranges, and parse directive state behavior.
- [x] Survey CAD consume point split batch 1 (July 2026): split oversized `src/hooks/surveyCad/useSurveyCadConsumePoint.ts` by extracting shared consume-point contracts plus report/perpendicular/inverse point-pick handlers while preserving command routing, COGO reports, history updates, and session replacement behavior.
- [x] App state types split batch 1 (July 2026): split oversized `src/appStateTypes.ts` by extracting run diagnostics/settings snapshot types while preserving exported type names, workspace recovery contracts, project settings contracts, and existing import compatibility.
- [x] Survey CAD command text split batch 1 (July 2026): split oversized `src/hooks/surveyCad/useSurveyCadCommandText.ts` by extracting command help text switch while preserving prompt/help copy, command key coverage, and command-line behavior.
- [x] CAD parcel diagnostics split batch 1 (July 2026): split oversized `src/engine/cad/cadCogoParcelDiagnostics.ts` by extracting parcel linework diagnostics while preserving closure candidate checks, node/overlap reporting, parcel overlap checks, gap loop reporting, and public exports.
- [x] CAD COGO math split batch 1 (July 2026): split oversized `src/engine/cad/cadCogoMath.ts` by extracting entity intersection helpers while preserving point construction, survey intersections, line/arc/polyline intersection ordering, labels, and CAD command behavior.
- [x] Industry listing coordinate sections split batch 1 (July 2026): split oversized `src/engine/industryListingCoordinateSections.ts` by extracting section contracts and geodetic/factor summary rendering while preserving adjusted coordinate rows, control component status, geodetic positions, convergence/factor wording, ordering, and parity output.
- [x] CAD batch COGO split batch 1 (July 2026): split oversized `src/engine/cad/cadBatchCogo.ts` by extracting public batch COGO contracts, row parsers, and preview primitive builders while preserving START/line/curve parsing, generated labels, warnings, preview geometry, and commit eligibility.
- [x] Project storage split batch 1 (July 2026): split oversized `src/engine/projectStorage.ts` by extracting OPFS support/path/file helpers while preserving IndexedDB project index/manifest/file behavior, OPFS read/write/delete behavior, and persisted storage status reporting.
- [x] Industry listing input summary split batch 1 (July 2026): split oversized `src/engine/industryListingInputSummary.ts` by extracting shared summary contracts and classic parity input-summary rendering while preserving listing wording, ordering, rounding, and parity output.
- [x] Run comparison panel split batch 1 (July 2026): split oversized `src/components/RunComparisonPanel.tsx` by extracting saved-run row rendering plus panel prop contracts while preserving saved-run editing, baseline selection, comparison thresholds, review actions, and QA click handlers.
- [x] Report preanalysis sections split batch 1 (July 2026): split oversized `src/components/report/ReportPreanalysisSections.tsx` by extracting shared section contracts/header plus locked planned observation table while preserving report wording, collapse/pin behavior, and preanalysis controls.
- [x] Industry listing formatter split batch 1 (July 2026): split oversized `src/engine/industryListingFormatters.ts` by moving classic parity DMS, traverse, coordinate-system, leveling, and instrument row formatters into a focused formatter module while preserving listing text, rounding, and sort behavior.
- [x] Control record parser split batch 1 (July 2026): split oversized `src/engine/parseControlRecords.ts` by extracting shared control parsing types/helpers plus geodetic `P/PH` handling while preserving control fixity, constraint weighting, projection logs, and deterministic parser state.
- [x] Adjustment equation assembly split batch 1 (July 2026): split oversized `src/engine/adjustmentEquationAssembly.ts` by extracting shared assembly types plus focused distance/GPS equation row builders while preserving solver equations, weights, sparse rows, logs, and parity output.
- [x] Conventional observation record split batch 1 (July 2026): split oversized `src/engine/parseConventionalObservationRecords.ts` by record family so the root dispatcher is near 400 lines or less, preserving D/A/V/DV/BM/M/B parsing, ambiguity diagnostics, sigma resolution, 3REDUCE handling, and deterministic observation order.
- [x] JobXML importer split batch 1 (July 2026): split point-record parsing and measurement conversion out of `src/engine/importers/jobXmlImporter.ts` so the root importer is near 400 lines, preserving JobXML detection, point import, setup/backsight/target contexts, normalized observations, trace messages, and deterministic output.
- [x] Survey CAD curve submit split batch 1 (July 2026): split curve-submit callback contracts out of `src/hooks/surveyCad/useSurveyCadCurveSubmit.ts` so the curve submit module is under 400 lines, preserving curve solver, selected-arc, PI curve, and chord-bearing curve behavior.
- [x] Map view root split batch 9 (July 2026): split scene/bounds derivation out of `src/components/mapView/MapViewRoot.tsx` so the root stays under 400 lines, preserving 2D/3D map scene inputs and render behavior.
- [x] Survey CAD command starters split batch 1 (July 2026): split command-starter contracts out of `src/hooks/surveyCad/useSurveyCadCommandStarters.ts` so the starter module is under 400 lines, preserving all CAD command session starts and selection guards.
- [x] GPS project options tab split batch 1 (July 2026): split geoid source file controls out of `src/components/projectOptions/tabs/GpsProjectOptionsTab.tsx` so the GPS options tab is under 400 lines, preserving CRS/GNSS/geoid controls and disabled states.
- [x] Run comparison state split batch 1 (July 2026): split run-comparison hook prop/result contracts plus comparison selection helpers out of `src/hooks/useRunComparisonState.ts` so the hook module is under 400 lines, preserving snapshot history, saved-run, restore, and baseline selection behavior.
- [x] Report precision sections split batch 1 (July 2026): split shared precision section header and prop helper contracts out of `src/components/report/ReportPrecisionSections.tsx` so the precision module is under 400 lines, preserving preanalysis covariance and weak-geometry wording/order/collapse behavior.
- [x] Loop diagnostics sections split batch 1 (July 2026): split loop diagnostics prop contracts and traverse diagnostics panel out of `src/components/report/LoopDiagnosticsSections.tsx` so the root module is under 400 lines, preserving loop wording, ordering, collapse, and load-more behavior.
- [x] Direction diagnostics sections split batch 1 (July 2026): split face, set, target-repeatability, and reject diagnostic sections out of `src/components/report/DirectionDiagnosticsSections.tsx` so the root report module is under 400 lines, preserving diagnostics wording, ordering, and collapse/load-more behavior.
- [x] Report diagnostics sections split batch 1 (July 2026): split prop contract out of `src/components/report/ReportDiagnosticsSections.tsx` so the report diagnostics module is under 400 lines, preserving diagnostics wording, ordering, and collapse behavior.
- [x] Solve profile diagnostics section split batch 1 (July 2026): split prop contract out of `src/components/report/SolveProfileDiagnosticsSection.tsx` so the report section is under 400 lines, preserving diagnostics wording and section rendering.
- [x] Build hygiene batch (July 2026): clear npm audit high-severity findings and remove the production chunk-size warning by updating dependency overrides plus Vite chunk routing, then validate install, lint, typecheck, tests, and build before continuing refactors.
- [x] Survey CAD parcel layout panel split batch 1 (July 2026): split prop contracts and option/style constants out of `src/components/surveyCad/SurveyCadParcelLayoutPanel.tsx` so the panel stays under 400 lines, preserving parcel layout controls and persisted settings behavior.
- [x] Industry output view split batch 1 (July 2026): split section-menu parsing helpers out of `src/components/IndustryOutputView.tsx` so the root view is under 400 lines, preserving context-menu navigation, sort restore, and source-line jump behavior.
- [x] Survey CAD workspace split batch 8 (July 2026): split render-surface overlay/panel composition out of oversized `src/components/SurveyCadWorkspace.tsx` so the root component falls under 400 lines, preserving CAD preview, panels, command bar, parcel layout, and traverse draft behavior.
- [x] Import review modal split batch 1 (July 2026): split oversized `src/components/ImportReviewModal.tsx` into focused modal section modules under 400 lines, preserving staged import review, compare, conflict, group editing, and import actions behavior.
- [x] Run session split batch 1 (July 2026): split oversized `src/engine/runSession.ts` until the root session runner is under 400 lines, preserving profile normalization, impact diagnostics, auto-adjust, and progress reporting.
- [x] Parse input core split batch 1 (July 2026): split oversized `src/engine/parseInputCore.ts` until the root parse-input workflow is under 400 lines, preserving parse ordering, diagnostics, include handling, and observation construction.
- [x] Input pane split batch 1 (July 2026): split oversized `src/components/InputPane.tsx` until the root component is under 400 lines, preserving input editing, diagnostics, import controls, and sample/project actions.
- [x] Survey CAD workspace hook split batch 2 (July 2026): continue splitting oversized `src/hooks/surveyCad/useSurveyCadWorkspace.ts` until the root hook is under 400 lines, preserving command state, geometry editing, import review, and parcel/traverse behavior.
- [x] Adjustment statistics split batch 1 (July 2026): split oversized `src/engine/adjustStatistics.ts` until the root statistics workflow is under 400 lines, preserving residual statistics, diagnostics, and precision outputs.
- [x] Import review workflow split batch 1 (July 2026): split oversized `src/hooks/useImportReviewWorkflow.ts` until the root hook is under 400 lines, preserving staged import review state, prompts, conflicts, and apply behavior.
- [x] Industry listing sections split batch 1 (July 2026): split oversized `src/engine/industryListingSections.ts` until the root listing-section module is under 400 lines, preserving listing wording, ordering, and classic parity layout.
- [x] Parse directive registry split batch 1 (July 2026): split oversized `src/engine/parseDirectiveRegistry.ts` until the root dispatcher is under 400 lines, preserving directive handling order, warnings, and parser state transitions.
- [x] Shared types split batch 1 (July 2026): split oversized `src/types.ts` until the root type barrel is under 400 lines, preserving exported type names and import compatibility.
- [x] Import review split batch 1 (July 2026): split oversized `src/engine/importReview.ts` until the root review module is under 400 lines, preserving staged import diagnostics, ordering, and applied override behavior.
- [x] Preanalysis planning split batch 2 (July 2026): split oversized `src/engine/preanalysisPlanning.ts` until the root planner is under 400 lines, preserving recommendation ordering and diagnostics text.
- [x] Parse engine split batch 1 (July 2026): split oversized `src/engine/parse.ts` until root parser is under 400 lines, preserving parse behavior, directive compatibility, and diagnostics ordering.
- [x] App shell split batch 2 (July 2026): continue splitting oversized `src/App.tsx` until the root shell is under 400 lines, preserving app wiring, modal behavior, and validation output.
- [x] Run results text builder split batch 3 (July 2026): continue splitting oversized `src/engine/runResultsTextBuilder.ts` until the root builder is under 400 lines, preserving generated report wording and ordering.
- [x] Adjust engine split batch 1 (July 2026): start reducing oversized `src/engine/adjust.ts` by extracting cohesive calculation helpers without changing solver behavior or output ordering.
- [x] Dependency audit cleanup (July 2026): run npm audit fix to resolve high-severity transitive `brace-expansion` and `js-yaml` warnings, then validate install, lint, typecheck, tests, and build.
- [x] Project file workflow split batch 5 (July 2026): continue splitting `useProjectFileWorkflow.ts` until the root hook is under 400 lines, preserving project file import, source-file editing, and local project behavior.
- [x] Project file workflow split batch 4 (July 2026): split derived project workspace/run state out of oversized `useProjectFileWorkflow.ts`, keeping effective input/include and file-view behavior stable.
- [x] Project file workflow split batch 3 (July 2026): split imported-project payload normalization and application out of oversized `useProjectFileWorkflow.ts`, keep project import/load state restoration behavior stable, and validate focused project workflow coverage plus full checks.
- [x] Map view under-400 continuation (July 2026): keep splitting the map-view root until the remaining implementation shell is under 400 lines before moving to another oversized source file; preserve 2D/3D interaction, planning controls, and renderer behavior.
- [x] Map view split batch 8I (July 2026): split render-model orchestration and `MapViewContent` prop assembly out of `MapViewRoot.tsx` so the root falls below 400 lines while preserving map behavior.
- [x] Map view split batch 8G (July 2026): split `MapViewRoot.tsx` render prop builders for SVG, 3D scene, context menu, and tool overlay into a focused props module under 400 lines, preserving map behavior.
- [x] Map view split batch 8H (July 2026): split map-view render-surface refs, WebGL initialization/fallback, and layer-renderer wiring out of `MapViewRoot.tsx`, keeping the new hook under 400 lines and preserving map behavior.
- [x] Map view split batch 8F (July 2026): split map-view coordinate helpers, Escape selection cleanup, and 3D projection/cube-view wiring out of `MapViewRoot.tsx`, keeping new files under 400 lines and preserving map behavior.
- [x] Map view split batch 8E (July 2026): split map-view viewport/interaction state, pan-preview commit wiring, reset lifecycle, and idle-prefetch gating out of `MapViewRoot.tsx`, keeping the new hook under 400 lines and preserving map behavior.
- [x] Map view split batch 8D (July 2026): split map-view selection, SVG hit-click, planning-vertex, and context-menu interaction wiring out of `MapViewRoot.tsx`, keeping the new hook under 400 lines and preserving map behavior.
- [x] Map view split batch 8C (July 2026): split drag, wheel, and mouse interaction handlers out of `MapViewRoot.tsx` into `useMapViewDragInteractions.ts`, keeping the new hook under 400 lines and leaving `MapViewRoot.tsx` as the active next target.
- [x] Map view split batch 8B (July 2026): split map-view shell effects, OSM obstacle fetching, and 2D derived/render state out of `MapViewRoot.tsx`, keeping new hook files under 400 lines and leaving `MapViewRoot.tsx` as the active next target.
- [x] Map view split batch 8A (July 2026): move the public map facade to `src/components/MapView.tsx`, relocate the implementation under `src/components/mapView/MapViewRoot.tsx`, and split the map render content/controls into focused subcomponents under 400 lines while keeping the map-view root as the active next target.
- [x] App shell split batch 1 (July 2026): split lazy view loading, level-loop preset helpers, adjusted-points transform selection modal, and JXL import-angle prompt modal out of oversized `App.tsx`, preserve app-shell behavior, and validate focused app coverage plus required checks.
- [x] Map view split batch 7 (July 2026): split `MapView` public snapshot/prop types and shared constants out of oversized `MapView.tsx`, preserve 2D/3D map behavior, and validate focused map coverage plus required checks.
- [x] Import review split batch 1 (July 2026): split model editing helpers, comparison summary builders, and exported review types out of oversized `importReview.ts`, preserve import-review public exports and generated text/order, and validate focused import workflow coverage plus full checks.
- [x] Run results text builder split batch 2 (July 2026): split coordinate/covariance, preanalysis geometry, and residual summary text sections out of oversized `runResultsTextBuilder.ts`, keep generated results text wording/order stable, and validate focused output coverage plus full checks.
- [x] Preanalysis planning split batch 1 (July 2026): split station path graph diagnostics and path-priority summary helpers out of oversized `preanalysisPlanning.ts`, keep preanalysis recommendation ordering stable, and validate focused preanalysis planning coverage plus full checks.
- [x] Project file workflow split batch 2 (July 2026): split portable project payload, parsed-session payload, flat manifest seed, and portable import seed builders out of oversized `useProjectFileWorkflow.ts`, keep project create/import/export behavior stable, and validate focused project workflow coverage plus full checks.
- [x] Run results text builder split batch 1 (July 2026): split run-context preparation and solve-profile diagnostics out of oversized `runResultsTextBuilder.ts`, keep generated results text wording/order stable, and validate focused output coverage plus full checks.
- [x] Project file workflow split batch 1 (July 2026): split associated project settings import helpers, project file naming helpers, and persisted-session merge helpers out of oversized `useProjectFileWorkflow.ts`, keep project load/import/export behavior stable, and validate focused project workflow coverage plus full checks.
- [x] Survey CAD preview split batch 1 (July 2026): split preview constants/types, projection/selection helpers, primitive SVG rendering, preview layers, overlay controls, and parcel report rendering out of oversized `SurveyCadPreview.tsx`, keep preview interaction/rendering behavior stable, and validate focused workspace coverage plus full checks.
- [x] Survey CAD workspace hook split batch 1 (July 2026): split `useSurveyCadWorkspace` result/command-hover contracts and focused hook internals out of oversized `useSurveyCadWorkspace.ts`, keep Survey CAD command behavior stable, and validate focused workspace coverage plus full checks.
- [x] Survey CAD workspace split batch 7 (July 2026): split traverse draft panel input/edit/insert/sideshot state and command-toolbar wiring out of oversized `SurveyCadWorkspace.tsx`, bring the workspace under 600 lines, keep traverse/command controls stable, and validate focused workspace coverage plus full checks.
- [x] Survey CAD workspace split batch 6 (July 2026): split floating side-panel dock/drag/resize state and command-display hint derivation out of oversized `SurveyCadWorkspace.tsx`, keep properties/parcel panel behavior and command hints stable, and validate focused workspace coverage plus full checks.
- [x] Survey CAD workspace split batch 5 (July 2026): split batch COGO draft overlay rendering out of oversized `SurveyCadWorkspace.tsx`, keep batch preview/commit controls stable, and validate focused workspace coverage plus full checks.
- [x] Survey CAD workspace split batch 4 (July 2026): split active traverse draft overlay rendering out of oversized `SurveyCadWorkspace.tsx`, keep traverse edit/insert/sideshot controls stable, and validate focused workspace coverage plus full checks.
- [x] Survey CAD workspace split batch 3 (July 2026): split parcel-layout workflow derivation and action handlers out of oversized `SurveyCadWorkspace.tsx`, keep parcel preview/create behavior stable, and validate focused workspace coverage plus full checks.
- [x] Survey CAD workspace split batch 2 (July 2026): split global keyboard shortcut and command typing event handling out of oversized `SurveyCadWorkspace.tsx`, keep CAD shortcut behavior stable, and validate focused workspace coverage plus full checks.
- [x] Survey CAD workspace split batch 1 (July 2026): split parcel-layout defaults, UI-state cloning, and preview primitive building out of oversized `SurveyCadWorkspace.tsx`, keep CAD parcel layout behavior stable, and validate focused workspace coverage plus full checks.
- [x] Industry listing split batch 7 (July 2026): split adjusted GNSS vector rendering, adjustment statistical summary, data-check/blunder diagnostics, and grid-vs-ground distance diagnostics out of oversized `industryListing.ts`, keep listing wording/order stable, and validate focused listing/parity coverage plus full checks.
- [x] Industry listing split batch 6 (July 2026): split auto-adjust, auto-sideshot, cluster, description reconciliation, and alias trace diagnostic sections out of oversized `industryListing.ts`, keep listing wording/order stable, and validate focused listing/parity coverage plus full checks.
- [x] Industry listing split batch 5 (July 2026): split post-adjusted sideshot, GPS rover offset, GPS loop, and differential leveling loop listing sections out of oversized `industryListing.ts`, keep listing wording/order stable, and validate focused listing/parity coverage plus full checks.
- [x] Industry listing split batch 4 (July 2026): split relationship precision, selected ellipse/relative pair, positional tolerance, and pair display-factor model building out of oversized `industryListing.ts`, keep listing wording/order stable, and validate focused listing/parity coverage plus full checks.
- [x] Industry listing split batch 3 (July 2026): split adjusted coordinate, control-status, geodetic position, and factor-diagnostic rendering out of oversized `industryListing.ts`, keep listing wording/order stable, and validate focused listing/parity coverage plus full checks.
- [x] Industry listing split batch 2 (July 2026): split classic/compact GNSS unadjusted input summary rendering out of oversized `industryListing.ts`, keep listing wording/order stable, and validate focused listing/parity coverage plus full checks.
- [x] Industry listing split batch 1 (July 2026): split lost-station filtering, entered-station classification, classic traverse station order, display factor setup, and GNSS display/covariance helpers out of oversized `industryListing.ts`, keep listing wording/order stable, and validate focused listing/parity coverage plus full checks.
- [x] Report view split batch 6 (July 2026): split observation table rendering, post-adjusted sideshot sections, report filter panel, adjusted-coordinate panel wiring, review diagnostics, closing sections, and shared render helpers out of oversized `ReportView.tsx`, keep report wording/order stable, and validate focused report coverage plus full checks.
- [x] Report view split batch 5 (July 2026): split preanalysis planning summary, locked planned observations, added-set recommendation, processing-log, and prop-contract sections out of oversized `ReportView.tsx`, keep report wording/order stable, and validate focused report coverage plus full checks.
- [x] Report view split batch 4 (July 2026): split preanalysis covariance, weak-geometry, type-summary, and relative-precision sections out of oversized `ReportView.tsx`, keep report wording/order stable, and validate focused report coverage plus full checks.
- [x] Report view split batch 3 (July 2026): split auto-adjust, auto-sideshot, setup-local, and GPS rover offset diagnostic panels out of oversized `ReportView.tsx`, keep report wording/order stable, and validate focused report coverage plus full checks.
- [x] Report view split batch 2 (July 2026): split alias traceability, description reconciliation, and cluster detection panels out of oversized `ReportView.tsx`, keep report wording/order stable, and validate focused report coverage plus full checks.
- [x] Report view split batch 1 (July 2026): split pending-run, adjustment-summary, data-check, blunder-detect, and suspect-impact summary sections out of oversized `ReportView.tsx`, keep report wording/order stable, and validate focused report coverage plus full checks.
- [x] Map view split batch 6 (July 2026): split transformed overlay derivation and station display model helpers out of oversized `MapView.tsx`, keep map styling/tool behavior stable, and validate focused map coverage plus full checks.
- [x] Map view split batch 5 (July 2026): split planning polygon action state and selection cleanup out of oversized `MapView.tsx`, keep station/observation/planning selection behavior stable, and validate focused map coverage plus full checks.
- [x] Map view split batch 4 (July 2026): split 2D layer render scheduling, render-input refs, and OSM basemap tile lifecycle out of oversized `MapView.tsx`, keep canvas/WebGL map rendering stable, and validate focused map coverage plus full checks.
- [x] Map view split batch 3 (July 2026): split tool panel state/metrics, frozen overlay state, and snapshot sync helpers out of oversized `MapView.tsx`, keep map interaction behavior stable, and validate focused map coverage plus full checks.
- [x] Map view split batch 2 (July 2026): split 2D projected geometry, density derivation, and planning overlay derivation out of oversized `MapView.tsx`, keep map rendering/selection behavior stable, and validate focused map coverage plus full checks.
- [x] Map view split batch 1 (July 2026): split interaction/type helpers, OSM basemap descriptor builders, and OSM obstacle parsing out of oversized `MapView.tsx`, keep map behavior stable, and validate focused map coverage plus full checks.
- [x] Adjustment engine split batch 13 (July 2026): split result packaging plus run-mode/context glue out of oversized `adjust.ts`, keep adjustment/parity outputs stable, and validate focused adjustment/parity coverage plus full checks.
- [x] Adjustment engine split batch 12 (July 2026): split data-check and main solve workflow helpers out of oversized `adjust.ts`, removed roughly another 1,000 lines while keeping adjustment/parity outputs stable, and validate focused adjustment/parity coverage plus full checks.
- [x] Adjustment engine split batch 11 (July 2026): split the large post-solve statistics/residual diagnostic phase out of oversized `adjust.ts`, removed roughly another 1,000 lines while keeping adjustment/parity outputs stable, and validate focused adjustment/parity coverage plus full checks.
- [x] Adjustment engine split batch 10 (July 2026): split bootstrap traverse approximation, GPS observation modeling, grid/distance reduction, TS-correlation weighting, solve timing, and coordinate warning helpers out of oversized `adjust.ts`, removed about 1,000 lines while keeping adjustment/GNSS/parity behavior stable, and validate focused adjustment/parity coverage plus full checks.
- [x] Adjustment engine split batch 9 (July 2026): split geoid height conversion and station ellipsoid-height helper logic out of oversized `adjust.ts`, keep coordinate-system/GNSS height behavior stable, and validate focused adjustment/parity coverage plus full checks.
- [x] Adjustment engine split batch 8 (July 2026): split normal-equation solve/covariance helpers and robust-weight helper logic out of oversized `adjust.ts`, keep adjustment statistics and robust diagnostics stable, and validate focused adjustment/parity coverage plus full checks.
- [x] Adjustment engine split batch 7 (July 2026): split GPS vector/frame helper math out of oversized `adjust.ts`, keep GNSS covariance/vector behavior stable, and validate focused adjustment/GPS coverage plus full checks.
- [x] Survey CAD command-hook split batch 17 (July 2026): split snap-fed point consumption and remaining default typed-submit handlers out of oversized `useSurveyCadCommands.ts`, keep command point-pick/report/arc behavior stable, and validate focused Survey CAD coverage plus full checks.
- [x] Survey CAD command-hook split batch 16 (July 2026): split remaining typed report, parcel split, construction-point, and alignment submit handlers out of oversized `useSurveyCadCommands.ts`, keep command reports/transactions stable, and validate focused Survey CAD coverage plus full checks.
- [x] Survey CAD command-hook split batch 15 (July 2026): split typed intersection submit handlers out of oversized `useSurveyCadCommands.ts`, keep intersection reports/alternatives/point creation behavior stable, and validate focused Survey CAD coverage plus full checks.
- [x] Survey CAD command-hook split batch 14 (July 2026): split curve typed-submit handlers out of oversized `useSurveyCadCommands.ts`, keep curve reports/point creation/arc creation behavior stable, and validate focused Survey CAD coverage plus full checks.
- [x] Survey CAD command-hook split batch 13 (July 2026): split arc, edit/modify, and parcel-split point-pick consumers out of oversized `useSurveyCadCommands.ts`, keep point-pick command behavior stable, and validate focused Survey CAD coverage plus full checks.
- [x] Survey CAD command-hook split batch 12 (July 2026): split report publishing, batch COGO draft construction, and command availability flag builders out of oversized `useSurveyCadCommands.ts`, keep command reports/drafts/result flags stable, and validate focused Survey CAD coverage plus full checks.
- [x] Survey CAD hook validation + command input split batch (July 2026): stop pre-commit from duplicating already-run full Vitest gates by default, add timeouts to CLI-spawn tests so child-process stalls fail instead of hanging, split command input action wrappers out of oversized `useSurveyCadCommands.ts`, and validate focused Survey CAD/CLI coverage plus full checks.
- [x] Survey CAD command-hook split batch 10 (July 2026): split command lifecycle handlers out of oversized `useSurveyCadCommands.ts`, keep finish/enter/escape and batch COGO commit behavior stable, and validate focused Survey CAD command coverage plus full checks.
- [x] Survey CAD command-hook split batch 9 (July 2026): split traverse draft action helpers out of oversized `useSurveyCadCommands.ts`, keep traverse row editing/adjustment behavior stable, and validate focused Survey CAD command coverage plus full checks.
- [x] Survey CAD command-hook split batch 8 (July 2026): split command starter factory out of oversized `useSurveyCadCommands.ts`, keep command launch guards/session payloads stable, and validate focused Survey CAD command coverage plus full checks.
- [x] Survey CAD command-hook split batch 7 (July 2026): split selected-line command point adapters and active edit command target builders out of oversized `useSurveyCadCommands.ts`, keep hook result shape stable, and validate focused Survey CAD command coverage plus full checks.
- [x] Survey CAD command-hook split batch 6 (July 2026): split command preview-state builder out of oversized `useSurveyCadCommands.ts`, keep live preview behavior stable, and validate focused Survey CAD command coverage plus full checks.
- [x] Survey CAD command-hook split batch 5 (July 2026): split snap construction-context builder out of oversized `useSurveyCadCommands.ts`, keep snapping context behavior stable, and validate focused Survey CAD command coverage plus full checks.
- [x] Survey CAD command-hook split batch 4 (July 2026): split active traverse and batch COGO draft view-model builders out of oversized `useSurveyCadCommands.ts`, keep panel data shape stable, and validate focused Survey CAD command coverage plus full checks.
- [x] Survey CAD command-hook split batch 3 (July 2026): split command prompt/help text helpers out of oversized `useSurveyCadCommands.ts`, keep command wording stable, and validate focused Survey CAD command coverage plus full checks.
- [x] Survey CAD command-hook split batch 2 (July 2026): split pure command input parsing helpers out of oversized `useSurveyCadCommands.ts`, keep typed command behavior stable, and validate focused Survey CAD command coverage plus full checks.
- [x] Survey CAD command-hook split batch 1 (July 2026): split command session types and command-session helper logic out of oversized `useSurveyCadCommands.ts`, keep hook behavior and public result shape stable, and validate focused Survey CAD command coverage before broader checks.
- [x] Adjustment engine split batch 6 (July 2026): split weak float-zenith display projection out of oversized `adjust.ts`, keep station display coordinate projection behavior stable, and validate focused adjustment/preanalysis behavior plus full checks.
- [x] Adjustment engine split batch 5 (July 2026): split TS-correlation residual diagnostic assembly out of oversized `adjust.ts`, keep weighted residual and diagnostic outputs stable, and validate focused adjustment/correlation behavior plus full checks.
- [x] Adjustment engine split batch 4 (July 2026): split post-adjust sideshot result assembly out of oversized `adjust.ts`, keep `LSAEngine` sideshot outputs stable, and validate focused sideshot/listing/export behavior plus full checks.
- [x] Adjustment engine split batch 3 (July 2026): split run-mode compatibility option normalization and diagnostic line formatting out of oversized `adjust.ts`, keep `LSAEngine` run-mode behavior stable, and validate focused run-mode/adjustment behavior plus full checks.
- [x] Adjustment engine split batch 2 (July 2026): split direction-set, direction-target, and repeatability diagnostic assembly out of oversized `adjust.ts`, keep `LSAEngine` behavior and public result fields stable, and validate focused adjustment/listing behavior plus full checks.
- [x] CRS catalog split batch 2 (July 2026): split Canada CRS data out of `crsCatalog.ts` and repartition US SPCS data chunks below the 600-line warning, keep public CRS lookup exports stable, and validate focused CRS/parse behavior plus full checks.
- [x] CRS catalog split batch 1 (July 2026): split shared CRS catalog types/helpers and the US SPCS data table out of oversized `crsCatalog.ts`, keep public CRS lookup exports stable, and validate focused CRS/adjustment behavior plus full checks.
- [x] Adjustment engine split batch 1 (July 2026): split `adjust.ts` top-level solver constants, internal GPS/bootstrap types, engine options, and parsed-result clone helper into focused modules, keep `LSAEngine` behavior stable, and validate focused adjustment plus full checks.
- [x] Survey CAD parcel layout evaluation split batch 26 (July 2026): split parcel layout constraint messages and generated/manual constraint evaluation out of warning-sized `cadCogoParcelLayoutEvaluation.ts`, keep existing exports stable, and validate focused CAD COGO behavior plus full checks.
- [x] Survey CAD parcel layout primitive split batch 25 (July 2026): split shared frontage/edge helpers plus slide and swing solvers out of warning-sized `cadCogoParcelLayoutPrimitives.ts`, keep compatibility exports stable, and validate focused CAD COGO behavior plus full checks.
- [x] Survey CAD spatial index split batch 24 (July 2026): split entity-level point/segment/arc snap candidate collection out of oversized `cadSpatialIndex.ts`, keep `buildCadSpatialIndex` behavior stable, and validate focused CAD snapping behavior plus full checks.
- [x] Survey CAD geometry curve split batch 23 (July 2026): split circle/arc intersection, tangent-point, and offset/parallel/perpendicular helpers out of oversized `cadGeometryCurves.ts`, keep `cadGeometry.ts` exports stable, and validate focused CAD geometry behavior plus full checks.
- [x] Survey CAD FILLET split batch 22 (July 2026): split general FILLET helper types, refs, trim choices, and candidate result builders out of oversized `cadTransactionsFillet.ts`, keep `buildCadGeneralFillet` exports stable, and validate focused CAD transaction behavior plus full checks.
- [x] Survey CAD frontage-reference auto-layout split batch 21 (July 2026): split generated-remainder replacement/fill helpers out of warning-sized `cadCogoParcelFrontageReferenceAutoLayout.ts`, keep frontage-reference auto-layout exports stable, and validate focused CAD COGO behavior plus full checks.
- [x] Survey CAD COGO summary split batch 20 (July 2026): split bearing/azimuth formatting, inverse/distance summaries, traverse adjustment, and named-point helper out of warning-sized `cadCogoMath.ts`, keep existing exports stable, and validate focused CAD COGO behavior plus full checks.
- [x] Survey CAD alignment split batch 19 (July 2026): split offset-alignment construction and station-equation helpers out of oversized `cadAlignment.ts`, keep alignment exports stable, and validate focused CAD alignment behavior plus full checks.
- [x] Survey CAD geometry split batch 18 (July 2026): split arc/curve geometry helpers out of oversized `cadGeometry.ts`, keep existing geometry exports stable, and validate focused CAD COGO/spatial behavior plus full checks.
- [x] Survey CAD parcel geometry split batch 17 (July 2026): split linework, overlap, and gap diagnostics out of oversized `cadCogoParcelGeometry.ts`, keep existing parcel geometry exports stable, and validate focused CAD COGO behavior plus full checks.
- [x] Survey CAD COGO math split batch 16 (July 2026): split curve metric and arc construction helpers out of oversized `cadCogoMath.ts`, keep existing exports stable, and validate focused CAD COGO behavior plus full checks.
- [x] Survey CAD parcel layout primitive split batch 15 (July 2026): split frontage path and generated parcel/depth strip primitive helpers out of oversized `cadCogoParcelLayoutPrimitives.ts`, keep existing exports stable, and validate focused CAD COGO behavior plus full checks.
- [x] Survey CAD spatial index split batch 1 (July 2026): split snap constants, candidate helpers, entity reference builders, and viewport bounds filters out of oversized `cadSpatialIndex.ts`, keep `buildCadSpatialIndex` behavior stable, and validate focused CAD snapping behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 14 (July 2026): split remaining frontage-reference automatic parcel layout helpers and orchestration out of oversized `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 12 (July 2026): split move/copy/paste and trim/extend/fillet/grip command definitions out of oversized `cadTransactions.ts`, preserve registry behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 11 (July 2026): split curve creation commands and entity edit command definitions out of oversized `cadTransactions.ts`, preserve registry behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 10 (July 2026): split polyline, traverse, and batch COGO command definitions out of oversized `cadTransactions.ts`, preserve registry behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 9 (July 2026): split parcel create, line split, bearing split, and area split command definitions out of oversized `cadTransactions.ts`, preserve registry behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 8 (July 2026): split parcel transaction command definitions and shared parcel split commit helper out of oversized `cadTransactions.ts`, preserve registry behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 7 (July 2026): split alignment transaction command definitions out of oversized `cadTransactions.ts`, preserve registry behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 6 (July 2026): split native FILLET transaction geometry and preview helpers out of oversized `cadTransactions.ts`, preserve command/preview behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 5 (July 2026): split CAD transaction TRIM/EXT common geometry, trim piece/preview logic, and extend piece/preview logic out of oversized `cadTransactions.ts`, preserve behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 4 (July 2026): split CAD transaction selection expansion plus entity transform/grip-edit helpers out of oversized `cadTransactions.ts`, preserve behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 3 (July 2026): split CAD transaction metadata, linked-point/dependency helpers, and transaction report helpers out of oversized `cadTransactions.ts`, preserve behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 2 (July 2026): split CAD transaction naming and manual point factory helpers out of oversized `cadTransactions.ts`, preserve behavior, and validate focused CAD behavior plus full checks.
- [x] Survey CAD TypeScript structure enforcement batch 1 (July 2026): split CAD transaction command/types surface out of oversized `cadTransactions.ts`, preserve public exports, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 13 (July 2026): extract single-frontage automatic parcel layout wrapper from `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 12 (July 2026): extract frontage-path parcel auto-layout drafting from `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 11 (July 2026): extract supported-remainder automatic parcel fill loop from `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 10 (July 2026): extract parcel auto-layout stabilization/remainder helpers from `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 9 (July 2026): extract closed-boundary parcel ring auto-layout from `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 8 (July 2026): extract corner-frontage geometry helpers from `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 7 (July 2026): extract generated-lot conflict helpers from `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 6 (July 2026): extract generated-draft/frontage-line auto-layout utilities from `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 5 (July 2026): extract parcel layout preview/evaluation helpers from `cadCogoParcel.ts`, preserve compatibility exports, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 4 (July 2026): extract parcel layout types plus frontage-path/slide/swing layout primitives from `cadCogoParcel.ts`, preserve compatibility exports, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 3 (July 2026): extract parcel frontage-reference and local-coordinate polygon helpers from `cadCogoParcel.ts`, keep compatibility exports stable, and validate focused CAD behavior plus full checks.
- [x] Survey CAD parcel COGO split batch 2 (July 2026): continue breaking `cadCogoParcel.ts` into focused parcel modules, keep `cadCogo.ts`/`cadCogoParcel.ts` compatibility exports stable, and prove no behavior loss with CAD COGO tests plus full validation.
- [x] Survey CAD COGO module split batch (July 2026): refactor massive `src/engine/cad/cadCogo.ts` into smaller behavior-preserving CAD COGO modules, keep existing import compatibility, update architecture docs for the new seams, and validate focused CAD coverage before broad checks.
- [x] Survey CAD parcel auto junction-fill batch (July 2026): finish multi-edge frontage fill so selected angled-to-bottom frontage chains keep generating lots from the shared corner instead of leaving a large bottom-frontage void or collapsing the angled bottom side to one lot.
- [x] Survey CAD parcel auto frontage-path batch (July 2026): stop auto layout from inventing lots off non-selected remainder boundary/frontage edges, suppress over-aggressive angled-corner prepass bundles that created wonky far-left lot linework, convert under-frontage edge leftovers at selected frontage junctions into combined-frontage corner lots, keep rear max-depth leftovers as remainder instead of oversized blue lots, and lock it with focused engine/workspace fixtures plus full validation.
- [x] Survey CAD floating panel shell batch (June 2026): make the parcel-layout floating popup resizable in both axes with persisted size, then extract that compact drag/resize shell into a shared Survey CAD popup pattern so future floating CAD panels reuse the same behavior instead of ad hoc panel chrome.
- [x] Survey CAD parcel layout shell polish batch (June 2026): fix floating panel body scrolling so the preview section stays reachable at standard size, match floating default size to the docked shell, restrict resize to wider-or-standard and shorter-or-standard directions, compact the parcel controls further, and add hover tooltips across the panel surface.
- [x] Survey CAD parcel properties merge batch (July 2026): restyle the Properties popup onto the shared CAD floating-panel chrome, merge the selected parcel report summary/table into that popup for parcel selections, and suppress the duplicate standalone parcel report card behind it.
- [x] Survey CAD side-panel shell controls batch (July 2026): switch shared panel close/collapse controls onto symbol buttons, add left/right docking controls to Properties, and tile same-side CAD panels horizontally in dock-order instead of letting them overlap.
- [x] Survey CAD properties float + dock clearance batch (July 2026): add a float mode with drag support to Properties and lower the default docked panel top offset so left/right pinned side panels clear more of the CAD toolbar.

- [x] Survey CAD naming fallback batch (June 2026): stop unlabeled arc/polyline/polygon hover and snap surfaces from leaking raw prefixed entity ids like `arc:*` or `polyline:*`, and keep the shared naming helper on readable type fallbacks instead.

- [x] Survey CAD stakeout rename-sync batch (June 2026): keep `STA PT` multiline stakeout labels synced when the created point is renamed from `Properties`, so the first-line station id updates without losing displayed station/offset text.

- [x] Survey CAD stakeout label selection batch (June 2026): make text labels pickable through the same widened CAD hit-target path as other entity types, then lock live `STA PT` / `STA INT` stakeout-label `Properties` selection coverage before broader alignment annotation polish continues.

- [x] Survey CAD extend-tool + centered helper batch (June 2026): move the viewport command helper/status copy into the centered lane above the command bar so long prompts stay readable, replace the unreliable `Shift`-held trim extension path with a dedicated repeatable `EXT` command that captures source then boundary picks, and keep focused CAD geometry/workspace coverage on the new extend flow.

- [x] Survey CAD fillet-corner + trim-extend batch (June 2026): fix reverse-side partial-circle fillet previews/commits so cursor-side corner selection stays logical, allow `FILLET` radius `0` to resolve as a hard intersection corner, and add `Shift`-held `TRIM` extension mode with live preview that extends the clicked entity to the next valid cut in its forward path.

- [x] Survey CAD traverse editor batch (June 2026): replace sequential `TRAVERSE` prompt with a live draft panel that now covers open/closed/point-to-point modes, selected-point close targets, append/edit/insert/reorder leg rows, sideshot rows, panel-native finish/cancel, and closure metrics on the current command/history seam, while leaving richer deed-style batch entry for the later deed/parser batch.

- [x] Survey CAD deed/batch COGO batch (June 2026): add a native `DEED` / `BATCH_COGO` panel with pasted `START`, bearing-distance, and tangent-curve row parsing, live preview rows/warnings plus generated point/line/arc preview geometry, and persisted computation history on the shared CAD command seam.

- [x] Survey CAD stakeout label batch (June 2026): make `STA PT` / `STA INT` created points carry displayed-station label text and make both the created points and picked stakeout labels surface shared alignment station/offset metadata in `Properties`, while leaving broader stakeout annotation polish for later.

- [ ] Survey CAD alignment/stationing batch (June 2026): current slice: add alignment label refinements plus global CAD naming consistency on top of the new selection-driven CAD `Properties` panel, keep hover/snap badge labels on the same operator-facing naming contract as `Properties`, keep shared displayed-station formatting across labels/report rows, make move/copy keep arc/polyline/polygon/parcel/traverse-linked point geometry in sync without extending that auto-move to ordinary lines, extend the new repeatable Civil3D-style `FILLET` so it now works across lines, polylines/traverses, and arcs with the same logical-side live preview plus side-of-corner control from cursor position while rejecting hook/sharp-angle arc branches that break tangency at the join and forcing interior arc picks to stay on the hovered retained branch for line/polyline fillets, and leave interval/stakeout annotation refinements for the next slice.

- [x] Survey CAD trim pair-pick loop batch (June 2026): make repeatable native `TRIM` follow the same first-entity/second-entity loop as `FILLET`, keep the live survivor preview on the hovered second pick, and reset cleanly for the next trim pair after each commit or failed same-entity pick.

- [x] Survey CAD alignment command-copy batch (June 2026): replace raw `ALIGNMENT_*` status, validation, and committed-history copy on the CAD command surface with operator-facing `ALIGN`, `ALIGN OFF`, `STA`, `STA EQ`, `STA PT`, and `STA INT` wording.

- [x] Survey CAD stakeout-properties wording batch (June 2026): make `Properties` show readable stakeout-kind labels like `Station offset` and `Interval` instead of raw metadata slugs such as `station-offset`.

- [x] Survey CAD anchored-label naming batch (June 2026): give anchored point and stakeout text labels stable operator-facing `... label` names inside `Properties`, and keep those names synced when the source point is renamed.

- [x] Survey CAD parcel-from-lines batch (June 2026): let selection-driven `PARCEL` promote a closed chain of selected line entities into a native parcel with the same closure metrics, COGO provenance, and workspace/property/report behavior as the existing traverse/polyline parcel path.

- [x] Survey CAD parcel-area units batch (June 2026): add shared parcel area conversions and surface hectare/acre/square-foot readouts alongside the existing square-meter parcel metrics in the native parcel report/COGO workflow.

- [x] Survey CAD area-sequence batch (June 2026): add a report-only `AREA` command that captures a picked/typed point sequence, auto-closes the loop for parcel-style math, and reports area/perimeter/closure plus converted area units without creating parcel entities.

- [x] Survey CAD parcel-course table batch (June 2026): surface explicit parcel course bearing rows in the selected parcel overlay and persisted `PARCEL_CREATE` COGO report so parcel tables carry readable course-by-course line data beyond the summary metrics.

- [x] Survey CAD parcel-check batch (June 2026): add a report-only `PARCEL CHECK` action for selected linework that reports open ends, branches, overlaps, and closed-loop readiness before parcel creation.

- [x] Survey CAD parcel-split-line batch (June 2026): add a first `PARCEL SPLIT` workflow that uses one selected parcel plus one selected line segment to replace the parent parcel with two child parcels when the line crosses the boundary exactly twice.

- [x] Survey CAD parcel-overlap-report batch (June 2026): add a report-only parcel overlap action for multi-selected parcels that lists overlapping parcel pairs and their computed shared area before broader parcel gap/area-split tools.

- [x] Survey CAD parcel-gap-report batch (June 2026): add a report-only parcel gap action for multi-selected parcels that detects enclosed gaps between parcel loops and reports their computed area before broader bearing/area split tools.

- [x] Survey CAD parcel-split-bearing batch (June 2026): add a first `PARCEL SPLIT` bearing workflow for one selected parcel that captures a through point plus typed bearing and replaces the parent parcel when that bearing line crosses the boundary exactly twice.
- [x] Survey CAD parcel-split-area batch (June 2026): add a first `PARCEL SPLIT` area workflow for one selected parcel that captures a through point plus target area in square meters and replaces the parent parcel when a solved split line through that point can hit the requested child area.
- [x] Survey CAD parcel-layout toolbar batch (June 2026): add a dedicated parcel-layout panel with persistent sizing settings, automatic-layout settings, active-parent/frontage state, and preview controls so the remaining frontage-aware parcel tools share one native workflow surface.
- [x] Survey CAD parcel-slide-swing batch (June 2026): add first frontage-aware single-lot `Split by Slide` and `Split by Swing` workflows that solve one child parcel from a selected parent/frontage setup using minimum area plus basic frontage validation, with accept/reject preview and alternative cycling on the shared parcel command/history seam.
- [x] Survey CAD parcel-constraint solver batch (June 2026): extend parcel-layout solving with frontage-at-offset, minimum width, minimum depth, optional maximum depth, solution-preference scoring, and failed-rule highlighting/warnings while keeping all measurements normalized to internal metric units.
- [x] Survey CAD parcel-auto-layout batch (June 2026): add repeated frontage-aware parcel generation for one selected parent parcel, including preview-all vs step-through creation, automatic fill mode, deterministic parcel naming, and remainder handling options that can place leftover area in the last parcel, create a remainder parcel, or redistribute it.
- [ ] Survey CAD parcel-advanced split/report batch (June 2026): add dedicated sliding-area and hinged-area split tools plus curved-frontage support, parcel-layout history/report rows, parcel label toggles, and richer parcel table/report surfacing for generated lot sets.
      Current slice: expand parcel-layout history/report row summaries beyond the new saved constraint settings so solved width/depth/frontage metrics are visible in history and export.

- [x] Survey CAD traverse adjustment batch (June 2026): add draft-panel angular balance plus Bowditch/transit closure adjustment on top of the native traverse editor, persist adjustment provenance/report rows through command history, and leave optional Crandall balancing for a later high-end pass.

- [x] Survey CAD curve calculator batch (June 2026): add shared curve-solver/report math, selected-arc radial bearing and point-on-curve/subdivision/offset tools, typed PI-radius-delta and chord-bearing constructors, reverse/compound continuations, and focused CAD math/workspace coverage.

- [x] Survey CAD intersection batch (June 2026): add dedicated bearing-bearing, bearing-distance, distance-distance, line-circle, perpendicular, offset, and skew intersection calculators with deterministic alternative ordering, native command-menu entry points, and focused report/workspace coverage.

- [x] Survey CAD core point-line batch (June 2026): add multi-point inverse, bearing-only and distance-only reports, turned/deflection point tools, point-along and offset-point calculators, extend-line helper flow, and the command UI/report coverage for them.

- [x] Survey CAD COGO report panel batch (June 2026): add a selected/latest COGO result overlay in the native CAD workspace, bridge live `INVERSE` query output into the shared report model, and add exportable TXT/CSV/Markdown report previews for persisted computation history.

- [x] Survey CAD properties panel batch (June 2026): replace the selected/latest COGO popup with a selection-driven `Properties` panel for points, lines, parcels, alignments, arcs, text, ellipses, and mixed multi-select dropdown collapse while leaving command status text plus persisted COGO history intact.

- [x] Survey CAD COGO foundation batch (June 2026): add shared COGO result/report/provenance types, project-level persisted COGO computation history, versioned CAD project migration, native alignment entity foundation, and command-history integration for existing COGO-producing commands.

- [x] Survey CAD trim preview batch (June 2026): while `TRIM` is active, dim the hovered removable span and overlay the surviving geometry so operators can preview exactly what will remain before they click.

- [x] Survey CAD trim command batch (June 2026): add a Civil3D-style `TRIM` flow that uses selected linework as cutting edges, lets operators click the side of a line/polyline/arc to remove, commits deterministic split/shorten edits through command history, and locks it with focused CAD geometry/workspace coverage.

- [x] Survey CAD move snap-priority batch (June 2026): keep `MOVE` / `COPY` target picks on exact object snaps instead of command-context construction snaps so clicking a real point like `109` does not resolve to parallel/extension compounds and overshoot transforms.

- [x] Survey CAD move-anchor + erase-hotkey batch (June 2026): keep mouse-picked MOVE base/target capture on the shared SVG frame so browser clicks land on the same snapped world point the operator sees, and let `Delete` / `Backspace` fire `ERASE` when entities are selected outside editable inputs.

- [x] Survey CAD grip-edit batch (June 2026): add selection grips for lines, polylines, polygons, parcels, and arcs so selected geometry exposes draggable vertex/radius handles and commits deterministic handle-move edits with focused workspace coverage.

- [x] Survey CAD global snap-cycle + exact-intersection + curve-perpendicular batch (June 2026): let any interactive point-pick command rotate nearby snaps with `Space`, make visible snap glyphs authoritative for left-click point capture, derive line-start perpendiculars from the tangent at captured curve starts, and strengthen exact intersection pickup in dense local views.

- [x] Survey CAD snap-cycle + click-latch batch (June 2026): let active line drafting rotate through nearby snap candidates with `Space`, and latch any visible snap glyph so a click commits that snap even if the cursor drifts before release.

- [x] Survey CAD zoom-depth + localized snap scope batch (June 2026): improve line/point/snap clickability, raise practical zoom depth far beyond the current cap, and restrict snap candidate reach as operators zoom into a local view so off-screen entities stop stealing snaps.

- [x] Survey CAD locked-direction intersection batch (June 2026): let locked `perpendicular` / `parallel` / `tangent` line drafting refine onto nearby line and arc bodies by snapping to the true intersection along that held direction, and lock it with focused workspace regression coverage.

- [x] Survey CAD tangent-line hold batch (June 2026): let line drafting keep a tangent guide when the start point comes from an arc, and lock a real workspace regression for tangent line attachment onto visible curves.

- [x] Survey CAD arc render/snap alignment batch (June 2026): keep rendered SVG arc sweep on the same side as the world-space snap geometry so hovering and snapping on visible created curves works again.

- [x] Survey CAD direct arc-body click batch (June 2026): make the visible line/arc/point/ellipse geometry itself act as a command click target alongside the widened invisible hit targets, and add a real same-session `ARC 3PT -> LINE from arc body -> LINE to arc body` regression so arc snapping survives normal operator clicks.

- [x] Survey CAD arc-snap audit batch (June 2026): lock command-created and constructor-built arc entities with shared on-body `arc-midpoint` / `nearest` regression coverage across the full native arc command family, so future arc-command changes cannot silently break snapping on committed curves.

- [x] Survey CAD arc-nearest priority batch (June 2026): let on-curve `nearest` override much farther exact snaps on small or dense arcs, so endpoint priority does not swallow the whole arc body during line drafting.

- [x] Survey CAD arc-body hit-target batch (June 2026): make arc body clicks in active commands resolve to the closest point on the curve, and harden arc hit targets so operators can start or finish linework directly on arcs instead of only at endpoints.

- [x] Survey CAD start-perpendicular + direction snap batch (June 2026): add a second `perpendicular` construction path from the captured start segment, restore practical local `parallel` pickup after the recent fail-closed scope change, and add low-priority 8-way direction snaps (`N/NE/E/SE/S/SW/W/NW`) as fallback drafting aids.

- [x] Survey CAD fail-closed parallel scope batch (June 2026): make `parallel` snap fail closed when the active draw start cannot resolve a valid local seed segment, so preview never falls back to whole-drawing parallel grabs during line drafting.

- [x] Survey CAD line-body seed batch (June 2026): treat direct first clicks on visible linework as segment-seeded starts, so `LINE` preview keeps local parallel scope even when no explicit snap kind won the first click.

- [x] Survey CAD command-start snap carryover batch (June 2026): keep the hovered snap alive when a draw command starts so an immediate first click still captures midpoint/nearest segment context instead of dropping back to raw world-point input and global parallel scope.

- [x] Survey CAD obstructed-guide snap batch (June 2026): reject `extension` and `apparent intersection` candidates whose derived guide path runs through other existing linework, and keep the remaining valid local construction cases locked with focused engine/workspace coverage.

- [x] Survey CAD snapped-start scope batch (June 2026): carry the captured snap segment through line-start/background-click input so construction snap scoping stays local even when the operator starts from midpoint/nearest-style line snaps.

- [x] Survey CAD endpoint-scoped construction batch (June 2026): scope `parallel` to segments within one hop of the captured endpoint, scope line-based `extension` / `apparent intersection` to two hops, and stop polyline-wide remote segments from leaking into those snaps.

- [x] Survey CAD parallel-filter + shift-lock batch (June 2026): add `Shift` construction-lock during drafting, highlight the source line for active parallel snaps, and limit dynamic parallel candidates to linework attached at the captured start point when available.

- [x] Survey CAD compound construction-snap batch (June 2026): let `perpendicular`, `extension`, and `parallel` stay locked while a second snap such as `intersection` or `apparent intersection` refines the final target on that same derived line.

- [x] Survey CAD snap-priority + zoom-tolerance batch (June 2026): make viewport snap range screen-scale-aware so it shrinks as operators zoom in, retune snap priority so endpoints then midpoints win cleanly, and stop `nearest`/arc-midpoint hover snaps from being swallowed by oversized world-space tolerance.

- [x] Survey CAD snap-guide batch (June 2026): add ghost guide lines for construction-style snaps so tangent, extension, perpendicular, parallel, and apparent-intersection targets show the derived geometry that produced the snap.

- [x] Survey CAD snap-badge batch (June 2026): add a live snap-kind badge with distinct kind wording/colors so tangent, apparent-intersection, perpendicular, center, and ordinary point snaps are visibly different while drafting.

- [x] Survey CAD apparent-arc + construction-hint batch (June 2026): extend command-context `apparent intersection` beyond line-arc into honest arc-arc circle-crossing cases, and add a small live base-point hint so operators can tell when construction/tangent snaps are armed from the captured point.

- [x] Survey CAD tangent-snap + apparent-curve batch (June 2026): add command-context tangent snaps from active base point onto visible arcs/circles, and extend `apparent intersection` beyond line-line into honest line-arc cases.

- [x] Survey CAD construction-snap batch (June 2026): add command-context construction snaps (`extension`, `perpendicular`, `parallel`, and line-like `apparent intersection`) so active draw/edit commands can lock onto derived geometry without turning those derived targets into always-on passive hover clutter.

- [x] Survey CAD curve-snap expansion batch (June 2026): expand native snapping beyond point/end/mid/intersection/nearest with honest curve-aware snaps (`center`, arc endpoints, arc midpoint, quadrants, line-arc intersections, arc-arc intersections, and nearest-on-curve) while leaving construction-only snaps for a later command-context batch.

- [x] Survey CAD arc-flip hint batch (June 2026): add a small live viewport hint for arc modes that support `Ctrl` flip/reverse so operators can see the modifier while drawing, and lock it with focused workspace coverage.
- [x] Survey CAD arc-direction audit batch (June 2026): audit all native arc constructors for start/end/center fidelity, fix clockwise or reverse-sweep cases that silently swap endpoints or break continuation tangency, and lock the edge cases with focused geometry/workspace coverage.
- [x] Survey CAD center-arc validation batch (June 2026): make center-driven arc commands reject off-radius end picks instead of silently drifting the committed curve away from the operator's supplied center/end geometry, and lock the behavior with focused CAD geometry/workspace coverage.

- [x] Survey CAD arc-label batch (May 2026): add geometry-derived arc/curve labels in the native renderer while keeping ordinary `LINE` geometry unlabeled so Phase 4 labeling closes without broadening annotations onto generic linework.
- [x] Survey CAD parcel-report batch (May 2026): add a geometry-derived parcel closure report overlay for selected parcel entities, including area, perimeter, closure values, and ordered course azimuth/distance rows without persisting extra report entities.
- [x] Survey CAD traverse-label readability batch (May 2026): keep traverse azimuth/distance labels upright instead of upside down and stop traverse/parcel point creation from adding anchored coordinate/station text labels, while preserving ordinary point-label behavior.
- [x] Survey CAD parcel-foundation + chrome-flatten batch (May 2026): remove the remaining framed brown-ish CAD shell bars so the dedicated workspace reads as one continuous blue viewport, then add first parcel creation/closure metrics on top of the native traverse/polyline seam.

- [x] Survey CAD interaction polish batch (May 2026): keep selection-only changes out of undo/redo history, fix viewport cursor/world-point drift away from screen center, and stop post-create auto-zoom resets after CAD entity edits.
- [x] Survey CAD arc-tool expansion batch (May 2026): replace blocky sampled arc rendering with true SVG arc paths, then add a dropdown-style multi-mode arc tool surface with broader surveying arc constructors and reverse-direction support before parcel/label work continues.

- [x] Survey CAD live-app undo persistence batch (May 2026): stop CAD history from resetting on parent rerenders with equivalent base-project inputs, and add a parent-backed regression that proves undo survives persisted-state updates in the real app wiring.

- [x] Survey CAD undo + clipboard insertion batch (May 2026): fix direct viewport-command keyboard undo when toolbar buttons still own focus, and replace fixed-offset CAD paste with a real insertion-point workflow plus live preview.

- [x] Survey CAD snap-menu + hit-target batch (May 2026): remove the redundant idle helper overlay, replace snap status text with a viewport snap-toggle menu, restore keyboard undo/redo reliability after direct CAD draws, and enlarge point/line selection hit targets so native picking feels less fragile.

- [x] Survey CAD overlay-input + keyboard batch (May 2026): move CAD help/snap text and command input into viewport overlays, remove the idle ready-status panel, allow active commands to capture typing without clicking the input first, and add keyboard copy/paste shortcuts for the native CAD workspace.

- [x] Survey CAD live-preview + traverse-start batch (May 2026): add in-viewport live preview overlays for line/polyline/arc/move/copy workflows, then start the first native `TRAVERSE` entry flow on the same direct-interaction command seam.

- [x] Survey CAD direct-interaction batch (May 2026): make the CAD page fully fit the browser window, remove the remaining visible helper buttons for select/pan/zoom and command submit/snap helpers, switch commands to click-driven point capture plus Enter/Escape keyboard flow, and make first drawing tools like `LINE` work directly from viewport clicks.

- [x] Survey CAD chrome-trim + tool-modes batch (May 2026): remove the duplicate results-area Survey CAD tab, collapse the dedicated CAD page down to toolbar plus model space, and start explicit CAD tool modes with top-toolbar select/pan/zoom-window controls.

- [x] Survey CAD dedicated-workspace batch (May 2026): promote Survey CAD from a result-tab panel into a dedicated full-page app workspace with its own top toolbars, hide the input editor while CAD is active, and add first CAD viewport interactions (wheel zoom, pan, zoom extents, and drag-box selection) before traverse/parcel work continues.

- [x] Survey CAD phase-4 arc-command batch (May 2026): add native `ARC 3PT` and tangent-curve creation commands on the command/history seam, then lock interactive and undo/redo behavior with focused CAD tests before moving on to traverse and parcel workflows.

- [x] Survey CAD phase-4 curve-helper batch (May 2026): add native simple-curve metric solvers plus three-point and tangent-curve arc constructors, then lock them with focused CAD geometry tests before wiring later command workflows.

- [x] Survey CAD phase-4 arc-geometry batch (May 2026): add native line-arc and arc-arc intersection helpers plus first offset/parallel/perpendicular COGO utilities, and lock the new geometry contracts with focused CAD tests before moving on to parcel/traverse workflows.

- [x] Survey CAD phase-1/2 completion batch (May 2026): expand native entity families, persist CAD project state inside current WebNet project storage and bundles, prove a simple DXF/runtime path through the published `mlightcad` data-model, and audit phases 0-3 acceptance criteria against focused tests/fixtures.

- [x] Survey CAD phase-4 cogo-core batch (May 2026): add native COGO inverse helpers, explicit bearing/azimuth-distance point creation, and first line-line intersection point workflow on top of the phase-3 command/history seams.

- [x] Survey CAD remaining phase-3 batch (May 2026): add `PLINE`, `MOVE`, and `COPY` on the native command/history seams, extend interactive command sessions to finish multi-step edits, and add full survey bearing-distance typed input alongside existing coordinate and azimuth-distance entry.

- [x] Survey CAD interactive-command batch (May 2026): add interactive command sessions with typed coordinate input, extend snapping through line intersections, and ship first create/query commands (`POINT`, `LINE`, `INVERSE`) on top of the native history/model seams.

- [x] Survey CAD snapping + phase-audit batch (May 2026): add layer/style + spatial-index + snap-manager seams with focused geometry tests, wire snap feedback into the Survey CAD spike workspace, and tighten Phase 1/2 docs so only real renderer-runtime blockers remain open.

- [x] Survey CAD command-foundation batch (May 2026): add native selection-set and transaction-history seams, wire them into the Survey CAD spike workspace, and ship first command-surface actions (`Select All`, `Clear Selection`, `ERASE`, `Undo`, `Redo`) without changing solver/parity behavior.

- [x] Survey CAD renderer/model spike batch (May 2026): add native CAD entity/adaptor seams under `src/engine/cad/`, prove them with an internal SVG Survey CAD preview built from current WebNet input/result state, and document why actual `mlightcad` runtime installation remains deferred behind the current LibreDWG/GPL package boundary.

- [x] Survey CAD planning groundwork batch (May 2026): add a branch-local Survey CAD workspace seam beside `Project Options`, preserve the attached high-level plan in `docs/`, and seed repo-fit architecture/TODO/licensing/evaluation/ADR docs for later CAD implementation batches.

- [x] Preanalysis one-click transform apply batch (May 2026): let bypass/decommission/move recommendations enter the shared apply+rereun workflow by normalizing preanalysis action state, transforming input for removal/relocation scenarios, and updating focused planner/report/run-session coverage.

- [x] Preanalysis advisory relocation batch (May 2026): make advisory move recommendations state-aware against active synthetic scenarios, carry corridor-pair metadata through synthetic candidates, and surface relocation advisories inside the bounded top recommendation slice.

- [x] Preanalysis advisory hardening batch (May 2026): tighten canonical path tie-breaks, suppress unsafe decommission recommendations on bridge-critical chains, improve advisory row/report coverage, and lock recommendation reranking after applied path changes.

- [x] Path-aware preanalysis recommendations batch (May 2026): remove artificial synthetic-family priority, rank scenarios by worst-station path-to-control bottlenecks, add advisory bypass/decommission/move recommendation families, and extend report/test coverage for path rationale and additive-vs-advisory actions.

- [x] Relative ellipse RLA batch (May 2026): add `RLA(2D)` and `PPM` columns to the industry-style `Relative Error Ellipses` error-propagation table using adjusted horizontal line length over the printed 95% semi-major axis, then lock the new output shape with focused listing coverage.
- [x] Test timeout stability batch (May 2026): relax the single heavy provisional data-check fixture test timeout in `adjust.spec.ts` if the failure is only the default Vitest 5 s cap, then rerun the full validation batch.
- [x] Map tool picker + highlight batch (May 2026): let inverse/angles popup text boxes stay editable, add on-screen point-pick buttons that sync with the text/select controls, and highlight the involved points/legs while the tools are active.
- [x] Map popup stack batch (May 2026): raise the right-click context menu and all map tool popups above render overlays so their actions stay clickable, and lock the stack with focused tests.

- [x] Report direction-face placement batch (May 2026): remove direction face treatment diagnostics from preanalysis reports and move that section to the very bottom of regular adjustment reports just above the processing log, with focused report coverage.

- [x] Map point-size trim batch (May 2026): reduce default 2D point symbol size by about half while preserving screen-consistent sizing across zoom levels, and lock the shared selector expectation.

- [x] Map label stack hardening batch (May 2026): make the SVG label/tool overlay use explicit top-layer stacking above basemap and geometry canvases so station labels stay visible with OSM enabled, and lock it with focused map tests.

- [x] Map selection layering + symbol contrast batch (May 2026): keep point labels/points above lines and polygons, move planning polygons below survey geometry, strengthen point/line selection contrast, and fix point/line click priority so polygon hits do not steal selection.

- [x] Map tile persistence + prefetch batch (May 2026): keep a wider idle-time OSM tile ring warm in memory, increase revisit tile retention, and reduce refetch churn when panning back across recently viewed areas.

- [x] Map smoothness perf batch (May 2026): freeze non-critical SVG overlay work during live interaction, gate OSM descriptor churn to tile-meaningful zoom bucket changes, cache resolved basemap surfaces, and extend browser perf assertions around pan/zoom regressions.

- [x] Map synthetic-label integration batch (May 2026): make synthetic brace/setup labels obey the shared `Show labels` toggle, keep all labels on the top draw layer, and rename synthetic point IDs to clean `B-#` / `P-#` forms.

- [x] Map geometry visibility batch (May 2026): strengthen default survey point/line colors against the basemap and stop 2D point/line symbols from shrinking to near-invisible sizes at higher zoom levels.

- [x] Map cover-fit + zoom tile-budget batch (May 2026): switch the 2D render slab from letterboxed contain-fit to uniform cover-fit so the map pane never shows empty side/top/bottom bars, then cap interaction-time OSM tile counts and mesh density so zoom-heavy WebGL redraws draw fewer tiles and triangles.

- [x] Map pane chrome + WebGL tile-buffer batch (May 2026): remove the side borders from the map pane, then cache OSM tile meshes in persistent GPU buffers so zoom-heavy WebGL redraws stop re-uploading the same tile vertices every frame.

- [x] Map aspect-ratio + interaction-basemap batch (May 2026): stop 2D map distortion when the workspace width changes, then keep the last settled OSM tile set active through live interaction so the browser harness can target the next real basemap hotspot without tile-churn noise.

- [x] OSM browser profiling + basemap optimization batch (May 2026): extend the Chromium Camp map harness to profile basemap-on pan/zoom/toggle actions against official OSM tile-use guidance, then trim the live OSM render/request path based on measured browser results.

- [x] Map browser pan-debug batch (May 2026): fix real-browser 2D pan delta/cancel issues and add a Camp startup browser harness for repeatable middle-mouse pan profiling outside jsdom.

- [x] Map compositor-pan preview batch (May 2026): stop recomputing live 2D pan frames during drag and instead translate the render layers together as one preview slab, then commit the pan once on drag end.

- [x] Map pan-overlay stabilization batch (May 2026): freeze the 2D derived SVG/culling basis during active middle-drag pan so the overlay moves as one stable slab instead of popping and jittering while the live transform updates.

- [x] Map snapshot-throttle batch (May 2026): stop pushing live pan/zoom view snapshots up to the app shell every interaction frame so map drags do not force avoidable parent rerenders.

- [x] Map drag-frame coalescing batch (May 2026): coalesce live 2D/3D drag-move handling to one frame at a time so pan/orbit updates stop processing every raw mousemove event.

- [x] Map SVG interaction render split batch (May 2026): split the 2D SVG overlay into memoized world-content sections behind cheap transform shells so live pan/zoom does not rebuild the full interaction subtree every frame.

- [x] Map derived-state stage split batch (May 2026): move selection/minor-geometry filtering ahead of the 2D view transform where possible, and break the remaining derived-state work into separately measured stages so the Camp harness can isolate the next real hotspot.

- [x] Map derived-state split batch (May 2026): cache base projected 2D geometry separately from view/selection filtering, then cull the current viewport in base projected space before applying the 2D view transform so pan/zoom avoids touching the full Camp network each frame.

- [x] Camp map performance profiling harness batch (May 2026): add internal map-layer/process capture plus a Camp Design preanalysis map benchmark matrix with feature toggles and scripted pan/zoom sweeps.

- [x] Map renderer stabilization follow-up batch (May 2026): restore non-selected obstacle rendering in WebGL mode and reduce 2D pan/zoom recompute churn without changing survey geometry correctness.

- [x] WebGL 2D map migration batch (May 2026): add a WebGL2-first 2D renderer for OSM + static survey geometry with automatic canvas fallback, while keeping SVG interaction semantics stable.

- [x] Map overlay performance follow-up batch (May 2026): add a visible renderer/status badge, instrument 2D layer performance, and move non-interactive planning polygon fills/strokes out of the SVG hot path while preserving selection/edit handles.

- [x] Map performance regression recovery batch (May 2026): fix layered-canvas basemap/geometry regressions, remove pan/zoom shake from the new 2D renderer, and only then reassess whether the basemap needs a WebGL path.

# TODO - WebNet

- [x] Map & Ellipses performance batch (May 2026): split 2D map rendering into separate basemap and geometry layers, move OSM tile lifecycle off React state into an imperative cache/controller with fallback coverage, and tighten interaction/hit-test performance without changing survey geometry correctness.

- [x] Map directional selection batch (May 2026): make left-to-right obstacle box selection require full enclosure, make right-to-left selection use crossing/touching semantics, and show the two box directions in different colors.

- [x] Map multi-select follow-up batch (May 2026): fix box-selection overlay alignment and preserve obstacle multi-selection on right-click context open.

- [x] Map basemap + selection UX batch (May 2026): add proper OpenStreetMap attribution, smooth tile loading/panning behavior, empty-click and Escape deselection, and box selection for obstacle cleanup.

- [x] Preanalysis synthetic-priority tuning batch (May 2026): make initial brace/promoted/synthetic setup recommendations outrank repeated-set redundancy when they offer the strongest expected geometry improvement.

- [x] Workspace panel visibility + preanalysis ranking batch (May 2026): add default-off toggles for Run Comparison and Review Queue, tighten workspace tab chrome spacing, and prioritize high-impact brace/synthetic scenarios earlier in the bounded preanalysis recommendation pool.

- [x] Canvas OSM basemap batch (May 2026): move OSM tile rendering off the SVG overlay and into the 2D canvas background, then upgrade it to per-tile mesh warping so Mercator tiles stitch cleanly while keeping the current SVG points/lines/polygons/tool overlays intact.

- [x] Planning-map pre-run access + obstacle editing batch (May 2026): make the planning map usable before the first run, tighten OSM fetch/render behavior, add obstacle edit/delete flows, and soften wooded-area blocking in synthetic recommendation search.
  - [x] Allow `Map & Ellipses` to open before a solve and load raw input points on demand from the current editor/project inputs
  - [x] Limit OSM obstacle fetch extents to measured-network bounds plus 100 m buffer while ignoring isolated non-observed coordinate outliers
  - [x] Improve OSM tile placement so seams and overlaps are hidden during pan/zoom
  - [x] Add obstacle selection plus edit/delete actions, with boundary editing on-map and context-menu delete
  - [x] Treat buildings/user blocks as hard no-shoot obstacles and wooded polygons as last-resort soft obstacles in planning

- [x] Preanalysis real-world feasibility planning batch (May 2026): add persistent planning-map overlays and obstacle-aware synthetic preanalysis scenarios so recommendation search can use real-world feasible setup/brace/cross-tie geometry.
  - [x] Phase 0 - planning-map contracts and persistence
    - [x] Record real-world feasibility scope in `TODO.md` before code changes
    - [x] Add shared planning-map/project-state contracts for basemap mode, input-point visibility, obstacle polygons, and scenario-family toggles
    - [x] Carry planning-map state through workspace recovery and portable/local project save-load
  - [x] Phase 1 - planner obstacle/search upgrade
    - [x] Extend preanalysis scenario families to include multi-setup brace, promoted setup, synthetic setup, and cross-tie candidates
    - [x] Reject synthetic candidates that fall inside or shoot through blocked/obstacle polygons
    - [x] Keep recommendation batches flowing by weak-seed-first then global feasible fallback with short-horizon threshold scoring
  - [x] Phase 2 - map planning workflow
    - [x] Add input-point planning layer and OSM basemap/obstacle toggles in 2D map mode
    - [x] Add draw/delete/clear blocked-polygon tools and persist them in planning-map state
    - [x] Render recommendation preview geometry and applied synthetic planning overlays on the map
  - [x] Phase 3 - coverage/docs/validation
    - [x] Add focused planner, map, workspace, and project-file regression coverage for obstacle-aware planning
    - [x] Run required validation matrix including parity gate and update docs if maintained behavior changes

- [ ] Planning-map ergonomics follow-up (May 2026): add tighter row-specific recommendation highlight behavior on the map.

- [x] Preanalysis synthetic-apply + brace-map batch (May 2026): make applied synthetic preanalysis scenarios actually enter the solve, surface brace preview points on the map, and verify tiny-impact display against real non-zero recommendation deltas.

- [x] Preanalysis recommendation refresh + brace naming batch (May 2026): drop the `WEB_` brace-point prefix and lock that applying one or many recommendations rebuilds the next recommendation set from the newly solved preanalysis state.

- [x] Preanalysis threshold-default + candidate-fallback batch (May 2026): default the preanalysis threshold to 1 mm and keep recommendation batches flowing by falling back from exhausted weak-seed templates to the remaining global addable templates.

- [x] Preanalysis brace-scenario + precision-display batch (May 2026): show tiny recommendation deltas in scientific notation when fixed display rounds to zero, and extend preanalysis recommendations/threshold planning with bounded synthetic brace-point scenarios near weak or worst geometry pairs.

- [x] Preanalysis recommendation UX clarification batch (May 2026): fix threshold decimal typing, tighten recommendation table readability, add apply-all action, and clarify the threshold-plan empty-template note when all usable setup sets are already active.

- [x] Preanalysis added-set recommendation batch (May 2026): replace single-observation preanalysis what-if exclusion with weak-geometry-driven added setup-set recommendations plus threshold planning controls.
  - [x] Phase 0 - backlog and contract update
    - [x] Record preanalysis added-set implementation scope in `TODO.md` before code changes
    - [x] Replace preanalysis obs-toggle diagnostics contract with scenario-based added-set recommendation diagnostics
    - [x] Add dedicated workflow state for applied synthetic preanalysis set additions separate from excluded observations
  - [x] Phase 1 - engine recommendation planner
    - [x] Extract repeatable planned setup-set templates deterministically from existing preanalysis input
    - [x] Limit candidate generation to existing setup patterns touching weakest stations/pairs
    - [x] Evaluate top added-set scenarios by rerunning preanalysis with synthetic appended set blocks
    - [x] Build greedy threshold plan using worst-station-major target and max-added-set cap
    - [x] Emit clear unmet diagnostic when no usable templates exist or threshold cannot be reached in-cap
  - [x] Phase 2 - workflow state and persistence
    - [x] Carry preanalysis accuracy threshold and max-added-set settings through app state, run request, saved runs, and project save/load
    - [x] Restore applied synthetic additions on saved-run reopen and clear them when input changes
    - [x] Keep compare/review state compatible with the new preanalysis planning workflow
  - [x] Phase 3 - report and project options UI
    - [x] Add preanalysis-only threshold and max-set controls in `Project Options -> Adjustment`
    - [x] Replace old `Planned Observation What-If Analysis` report table with added-set recommendation wording, metrics, and apply action
    - [x] Add threshold-plan summary card and hide obsolete remove/add-back preanalysis wording
  - [x] Phase 4 - focused coverage and validation
    - [x] Add focused engine/workflow/report/project round-trip tests for added-set recommendations and threshold planning
    - [x] Run required validation matrix including parity gate and update docs if maintained behavior changes

- [ ] UI speed first improvement backlog (May 2026): reduce editor/view repaint cost, tighten narrow-screen workflows, shrink hot-path serialization overhead, and continue splitting large UI/orchestration seams without solver or parity drift.
  - [x] Phase 0 - backlog codification and baseline capture
    - [x] Record phased UI speed first roadmap in `TODO.md` before code changes
    - [x] Keep current `useUiPerfMonitor` as perf timing source of truth for report/map/tab-ready baselines
    - [ ] Capture current build chunk-size baseline and track deltas as batches land
    - [ ] Add explicit typing-latency baseline coverage for large input editing
  - [ ] Phase 1 - UI responsiveness and mobile fit
    - [x] Current slice - reduce `InputPane` repaint cost by windowing syntax-highlight and line-number rendering to visible rows
    - [x] Current slice - tighten `InputPane` and `ImportReviewModal` narrow-screen sizing so popovers/forms stop forcing horizontal overflow
    - [x] Current slice - make `AppToolbar` wrap/stack cleanly on smaller screens without hiding run/export controls
    - [x] Current slice - stop eager inactive-tab JSX construction in `App.tsx`/`WorkspaceChrome` so hidden heavy tabs do not rebuild on unrelated app-shell churn
    - [x] Current slice - start `MapView` breakup by extracting typed 2D projection/filter helpers out of the component body
    - [x] Current slice - extract the `MapView` 2D SVG overlay into a focused presentational layer
    - [x] Current slice - extract the `MapView` 3D scene renderer into focused helper/presentational seams
    - [x] Current slice - extract the `MapView` 2D canvas render layer into a focused helper seam
    - [x] Current slice - extract the `MapView` context menu and tool-panel surface into focused presentational seams
    - [x] Current slice - collapse the `MapView` 2D derived visibility/filter pipeline behind focused selector helpers so the component coordinates fewer broad memo chains
    - [x] Current slice - collapse the `ReportView` preanalysis precision/filter selector cluster behind a focused helper so repeated covariance and query scans stay out of the component body
    - [x] Current slice - collapse the `ReportView` observation-filter and suspect-diagnostic selector cluster behind a focused helper so filtering and ranking logic stop spanning the component body
    - [x] Current slice - collapse the `ReportView` traceability, cluster-review, and sideshot-derived selector cluster behind a focused helper so the component coordinates fewer result-shaping memos
    - [x] Current slice - move the preanalysis-heavy `ReportView` tables behind default-collapsed detail sections so covariance, what-if, and weak-geometry rows do not render until opened
    - [x] Current slice - move the heavy `ReportView` traceability and cluster-review tables behind default-collapsed detail sections so summary cards stay visible while row-heavy review tables wait until opened
    - [x] Current slice - keep `ReportView` auto-adjust and auto-sideshot summary cards visible while their row-heavy detail tables stay deferred behind collapsed sections
    - [x] Current slice - keep `ReportView` suspect-impact and setup-diagnostics summary cards visible while their row-heavy tables stay deferred behind collapsed sections
    - [x] Current slice - keep residual/robust/correlation/traverse/GPS/leveling diagnostic summary cards visible while their row-heavy tables stay deferred behind collapsed sections
    - [x] Current slice - keep ranked loop-suspect summary cards visible while traverse/GPS/leveling suspect tables stay deferred behind collapsed sections
    - [x] Current slice - keep direction-diagnostic summary cards visible while set/repeatability/reject/detail tables stay deferred behind collapsed sections
    - [x] Current slice - keep robust-comparison and observation-summary cards visible while suspect/type/relative-precision tables stay deferred behind collapsed sections
    - [x] Current slice - keep GPS rover-offset summary cards visible while the rover-offset detail table stays deferred behind the collapsed section
    - [x] Current slice - keep preanalysis covariance summary cards visible while station and relative covariance detail tables stay deferred behind collapsed sections
    - [x] Current slice - collapse the remaining `ReportView` inline summary reducers and repeated row-window shaping behind focused selector helpers so the component stops owning the last broad report row-shaping block
    - [x] Current slice - finish the remaining `MapView` selector/memo extraction by moving bounds, overlay, 3D projection, and tool-derived calculations behind focused helper seams so `MapView.tsx` stops coordinating the last broad result-shaped memo cluster
    - [x] Split `MapView.tsx` into selector/math helpers, canvas layer, SVG overlay layer, and tools/context surfaces
    - [x] Narrow broad `useMemo` blocks in `MapView.tsx` to stable result identity plus minimal filter keys
    - [x] Add tab-level render guards so hidden heavy tabs do not recompute derived models from unrelated app-state churn
    - [x] Move more expensive report row shaping behind expand/open actions while preserving current progressive disclosure wording/ordering
  - [ ] Phase 2 - bundle and hot-path performance
    - [x] Current slice - replace repeated JSON clone/stringify hot paths in recovery/run-review/project-session syncing with shared plain-data helpers where safe
    - [x] Current slice - add browser draft size guardrails with explicit failure copy before quota churn writes
    - [x] Current slice - split Vite chunk routing so CRS catalog stops riding the broad `engine-core` bucket and the routing stays covered by a focused config test
    - [ ] Split Vite manual chunks further so CRS catalog, listing/report text builders, and export serializers stay off lighter startup paths
    - [ ] Move more non-urgent project/import utilities out of `App.tsx` so base `index` chunk shrinks further
    - [ ] Review `scenarioParsedModelCache.ts` request-key serialization and trim large cache key material without changing cache semantics
  - [ ] Phase 3 - maintainability follow-through
    - [x] Finish `App.tsx` composition-shell extraction
    - [x] Split `useProjectFileWorkflow.ts` into storage, import/export, source-file tabs, and associated-settings seams
    - [x] Split `InputPane.tsx` into editor, project-files popover, context menu, and tab-strip components
    - [ ] Introduce shared typed browser file-IO/download/storage helpers across project/import/export paths
  - [ ] Phase 4 - security and hardening
    - [x] Current slice - add explicit size-limit handling for browser-local draft persistence
    - [ ] Add explicit size limits and validation failures for imported project/settings payloads before full parse/use
    - [ ] Guard local recovery storage toward minimal recoverable state where feasible, not full expanded snapshots by default
    - [ ] Document import trust boundaries and accepted offline file assumptions in `docs/IMPORT_WORKFLOW.md`

- [x] Big-file refactor batch (May 2026): split code-heavy monoliths into smaller seams with no behavior drift.
  - [x] Batch 0 - baseline lock + extraction guards
    - [x] Record current big-file scope and target caps in planning
    - [x] Add focused characterization coverage for extracted App helpers before broader refactors
    - [x] Freeze App/project-options helper contracts while moving pure helpers and config out of `App.tsx`
  - [x] Batch 1 - app shell shrink
    - [x] Move pure App helper logic into `src/app/appHelpers.ts`
    - [x] Move project-options/static app config into `src/app/appConfig.ts`
    - [x] Move shared project-options shell controls into `src/components/projectOptions/SettingsControls.tsx`
    - [x] Current slice - extract App industry-output, review-queue, and project-options modal orchestration hooks
    - [x] Current slice - extract App workspace draft/recovery orchestration hook
    - [x] Current slice - extract App run-comparison and workspace-reset orchestration hook
    - [x] Current slice - extract App project/import/workspace orchestration hook around project-file and import-review workflows
    - [x] Extract App orchestration hook and keep `App.tsx` as composition shell
    - [x] Current slice - extract remaining App run/workspace orchestration hook
  - [x] Batch 2 - heavy UI surfaces
    - [x] Split `ReportView.tsx` config/sections into focused modules
      - [x] Extract report formatters, sideshot section, header-tooltip resolver, residual/robust/TS-correlation diagnostics, traverse/GPS/leveling loop diagnostics, and direction diagnostics into `src/components/report/*`
    - [x] Split `ProjectOptionsModal.tsx` tabs and repeated controls into focused modules
      - [x] Extract `adjustment`, `general`, `instrument`, `listing-file`, `project-files`, `special`, `gps`, and `other-files` tab panels into dedicated components
  - [x] Batch 3 - listing/importer monoliths
    - [x] Current slice - extract `industryListing.ts` shared formatters/sort helpers
    - [x] Current slice - extract `industryListing.ts` leveling-only section builder
    - [x] Current slice - extract `industryListing.ts` top/settings/project-metadata, adjusted-observation, and error-propagation section builders
    - [x] Current slice - move shared importer runtime helpers into `src/engine/importers/shared.ts` so importer-family modules no longer pin the importer shell into the lazy import chunk
    - [x] Split `industryListing.ts` into formatting and section-builder seams
    - [x] Split `importers.ts` into importer-family modules plus shared helpers
    - [x] Current slice - extract importer families and shared importer helpers
  - [x] Batch 4 - solver-core decomposition
    - [x] Extract private `adjust.ts` helpers into focused engine modules without changing solve flow
    - [x] Current slice - extract adjust math, GPS, covariance, and diagnostics helpers

- [x] Worker lazy-load split batch (May 2026): move heavy solve/export modules behind worker-side dynamic imports so worker entry bundles shrink further without changing solve/export behavior.
- [x] Dependency hardening + worker bundle reduction batch (May 2026): clear `npm install`/audit dependency warnings with safe direct upgrades and transitive overrides, split artifact export work out of the adjustment worker to reduce the solve-worker production bundle, and keep worker/export behavior covered with focused regression checks.
- [x] Security, robustness, and optimization batch (May 2026): remove dynamic geoid runtime loading, harden worker/storage/persistence surfaces, replace high-value weak typing and ad hoc IDs with shared typed helpers, optimize IndexedDB single-project file access, and trim redundant CLI process-spawn coverage while preserving parity and user-visible behavior.
- [x] Build warning cleanup batch (May 2026): eliminate remaining production build warnings by keeping worker/app chunking warning-free and refreshing stale Browserslist data without changing runtime behavior.

- [x] Project coordinate-system settings save/open batch (May 2026): audit project-file and local-project save/open persistence for CRS/grid/geoid settings, restore browser-loaded geoid/grid source state through save/load, stop stale CRS group filters from overwriting reopened provincial/grid CRS selections, and add focused round-trip regression coverage for coordinate-system parse settings.

- [x] Special-mode report + data-check direction-set batch (May 2026): move mode-relevant report sections to the top for `Data Check` / `Blunder Detect`, hide adjustment-only report sections that do not apply in those modes, and make `Data Check` compare reduced direction sets against approximate set orientation instead of raw azimuth so mixed-face direction data does not show false giant residuals.

- [x] Adjustment report workflow-priority batch (May 2026): when cluster detection or auto-adjust diagnostics are active, move those workflow-action sections to the top of normal adjustment reports so operators see the active review/removal workflow before lower-priority summary tables.

- [x] Data Check provisional-approximation batch (May 2026): add a bounded provisional coordinate-fit stage ahead of Data Check inverse comparisons so weak bootstrap geometry can be refined before observation-difference reporting, while retaining the reduced-direction internal-consistency fallback only when the provisional fit still cannot supply trustworthy geometry.

- [x] Run-mode audit/correction batch (May 2026): audit `Data Check`, `Blunder Detect`, and `Auto-Adjust`; fix any mismatched behavior with minimal solver/session changes; add focused regression coverage proving data-check skips full adjustment, blunder-detect deweights iteratively without removing rows, and auto-adjust iteratively excludes poor-fitting observations across direct-engine and shared run-session paths.

- [x] Camp Design preanalysis project-metadata/page-break parity batch (May 2026): carry the Camp startup case as a named virtual run file/project so the classic listing can emit the expected `Project Folder` / `Data File List` metadata, and restore the Camp-style page breaks in preanalysis output.

- [x] Camp Design preanalysis weighting/header parity batch (May 2026): align the Camp startup instrument defaults with `camp_design.snproj`, stop listing unused project-library instruments in the classic preanalysis header, and re-baseline the remaining Camp raw-observation/error-propagation parity gaps.

- [x] Camp Design preanalysis listing-parity batch (May 2026): diff the current industry-style preanalysis listing against `Camp_Design.lst`, remove adjustment-only sections that should not appear in preanalysis, align the included section set/order/headers with the Camp reference, and lock the structure with focused regression coverage.

- [x] Camp Design preanalysis control-covariance parity batch (May 2026): preserve component slot alignment in control-record sigma/fixity parsing so mixed weighted/fixed control like `GPS4 0.005 0.005 !` stays weighted in horizontal and fixed in height, then recheck the preanalysis error-propagation parity baseline.
- [x] Camp Design preanalysis model-alignment batch (May 2026): switch the startup default input to `Traverse_Only.dat`, audit how planned pre-analysis observations are parsed/synthesized/weighted/reported, and align the solve plus industry-style output with the actual Camp Design pre-analysis contract.
- [x] Camp Design exact preanalysis parity tightening batch (April 2026): switch the pre-analysis parity fixture to the real `Traverse_Only.dat` reference input, measure the remaining listing diffs against `Camp_Design.lst`, and lock exact parity wherever the current pre-analysis output contract already matches.
- [x] Camp Design preanalysis startup/parity batch (April 2026): bundle the current combined startup project as a portable example, switch the app startup defaults to the Camp Design pre-analysis case, import `.snproj` pre-analysis mode correctly, and add focused pre-analysis parity coverage against the provided Camp Design reference listing.

- [x] Beginner documentation overhaul batch (April 2026): refresh onboarding docs for survey users, add a reproducible Canada-first mixed TS/GNSS/leveling tutorial example, rewrite the main user guide into an install-to-adjustment workflow, and realign README/example references with the current app UI.

- [x] Industry output pane navigation/sort batch (April 2026): add right-click `Go to section` and `Sort by` submenus in Industry Standard Output, support auto-detected section jump targets, add new observation sort modes (`Residual`, `Std Error`, `Std Residual`) with legacy `residual` migration mapping, and lock UI/listing-sort coverage.

- [x] UI navigation smoothness batch (April 2026): gate/cached industry listing generation to industry tab, transition tab switches, one-time lazy-tab prewarm after first solve, persist map navigation state across tab remounts, and add focused UI perf/regression coverage.

- [x] Report progressive-disclosure batch (May 2026): keep heavyweight late-report sections collapsed by default, reduce initial row windows, and add explicit `Show all` controls so solved reports feel lighter on first open.
  - [x] Collapse `Processing Log` by default and avoid rendering its rows until opened
  - [x] Default-collapse late detail sections from residual diagnostics downward, including observation-family tables and lower-priority diagnostics
  - [x] Reduce the default report row window from 100 to 25 and add `Show all` beside `Show more`
  - [x] Update focused report state/windowing/operator tests for the new progressive-disclosure contract

- [x] Post-adjustment UI responsiveness batch (May 2026): reduce main-thread blocking after worker solve completion so toolbar, tabs, and review controls stay responsive immediately after a run.
  - [x] Add app-side post-solve timing instrumentation for worker-success handoff, result commit, deferred QA-model build, tab-ready milestones, and first tab-click latency
  - [x] Defer non-critical post-solve state work out of the urgent result handoff path and keep QA-derived review models lazy/progressive
  - [x] Replace eager multi-tab prewarm with staged/cancellable lazy-tab prewarm and progressive heavy-tab hydration
  - [x] Trim expensive report/map/processing-summary recomputation on first post-run interaction without changing solve/report behavior
  - [x] Extend UI responsiveness tests and browser benchmark guardrails for post-solve clickability

- [x] Level-weight precedence batch (May 2026): make leveling sigma selection use fallback priority instead of additive stacking.
  - [x] Prove inline `.LWEIGHT` overrides project `General -> .LWEIGHT` and instrument differential-level defaults
  - [x] Make project `General -> .LWEIGHT` override instrument differential-level defaults for both `L` and non-`L` leveling-producing records
  - [x] Keep instrument differential-level setting as last-resort fallback only when no project or inline `.LWEIGHT` is active
  - [x] Verify the `public/examples/Complex Combined Adjustment` project keeps `levelWeight: 1.5` in the project settings payload

- [x] Project Options UI refresh (April 2026): make `Project Files` the first tab, keep `Other Files` export-focused, move Coordinate System controls into `Adjustment`, move Leveling/Weighting into `General`, merge stochastic modeling controls into `Special`, remove legacy-removal copy, and tighten modal density/layout spacing.

- [ ] add planned improvements to todo list in phased implementations before starting on imlementation
  - [x] Phase 0 - backlog codification
    - [x] Add the full phased implementation plan to TODO before code changes
    - [x] Break each roadmap phase into explicit checklist items instead of summary-only bullets
    - [x] Keep README.md and agents.md updates as a required closeout step after each completed batch
  - [x] Phase 1 - import and reconciliation workflow
    - [x] Batch 1 - staged conflict scanning and crash-safe review recovery
      - [x] Detect and surface exact station-ID collisions between the current editor content and the incoming import
      - [x] Detect and surface coordinate conflicts for the same station ID in the staged import workspace
      - [x] Detect and surface description conflicts for the same station ID in the staged import workspace
      - [x] Detect and surface control-state conflicts for the same station ID in the staged import workspace
      - [x] Detect and surface duplicate observation-family buckets by canonical endpoints against the current editor content
      - [x] Persist staged import review/conflict state through browser-local draft recovery while the import modal is open
      - [x] Add focused regression coverage for conflict detection, conflict rendering, and import-review snapshot restore
    - [x] Batch 2 - deterministic conflict resolution controls
      - [x] Support per-conflict `keep existing`
      - [x] Support per-conflict `replace with incoming`
      - [x] Support per-station `rename incoming station`
      - [x] Support per-conflict `keep both` with an explicit emitted comment marker in final text
      - [x] Apply conflict resolutions when writing reconciled text back into the editor instead of replacing the workspace blindly
    - [x] Batch 3 - multi-source reconciliation workspace
      - [x] Expand the current compare-file flow into a true multi-source reconciliation workspace instead of count-only comparison
      - [x] Keep reconciliation state browser-local and recoverable until the import session is applied or cancelled
      - [x] Add focused regression coverage for multi-source reconciliation, final emitted text shape, and crash recovery mid-review
      - [x] Track multiple imported reconcile sources inside a single staged import session with source-aware row/group metadata
      - [x] Replace the two-file compare block with a source matrix that highlights mismatched setup-target-family buckets across all loaded sources
      - [x] Emit deterministic source-separated imported text when multiple reconcile sources are committed into the editor
    - [x] Batch 4 - JobXML industry-style import fidelity
      - [x] Add committed trimmed JobXML and reference direction-set fixtures for the `260215 TRAVERSE` comparison path
      - [x] Preserve JobXML raw fieldbook metadata on imported observations without changing the generic serializer contract
      - [x] Parse JobXML atmosphere, prism, round, and ordered direct-reading context needed for fidelity export
      - [x] Add an opt-in JXL `Industry Style` import mode in the prompt and review workflow
      - [x] Emit round-grouped `DB/DM/DE` import-review output using raw HZ, corrected slope distance, raw vertical, and HI/HT while leaving generic presets unchanged
      - [x] Add focused importer and import-review regression coverage for the new opt-in path and existing generic fallback behavior
    - [ ] Batch 5 - JobXML industry-style workflow closeout
      - [x] Lock the JXL `Industry Style` prompt as a fixed raw-fieldbook mode and default review exclusion of MTA rows
      - [x] Preserve exact JobXML code/description plus HI/HT provenance so industry-style DM comments and height tokens use source fidelity values
      - [x] Tighten industry-style direction-set grouping to suppress standalone backsight-only blocks and emit `DN` orientation rows where needed
      - [x] Add staged-review actions for `Import as New File` and associated project-settings import with project-workspace integration
      - [x] Add focused regression coverage for the locked prompt, DM fidelity, grouping parity, project-file import destination, and associated settings import
      - [x] Stage associated `.wnproj*` / `.snproj` settings inside import review until the operator commits the reviewed import
      - [x] Persist staged associated-settings payloads through import-review draft restore and show pending-settings status in the modal
      - [x] Apply staged associated settings only after reviewed text import succeeds for both replace-editor and new-project-file actions
  - [x] Phase 2 - persistent run artifacts and deliverables
    - [x] Batch 1 - saved run snapshot model and project/local persistence
      - [x] Add named saved run snapshots beyond the current in-memory compare history
      - [x] Store input/settings fingerprint, run summary, reopenable result payload, and lightweight label/notes metadata
      - [x] Limit saved snapshots to a bounded, user-managed list per workspace/project
      - [x] Add a minimal manual `Save current run` path so saved snapshots can be created without waiting for the full saved-runs workspace UI
      - [x] Persist saved snapshots through browser-local recovery state without restoring them into the active report/map result automatically
      - [x] Persist saved snapshots through project save/load paths without breaking existing v1-v3 project-file compatibility
      - [x] Add focused regression coverage for saved-run creation, persistence, and bounded-cap behavior
    - [x] Batch 2 - saved runs UI and recovery wiring
      - [x] Add a small `Saved Runs` surface for restore, compare, rename, note editing, and delete actions
      - [x] Restore compare/report reopen state from a saved snapshot without rebuilding ad hoc view models
      - [x] Persist saved snapshot metadata through browser recovery and project load/save paths
      - [x] Extend saved snapshots with persisted workspace reopen metadata for report/map review state restoration
      - [x] Add hook-level saved-run actions for rename, notes updates, delete, and restore target selection without breaking transient compare history
      - [x] Rehydrate active result diagnostics, review state, and compare baseline selection directly from a saved snapshot reopen action
      - [x] Add focused regression coverage for saved-run management UI, restore flows, and persisted metadata round-trips
    - [x] Batch 3 - browser-first export deliverables
      - [x] Add a first-class adjusted-points CSV export target in the main export selector without removing the existing configurable adjusted-points export path
      - [x] Add an observations-and-residuals CSV export target with deterministic per-row observation metadata and QC fields
      - [x] Add a GeoJSON export target for adjusted stations and connection geometry with stable feature metadata
      - [x] Extend toolbar/project-options export selectors, labels, extensions, and descriptions for the new browser-first deliverables
      - [x] Keep project save/load and browser recovery export-format persistence compatible when new selector values are introduced
      - [x] Add focused regression coverage for export workflow routing, export selector persistence, and project-file export-format round-trips
  - [ ] Phase 3 - heavy-job performance and background artifact work
    - [x] Batch 1 - unchanged-topology rerun planning cache
      - [x] Cache equation-planning metadata when topology is unchanged, not only parsed-model state
      - [x] Reuse that planning cache for exclusion, override, cluster-review, and saved-run compare reruns
      - [x] Expose planning-cache hit/miss coverage through the shared scenario-run stats and regression suites
    - [x] Batch 2 - worker-backed artifact generation
      - [x] Move QA bundle generation off the main thread through the existing worker path
      - [x] Move CSV, GeoJSON, and report-package generation off the main thread through the existing worker path
      - [x] Keep solve request shapes backward-compatible while extending worker artifact messages
      - [x] Add focused regression coverage for worker-backed artifact routing plus direct-fallback parity
    - [x] Batch 3 - performance budgets and imported-job benchmarks
      - [x] Extend the benchmark suite with imported dense TS/GNSS/leveling fixtures
      - [x] Add explicit budgets for initial solve, cached rerun, first render, and artifact-build latency
      - [x] Add focused regression coverage for worker artifact responsiveness and cache-hit paths
      - [x] Add imported benchmark fixture profiles for a JobXML-style TS job, a GNSS loop-heavy job, and a differential-leveling job
      - [x] Benchmark a heavy artifact export path alongside solve and render timing so worker-offloaded deliverables keep a bounded latency budget
      - [x] Prove the worker artifact hook stays asynchronous through progress updates and that cached reruns still hit parse/planning caches on imported-style jobs
    - [x] Batch 4 - sparse design-matrix assembly on solve path
      - [x] Emit sparse design rows directly from shared equation assembly
      - [x] Skip dense design-matrix allocation in iterative solve and covariance recovery when those paths only need sparse rows
      - [x] Keep only changes that hold parity and at least match the current benchmark timing
    - [x] Batch 5 - standardized-residual diagnostics assembly reuse
      - [x] Replace the duplicated diagnostics-only design-matrix assembly with the shared equation assembler
      - [x] Reuse sparse design rows directly in standardized-residual covariance math
      - [x] Keep only changes that preserve diagnostics output and benchmark timing
    - [x] Batch 6 - matrix-helper allocation trims
      - [x] Reduce hot-path zero-matrix allocation overhead in shared matrix helpers
      - [x] Keep only changes that preserve results and at least match the current large-project benchmark timing
    - [x] Batch 7 - production bundle chunking
      - [x] Split the main Vite entry bundle into stable vendor and WebNet engine/workflow chunks so the production build clears the large-chunk warning without changing runtime behavior
      - [x] Keep lazy-loaded report/map/project/import modal chunks aligned with the new shared chunk boundaries instead of forcing a monolithic shared app bundle
      - [x] Validate the production build output after chunking and keep the chunking rules maintainable in `vite.config.js`
  - [ ] Phase 4 - industry-standard result parity
    - [x] Batch 1 - reference fixture, diff harness, and rollback gates
      - [x] Add a repo fixture set for the attached industry-standard reference case using generic fixture names
      - [x] Add a parity harness inside the computational-parity test family for summary metrics, selected coordinates, selected observation StdErr rows, station sigmas, station/relative ellipses, and relative-confidence rows
      - [x] Add a machine-readable deviation baseline so later parity batches can prove they improved or held the reference-case diff
      - [x] Add a dedicated parity gate command in `package.json` so every later batch can rerun the reference diff directly
      - [x] Keep the batch rule explicit in repo docs: update TODO/README/agents, run the full npm suite plus the parity gate, and revert parity changes that worsen the reference diff
    - [x] Batch 2 - error propagation, ellipse, and relative-covariance parity
      - [x] Change reported station sigmas and station/relative ellipse output to use unscaled propagated covariance by default
      - [x] Add a persisted precision-reporting selector with `industry-standard` default and `posterior-scaled` override
      - [x] Store dual precision models in the solve result payload so report/listing/export toggles do not require rerun
      - [x] Convert station and relative ellipse azimuth reporting to the survey azimuth convention (north-based clockwise, modulo 180)
      - [x] Apply the industry-standard 95% confidence scale consistently to relative azimuth, distance, ppm, and ellipse outputs
      - [x] Keep project save/load, saved-run snapshots, and browser recovery compatible with the new precision-reporting state
    - [ ] Batch 3 - observation sigma and stochastic-display parity
      - [x] Add a per-observation weighting/effective sigma field to solved observation payloads
      - [x] Use the weighting sigma in industry-style Angle/Distance StdErr output while keeping parsed/base sigma for traceability
      - [x] Keep explicit sigma overrides unchanged in weighting/display unless a later reference diff proves a better rule
      - [x] Make industry-style distance StdErr display follow the same weighting sigma path used by the solver
      - [x] Audit alternative geometry stages for displayed/weighted angle sigmas and keep only changes that improve the reference diff
    - [ ] Batch 4 - solver-loop, residual, ordering, and formatting closeout
      - [x] Batch 4A - Coldstream float-zenith leaf parity cleanup
        - [x] Investigate remaining Coldstream adjusted-coordinate outliers at `108` and `109` after mixed-face/raw-direction fixes
        - [x] Fix imported parity output so weak float-zenith leaf stations no longer drift in adjusted coordinate/bearing display
        - [x] Add focused shared-run-session regression coverage for the remaining Coldstream outlier stations
      - [x] Batch 4B - Coldstream weak float-zenith precision parity
        - [x] Verify `108` / `109` remain intentionally weak in the industry reference while `2002` / `2014` stay normal
        - [x] Trace why WebNet overstates `108` / `109` covariance relative to the reference after coordinate parity is fixed
        - [x] Restore float-zenith weak-point precision behavior only where it improves the Coldstream parity case
        - [x] Add focused regression coverage for `108` / `109` station and connected-pair precision against the Coldstream reference
      - [x] Set the industry-parity default convergence limit to `0.001` while keeping the WebNet-default profile threshold unchanged
      - [x] Flip printed residual signs in industry-style observation tables without changing the solver math
      - [x] Match the industry-style reported iteration count in listing output without changing the underlying solver loop
      - [x] Make `|StdRes|` listing/report ordering deterministic by adding stable station/input tie-breaks
      - [x] Print adjusted/computed values plus sigma-normalized displayed `StdRes` values in industry-style observation tables
      - [x] Replace the truncated 95% confidence constant with the exact 2D industry-style scale and compute PPM from full adjusted distance instead of rounded display text
      - [x] Prefer connected-pair direction in industry-style azimuth and relative-ellipse sections so reference-oriented rows like `10-9`, `200-3`, and `2000-3` stay aligned with connected covariance output
      - [x] Use the exact `1 / sqrt(2)` face-2 weighting factor in angular record parsing instead of the rounded `0.707` constant
      - [x] Apply a tiny parity-only non-explicit angular sigma calibration to close the remaining covariance drift without touching explicit overrides
      - [ ] Audit geometry-dependent weight recomputation order across iterations and keep only improvements proven by the reference diff
      - [ ] Lock standardized-residual covariance behavior with focused parity coverage if the current implementation already matches
      - [ ] Tighten the remaining row inclusion and final rounding policies for closer listing parity
      - [x] Tighten GNSS/listing parity by matching the industry blank-description station/error-propagation layout for the GNSS-only case, hiding the extra control-component block unless mixed control states require it, and adding the missing GNSS wrapper/empty sections (`Inline Option Usage Notes`, `Summary of Inconsistent Descriptions`, `Network Stations`, `Sideshots`) from the stored reference listing
      - [x] Tighten the remaining GNSS top-block/listing parity by adding the industry-style file-summary and compact option rows, switching GNSS-only unadjusted summaries back to the reference-style fixed/free station + vector-table layout, and using the compact adjusted bearing-distance heading/spacing contract without reopening solver behavior
      - [x] Trace the remaining GNSS adjusted bearing-distance confidence drift by comparing the relationship-row precision path against the stored reference and keep only the GNSS-only connected-covariance display blends that improve the weak `GPS2-*` relationship and relative-ellipse rows without regressing traverse or underground listings
      - [x] Split the GNSS-only compact relationship-row and relative-ellipse display blends so one-fixed-linked single-vector pairs keep the improved row-confidence values while the ellipse rows preserve propagated `cEN` orientation and blend only endpoint variances where that moves the remaining `GPS2-*` precision rows closer to the stored reference
      - [x] Apply the legacy convergence-angle display correction to GNSS-only fixed-linked station and relative ellipse azimuth rows so the compact precision sections better match the stored industry `FRDN-*` and direct-fixed-link reference rows without changing propagated covariance magnitudes
      - [x] Keep the compact GNSS industry listing's unadjusted GPS input block and adjusted GPS vector block visible even when the generic adjusted-observations toggle is off, so those GNSS vector sections still match the stored industry output contract
      - [x] Narrow the classic-parity listing gate so live `industry-parity-current` GNSS-only runs in the app still use the compact GNSS vector listing path even when a default instrument library is loaded
      - [x] Make the listing header prefer the solved parse-state vertical deflection over stale external run diagnostics so the industry output pane reports the actual GNSS run deflection values
      - [x] Add the committed Coldstream 3rd Dimension traverse fixtures and shared run-session regression for the two-file + `.snproj` imported parity case
      - [x] Support compact STAR*NET control/sigma shorthand tokens such as `!!*`and`&&\*` without changing existing spaced-token behavior
      - [x] Keep strict unresolved mixed-face rejection for reduced direction-set handling, but allow raw-mode fallback split emission for the imported parity path
      - [x] Normalize weighted raw `DM` face-2 zenith readings, skip float zenith equations, and preserve fixed-height-only control during bootstrap seeding so the Coldstream parity case converges with the imported settings
      - [x] Map imported `.snproj` refraction, fixed-control sigma, and leveling-default settings needed by the Coldstream parity fixture
      - [ ] Trace the remaining GNSS relative-ellipse drift outside the `GPS2-*` seam, with the current focus narrowed to the remaining covariance-magnitude gap in `GPS2`, `GPS3`, `GPS1-GPS6`, and `GPS3-GPS5` after the fixed-linked azimuth display correction
  - [x] Phase 5 - browser-native project storage and multi-file workspace
    - [x] Batch 1 - OPFS/IndexedDB storage foundation
      - [x] Add a `ProjectStorage` abstraction with OPFS-primary and IndexedDB fallback backends plus a small IndexedDB recent-project catalog
      - [x] Add storage-availability and persistence-status detection (`navigator.storage.getDirectory` / `persist`) and surface it in the project workflow
      - [x] Add focused adapter coverage for OPFS happy path, IndexedDB fallback, and storage-status detection
    - [x] Batch 2 - v4 manifest and legacy migration
      - [x] Introduce manifest-first `webnet-project` schema v4 with stable source-file IDs, `mainFileId`, project metadata, and lightweight file-workspace UI state
      - [x] Preserve v1-v3 `.wnproj` loading by migrating legacy payloads into the v4 in-memory shape before use
      - [x] Keep phase-1 v4 manifest scope to sources/settings/UI state only; do not move saved-run payloads, reports, or caches into named-project autosave storage
      - [x] Add focused v1-v4 project-file migration and flattened portable snapshot coverage
    - [x] Batch 3 - named-project session and autosave wiring
      - [x] Add a project-session model that tracks the active named project, active source file, source-text cache, and dirty state while preserving the existing single editor
      - [x] Materialize solve/export input from the named-project main file plus enabled non-main source members so the engine still receives `mainInput + includeFiles`
      - [x] Add debounced autosave for named projects with separate manifest/file dirty tracking while leaving untitled browser draft recovery as a separate safety net
      - [x] Add focused workflow coverage for create/open/autosave/add blank file/add imported file/rename/reorder/enable-disable/remove/switch active file
    - [x] Batch 4 - project workflow UI and portable import/export
      - [x] Add a project-files surface for create/open recent/delete plus active-project/source-file management inside the existing project workflow
      - [x] Keep `.wnproj` compatibility export/import as a flattened portable snapshot on top of the manifest model
      - [x] Add zipped manifest-plus-sources export/import for cross-browser backup/share workflows
      - [x] Document the zip dependency reason in batch closeout because native browser storage APIs do not provide ZIP packaging
      - [x] Add compatibility/bundle round-trip coverage plus recovery tests proving named-project autosave stays separate from untitled local draft recovery
    - [x] Batch 5 - v5 ordered multi-file run workflow
      - [x] Add a generalized `docs/run-semantics.md` contract for checked-file run order, per-file resets, alias carry-forward, `.INCLUDE` coexistence, and duplicate include handling
      - [x] Bump the manifest/portable schema to v5, adopt the new file-kind/workspace model, and migrate v1-v4 projects without losing file order or enabled state
      - [x] Replace the current main-input-plus-includes solve assembly with an ordered checked-file run plan that feeds one shared adjustment
      - [x] Reset parser defaults at each checked project-file boundary while preserving alias definitions and accumulated network state across files
      - [x] Add workflow actions for checked files, tabs, duplicate/rename/delete/reorder, and zero-checked-file validation
      - [x] Add a minimal checked-file UI, editor tabs, and an Input Data header `Project Files` popover with rename/duplicate/context-menu/reorder controls
      - [x] Keep portable export/import, bundle export/import, and named-project autosave aligned with the new checked/open/focused file state
      - [x] Add focused migration/workflow/parser/UI regression coverage, then run lint/typecheck/test/build plus the industry-reference parity gate
    - [x] Batch 6 - project-files workflow polish
      - [x] Keep the Input Data `Project Files` entry point visible even before a named project exists
      - [x] Make the first `Project Files` action create a named project from the current workspace instead of hiding the workflow behind Project Options
      - [x] Fix named-project open flows so existing local projects reopen reliably across supported storage backends
      - [x] Keep open editor tabs stable when project-file manifest order changes
      - [x] Reduce project-files workflow churn from unnecessary recent-project refreshes during autosave-heavy editing
      - [x] Add focused regression coverage for the new project-files entry behavior, storage reopen path, and stable tab ordering
    - [x] Batch 7 - project access entry polish
      - [x] Make the toolbar open-project action open the Project Options workspace tab instead of auto-opening the most recent local project
      - [x] Keep the project workspace modal copy aligned with the current v5 manifest schema
      - [x] Add focused UI regression coverage for the toolbar open-project entry path
    - [x] Batch 8 - recent project reopen freshness
      - [x] Refresh `Recent Local Projects` ordering metadata when an existing local project is reopened
      - [x] Keep workspace/persistence docs aligned with the current v5 manifest wording
      - [x] Add focused storage regression coverage for local-project reopen recency
    - [x] Batch 9 - stable workspace file-content mapping
      - [x] Preserve workspace file contents by stable file ID during portable project round-trips instead of rebuilding them only through file-name maps
      - [x] Add focused regression coverage for loading a non-main focused workspace file with multiple open tabs
    - [x] Batch 10 - autosave race hardening
      - [x] Prevent stale autosave completions from overwriting newer project-file checked/open/focused state
      - [x] Add focused regression coverage for rapid project-file toggles across overlapping autosave saves
    - [x] Batch 11 - project-files marker responsiveness
      - [x] Reduce checkbox/reorder churn by avoiding unnecessary editor/include-file resync when focused text does not change
      - [x] Make project-files popup active/open/main state more explicit and less drag-prone
      - [x] Add focused regression coverage for marker rendering and checkbox-only interactions
    - [x] Batch 12 - source import and autosave timing
      - [x] Debounce named-project autosave to one minute after the latest dirty edit instead of half-second churn
      - [x] Route plain `.dat` imports into the current project as new source files instead of replacing the focused editor text
      - [x] Support multi-select source import so several `.dat` files append into one project in one action
      - [x] Add focused regression coverage for autosave delay and multi-file source import naming
    - [x] Batch 13 - project-files quick actions
      - [x] Add quick source-import and project-management entry points directly inside the Input Data `Project Files` popover
      - [x] Expose the most common per-file actions in the popover rows so duplicate/remove/open-edit no longer depend on the right-click menu
      - [x] Add focused UI regression coverage for the new quick-action controls
  - [ ] Batch 5 - error propagation precision closeout
    - [x] Expand the parity harness to lock the full error-propagation sections (all station sigmas, station ellipses, and relative ellipses)
    - [x] Add an error-propagation-focused machine-readable deviation summary alongside the broader reference diff
    - [x] Centralize shared covariance-to-ellipse decomposition
    - [x] Centralize relative covariance-from-endpoints construction
    - [x] Remove any non-display rounding or string conversion from the propagated-precision path
    - [x] Replace defensive `Math.abs` precision propagation with explicit tiny-negative clamping only
    - [x] Treat sub-display floating-point noise as neutral in the industry-reference diff gate so retained parity improvements are not rejected by machine-epsilon drift
    - [ ] Test solver-order / reweight timing only if covariance-path cleanup does not materially reduce the remaining reference diff
    - [ ] Batch 6 - multi-case parity fixture and normalized text-diff foundation
      - [x] Keep `manual/` local-only and mirror the 4 source input/output pairs into committed fixture files under `tests/fixtures/` with generic industry-case naming
      - [x] Add a committed exact-output regression lock for the parity-clean WebNet underground 2D reference case under `tests/fixtures/`
      - [x] Add a shared industry parity case registry with per-case input path, expected output path, header-normalization rules, startup-default settings patch, and selected instrument
      - [ ] Extend the parity harness so each case can assert both structured solve metrics and normalized exact text parity against its paired industry output
      - [x] Add text-normalization helpers that ignore volatile header values only for run date, software version, project folder, and source-file path lines
      - [x] Define the active-batch startup-default contract: each parity batch updates the hard-coded startup input and default app settings to that case until the next batch starts
    - [ ] Batch 7 - levelling-only parity
      - [x] Support leveling records whose station pair is encoded as one hyphenated token (`FROM-TO`) followed by elevation difference, length, and optional explicit sigma
      - [x] Preserve current explicit level sigma behavior and apply project/instrument default differential-level weighting when the record relies on default weighting
      - [x] Match the levelling case’s project-default setup in app startup defaults, including the `0.001500 meters/km Elev. Diff.` instrument weighting
      - [x] Add focused parser, weighting, listing, and parity coverage for the levelling-only case
      - [x] Require normalized exact listing parity for the levelling output before moving on
    - [ ] Batch 8 - traverse-only parity
      - [ ] Current scope: trace the remaining classic traverse display seams without disturbing the raw traverse exact sections or the underground guard, including the adjusted geodetic-position display contract for NewBrunswick83 listings
      - [x] Add the classic industry `Sideshot Coordinates Computed After Adjustment` section for post-adjusted TS sideshots
      - [x] Fix combined-case TS sideshot azimuth/coordinate parity so post-adjusted sideshots match the stored industry positions instead of being rotated off by projected-convergence handling
      - [x] Tighten remaining TS sideshot parity to the sub-millimeter / low-millimeter level by using the occupied station's reduction basis and curvature/refraction-aware zenith inversion for post-adjusted SS coordinates
      - [x] Start legacy-settings cleanup by collapsing the live workflow to strict `industry-parity`, removing the posterior-scaled precision selector, and normalizing old project/profile aliases on load/save without breaking backward compatibility
      - [x] Continue legacy-settings cleanup by removing the live legacy CRS transform controls and normalizing old project CRS-transform fields back to the current Canada-first CRS workflow on load/save
      - [x] Continue legacy-settings cleanup by removing the retired legacy CRS-transform block from the WebNet processing summary and report diagnostics while keeping the parity-backed industry listing unchanged
      - [x] Continue legacy-settings cleanup by dropping the retired CRS-transform fields from the WebNet report and processing-summary prop contracts while leaving engine/listing diagnostics intact
      - [x] Continue legacy-settings cleanup by dropping the retired CRS-transform trio from the shared WebNet run-diagnostics contract while letting the parity-backed industry listing source that detail from parse state when needed
      - [x] Continue legacy-settings cleanup by dropping the retired parser-compatibility knobs for directive-abbreviation mode and unknown-directive policy, while keeping the strict-vs-legacy parse behavior unchanged
      - [x] Continue legacy-settings cleanup by removing the dead `.VLEVEL` parse-state fields while keeping the directive accepted as a compatibility no-op
      - [x] Stabilize the flaky GNSS browser benchmark render-budget guard by timing deterministic post-mount selection rerenders with a median check instead of a single cold jsdom mount
      - [x] Invert classic traverse displayed NB83 coordinates through the CSRS double-stereographic geodetic display contract and honor positive-west longitude formatting in the adjusted geodetic summary
      - [x] Add a focused traverse parity guard that proves the raw fixed-bearing solve already stays exact and that representative connected covariance rows still match the reference confidence values before changing more classic display math
      - [x] Zero-pad classic quadrant-bearing degrees below 10 in the adjusted traverse bearing-distance section so rows like `109-GPS2` match the stored industry formatting without touching solver output
      - [x] Mirror the local underground industry output into a committed fixture and use it as a secondary structured comparator while tracing the remaining fixed-bearing orientation / connected-covariance seam on traverse
      - [x] Apply the tiny classic traverse display-rotation calibration around the displayed network centroid so fixed-bearing-style relationship rows move closer to the stored industry bearings without disturbing the underground exact lock
      - [x] Restore the classic traverse relative-ellipse vertical 95% column using connected-pair vertical sigma and one-dimensional confidence scaling
      - [x] Make the traverse case the startup default with its matching grid/project defaults from the reference output, including coordinate-system mode, CRS, longitude-sign convention, coordinate order, convergence limit, and instrument library
      - [x] Make `.INST <code>` reliably set the current instrument for the following traverse/direction-set block without breaking existing conventional parsing
      - [x] Keep projected grid traverse jobs from false-failing the grid-input gate when CRS inverse diagnostics populate derived lat/lon on auto-created stations
      - [x] Recompute derived geodetic inverses/grid factors from live projected coordinates for non-geodetic traverse stations instead of reusing stale cached lat/lon
      - [x] Carry the traverse reference case's curvature/refraction startup defaults into the active parity startup contract so zenith-family parity no longer runs on the old no-curvature fallback
      - [x] Use a dedicated NewBrunswick83 parity CRS with industry-matching convergence/grid-factor evaluation instead of assuming the EPSG:2953 scale contract for the traverse startup case
      - [x] Add a pre-solve approximate-coordinate bootstrap for traverse direction sets so unknown setups can be resected from known targets and forward-seeded into connected observations
      - [x] Keep traverse direction-set reductions plus `DM`-derived distance/zenith rows on one global observation-ID stream with the active set ID preserved for parity-safe grouping and diagnostics
      - [x] Match the traverse zenith-family curvature/refraction coefficient convention so `k=0.070000` reproduces the industry zenith summary instead of behaving like the older `1-k` correction
      - [x] Separate traverse measured directions from grid azimuth/bearing counts in the industry-style listing and keep the fixed bearing in its own `Az/Bearings` statistical family
      - [x] Use industry-style lateral residual distance in adjusted measured direction rows instead of printing the geometric shot length in that column
      - [x] Use the compact industry settings/instrument block for 3D traverse-style parity listings and summarize entered control stations from the original input coordinates as fixed/free/unused instead of treating the whole solved station map as entered data
      - [x] Add the traverse reference report's raw unadjusted distance, zenith, measured-direction-by-set, and fixed-bearing tables ahead of the statistical summary so the listing structure now matches the industry report through the main observation sections
      - [x] Tighten the raw traverse row formatting so the classic parity listing uses industry-style column widths, truncated `t-T` display, and closer `Comb Grid` rounding in the unadjusted observation tables
      - [x] Reduce the remaining raw traverse listing drift by splitting the classic `Comb Grid` display convention into the same mixed truncation/down-bias bands used by the reference and by damping the raw `t-T` display toward the industry report's classical output
      - [x] Close the raw traverse unadjusted exact diff so the classic parity listing now matches the industry reference line-for-line through the measured distance, zenith, and measured-direction sections
      - [x] Bring the later traverse listing closer to the reference by using first-occurrence station order in the convergence/factor block, true source-file line numbers in adjusted sections, and canonical observed-pair ordering in the adjusted bearing-distance relationship table
      - [x] Apply the classic traverse display-factor decomposition around the fixed anchor so adjusted coordinates and the bearing-distance relationship table use the closer industry-style horizontal scale in the parity listing
      - [x] Tighten the remaining traverse adjusted-coordinate and relationship-distance drift by using project-average grid/elevation factors for the classic ground-style display transform instead of the single anchor-station factors
      - [x] Apply the tiny retained classic traverse display-scale calibration so adjusted coordinates and relationship distances stay aligned with the stored industry output after the factor-decomposition cleanup
      - [x] Refine the classic traverse display basis to average only the entered traverse/control framework before applying the remaining tiny adjusted-coordinate and fixed-bearing residual calibration
      - [x] Replace the remaining naked classic traverse NB83 display scale/rotation residuals with a legacy-display-contract factor/convergence derivation so the later adjusted sections keep the same parity without another hardcoded listing-only tweak
      - [x] Lock the adjusted traverse geodetic-position rows against the stored reference after the display-CRS fix, confirming the remaining drift there is already sub-millimeter equivalent rather than a new centimeter-level coordinate-transform seam
      - [x] Keep the classic adjusted-direction sets in source/input order and suppress the WebNet-only grid/ground diagnostics block so the later traverse parity listing stays aligned with the industry report structure
      - [x] Model traverse zenith observations from the same ground-equivalent horizontal geometry used by grid-mode measured slope distances so the remaining adjusted-height drift in the parity case collapses to the reference
      - [x] Reduce grid-mode slope distances for the traverse parity path by applying the scale factor to the horizontal component before rebuilding the 3D slope length, while preserving the legacy derivative algebra for non-parity paths
      - [x] Keep parity-profile initial geometry frozen only for angular sigma behavior; distance/zenith/live slope modeling in the shared run-session path must continue using current geometry so the browser traverse startup still converges
    - [ ] Batch 9 - GNSS-only parity startup rotation
      - [x] Current scope: keep closing the last mixed combined-case parity drift while also implementing the stored industry `.ELLIPSE`, `.REL /CON`, and `.PTOL /CON` directives as functional output controls instead of compatibility no-ops; this pass must make the combined precision sections honor the selected station/pair lists, add configurable positional-tolerance checking, and preserve the retained leveling-only/traverse/GNSS locks
      - [x] Compare the GNSS-only fixture/listing against the local `manual/industry examples` output and close the statistical-summary gap starting from the `GPS Deltas` sum-of-squares / error-factor mismatch
      - [x] Make the covariance-vector listing sections expose the same GNSS component/frame contract used by the industry output so displayed residual/stdErr rows can be used to trace the remaining summary drift
      - [x] Check whether the adjusted GNSS covariance-vector display contract should round station heights to the same precision shown in the listing before building local-topocentric display vectors
      - [ ] Trace the remaining GNSS adjusted-vector/listing drift, with the current focus on the adjusted vector display frame (`Delta-N/E/U`) and the covariance-derived vertical/component `StdErr` rows rather than on coordinate/geodetic startup defaults
      - [x] Restore the classic mixed industry listing's adjusted GNSS vector section so combined parity runs print the same `Adjusted GPS Vector Observations` block as the stored reference instead of skipping directly from adjusted bearing observations to the relationship section
      - [x] Make `.ELLIPSE <stations...>` functional so station ellipse output honors the selected station list while retaining explicit fixed-control zero rows where the industry output does
      - [x] Make `.REL /CON <stations...>` functional so mixed/industry listings include the requested exhaustive pairwise relative-precision rows even when those pairs were not directly observed
      - [x] Make `.PTOL /CON <stations...>` functional with project-level positional-tolerance settings (constant, PPM, confidence) and a dedicated pass/fail listing section
      - [x] Test whether the remaining GNSS height/statistical-summary drift comes from the local-topocentric covariance-vector solve formulation itself rather than the listing-only display contract
      - [ ] If the solve formulation is not the seam, derive GNSS covariance-vector component precision/statistics in the same component space used by the industry adjusted-vector rows and summary
      - [x] Bring the GNSS/NewBrunswick listing structure closer to the industry output by adding the missing station/observation wrapper sections, switching the grid-factor table onto the legacy display-factor contract, and printing the adjusted bearing-distance block with both grid and ground distance rows from that same display contract
      - [ ] Investigate whether the remaining GNSS station/relative precision drift comes from noisy finite-difference modeled-vector derivatives in the covariance propagation path rather than from listing-only formatting
      - [x] Keep the fixed GNSS control station visible as an explicit zero ellipse row and align GNSS station/relative ellipse field widths with the stored industry precision-section formatting
      - [x] Make the GNSS-only case the active startup default, loading the committed GNSS input fixture into the editor and matching the supported grid/project defaults from the reference output
      - [x] Update the active-startup regression contract so the app now expects the GNSS case, while retaining dedicated leveling and traverse parity fixtures and tests for their own workflows
      - [x] Add parser/state support for GNSS vertical deflection and carry it through project-option/listing output
      - [x] Add parser support for the industry GNSS covariance-vector dialect with deterministic `G0/G1/G2/G3` grouping and `.GPS FACTOR <xy> VERT <z>` handling
      - [x] Extend the GNSS solver/listing path to use local-topocentric 3D covariance-vector adjustment output (`Delta-N`, `Delta-E`, `Delta-U`, covariance-derived sigmas/std-residuals, and adjusted geodetic rows) for the GNSS parity case
      - [x] Tighten the remaining GNSS coordinate/geodetic drift by switching the GNSS parity startup and listing contract to the CSRS New Brunswick double-stereographic definition that matches the stored industry coordinates while preserving the printed `NewBrunswick83` label
      - [x] Remove the obsolete `CA_NAD83_NB83_STEREO_DOUBLE` catalog CRS and move retained parity startup/test contracts onto `CA_NAD83_CSRS_NB_STEREO_DOUBLE`, leaving only the display-only legacy NB formula helper for classic traverse listing calibration
      - [x] Remove the residual classic traverse adjusted-coordinate/geodetic/relationship display rescaling path so those rows stay on the solved CSRS coordinates, while keeping the separate convergence-factor table on the isolated legacy display helper
      - [x] Move covariance-vector GNSS adjusted listing rows onto the same non-deflected local-topocentric display frame used by the stored industry reference, and transform ECEF covariance display sigmas/correlations into that local frame for the adjusted vector section
      - [x] Report GNSS adjustment statistical-summary counts by scalar component equations (`GPS Deltas`) so the listing and unknown-count math align with the industry 45-observation contract instead of the old 15-vector row count
    - [ ] Batch 10 - Solver performance pass
      - [x] Current scope: investigate safe adjustment speed improvements with benchmark-backed keep/revert discipline; first target replacing dense `AᵀP`/`AᵀPA` matrix products with sparse-row normal-equation accumulation while preserving current solver behavior, parity results, and benchmark/test budgets
      - [x] Benchmark the current dense imported-project solve path before and after changes using the existing large-project benchmark guardrails
      - [x] Replace generic dense normal-equation matrix products in the main solve/statistics paths with sparse-row accumulation only if the benchmark confirms a real speed win
      - [x] Revert any large performance refactor that regresses the benchmark path or broad test runtime instead of keeping speculative complexity
      - [x] Reapply `.GPS FACTOR <xy> VERT <z>` in local topocentric covariance space for covariance-vector GNSS solves/adjusted rows instead of raw ECEF axis space, collapsing the remaining solved station-height drift to near-reference levels while keeping the unadjusted `G0/G1/G2/G3` source-frame block unchanged
      - [ ] Close the remaining fixed-bearing orientation parity gap for a network constrained by one fixed point plus one fixed bearing
      - [ ] Match direction-set, distance, zenith, target-height, and set-scoped weighting behavior used by the traverse reference case
      - [x] Lock slot-preserving `DM` sigma parsing so `& & 30` keeps default direction and distance weighting while applying an explicit 30" zenith sigma
      - [x] Remove the WebNet-only observation-weighting traceability section from the industry listing, tighten the classic traverse ground/grid distance alignment, switch geodetic summary rows to fixed DMS formatting, and add the 3D vertical precision column to station standard-deviation and ellipse sections
      - [x] Add focused GNSS parser/solver/listing coverage for covariance-vector parsing, vertical deflection, local-topocentric modeled vectors, and the new GNSS listing sections
    - [ ] Batch 9 - GNSS-only parity
      - [ ] Make the GNSS case the startup default with its matching grid/project defaults from the reference output
      - [ ] Add parser support for the industry GNSS covariance-vector dialect: `.GPS WEIGHT COVARIANCE`, `.GPS FACTOR <xy> VERT <z>`, and `G0/G1/G2/G3` record groups
      - [ ] Map `G0/G1/G2/G3` groups into full GNSS vector observations with preserved file/session labels, component deltas, variances, and correlations
      - [ ] Keep factor application, covariance ingestion, and reporting deterministic and parity-tested
      - [ ] Add focused GNSS-only structured parity assertions plus normalized exact output diff
    - [ ] Batch 10 - final combined parity
      - [x] Make the combined case the startup default with its matching project defaults from the reference output
      - [ ] Current scope: diff the final combined industry case against the committed reference output, close the first mixed-network listing/statistics seams that remain after the single-domain parity batches, and keep the retained levelling/traverse/GNSS locks neutral or improved
      - [ ] Integrate levelling, traverse, and GNSS fixes without regressing any previously locked single-domain case
      - [ ] Resolve mixed-network interaction gaps only exposed in the combined case, including shared control handling, section ordering, and cross-family weighting/reporting interactions
      - [ ] Add the combined case to the structured parity harness and normalized exact text-diff gate
      - [ ] Keep the change only if levelling, traverse, GNSS, and combined parity all improve or remain neutral together
    - [ ] Batch 11 - parity closeout and workflow documentation
      - [ ] Document the four-case parity workflow and batch-default rotation rule in `docs/PARITY_WORKFLOW.md`
      - [ ] Update `docs/CURRENT_BEHAVIOR.md` with the newly supported levelling hyphen-pair syntax, traverse `.INST` instrument scoping, GNSS covariance-vector dialect, and expanded industry parity coverage
      - [x] Keep `README.md` unchanged unless the user-facing startup/example workflow is intentionally documented there
      - [ ] Run and record the required validation matrix for each completed batch: focused tests, `npm run lint`, `npm run typecheck`, `npm run test:agent` (or `test:full` at closeout), `npm run build`, and `npm run parity:industry-reference`
  - [x] Phase 5 - review and map/report UX polish
    - [x] Batch 1 - guided run-state and action safety
      - [x] Add explicit run-state strip with `Ready`, `Dirty (needs rerun)`, `Running`, `Result stale`, and `Reviewing`
      - [x] Add shared action guards with exact-scope confirmations for high-risk review/import actions
      - [x] Add inline disabled-reason hints and last-changed-settings warnings near run actions
    - [x] Batch 2 - issue-driven review queue
      - [x] Add a shared review queue populated by import conflicts, suspect observations, cluster candidates, and saved-run diffs
      - [x] Make queue selection drive synchronized report/map/editor source-line navigation
      - [x] Add queue filters (severity/source/unresolved/imported-group) plus next-unresolved navigation
    - [x] Batch 3 - dense-network imported-job review controls
      - [x] Add conflict-only filtering
      - [x] Add imported-group filtering
      - [x] Add adjusted-only review filtering
      - [x] Add faster map declutter toggles tuned for imported dense jobs with preset support
    - [x] Batch 4 - UX regression lock and coverage
      - [x] Preserve current report ordering and existing QA compare semantics while adding the new review queue controls
      - [x] Add focused coverage for guards, queue derivation/ordering, and filter persistence/rehydration

- [x] Scalability batch 1 - worker run pipeline foundation:
  - [x] Extract shared browser/CLI solve helpers into a pure run-session module
  - [x] Add typed worker protocol (`RunRequest` / `RunProgress` / `RunSuccess` / `RunFailure` / `RunCancel`) and browser worker entrypoint
  - [x] Add a worker-backed UI run hook with fallback direct execution, progress state, and cancel support
  - [x] Surface long-run worker solve detail in the toolbar with elapsed time, current stage, solve-count progress, and per-solve iteration progress
  - [x] Add `Suspect Impact` project setting (`AUTO` / `ON` / `OFF`) and make `AUTO` skip the expensive what-if reruns once the main solve is already heavy
  - [x] Trim repeated per-iteration condition-estimate work so the main solve only records the normal-matrix condition on the first outer iteration
  - [x] Add main-solve timing breakdown profiling for setup, equation assembly, matrix factorization, and precision/report diagnostics
  - [x] Defer full normal-equation covariance recovery until the final adjusted state instead of inverting `Qxx` on every outer iteration
  - [x] Build the industry-standard precision model once per solve and derive posterior-scaled station/relative covariance rows by scaling instead of recomputing from raw `Qxx`
  - [x] Keep last successful result visible during rerun and surface run-phase status in the toolbar
  - [x] Add regression coverage for worker-path parity and in-flight UI behavior

- [x] Scalability batch 2 - report filtering and windowing:
  - [x] Add report-side filters for station/source-line text, observation type, and exclusion status
  - [x] Add explicit row windowing for heavy report tables (coordinates, observations, covariance/precision, selected diagnostics)
  - [x] Keep filter/windowing state local to the report without changing solve outputs
  - [x] Add regression coverage for report filtering and load-more behavior

- [x] Scalability batch 3 - operator workflows and parity closeout:
  - [x] Add source-line jump from report rows back to the editor
  - [x] Add pinned/favorite report sections for quicker navigation
  - [x] Add a compact current-vs-last-run settings diff before rerun
  - [x] Complete remaining Section 5.6 compatibility work
  - [x] Add browser-oriented large-project benchmark fixture coverage

- [x] QA-focused workflow batches:
  - [x] Add shared QA derived-data models for report/map comparison workflows
  - [x] Add unified report/map selection state with suspect navigation and pinned review rows
  - [x] Add in-memory run history, baseline pinning, comparison summaries, and thresholded QA diffing
  - [x] Add deterministic QA bundle export presets with optional LandXML inclusion
  - [x] Add dense-map guards (viewport clipping, label suppression, active-selection overlays) and extend browser benchmark coverage
  - [x] Refine comparison/export UX: collapsible comparison panel, comparison reset on refresh, single export selector with points default, and bundled export descriptions

- [x] WebNet next-cycle plan - shell hardening, draft recovery, and faster QA reruns:
  - [x] Phase 0 - backlog codification
    - [x] Add the full phased implementation plan to TODO before code changes
    - [x] Break each phase into explicit checklist items instead of summary-only bullets
    - [x] Separate architecture, UX, performance, and test-gate work so execution order stays locked
    - [x] Update README.md and agents.md after each completed implementation batch
  - [x] Phase 1 - app shell completion and browser recovery
    - [x] Extract remaining App shell seams:
      - [x] Extract top toolbar/status area into a dedicated shell component
      - [x] Extract draft-recovery banner/actions into a dedicated shell component
      - [x] Extract tab/workspace chrome into a dedicated shell component or hook-backed module
      - [x] Remove remaining direct engine/run orchestration paths from App.tsx so browser runs flow through one workflow path
    - [x] Add browser-local draft recovery:
      - [x] Persist input text and include-file bundle locally
      - [x] Persist settings, parse settings, selected instrument, and export settings locally
      - [x] Persist active tab, split layout, sidebar visibility, report/map selection, comparison thresholds, and pinned review state locally
      - [x] Restore draft state on startup with explicit recover/discard behavior
      - [x] Add a clear-current-draft action
    - [x] Lazy-load heavy browser-only surfaces not needed on first paint:
      - [x] Import-review workflow/modal
      - [x] Project Options modal body
      - [x] External importer registry/converters
      - [x] Large detail panels tied to import/settings workflows
        - [x] Keep remaining minor detail panels inline after profiling; no additional first-paint hotspots justified another split in this phase
    - [x] Add focused regression coverage for browser recovery, recover/discard/clear flows, and project-load replacement behavior
      - [x] Add focused recovery-hook coverage for recover/discard/clear flows
      - [x] Add focused workspace-shell coverage for tab switching and empty-state behavior
      - [x] Add project-load replacement behavior coverage against persisted local draft state
  - [x] Phase 2 - ReportView decomposition and QA workspace UX
    - [x] Split ReportView into section components backed by a shared report-section registry
    - [x] Extract shared report table/filter primitives for load-more, filtering, and row selection behavior
    - [x] Batch complete - persistable workspace review-state extraction
      - [x] Hoist report view UI state out of `ReportView` so it survives tab switches
      - [x] Fold report/map selection + pinned observation review context into the shared review-state module
      - [x] Persist and restore report review snapshot data through browser draft recovery
      - [x] Add focused hook/recovery regression coverage for restored review state
    - [x] Move report-local UI state into a dedicated hook/module:
      - [x] Filters
      - [x] Load-more window sizes
      - [x] Collapsed and pinned sections
      - [x] Selected station/observation context
    - [x] Persist report/map review state through the browser recovery layer
    - [x] Batch complete - QA review quick actions and decomposition regression lock:
      - [x] Add one-stop QA review actions for previous/next suspect, jump to input, pin selected observation, and focus report filter
      - [x] Wire report filter focusing through the decomposed report modules without breaking tab-switch persistence
      - [x] Add integration-style regression coverage for review actions plus stable ordering/selection behavior
    - [x] Add fast QA navigation actions for next/previous suspect, jump-to-input, pin selected observation, and focus filter
    - [x] Add UI regression coverage proving report ordering/content/selection behavior remains stable after decomposition
    - [x] Batch complete - compare-panel QA action consolidation:
      - [x] Move the shared QA review actions into the collapsible Run Compare section
      - [x] Remove duplicate suspect-navigation buttons from Run Compare
      - [x] Add hover-help tooltips to Run Compare labels and QA action buttons
      - [x] Update compare-panel regression coverage for collapsed/expanded toolbar behavior
  - [x] Phase 3 - scenario runtime and rerun performance
    - [x] Batch complete - nested scenario cache reuse and compare execution:
      - [x] Route nested blunder-detect and cluster dual-pass reruns through the shared parsed-state cache
      - [x] Add an explicit grouped comparison-scenario executor on top of the shared scenario service
      - [x] Add regression coverage for nested rerun cache hits and grouped comparison reuse
    - [x] Batch complete - shared scenario cache foundation:
      - [x] Add a shared parsed-model/scenario-run service under the solve path
      - [x] Reuse parsed state for unchanged-input reruns plus exclusion/what-if/auto-adjust scenario solves
      - [x] Move the browser direct fallback onto the shared run-session executor path
      - [x] Extend benchmark/regression coverage for cached reruns and scenario cache reuse
    - [x] Introduce a parsed-model/scenario-run service shared by browser and CLI
    - [x] Parse once per input/settings fingerprint and reuse parsed state for same-input reruns
    - [x] Reuse parsed state for exclusion trials, suspect-impact analysis, auto-adjust cycles, and compare scenarios
    - [x] Replace repeated parse/setup work for unchanged-input reruns without changing worker protocol or CLI behavior
    - [x] Move scenario workflows off ad hoc engine re-instantiation paths onto the shared scenario executor contract
    - [x] Extend the large-project benchmark with rerun latency budgets
    - [x] Add regression/performance coverage for cached reruns and scenario execution parity
  - [x] Phase 4 - remaining parser and solver core split
    - [x] Batch complete - parser sigma helpers and cluster dual-pass extraction:
      - [x] Extract parser sigma/default-resolution helpers into a dedicated module
      - [x] Extract solver cluster dual-pass orchestration into a dedicated module
      - [x] Add focused module regression coverage for the extracted parser/solver seams
    - [x] Batch complete - parser alias pipeline extraction:
      - [x] Extract alias-resolution and canonical-ID pipeline into a dedicated parser module
      - [x] Rewire `parse.ts` and include-scope handling to consume the extracted alias pipeline
      - [x] Add focused regression coverage for alias resolution, directive handling, and cluster-merge preload behavior
    - [x] Batch complete - direction-set reduction seam extraction:
      - [x] Extract direction-set reduction and treatment logic into a dedicated parser module
      - [x] Rewire `parse.ts` to route direction-set flushing and diagnostics through the extracted module
      - [x] Add focused regression coverage for reduced/raw treatment decisions and reject diagnostics
    - [x] Batch complete - directive dispatch registry extraction:
      - [x] Extract non-core inline directive handling into a dedicated parser dispatch/registry module
      - [x] Rewire `parse.ts` to dispatch parser directives through the registry instead of the inline command chain
      - [x] Add focused regression coverage for registry dispatch, no-op compatibility directives, and `.END` termination
    - [x] Batch complete - solver statistical-test math extraction:
      - [x] Extract chi-square probability/quantile math into a dedicated solver helper module
      - [x] Rewire `adjust.ts` to consume the extracted chi-square summary builder instead of inlining the test math
      - [x] Add focused regression coverage for quantile inversion and chi-square summary shaping
    - [x] Batch complete - weak-geometry diagnostics extraction:
      - [x] Extract preanalysis weak-geometry median/severity/ranking helpers into a dedicated solver module
      - [x] Rewire `adjust.ts` to consume the extracted weak-geometry diagnostics builder
      - [x] Add focused regression coverage for station/pair cue severity and ranking behavior
    - [x] Batch complete - reduction-usage summary extraction:
      - [x] Extract parsed/used-in-solve reduction-usage summarization into a dedicated shared engine helper
      - [x] Rewire `adjust.ts` and parse post-processing to consume the extracted summary helper
      - [x] Add focused regression coverage for reduction-mode counting across parsed and filtered solve paths
    - [x] Batch complete - loop/setup/GPS diagnostic builder extraction:
      - [x] Extract GPS and differential-level loop diagnostic builders into a dedicated solver module
      - [x] Extract setup-summary and traverse-closure ranking builders into a dedicated solver module
      - [x] Rewire `adjust.ts` to consume the extracted diagnostic builders without changing report/log payloads
      - [x] Add focused regression coverage for extracted loop/setup/traverse diagnostic builders
    - [x] Batch complete - Phase 4 orchestration closeout:
      - [x] Extract auto-sideshot and cluster candidate diagnostics into dedicated solver builders
      - [x] Rewire `adjust.ts`/`buildResult()` to consume the extracted builders while preserving log and result payload behavior
      - [x] Add focused regression coverage for the extracted review-diagnostic builders
      - [x] Close out the remaining Phase 4 monolith-extraction meta checklist items once `adjust.ts` is reduced to solve orchestration
    - [x] Finish parser monolith extraction:
      - [x] Sigma/default-resolution helpers
      - [x] Alias-resolution and canonical-ID pipeline
      - [x] Direction-set reduction and treatment logic
      - [x] Directive command dispatch and handler registry
    - [x] Finish solver monolith extraction:
      - [x] Statistical test math and quantile helpers
      - [x] Weak-geometry classification and ranking
      - [x] Reduction-usage summarization
      - [x] Loop/setup/GPS diagnostic builders
      - [x] Cluster dual-pass orchestration
    - [x] Keep parse.ts as orchestration over parser handlers and adjust.ts as solve control over prepared services/builders
    - [x] Add focused module regression coverage for each extracted parser/solver seam

- [x] Architecture follow-up - phased implementation:
  - [x] Phase 1 - `App.tsx` decomposition
    - [x] Extract run-comparison state/history into a dedicated hook
    - [x] Extract workspace/project state and workspace-artifact reset semantics into a dedicated hook
    - [x] Extract Project Options draft state and apply/cancel orchestration into dedicated hooks/modules
    - [x] Extract import-review workflow state and handlers into a dedicated hook/module
    - [x] Extract adjustment run/review orchestration (exclusions, overrides, cluster review state, rerun handlers, and outcome application) into a dedicated hook
    - [x] Add focused hook regression coverage for adjustment run/review orchestration state transitions
    - [x] Extract project save/load orchestration and project-file migration/apply fan-out into a dedicated hook/module
    - [x] Extract export dispatch/browser download helpers into a dedicated hook/module
      - [x] Move text/XML save-picker + fallback download helpers out of `App.tsx`
      - [x] Move export-format dispatch for points/WebNet/listing/LandXML/QA bundles into a dedicated hook
      - [x] Add focused regression coverage for export dispatch and blocked-transform notice paths
    - [x] Batch complete - Project Options modal controller extraction:
      - [x] Extract Project Options modal change/select/apply orchestration into `src/hooks/useProjectOptionsModalController.ts`
      - [x] Rewire `App.tsx` to provide static modal context and consume the extracted controller hook
      - [x] Add focused regression coverage for extracted project-options controller behavior
    - [x] Extract remaining top-level orchestration helpers so `App.tsx` becomes a shell/composition layer
      - [x] Extract run-profile/output builder logic (`resolveProfileContext`, run diagnostics, WebNet text, listing text, LandXML wrapper) into a dedicated hook/module
        - [x] Extract listing/LandXML output wrapper composition into `src/engine/runOutputBuilders.ts`
        - [x] Extract `resolveProfileContext` and run-diagnostics composition out of `App.tsx`
        - [x] Add focused regression coverage for extracted run-profile/run-diagnostics composition
        - [x] Extract WebNet text report builder out of `App.tsx` into `src/engine/runResultsTextBuilder.ts`
        - [x] Add focused regression coverage for extracted WebNet text report builder
  - [x] Phase 2 - shared derived-model cleanup
    - [x] Add shared QA derived-data model for report/map comparison workflows
    - [x] Expand shared derived models so report/map/listing/processing-summary consume normalized data instead of rebuilding view-specific arrays
      - [x] Add `src/engine/resultDerivedModels.ts` for shared observation sorting/grouping, data-check diff rows, traceability normalization, and statistical-summary fallback shaping
      - [x] Rewire `ReportView`, `ProcessingSummaryView`, `industryListing`, and `runResultsTextBuilder` to consume the shared derived helpers instead of rebuilding those models inline
      - [x] Add focused regression coverage for shared derived-model helpers in `tests/result_derived_models.test.ts`
      - [x] Extend the shared derived-model path into remaining map-side non-geometry selectors (visible station IDs, weak-station severity, station ID lookup, and observation-pair link lookup) so `MapView` no longer rebuilds those selectors inline
      - [x] Extend the shared derived-model path into visible-station row metadata and shared station-priority scoring for `MapView` tool panels and dense-label prioritization
      - [x] Finish the remaining map-side selector cleanup by centralizing pair-key normalization plus severity-based station/ellipse styling helpers used by `MapView`
  - [x] Phase 3 - parser modular split
    - [x] Split parser directive-state management out of the current parser monolith
      - [x] Extract observation-mode normalization, directive transition snapshots, and no-effect warning synthesis into `src/engine/parseDirectiveState.ts`
      - [x] Extract core parser state-directive handling (`.UNITS/.SCALE/.MEASURED/.GRID/.COORD/.ORDER/.2D/.3D/.3REDUCE/.DELTA/.MAPMODE/.MAPSCALE/.DATA/.SEPARATOR`) into `src/engine/parseDirectiveState.ts`
      - [x] Add focused module regression coverage for directive-state normalization, transitions, and no-effect warnings
    - [x] Split record-family parsing into stable modules by observation/control family
      - [x] Extract control/geodetic record handling (`C`, `P/PH`, `CH/EH`, `E`) into `src/engine/parseControlRecords.ts`
      - [x] Add focused parser regression coverage for control/geodetic record families
      - [x] Extract conventional primitive observation handling (`D`, `A`, `V`, `DV`) into `src/engine/parseConventionalObservationRecords.ts`
      - [x] Extend `src/engine/parseConventionalObservationRecords.ts` to cover bearing/measurement families (`B`, `BM`, `M`)
      - [x] Add focused parser regression coverage for conventional primitive and bearing/measurement observation families
      - [x] Batch complete - field/GNSS/leveling record-family extraction:
        - [x] Extract inline `SS` / `GS` / `G` / `G4` / `L` record handling into a dedicated parser module
        - [x] Rewire `parse.ts` to delegate those record families without changing parser behavior
        - [x] Add focused parser regression coverage for extracted sideshot/GNSS/leveling record handling
      - [x] Extract traverse and direction-set families (`TB`, `T`, `TE`, `DB`, `DN`, `DM`, `DE`) into dedicated parser modules
        - [x] Extract traverse-family handling (`TB`, `T`, `TE`) into `src/engine/parseTraverseRecords.ts`
        - [x] Add focused parser regression coverage for traverse-family handling
        - [x] Extract direction-set families (`DB`, `DN`, `DM`, `DE`) into `src/engine/parseDirectionSetRecords.ts`
        - [x] Add focused parser regression coverage for direction-set families
    - [x] Split include expansion and include-scope state restoration into focused modules
      - [x] Extract include expansion, path normalization, and include-file-map resolution into `src/engine/parseIncludes.ts`
      - [x] Add focused module regression coverage for include expansion and cycle detection
      - [x] Extract include-scope snapshot/restore orchestration out of `src/engine/parse.ts` into `src/engine/parseIncludeScope.ts`
      - [x] Add focused module regression coverage for include-scope snapshot/restore behavior
    - [x] Split canonicalization/traceability/post-parse passes into dedicated modules
      - [x] Extract alias canonicalization, lost-station normalization, description reconciliation, directive-finalization, and final parse/log summary shaping into `src/engine/parsePostProcessing.ts`
      - [x] Keep parser fixture behavior stable while reducing the final `parse.ts` post-processing tail
      - [x] Add focused regression coverage for the extracted post-processing module
  - [x] Phase 4 - solver modular split
    - [x] Split preprocessing into a dedicated module boundary
      - [x] Extract solve-parameter preparation, auto-held height detection, direction-set ID collection, and weighted-control constraint planning into `src/engine/adjustmentPreprocessing.ts`
      - [x] Extract shared control-constraint builders/correlation weighting into `src/engine/adjustmentConstraints.ts`
      - [x] Add focused module regression coverage for solve-parameter preparation and correlated control constraints
    - [x] Split equation assembly / stochastic model setup into dedicated modules
      - [x] Extract solve-loop `A/L/P` assembly for observation families and weighted-control rows into `src/engine/adjustmentEquationAssembly.ts`
      - [x] Keep `adjust.ts` as iteration orchestration while delegating solve-matrix assembly to the extracted module
      - [x] Add focused module regression coverage for solve-matrix assembly
    - [x] Split solve / iteration control into a dedicated module
      - [x] Extract robust/classical per-iteration normal-equation solve flow into `src/engine/adjustmentIteration.ts`
      - [x] Extract station/orientation correction application into `src/engine/adjustmentIteration.ts`
      - [x] Add focused regression coverage for iteration-step solving and correction application
    - [x] Split diagnostics/statistics shaping into a dedicated module
      - [x] Extract statistical-summary, residual-diagnostics, and per-type residual summary builders into `src/engine/adjustmentStatisticsBuilders.ts`
      - [x] Keep solver math and covariance diagnostics behavior unchanged while reducing `calculateStatistics` responsibility in `src/engine/adjust.ts`
      - [x] Add focused regression coverage for the extracted statistics builders
    - [x] Split final result shaping/export-ready data assembly into a dedicated module
      - [x] Extract parse-state finalization and outward-facing `AdjustmentResult` payload assembly into `src/engine/adjustmentResultBuilder.ts`
      - [x] Keep run-mode/auto-sideshot/cluster gating behavior stable while reducing `buildResult()` responsibility in `src/engine/adjust.ts`
      - [x] Add focused regression coverage for the extracted result builder
  - [x] Next architecture follow-up
    - [x] Expand shared derived models so report/map/listing/processing-summary consume normalized data instead of rebuilding view-specific arrays

- [x] Add post-adjust adjusted-points Transform support (rotation-only v1): persisted transform settings, Project Options -> Other Files -> Transform UI (rotation with pivot/scope/select-points popup + translation/scale placeholders), export-time validation, and dual-section adjusted-points export output (original + transform notes + rotated coordinates)
- [x] Add TypeScript tooling (tsconfig, TSX entry) and migrate LSA engine/UI to TS modules
- [x] Industry-standard input-compatibility parity batch 1: parser directive normalization aliases/unique-prefix handling, `.DATA` block gating, `.SEPARATOR` station token support, `.3REDUCE` horizontal-equation behavior, include preprocessor with source-file traceability + scoped state restore, CLI include filesystem resolution + hard-fail diagnostics, new CLI run modes (`data-check`, `blunder-detect`), and project file schema v3 bundle support (`mainInput` + `includeFiles`) with v1/v2 migration
- [x] Industry-parity Phase 1 foundation: add dual parser compatibility modes (`legacy` fallback-aware vs `strict` deterministic), coded compatibility diagnostics/counters, and numeric-token station misparse guards for D/A/G/L parsing paths
- [x] Industry-parity migration plumbing: bump project file schema to v2 with parser-mode migration metadata while preserving v1 backward loading in legacy mode
- [x] CLI parity controls: add `--parse-mode` and geoid-source CLI options (`--geoid-model-id`, `--geoid-interpolation`, `--geoid-source-format`, `--geoid-source-path`) with parse-state propagation and regression coverage
- [x] Geodetic expansion follow-up: complete external GTX/BYN loader support (binary parser implementation, engine/CLI source-byte wiring, deterministic built-in fallback diagnostics, and GTX/BYN regression fixtures/tests)
- [x] Browser geoid-source completion: add Project Options geoid file-byte loading for GTX/BYN so browser runs consume external geoid data directly (not path-only metadata), with NAD83(CSRS) built-in preset selection support
- [x] Computational parity harness expansion: roll out tiered tolerance gates (summary/coordinate/residual) across multi-fixture reference projects
- [x] Add Canada-first coordinate-system Phase 1 support (Local/Grid mode, NAD83(CSRS) UTM catalog, `.SCALE` + `.MEASURED`/`.GRID` directives, mode-aware reductions, GPS/CRS UI controls, report/listing diagnostics, CLI flags, and parser/engine/UI regression coverage)
- [x] Start Canada-first coordinate-system Phase 2 catalog expansion (NAD83(CSRS) MTM zones, priority provincial CRS entries, NB Stereographic Double Projection, and EPSG alias normalization in parser/CLI)
- [x] Complete Canada-first coordinate-system accuracy pass (CRS metadata expansion with proj params/area bounds/datum-op support, projection-formula factor engine with diagnostic fallback path, solver coord-system diagnostics + area-of-use warnings, standardized `observationMode` parse state, CRS search UI + enriched CRS detail panel, and regression coverage for parser/engine/catalog/UI)
- [x] Complete coordinate-system parity pass A (authoritative reduction context + `.SCALE` replacement path, grid/local/geodetic + GNSS-frame pre-solve gates, datum sufficiency hard/soft checks with suggestions, expanded coord-system diagnostics reporting, GNSS frame default/confirm controls in GPS options, and matching CLI flags/tests)
- [x] Keep `manual/` local-only by adding repo ignore/docs policy (`manual/` in `.gitignore`, process note in `agents.md`, no `.gitattributes` exclusion semantics)
- [x] Fix Vitest/jsdom worker startup compatibility on current Node runtime by pinning `jsdom` to `26.1.0`
- [x] Clean up Project Options -> Adjustment action controls (keep only adjustment workflows) and move map display toggles to General -> Local/Grid Reduction
- [x] Update ESLint to parse TS/TSX and ensure lint/build pass
- [x] Split UI into smaller components (Layout, Report, InputPane) to reduce App.tsx size
- [x] Add CI + coverage for Vitest suite (matrix/angles/parser/engine fixtures added; GitHub Actions workflow + coverage script)
- [x] Add Prettier + lint-staged for consistent formatting; remove unused src/App.css
- [x] Improve UX: file upload/save for .dat, unit-scaled outputs, map/ellipse view, exclusion toggles (re-run)
- [x] Editable observation tables (inline edits to values/weights) and true computational unit conversion
- [x] Performance hardening: add conditioning guard and residual spike warnings; consider Web Worker offload (future)
- [x] Consolidate toolbar settings into a dropdown menu
- [x] Export adjustment results as text + refresh-to-last-run input
- [x] Input editor comment workflow: line-wise block toggle via `Shift+3` (`#`) on multiline selections plus right-click `Toggle # Comment` action
- [x] Import-review fixed toggles: emit family-appropriate fixed `!` markers for imported control/TS/GNSS/vertical rows, and accept fixed GNSS/leveling sigma tokens on parse/reimport
- [x] Input editor/context menu polish: route custom `Paste` through the app menu immediately (while still respecting any browser clipboard-permission prompt), and replace the built-in starter file with a convergent mixed regression example that keeps `SEUW` near `1.0` for quick regression checks
- [x] Project Options refresh: add user-editable named level-loop tolerance presets, replace the Other Files placeholder with real export/output controls, and condense the Adjustment, GPS, General, Instrument, Special, and Modeling settings tabs into cleaner card/grid layouts with inline toggles and right-aligned inputs
- [x] Project Options modal regression coverage: add focused layout tests for Adjustment, General, Instrument, GPS, Other Files, and Modeling tab rendering so condensed modal structure stays stable
- [x] Project Options modal interaction coverage: add jsdom interaction tests for tab switching plus `Apply`/`Cancel` draft persistence/discard behavior
- [x] Adjustment convergence controls: add user-facing `Convergence Limit` beside `Max Iterations`, persist it in project settings, and wire solver stopping to iteration-to-iteration `vTPv` delta with listing output parity
- [x] Field-collection parser expansion: support SS shorthand station tokens (`SS at-to` / `SS at-from-to`) with deterministic angle defaults + HI/HT parsing, and add `GS` GNSS topo coordinate records as post-adjust (equation-excluded) outputs across report/summary/map/export
- [x] Workspace persistence + adjusted-points deliverables: add save/open project files (`webnet-project` JSON with input + settings + instruments + export prefs), add configurable adjusted-points export (preset/custom columns, delimiter, optional LAT/LON, max-6 dynamic ordering), and expose both via toolbar and Project Options -> Other Files with regression tests
- [x] No-result-change optimization batch plan:
  - [x] Batch 1 (solver): cache active observations per solve stage and add per-iteration geometry caching for repeated azimuth/zenith lookups in `adjust.ts`
  - [x] Batch 2 (UI/report): memoize heavy derived report arrays and lazy-load heavy result tabs to reduce initial payload/first render cost
  - [x] Batch 3 (parser/import): reduce parser directive hot-loop allocations and trim repeated importer regex rescans for large external files
- [x] Run-mode parity completion (UI/report path + compatibility constraints):
  - [x] Phase 1: wire UI solve path and Project Options state so run mode supports `adjustment`/`preanalysis`/`data-check`/`blunder-detect` instead of forcing `adjustment|preanalysis` in `App.tsx`
  - [x] Phase 2: add run-mode-specific report/listing/processing-summary sections (including data-check differences and blunder-detect warning/profile text)
    - [x] Phase 2A: add report-view run-mode sections for `data-check` (Differences from Observations) and `blunder-detect` warning/profile messaging
    - [x] Phase 2B: add processing-summary run-mode blocks for `data-check` differences and `blunder-detect` cycle/warning notes
    - [x] Phase 2C: add industry-listing run-mode sections and regression tests for `data-check` and `blunder-detect` output
  - [x] Phase 3: implement exhaustive run-mode incompatibility matrix with clear diagnostics and mode-gated regression coverage
- [x] Include pipeline test-gate completion:
  - [x] Phase 1: add parser regression tests for include cycle and include depth failures with exact source-file/line diagnostics
  - [x] Phase 2: add relative-path inheritance and nested include-order tests for both parser bundle mode and CLI filesystem mode
  - [x] Phase 3: add CLI hard-fail regression coverage for missing/cycle/depth/relative-path include scenarios with deterministic exit behavior
- [x] Legacy compatibility corpus gate:
  - [x] Phase 1: curate mixed legacy-project corpus fixture set and define expected run-mode outcomes per project
  - [x] Phase 2: add corpus harness command that enforces parse success, run success, and no silent directive drops
    - [x] Add `npm run corpus:legacy` harness entrypoint (`src/legacyCorpusHarness.ts`) with include-aware filesystem resolution and deterministic pass/fail summary output
    - [x] Enforce gate checks per corpus project: expected parse success, expected run success, required run-mode diagnostics, and no silent unknown/ambiguous inline-directive drops
    - [x] Add harness regression tests for default pass path, deterministic mismatch failure path, and silent-directive-drop detector behavior (`tests/legacy_corpus_harness.test.ts`)
  - [x] Phase 3: wire corpus harness into CI with failure summary artifacts and parity baseline tracking
    - [x] Add harness summary-file outputs (JSON + text) suitable for CI artifact upload
    - [x] Add baseline snapshot fixture + harness baseline-compare mode for deterministic parity tracking
    - [x] Wire CI workflow to run corpus harness with baseline comparison and upload artifact files
- [x] Exclude fixed planned observations from preanalysis what-if candidates so fixed bearing/control planning rows are not presented as removable
- [x] Add a separate locked planned-observations preanalysis section and hover-help coverage for preanalysis report headers/labels
- [x] Extend hover-help coverage to remaining report summary-card labels and Project Options modal tabs/sections/field labels/instrument editor controls
- [x] Match industry-standard centering inflation for slope distances and zeniths using horizontal/vertical centering geometry factors, with fixture-locked reference coverage
- [x] Fix robust reweighting to use postfit residual IRLS with cheap diagonal/block weight updates
- [x] GPS Phase 1 foundation: CRS state model + projection abstraction (`LEGACY`/`ENU`) with default-off transform controls in Project Options -> GPS
- [x] GPS Phase 2 modeling: optional CRS grid-ground scale + convergence corrections in solver/inverse workflows with default-off GPS controls
- [x] GPS Phase 3 diagnostics: surface CRS projection/scale/convergence state in report/processing-summary/industry-listing outputs with regression coverage
- [x] GPS Geoid/Grid Phase 1: default-off model ingestion pipeline with cached loading, interpolation selection, validation, and metadata diagnostics
- [x] Standardized residuals (Qvv), redundancy numbers, chi-square test, per-type residual summaries
- [x] Point precision (σN/σE/σH, ellipse azimuth, 95% toggle) + relative precision between unknown points
- [x] Map/ellipse viewer interaction upgrades: wheel zoom, middle-button pan, middle-double-click reset-to-extents, zoom-aware symbol scaling
- [x] Map/ellipse viewer context tools: right-click menu with Points list, Inverse calculator (azimuth both directions + distance), and Angles Between calculator (inside/outside at pivot)
- [x] Prevent stale exclusion/override carryover between changed inputs (auto-clear on changed run context + file load)
- [x] Add Industry Standard parity profile (classical weighting + raw direction-set solving + industry-like default instrument fallback for missing instrument assignments)
- [x] Phase 2 parity: geometry-aware angle centering inflation model (ray-correlation term) + industry benchmark regression fixture/test
- [x] Phase 3 parity: add run-pinned solve-profile diagnostics section in report/export (profile, direction-set mode, fallback/defaults, stochastic/reduction switches)
- [x] Phase 4 parity: expand industry benchmark fixture coverage (coords/SEUW/residual signatures) + expose active default stochastic model and default-sigma usage counts in report/export
- [x] UI parity Phase A: replace settings dropdown with industry-style Project Options modal (tab shell + Apply/Cancel draft state + mapped existing solver controls + placeholder tabs for upcoming industry options)
- [x] UI parity Phase B: add Processing Summary tab with industry-style monospaced run summary (iteration flow, per-type error factors, chi-square pass/fail, run profile flags, elapsed time, processing notes)
- [x] UI parity Phase C: add industry-style output tab + export format selector (WebNet report or industry-style listing output file)
- [x] UI parity Phase D: wire Listing File tab controls to industry-style output content/sorting options (sections, sort order, row limit, azimuth/bearing visibility)
- [x] Dev workflow: update Husky pre-commit hook to run one-shot tests (`npm run test:run`) instead of watch mode
- [x] Sync README/agents docs with current parser/engine/UI state
- [x] Audit remediation upgrade: force-updated dev tooling to Vite 6 / Vitest 4 / esbuild 0.25 and revalidated lint + test:run + build on `chore/audit-force-upgrade-test`
- [x] Clean up lingering build/install caveats: override transitive `flatted` to clear `npm audit`, and tune Vite chunk-warning thresholds so production builds no longer emit the old oversized-engine warning
- [x] Instrument settings overhaul: editable Project Options instrument library (with New Instrument flow, unit-aware fields, and 2D/3D gating), solver wiring to UI-selected defaults, external-instrument precedence over inline `I` records, and zeroed fallback precision defaults unless explicitly provided
- [x] Replace explicit normal-matrix inversion in the primary adjustment solve with Cholesky-based SPD solves, and use the same factorization path for dense `Qxx` covariance inversion
- [x] Add scaled diagonal-damped Cholesky regularization for ill-conditioned normal matrices, while recovering undamped scaled covariance for diagnostics when regularization would bias residual/redundancy outputs
- [x] Replace the remaining diagnostic covariance-recovery inversion with pivoted symmetric `LDLᵀ` solving and add a singular `industry_demo.dat` regression test for the fail-cleanly path
- [x] Strengthen symmetric-indefinite covariance recovery with 2x2 pivot-block `LDLᵀ` support and make singular damped solves fail earlier with an explicit singular-after-damping message
- [x] Add LDLT pivot-block diagnostics and a weak-but-recoverable regularized network fixture to lock the boundary between usable regularization and singular-after-damping failure
- [x] industry-style summary parity: compute per-type statistical summary from weighted residual contributions (`vTPv` by observation family) and display both error-factor and variance-factor chi-square bounds in summaries/listings
- [x] industry-style summary parity Phase 2: normalize per-type error factors using industry-style global DOF scaling (`sqrt(totalCount/dof)`) so group factors align with industry listings
- [x] Cluster UX controls: add Project Options -> Adjustment toggle for cluster detection ON/OFF and top-level report button to revert applied cluster merges
- [x] Set default adjustment startup profile to Industry Standard parity, default cluster detection OFF, and default project instrument to `S9` (`industry standard S9 0.5"`) with parity baseline stochastic values
- [x] Industry-style listing readability/parity plan (format + section structure):
  - [x] Phase 1: fix spacing/alignment defects in listing text columns so Northing/Easting and similar adjacent numeric fields are always separated by explicit padding (coordinates, station std devs, and adjusted obs rows such as direction/azimuth families)
  - [x] Phase 2: remove "Processing Notes" block from industry-style output and replace with observation-family blocks that match industry style headings/order
  - [x] Phase 3: split adjusted observation listing into dedicated family sections:
    - [x] `Adjusted Angle Observations (DMS)`
    - [x] `Adjusted Distance Observations (Meters)`
    - [x] `Adjusted Direction Observations (DMS)`
    - [x] `Adjusted Azimuths (DMS) and Horizontal Distances (Meters)` with relative azimuth confidence text
  - [x] Phase 4: expand error-propagation listing with parity sections:
    - [x] `Station Coordinate Standard Deviations (Meters)`
    - [x] `Station Coordinate Error Ellipses (Meters)` with `Confidence Region = 95%`
    - [x] `Relative Error Ellipses (Meters)` with `Confidence Region = 95%`
    - [x] Drive azimuth confidence + relative ellipse rows from observed station relationships (including fixed-to-adjusted pairs), instead of listing all unknown-pair combinations
  - [x] Phase 5: add fixtures/tests to lock spacing, headings, section ordering, and key row formats for industry-style listing output (`tests/industry_listing_format.test.ts` + `tests/fixtures/industry_listing_phase5_expected_headings.json`)
- [x] Adjustment report parity roadmap (industry-standard alignment):
  - [x] Implement weighted control constraints from coordinate/elevation std errors (not only fixed/free) so control uncertainty participates in the solve
  - [x] Wire and report normal-matrix conditioning diagnostics (condition estimate + warnings for weak geometry)
  - [x] Upgrade outlier handling from fixed sigma thresholds to formal local tests with decision limits and MDB
  - [x] Expand global model test output to include chi-square critical bounds and variance-factor acceptance interval
  - [x] Add source line traceability (`sourceLine`) from parser through residual/report/export tables
  - [x] Improve exported `.txt` report to be unit-correct, sorted by blunder likelihood, and include suspect ranking
  - [x] Extend GNSS weighting from simplified isotropic XY to richer component/covariance models
  - [x] Resolve fixity/free-marker semantics mismatch (`*` meaning) between docs/parser/TODO and standard industry-standard expectations
- [x] Adjustment report results-pane cleanup plan (ordering, visibility gates, and profile trimming):
  - [x] Phase 1: make `Adjustment Summary` the first report block, remove the top outlier-analysis note, keep `Solve Profile Diagnostics` immediately under summary, then enforce section order: `Adjusted Coordinates` -> `Observations & Residuals` -> `Residual Diagnostics` -> `Setup Diagnostics` -> `Suspect Impact Analysis (what-if exclusion)`
  - [x] Phase 2: remove redundant sections `Top Suspects (ranked)` and `Setup Suspects (ranked)` from the adjustment report pane
  - [x] Phase 3: add conditional visibility gates so optional sections render only when both conditions are true: feature toggle is ON and section has applicable data (`TS Correlation Diagnostics`, `Leveling Loop Diagnostics`, `Auto Sideshot Candidates`)
  - [x] Phase 4: hide cluster actions when not applicable (do not show `Revert cluster merges` unless cluster workflow is enabled for the run context)
  - [x] Phase 5: trim `Solve Profile Diagnostics` to show only active/non-default settings (hide OFF/default rows such as TS correlation OFF, robust NONE, map-scale off/1.0, vertical/curvref none/off, zero rotation, unused lost-station and CRS/geoid details)
  - [x] Phase 6: local-job profile text cleanup: when coordinate system mode is LOCAL, do not display grid CRS/projection identifiers in the coordinate-system summary line
  - [x] Phase 7: add report-view regression tests for section ordering, removal of redundant blocks, and conditional visibility behavior
- [x] industry-standard format compatibility (Section 5.6):
  - [x] Elevation-only parsing (E records) with std errors/fixity and unit conversion
  - [~] Implement global inline options: .UNITS, .COORD 2D/3D, .ORDER NE/EN, .2D/.3D, .DELTA ON/OFF, .MAPMODE, .LWEIGHT, .NORMALIZE, .END (core parser support and baseline behavior are implemented; .LWEIGHT is applied for leveling and run headers expose active defaults; remaining work is explicit parity-audit coverage/matrixing of directive effects by observation family and edge-case code paths)
    - [x] Phase 1: build directive-application matrix by observation family to identify remaining per-code-type wiring gaps (`docs/INLINE_OPTION_APPLICATION_MATRIX.md`)
    - [x] Phase 2: close matrix-identified wiring gaps by code family (implemented `.LWEIGHT` fallback propagation for non-`L` leveling-producing paths: `V`/`DV`/`M`/`BM`/`T`/`DM`/`SS` when vertical sigma is omitted)
    - [x] Phase 3: add fixture-locked semantics coverage for each directive family and verify report/listing run-profile parity output
  - [x] Coordinate/position/elevation records: C (2D/3D with per-component std errs and !/\* fixity, NE/EN order), P geodetic positions (lat/long [+H], std errs mapped to NE, longitude sign), E elevation-only, ellipsoid variants CH/PH/EH (parsed; P/PH projected to local EN via first P origin; ellipsoid height flagged)
  - [x] Single obs: A angles (At-From-To or From-At-To), D distances (2D HD; 3D slope/HD per mode with HI/HT), V vertical (zenith or dH per mode with HI/HT), B bearings/azimuths (solved)
  - [x] Multiple obs: M (angle+dist+vertical with std err rules, HI/HT), BM (bearing/az+dist+vertical), DV (dist+vertical) with std errs and HI/HT (delta-mode emits dH; slope-mode emits zenith; face-2 weighting applied; defaults for stds/HI/HT captured)
  - [x] Traverse: TB (backsight station or bearing/az; dummy backsight handling), T legs (angle+dist+vertical with std err rules, HI/HT), TE closing angle to station or bearing/az with optional std err (basic conversion to angle/dist/vert added; mixed-face rejection when .NORMALIZE OFF; closure residuals/misclosure vectors logged)
  - [x] Direction sets: DB begin, DN directions, DM directions+measurements, DE end; face-1/face-2 handling, reject mixed-face when .NORMALIZE OFF (basic conversion to angle/dist/vert added; face-1/face-2 fixture added; face-2 weighting tweak)
  - [x] Direction sets: solve raw circle readings with per-set orientation parameters (for unreduced circle readings)
  - [x] A records auto-classify as DIR (azimuth) vs turned angle using initial coords; DIR uses closest residual with optional 180° flip
  - [x] Sideshots: SS diverted from adjustment, compute post-adjust, disallow occupy/backsight, optional name checking (basic parse to dist + vert/zenith; excluded from adjustment; occupy/backsight validation added; cannot target fixed control)
  - Leveling: L dH with distance or turns, std err or per-unit via .LWEIGHT (legacy `L` lengths now use project linear units and convert to km internally; omitted-sigma parity path uses the industry-style `sqrt(km)` weighting for `L` rows), allow fixity/free
  - [x] Fixity/weights: support ! fixed per component, legacy \* fixed, numeric std errs per obs/component (coordinate free-marker semantics are now wired and traced in report/listing outputs, observation-weight traceability now surfaces explicit/default/fixed/float handling across report/listing outputs including mixed GNSS component sources, and directive-family semantics coverage is now fixture-locked through parser + report/listing regression tests)
    - [x] Phase 1: define coordinate-record free-marker semantics and precedence relative to `!`, legacy `*`, and numeric sigma tokens
    - [x] Phase 2: implement parser support + diagnostics so per-component `*` clears fixed/weighted control constraints on `C/P/CH/E/PH/EH` records, with regression coverage
    - [x] Phase 3: finish the remaining Section 5.6 fixity/weight parity closeout and keep regression coverage fixture-locked
  - [x] Instrument library: EDM const/ppm, HZ/VA precision, instrument/target centering; .I to set current instrument
  - [x] Std error tokens (&/!/\*/numeric) for observations; .EDM additive/propagated; .CENTERING on/off; .ADDC add-centering mode
  - [x] Debug logging toggle (.DEBUG) to dump per-observation calc/residual/sigma by iteration
  - [x] Auto-drop height unknowns for stations with no vertical-sensitive observations (prevents 3D singularity)
  - Units/normalization: honor .UNITS for ft/m parsing/HI/HT, convert angles/bearings; normalize engine to meters/radians
  - Validation/logging: strict line validation, mode/order scope, HI/HT parsing, counts per type, logs for defaults/skips/flags
  - [x] Validation/logging: basic network diagnostics for under-observed stations
  - [x] Validation/logging: per-direction-set residual summaries in processing log
  - [x] Validation/logging: per-direction-set prefit summaries (using initial coords)
  - Inline modes & obs metadata: track delta mode/map mode/lon sign; capture HI/HT on distances; projection currently equirectangular (improve later)
  - Engine: map codes to internal types, apply HI/HT/dH/slope-HD, include bearings/azimuths, leveling weights, preserve obs IDs
  - [x] Engine: honor 2D solve mode (drop height parameters; skip lev/zenith in 2D)
  - Fixtures/tests: add industry-standard-style fixtures for 2D/3D, TS+GPS+Leveling, traverses, direction sets, sideshots; Vitest cases for parsing/mode toggles and solves (bearing/zenith added)
  - Docs/UI: [x] Created `docs/USER_GUIDE.md` and `public/examples/industry_demo.dat`; list supported industry-standard codes/options, UI hints, surface parser errors with line numbers

- [x] Total Station parity roadmap:
  - [x] Phase 1: add TS parity harness baseline fixture + tests for stable TS-only outputs
  - [x] Phase 1: add safer A-record interpretation with explicit `.AMODE` controls (AUTO/ANGLE/DIR)
  - [x] Phase 2: full face-set reduction workflow (face1/face2 set means, reduced set sigmas, raw-vs-reduced diagnostics)
  - [x] Phase 2: strengthen traverse/setup diagnostics (closure ratios, per-setup orientation quality, setup summaries)
  - [x] Phase 3: expand TS reduction options (.MAPMODE detail, curvature/refraction controls, vertical reduction options)
  - [x] Phase 3: post-adjusted sideshot coordinate/precision reporting section
  - [x] Phase 4: sideshot explicit azimuth support (`SS ... AZ=...`) so post-adjust SS coordinates can be computed without target approximations
  - [x] Phase 5: setup-based sideshot orientation (`SS ... HZ=/HA=/ANG=`) tied to current backsight, with propagated azimuth uncertainty
  - [x] Phase 6: add TS example pack (one realistic `.dat` per TS observation family + one all-in-one combined file in `public/examples`)
  - [x] Phase 7: add setup-level suspect diagnostics (`RMS|t|`, `Max|t|`, local-test fail counts, worst obs + source line) with ranked setup suspect tables in report/export
  - [x] Phase 8: add direction-target repeatability diagnostics (per-target raw spread, face balance, local-test/MDB, ranked suspect score) with report/export tables
  - [x] Phase 9: add multi-set direction repeatability trends by occupy-target (cross-set range/RMS, face-balance counts, suspect ranking) in report/export
  - [x] Phase 10: add automated suspect impact analysis (what-if exclusion: dSEUW, dMax|t|, chi-square change, max unknown shift, score) with one-click exclude+rerun
  - [x] Phase 11: add hover-tooltips for report column headers, key summary statistics (SEUW, chi-square), and all settings controls to improve interpretation
  - [x] Phase 12: add TS angular correlated stochastic model (`.TSCORR` on/off, scope, rho), apply correlated weights in solve/Qvv, and report/export correlation diagnostics
  - [x] Phase 13: add robust adjustment mode (`.ROBUST HUBER k`) with iterative downweight diagnostics and robust-vs-classical top-suspect comparison in report/export
  - [x] Phase 14: expand traverse closure diagnostics with linear ppm + angular/vertical misclosure checks, threshold pass/warn flags, per-loop severity ranking, and traverse suspect tables in report/export
  - [x] Phase 15: expand direction reduction diagnostics (raw max residual, face-pair delta, per-face spreads), and add structured direction reject diagnostics (line/set/record/expected-vs-actual face) in report/export
  - [x] Phase 16: add residual-quality diagnostics summary (|t| bins, local-test fail totals, redundancy weakness counts, worst-observation trace, and by-type screening table) in report/export
  - [x] Phase 17: improve 2D triangulation/trilateration compatibility by parsing 2D `M` lines with angle/dist sigma tokens (no forced vertical token) and auto-creating missing non-sideshot stations referenced in observations
  - [x] Phase 18 - automated Canadian CRS synthetic adjustment harness
    - [x] Batch 1 - authority-backed catalog foundation
      - [x] Add a dedicated machine-readable Canadian CRS test catalog for synthetic harness coverage
      - [x] Record authority code, name, family, datum/realization, projection method, axis order, units, area-of-use bounds, provenance, and current/superseded status per CRS
      - [x] Seed the catalog from the existing WebNet CRS support surface first, while explicitly tracking where the current app alias maps to an ensemble/superseded EPSG definition versus a current explicit realization
      - [x] Add a small developer report script that dumps CRS id, authority code, name, projection method, datum/realization, axis order, area of use, and source provenance
      - [x] Add focused catalog-validation tests for required metadata completeness, deterministic ordering, and source provenance presence
    - [x] Batch 2 - transform-validation and harness primitives
      - [x] Add deterministic seedable random helpers for CRS harness generation
      - [x] Add independent forward/inverse CRS round-trip tests that compare WebNet transform behavior against external `proj4`-driven projection results instead of only WebNet's own helpers
      - [x] Add catalog-driven sanity checks for axis-order handling, area-of-use bounds, and projected/geodetic round-trip tolerances
      - [x] Keep transform-validation layer isolated from the synthetic network generator and solver-under-test path
    - [x] Batch 3 - synthetic network and observation generation
      - [x] Add projected-coordinate truth-network generators for traverse, braced traverse, loop, weak leaf, and tied side-point templates
      - [x] Add synthetic observation generation for directions/angles, slope distances, zeniths, optional bearings, and control coordinates
      - [x] Support both noise-free and noisy generation modes with deterministic seeds and realistic metadata such as HI/HT and standard-error sets
      - [x] Keep generator output modular so synthetic jobs can be reused by tests, reports, and future CLI/CI harness entrypoints
      - [x] Batch 3A - add a dedicated mixed-3D template that emits `DV` slope+zenith rows so HI/HT stays shared between the paired distance and zenith observations in grid-mode CRS tests
    - [x] Batch 4 - end-to-end Canadian UTM harness
      - [x] Add noise-free end-to-end synthetic adjustment tests across Canadian UTM coverage with CRS active during solve
      - [x] Add comparison metrics for horizontal/vertical error, residual RMS, SEUW, covariance sanity, and ellipse sanity
      - [x] Add invariance checks for observation reordering, setup reordering, and point renaming
      - [x] Add machine-readable per-run summary output for UTM harness runs
      - [x] Batch 4A - pin Canadian UTM harness rows to explicit current EPSG realization codes instead of leaving legacy-app placeholder aliases in the synthetic catalog
    - [x] Batch 5 - MTM and provincial expansion
      - [x] Expand synthetic harness coverage across Canadian MTM zones with SCoPQ alias notes where relevant
      - [x] Add provincial-system synthetic tests starting with New Brunswick stereographic and Alberta 3TM families, then other supported provincial projected CRSs
      - [x] Add edge-of-area jobs near west/east/north/south bounds where applicable
      - [x] Add noisy Monte Carlo coverage with statistical tolerances and weak-versus-tied precision assertions
      - [x] Batch 5A - add Alberta 3TM realization-backed CRS rows plus focused catalog/harness regression coverage
    - [x] Batch 6 - CI, docs, and closeout
      - [x] Wire harness runs into the normal validation workflow with clear failure categorization for CRS definition, transform path, generator, solver, or covariance/reporting faults
      - [x] Add markdown and machine-readable summary artifacts grouped by CRS family
      - [x] Update docs for new harness architecture, validation commands, and supported Canadian CRS coverage status
      - [x] Keep parity workflow isolated so synthetic CRS harness complements, but does not replace, industry-reference regression coverage
  - [ ] Phase 19 - Canadian CRS coverage expansion (post-Phase-18)
    - [ ] Batch 1 - authority shortlist and ID contract (priority order lock)
      - [x] Confirm the next provincial/territorial expansion set against EPSG + provincial authority docs before coding
      - [x] Lock proposed internal CRS IDs and mapped authority codes for each candidate row
      - [x] Keep all additions NAD83(CSRS)-first unless a province requires a different operational realization
      - [x] Priority 1 shortlist confirmed (authority-backed):
        - [x] `CA_NAD83_CSRS_QC_LAMBERT` -> EPSG `3799` (`NAD83(CSRS) / MTQ Lambert`) as primary implementation target; keep EPSG `6622` (`NAD83(CSRS)v2 / Quebec Lambert`) tracked as a compatibility alias candidate if user workflows require the explicit realization-specific code
        - [x] `CA_NAD83_CSRS_NS_MTM_2010_4` -> EPSG `8082` (`NAD83(CSRS)v6 / MTM NS 2010 zone 4`)
        - [x] `CA_NAD83_CSRS_NS_MTM_2010_5` -> EPSG `8083` (`NAD83(CSRS)v6 / MTM NS 2010 zone 5`)
      - [x] Priority-1 candidate corrections from the initial draft:
        - [x] Replace draft `CA_NAD83_CSRS_NS_STEREOGRAPHIC` with Nova Scotia MTM 2010 zone-based targets (`8082`, `8083`) because the authority-backed NS operational CSRS projected targets in EPSG are MTM-zone based
        - [x] Remove draft `CA_NAD83_CSRS_NL_LAMBERT` from Priority 1 until an authority-backed NL Lambert projected CSRS target is confirmed; Newfoundland and Labrador operational coverage currently relies on already-supported UTM/MTM zones in the catalog
      - [ ] Priority 2 candidates (next provincial coverage wave):
        - [x] `CA_NAD83_CSRS_SK_ATS` (Saskatchewan ATS-style provincial projected workflow)
        - [x] `CA_NAD83_CSRS_MB_3TM` (Manitoba provincial 3TM-style workflow where used operationally)
        - [x] `CA_NAD83_CSRS_NU_STEREOGRAPHIC` (Nunavut projected workflow alias mapped to EPSG:3977 per NRCan metadata usage guidance)
      - [ ] Priority 3 candidates (specialized or lower-frequency provincial/territorial workflows):
        - [x] `CA_NAD83_CSRS_YT_TM` (Yukon territorial projected workflow)
        - [x] `CA_NAD83_CSRS_NT_TM` (Northwest Territories projected workflow)
        - [x] `CA_NAD83_CSRS_QC_MUNICIPAL_LCC` (Quebec municipal/regional LCC-style workflow if maintained as distinct contract)
      - [ ] Priority 4 candidates (remaining CSRS-only coverage from cleaned EPSG audit):
        - [x] `CA_NAD83_CSRS_AB_10TM_FOREST` -> EPSG `3402` (`NAD83(CSRS) / Alberta 10-TM (Forest)`)
        - [x] `CA_NAD83_CSRS_YT_ALBERS` -> EPSG `3579` (`NAD83(CSRS) / Yukon Albers`)
        - [x] `CA_NAD83_CSRS_NT_LAMBERT` -> EPSG `3581` (`NAD83(CSRS) / NWT Lambert`)
        - [x] `CA_NAD83_CSRS_CA_ATLAS_LAMBERT` -> EPSG `3979` (`NAD83(CSRS) / Canada Atlas Lambert`)
        - [x] `CA_NAD83_CSRS_ON_TERANET_LAMBERT` -> EPSG `5321` (`NAD83(CSRS) / Teranet Ontario Lambert`)
        - [x] `CA_NAD83_CSRS_ARCTIC_LCC_3_29` -> EPSG `6103` (`NAD83(CSRS) / EPSG Arctic zone 3-29`)
    - [ ] Batch 2 - catalog and parser/CLI support for Priority 1
      - [x] Add CRS catalog entries, alias normalization, and EPSG lookup mapping for Priority 1 rows
      - [x] Add focused catalog tests for metadata completeness, deterministic ordering, and provenance
      - [x] Add parse/CLI CRS-id acceptance tests (canonical id + EPSG alias where defined)
    - [ ] Batch 3 - synthetic harness expansion for Priority 1
      - [x] Extend synthetic harness catalog and area-of-use edge jobs for new Priority 1 rows
      - [x] Add noise-free + noisy harness coverage with current metric thresholds and perfect-mode export checks
      - [x] Keep grouped markdown/json artifact generation deterministic for all new CRS rows
    - [ ] Batch 4 - Priority 2 implementation and harness
      - [x] Implement Priority 2 catalog rows with the same parser/CLI + harness validation gates as Priority 1
      - [x] Implement `CA_NAD83_CSRS_SK_ATS` and `CA_NAD83_CSRS_MB_3TM` as provincial workflow aliases with parser/CLI + harness validation gates
      - [x] Added `CA_NAD83_CSRS_NU_STEREOGRAPHIC` as a Nunavut projected workflow alias mapped to EPSG:3977 (Canada Atlas Lambert) with parser/CLI + harness validation gates
      - [x] Add focused docs for provincial naming/alias differences from EPSG titles (SK ATS and MB 3TM aliases)
    - [x] Batch 5 - Priority 3 decision gate and closeout
      - [x] Implement only Priority 3 rows that are justified by active user workflows or interoperability requirements
      - [x] Document deferred candidates with explicit rationale when not implemented
      - [x] Re-run full validation matrix and refresh Canadian CRS coverage docs/summary counts
    - [ ] Batch 6 - Priority 4 CSRS clean-list implementation and harness
      - [x] Implement all 6 remaining CSRS-only rows from the cleaned EPSG list with canonical IDs and EPSG alias normalization
      - [x] Add parser/CLI acceptance tests and CRS catalog resolver tests for all 6 rows
      - [x] Add synthetic harness coverage (catalog presence, noisy Monte Carlo bounds, edge-of-area jobs, perfect-mode checks)
  - [ ] Phase 20 - USA State Plane CRS expansion
    - [x] Batch 1 - authority-backed foundation and Priority 1 state set
      - [x] Lock USA State Plane ID contract and EPSG alias normalization rules
      - [x] Add first Priority 1 meter-based NAD83(2011) State Plane set to CRS catalog
      - [x] Add focused parser/CLI acceptance tests for canonical IDs and EPSG aliases
      - [x] Add catalog/group-filter coverage for USA State Plane entries
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 2 - unit-paired zone expansion for active states
      - [x] Add ftUS companion CRS rows for existing New York and California meter zones
      - [x] Add Pennsylvania North/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for all new rows
      - [x] Extend focused parser/CLI/catalog coverage for newly added rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 3 - California full zone closeout
      - [x] Add California zones 4-6 meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for newly added zones
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new zones
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 4 - Texas zone rollout
      - [x] Add Texas North/North Central/Central/South Central/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new Texas zones
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for Texas zones
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 5 - Florida zone rollout
      - [x] Add Florida East/North/West meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new Florida zones
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for Florida zones
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 6 - Georgia zone rollout
      - [x] Add Georgia East/West meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new Georgia zones
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for Georgia zones
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 7 - North Carolina and Alabama rollout
      - [x] Add North Carolina statewide meter + ftUS CRS rows
      - [x] Add Alabama East/West meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 8 - Tennessee and Kentucky rollout
      - [x] Add Tennessee statewide meter + ftUS CRS rows
      - [x] Add Kentucky North/Single Zone/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 9 - Rhode Island and South Dakota rollout
      - [x] Add Rhode Island meter + ftUS CRS rows
      - [x] Add South Dakota North/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 10 - Vermont, Virginia, and Washington rollout
      - [x] Add Vermont meter + ftUS CRS rows
      - [x] Add Virginia North/South meter + ftUS CRS rows
      - [x] Add Washington North/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 11 - West Virginia, Wisconsin, and Wyoming rollout
      - [x] Add West Virginia North/South meter + ftUS CRS rows
      - [x] Add Wisconsin North/Central/South meter + ftUS CRS rows
      - [x] Add Wyoming East/East Central/West Central/West meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 12 - Utah zone rollout
      - [x] Add Utah North/Central/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 13 - Colorado zone rollout
      - [x] Add Colorado North/Central/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 14 - Connecticut and Delaware rollout + unit-aware SPCS filtering
      - [x] Add Connecticut and Delaware meter + ftUS CRS rows
      - [x] Hide `us-spcs` ftUS rows when project units are meters and hide meter rows when project units are feet
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows and filtered selection behavior
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows and unit-aware filtering
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 15 - Kansas zone rollout
      - [x] Add Kansas North/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 16 - Louisiana zone rollout
      - [x] Add Louisiana North/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 17 - Maine east/west zone rollout
      - [x] Add Maine East/West meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 18 - Maryland and Massachusetts zone rollout
      - [x] Add Maryland plus Massachusetts Island/Mainland meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 19 - Minnesota zone rollout
      - [x] Add Minnesota North/Central/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 20 - Illinois and Indiana zone rollout
      - [x] Add Illinois East/West meter + ftUS CRS rows
      - [x] Add Indiana East/West meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 21 - Mississippi and Missouri zone rollout
      - [x] Add Mississippi East/TM/West meter rows plus East/West ftUS rows
      - [x] Add Missouri East/Central/West meter rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 22 - New Jersey, Nevada, and New Mexico rollout
      - [x] Add New Jersey meter + ftUS CRS rows
      - [x] Add Nevada East/Central/West meter + ftUS CRS rows
      - [x] Add New Mexico East/Central/West meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 23 - Nebraska, New Hampshire, and Ohio rollout
      - [x] Add Nebraska meter + ftUS CRS rows
      - [x] Add New Hampshire meter + ftUS CRS rows
      - [x] Add Ohio North/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 24 - Idaho and Iowa zone rollout
      - [x] Add Idaho East/Central/West meter + ftUS CRS rows
      - [x] Add Iowa North/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 25 - Arkansas and Oklahoma zone rollout
      - [x] Add Arkansas North/South meter + ftUS CRS rows
      - [x] Add Oklahoma North/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 26 - Alaska zone rollout
      - [x] Add Alaska zones 1-10 meter CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 27 - Michigan and North Dakota meter rollout
      - [x] Add Michigan North/Central/South meter CRS rows
      - [x] Add North Dakota North/South meter CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 28 - Arizona meter zone rollout
      - [x] Add Arizona East/Central/West meter CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 29 - Oregon zone rollout
      - [x] Add Oregon North/South meter + ftUS CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary
    - [x] Batch 30 - remaining NAD83(2011) SPCS closeout
      - [x] Add Arizona East/Central/West ft CRS rows
      - [x] Add Michigan North/Central/South ft CRS rows
      - [x] Add Montana meter + ft CRS rows
      - [x] Add New York Long Island meter + ftUS CRS rows
      - [x] Add North Dakota North/South ft CRS rows
      - [x] Add Puerto Rico and Virgin Islands meter CRS row
      - [x] Add South Carolina meter + ft CRS rows
      - [x] Keep canonical ID + EPSG alias normalization deterministic for new rows
      - [x] Extend focused parser/CLI/catalog/UI filter coverage for new rows
      - [x] Run required validation matrix and update docs coverage summary

- [x] Industry Standard v6-v14 parity gaps (prioritized from release notes; missing in WebNet today):
  - [x] Conventional surveying improvements (highest impact -> lowest):
    - [x] Implement `.ALIAS` point aliasing (explicit and pattern-based alias rules) with full solve/report traceability
      - [x] Phase 1: parser/state support for `.ALIAS` (explicit map, prefix/suffix/additive patterns, validation diagnostics)
      - [x] Phase 2: station/observation identity resolution so aliases collapse to canonical point IDs before unknown-building
      - [x] Phase 3: reporting/export traceability (canonical ID + source alias references in listing/errors/find)
      - [x] Phase 4: parity tests for mixed alias scenarios (conventional/GNSS/leveling references across files)
    - [x] Add cluster-detection adjustment mode (dual-pass solve with user approval/override of point-cluster aliasing)
      - [x] Phase 1: implement post-pass cluster candidate detection (2D/3D tolerance, linkage mode, deterministic cluster keys)
      - [x] Phase 2: dual-pass solve pipeline (pass-1 detect, pass-2 apply approved merges, retain reproducible run profile)
      - [x] Phase 3: UI review/override table (approve/reject cluster merges, choose retained key point)
      - [x] Phase 4: reporting section for cluster outcomes (merged points, deltas from retained point, rejected proposals)
    - [x] Add Auto-Adjust workflow (iterative solve + automatic outlier exclusion thresholds + removed-observation listing section)
      - [x] Phase 1: solver loop controller for repeated adjust cycles (max |t| threshold, max removals/cycle, max cycles)
      - [x] Phase 2: robust outlier candidate selection policy (local-test aware, redundancy guards, tie-break consistency)
      - [x] Phase 3: run log/report output for each cycle and final "removed observations" listing with line traceability
      - [x] Phase 4: UI controls + CLI hook parity (`/autoadjust`-style options) and regression tests against fixed fixtures
    - [x] Add automatic sideshot detection for non-redundant `M` records (project option, default-on profile toggle)
      - [x] Phase 1: compute redundancy-aware sideshot candidates from solved `M` records (with safety exclusions for control/closure legs)
      - [x] Phase 2: add project/profile toggle and parser/engine plumbing to switch between legacy and auto-detect behavior
      - [x] Phase 3: expose detected/reclassified records in processing logs, report tables, and export annotations
      - [x] Phase 4: add parity fixtures to verify observation-count, chi-square, and sideshot coordinate outputs
    - [x] Implement `.PRISM` prism offset correction support in parser/weighting/residual reporting
      - [x] Phase 1: parse `.PRISM` option state (global and scoped behavior) with unit-safe validation diagnostics
      - [x] Phase 2: apply prism-offset correction in distance/vertical observation modeling before residual and weight computation
      - [x] Phase 3: show correction source/magnitude in report/export rows and add regression fixtures for offset cases
    - [x] Implement `.ROTATION <angle>` plan-rotation for bearing/azimuth observations (`B/BM` and related records)
      - [x] Phase 1: parse and track cumulative `.ROTATION` state with DMS/DD compatibility and wrap-safe normalization
      - [x] Phase 2: apply plan rotation to affected azimuth-bearing observations in parser/engine consistently
      - [x] Phase 3: add listing/profile diagnostics and parity tests for rotated vs unrotated network outputs
    - [x] Implement `.LOSTSTATIONS` handling (include in solve, optional omit/filter in plot/listing outputs)
      - [x] Phase 1: parse `.LOSTSTATIONS` and persist station metadata flags through solve results
      - [x] Phase 2: ensure lost stations remain valid network points in adjustment while tagged in diagnostics
      - [x] Phase 3: add plot/listing/export filter options to hide/show lost stations and verify with UI tests
    - [x] Add configurable fixed standard-error constants (`.QFIX`-style) instead of hard-coded fixed sigmas
      - [x] Phase 1: add project options and inline override parsing for fixed angular/linear sigma constants
      - [x] Phase 2: replace hard-coded fixed defaults in weighting logic with configured constants and profile reporting
      - [x] Phase 3: add parity tests confirming expected SEUW/residual effects under alternate fixed sigma settings
    - [x] Add inconsistent-description reconciliation (append/first behavior) plus listing summary for repeated point IDs
      - [x] Phase 1: build description consistency scanner across loaded files/records grouped by station ID
      - [x] Phase 2: implement reconciliation policies (first vs append with custom delimiter) applied to rendered outputs
      - [x] Phase 3: add listing/report summary section with file-line references for conflicts and update tests
    - [x] Add industry-style "effective distance" reporting alongside angular residuals in adjusted observation tables
      - [x] Phase 1: compute effective distance metrics for angular observation families during statistics pass
      - [x] Phase 2: add columns/formatting in report, processing summary, and industry-style export outputs
      - [x] Phase 3: add fixture assertions for value correctness across angle/direction/bearing scenarios
    - [x] Add optional 3D network plot mode (3D ellipsoids/orbit/view-cube style controls) for deeper parity with Industry Standard 10-14 visualization
      - [x] Phase 1: introduce 3D plot architecture (camera, scene graph, station/connection primitives) behind feature flag
      - [x] Phase 2: render 3D confidence ellipsoids and implement orbit/pan/zoom/view-cube interactions
      - [x] Phase 3: add mode toggle, performance fallbacks, and visual regression checks for desktop/mobile layouts
    - [x] Add CLI/batch run entrypoint for scripted adjustments (including auto-adjust flags) for automation parity
      - [x] Phase 1: add Node CLI wrapper to run parser/engine headlessly with input/profile/options arguments
      - [x] Phase 2: expose machine-readable and listing-style outputs (text/json) with deterministic exit codes
      - [x] Phase 3: add auto-adjust CLI flags, docs, and CI smoke tests for batch execution workflows
    - [x] Add preanalysis / network-planning mode for predicted precision and ellipse review
      - [x] Phase 1: add a dedicated preanalysis run mode plus planned-observation parsing/state so observations can be declared without measured values and resolved from approximate coordinates
      - [x] Phase 2: reuse the current adjustment row-building path to generate dummy observation values from approximate geometry (`L = 0`), keep needed nuisance/orientation unknowns, and build `A`/`P` without residual-driven iteration
      - [x] Phase 3: compute preanalysis covariance with a-priori unit variance (`sigma0^2 = 1`) and expose station covariance blocks plus relative covariance blocks for connected station pairs without relying on residual-based QC
      - [x] Phase 4: render predicted station standard deviations, station error ellipses, relative error ellipses, and weak-geometry cues in report/map/output views, with residual-based checks disabled in preanalysis mode
      - [x] Phase 5: add what-if planning UX for adding/removing planned observations and comparing predicted precision changes, plus CLI/docs/examples/fixture coverage for preanalysis workflows (including omitted-value `D`/`A` planning rows)
  - [x] GPS improvements (highest impact -> lowest):
  - [x] Add full coordinate-system/geodetic engine integration (project CRS selection, grid-ground factors, convergence reporting)
    - [x] Phase 1: add CRS selection/state model and projection abstraction for input/output transformations
    - [x] Phase 2: integrate grid-ground scale/convergence calculations into observation modeling and inverse tools
    - [x] Phase 3: surface CRS/scale/convergence diagnostics in reports/exports with parity fixtures
    - [x] Add direct geoid/grid model support (NGS/NRC/BYN-style models) plus input/output ellipsoid selection controls
      - [x] Phase 1: add geoid/grid file ingestion pipeline (metadata, interpolation, caching, validation)
      - [x] Phase 2: wire ellipsoid/geoid model choice into height conversions for parse/solve/export paths
      - [x] Phase 3: add project options UI + diagnostics for active model and tests against known checkpoints
    - [x] Implement `.GPS NETWORK` / `.GPS SIDESHOT` modes for vector records with post-adjust sideshot handling
      - [x] Phase 1: parse `.GPS NETWORK`/`.GPS SIDESHOT` mode state and tag incoming GNSS vectors accordingly
      - [x] Phase 2: exclude GPS sideshot vectors from adjustment while computing post-adjust coordinate/precision outputs
      - [x] Phase 3: add report/export sections and parity tests for mixed network+sideshot GPS datasets
    - [x] Implement `.GPS AddHiHt [HI] [HT]` antenna-height correction option in preprocessing
      - [x] Phase 1: parse `.GPS AddHiHt` option with scoped defaults and validation warnings
      - [x] Phase 2: apply antenna-height corrections during GNSS observation preprocessing consistently for base/rover
      - [x] Phase 3: add diagnostics and regression fixtures covering positive/negative and missing-height cases
    - [x] Add GPS vector loop-closure diagnostics/check command with loop summaries in report/export
      - [x] Phase 1: detect GNSS loop candidates and compute closure vectors/magnitudes independent of adjustment residuals
      - [x] Phase 2: add tolerance checks/ranking and expose loop diagnostics in processing and report views
      - [x] Phase 3: add export block and tests for known loop-pass/fail datasets
    - [x] Add OPUS/OPUS-RS import path with full covariance ingestion to GPS observation records
      - [x] Phase 1: implement OPUS/OPUS-RS parser for station coordinates, covariance, and metadata extraction
      - [x] Phase 2: map imported covariance into GNSS observation/control constraints with unit/axis normalization
      - [x] Phase 3: add importer UX/docs and parity tests using representative OPUS samples
    - [x] Add GPS offset observation support (G4-style rover-offset handling) in parse/adjust/report pipelines
      - [x] Phase 1: extend parser/types for rover-offset observation records and associated orientation metadata
      - [x] Phase 2: apply offset transformation into effective GNSS vectors in adjustment equations
      - [x] Phase 3: add report/export transparency for applied offsets and validate via dedicated fixtures
    - [x] Add LandXML export for adjusted coordinates/connections
      - [x] Phase 1: define LandXML export schema mapping for stations, observations, ellipses, and metadata
      - [x] Phase 2: build deterministic serializer with unit/profile annotations and file-write UX
      - [x] Phase 3: add round-trip/interoperability checks against common LandXML consumers
    - [x] Add native field-data importer pipeline parity for major Industry Standard sources (JobXML/DBX/FieldGenius/Carlson/TDS) instead of `.dat`-only workflows
      - [x] Phase 1: design importer plugin interface and normalized intermediate observation model
      - [x] Phase 2: implement first-party JobXML + FieldGenius importers with error-log traceability, including JobXML setup-context station/measurement conversion when occupy/backsight/target references can be resolved
      - [x] Phase 3: implement DBX/Carlson/TDS importers with converter-option parity where practical
      - [x] Phase 3A: improve industry-software-style JobXML station-setup imports (`Reductions -> Point`, face-circle backsights, `TheodoliteHeight`, `MTA` preference, deleted-shot filtering) so exported S-series datasets import as usable TS observations instead of mostly duplicate reduced points
      - [x] Phase 4: integrate staged import UI workflow and add fixture-based conformance tests
      - [x] Phase 4A: add import-review staging model so external imports open in a review dialog instead of writing directly into the input editor
      - [x] Phase 4B: build grouped import-review UI with control block at top, setup/station grouping where available, per-row source type/source line visibility, include-exclude toggles, and warning/error section
      - [x] Phase 4C: support editable/importable comment rows (for example `# CONTROL`, `# SETUP 1`, custom user notes) and serialize only included reviewed rows into clean editor text without raw trace comments
      - [x] Phase 4D: add importer grouping metadata/helpers across JobXML/FieldGenius/Carlson/TDS/DBX so setup-aware sources can render meaningful group labels in the import-review UI
      - [x] Phase 4E: add regression coverage for staged import review, exclusion behavior, and clean final text generation
      - [x] Phase 4F: add row-level editable imported text plus output-style presets (`Clean WebNet`, `Field Grouped`, `TS Direction Set`) so users can tune final grouped import text before committing it to the editor
      - [x] Phase 4G: preserve raw JobXML direct readings alongside MTA rows, carry setup/shot metadata through import review, and only classify true JobXML `StandardResection` setups as resection blocks so grouped presets match field reports more closely
      - [x] Phase 4H: add staged-row bulk exclude toggles for `MTA` vs raw imported observations, plus row-level duplicate/comment/move actions and smarter `Field Grouped` subheaders for setup-style review output
      - [x] Phase 4I: tune `Field Grouped` ordering toward setup/target-style field output and add row actions to spin selected staged rows into brand-new setup groups before final import
      - [x] Phase 4J: tune the `TS Direction Set` preset toward cleaner `DB/DN/DM/DE` + `D/M/A` setup output and add richer setup-editing controls (inline setup labels plus empty setup-group staging)
      - [x] Phase 4K: emit imported zenith values in DMS instead of rounded decimal degrees, use target-aware ordering in `TS Direction Set` groups by default, and add manual row up/down reordering within a setup group
      - [x] Phase 4L: add import-review fixed toggles plus per-row type overrides for final import shaping, simplify source-line display to line numbers only, and keep 2D slope-distance imports reducing to HD when a zenith is available
      - [x] Phase 4M: expand import-review row-type overrides with explicit `D`/`DV`/`DN`/`DM` setup-aware shaping for JobXML imports and add industry software survey-report `.htm` fallback importing for station/shot tables when no `.jxl` is available
      - [x] Phase 4N: add `.jxl`/`.htm` to the default import picker filter, add compare/reconcile review support for pairing JobXML with survey-report imports, and carry JobXML angle-only verticals so `UNDERT2.jxl` and `UNDERT2.htm` reconcile to the same comparable observation count when `MTA` rows are excluded
      - [x] Phase 4O: add per-setup staged exclude toggles in import review, emit imported `V` rows as hyphenated `from-to` tokens, and lock parser/import-review coverage for hyphenated vertical observation formatting
      - [x] Phase 4P: add compare-mode presets (`Non-MTA Only` vs `All Raw Rows`), highlight staged rows that belong to mismatched comparison buckets, and expand the input-editor context menu with standard edit actions above `Toggle # Comment`
      - [x] Phase 4Q: split editor comment actions into explicit `Block Comment` / `Block Uncomment`, preserve native undo/redo history for comment/paste/delete/cut menu edits, and make `Tab` insert a literal tab in the input textarea
      - [x] Phase 4R: add import-time angle-mode selection for `.jxl/.jobxml/.htm/.html` (`Raw Angles` vs `Reduced Angles (BS = 0)` before parsing), and add an import-review action to convert all SD+zenith observations to HD-only 2D output (strip HI/HT, strip control Z/elevation fields, emit `.2D`)
  - [x] Leveling improvements (highest impact -> lowest):
    - [x] Add dedicated differential leveling loop-closure check workflow (independent of traverse diagnostics)
      - [x] Phase 1: detect and enumerate leveling loops from `L`/level-sensitive records with station-path traceability
      - [x] Phase 2: compute closure and misclosure statistics per loop independent of TS traverse metrics
      - [x] Phase 3: add dedicated leveling loop section in processing/report/export with ranked suspect loops
    - [x] Add level-loop tolerance checks (length-based configurable criteria with pass/warn flags)
      - [x] Phase 1: add configurable tolerance model (e.g., k\*sqrt(Km), fixed constants) in project options
      - [x] Phase 2: evaluate each loop against configured tolerances and produce pass/warn/fail flags
      - [x] Phase 3: include tolerance outcomes in listing/export and pin behavior with tolerance fixture tests
    - [x] Add detailed level-loop report blocks (total loop length, per-segment lengths, closure/tolerance outcomes)
      - [x] Phase 1: compute and persist per-segment and total loop lengths for leveling loops
      - [x] Phase 2: add detailed report tables and industry-style listing rows for leveling loop details
      - [x] Phase 3: add export formatting and regression tests for report structure/content parity
    - [x] Add level-loop preset shortcuts and segment-level QA ranking
      - [x] Phase 1: add built-in level-loop tolerance presets in Project Options for quick switching between common screening models
      - [x] Phase 2: aggregate suspect segments across warn loops and surface ranked segment suspects in report/summary/listing outputs
- [x] Survey CAD parcel auto closed-frontage ring batch (July 2026): make all-boundary frontage selection create a continuous max-depth parcel ring with corner transition lots and no empty frontage pockets, keeping generated lots at four or more property lines.
- [x] Survey CAD parcel auto ring-depth batch (July 2026): make full-boundary auto-layout ring lots perpendicular to frontage through straight runs, reserve only 1-2 frontage widths for corner transition lots, and solve lot depth from minimum area instead of always using maximum depth.
- [x] Survey CAD parcel auto ring continuity batch (July 2026): stop closed-boundary ring generation from dropping straight-run lots and make corner-lot rear lines connect cleanly to adjacent straight-run rear lot lines.
- [x] Survey CAD parcel auto corner-depth batch (July 2026): align closed-boundary corner lot rear endpoints to the adjacent straight-run solved depth instead of using deeper minimum-frontage depth.
- [x] Survey CAD parcel auto corner-split batch (July 2026): split closed-boundary corner transition lots from parent corner to rear-line midpoint when both resulting lots pass layout minimums.
- [x] Survey CAD parcel auto corner-absorb batch (July 2026): when a closed-boundary corner transition lot cannot satisfy large layout minimums, absorb its wedge into the two adjacent straight-run lots instead of leaving a failed corner parcel.
- [x] Survey CAD parcel auto remainder-alignment batch (July 2026): build the closed-boundary remainder from the final generated lot rear edges so the dashed parent remainder follows absorbed and split corner lots exactly.
- [x] Field observation record split batch 1 (July 2026): split oversized `src/engine/parseFieldObservationRecords.ts` by record family so the root dispatcher is near 400 lines or less, preserving SS, GS, G/G0-G4, and L parsing, ambiguity diagnostics, covariance state, sigma resolution, and deterministic observation order.
- [x] Report view split review (July 2026): reviewed `src/components/ReportView.tsx` and deferred further splitting because the remaining oversized root is already a section composer over split report modules; another split would mostly add artificial prop-bag layers with high UI regression risk.
- [x] Project options modal controller split batch 1 (July 2026): split oversized `src/hooks/useProjectOptionsModalController.ts` into focused controller helpers so the root hook is near 400 lines or less, preserving staged draft state, option setters, geoid/geodesy settings, and validation behavior.
- [x] Project file split batch 1 (July 2026): split oversized `src/engine/projectFile.ts` into focused serialization/normalization modules so the root project-file API is near 400 lines or less, preserving project open/save JSON shape, parse settings, instruments, source files, and validation behavior.
- [x] App controller split review (July 2026): reviewed `src/hooks/useAppController.ts` and deferred further splitting because it is app-level composition over already-split hooks; another split would mostly move a huge return object or add prop-bag plumbing without reducing functional ownership.
- [x] Adjust solve workflow split batch 1 (July 2026): split oversized `src/engine/adjustSolveWorkflow.ts` into focused solver workflow helpers so the root workflow is near 400 lines or less, preserving matrix assembly, constraints, diagnostics, and deterministic solve output.
- [x] Geodesy split batch 1 (July 2026): split oversized `src/engine/geodesy.ts` into focused constants, ECEF/covariance, projection, and factor helpers while preserving CRS projection, inverse, and grid-factor behavior.
- [x] Survey CAD command line split batch 1 (July 2026): split oversized `src/components/surveyCad/SurveyCadCommandLine.tsx` into focused prop contracts, constants, and dropdown menu components while preserving CAD toolbar commands and disabled states.
- [x] Map SVG 2D split batch 1 (July 2026): split oversized `src/components/mapView/MapViewSvg2d.tsx` into focused layer modules while preserving SVG map rendering, selections, planning overlays, labels, and perf counters.
- [x] Importer shared split batch 1 (July 2026): split oversized `src/engine/importers/shared.ts` into focused text, markup, and dataset conversion helpers while preserving importer compatibility exports and generated WebNet input.
- [x] Map WebGL 2D split batch 1 (July 2026): split oversized `src/components/mapView/mapViewWebgl2d.ts` into focused shader/program and vertex-buffer helpers while preserving WebGL map draw behavior and metrics.
- [x] Adjustment project options tab split batch 1 (July 2026): split oversized `src/components/projectOptions/tabs/AdjustmentProjectOptionsTab.tsx` into focused solver, geodetic framework, and coordinate-system cards while preserving option controls and draft updates.
- [x] CLI split batch 1 (July 2026): split oversized `src/cli.ts` argument parsing into a focused CLI args module while preserving batch solve options, output modes, and exit codes.
- [x] Geoid split batch 1 (July 2026): split oversized `src/engine/geoid.ts` into focused model types, grid helpers, and external GTX/BYN parser modules while preserving geoid load, cache, and interpolation behavior.
- [x] Industry listing split batch 8 (July 2026): reduce oversized `src/engine/industryListing.ts` by extracting derived listing context and adjusted-observation workflow helpers while preserving listing wording, ordering, and parity behavior.
- [x] Report view split batch 7 (July 2026): reduce oversized src/components/ReportView.tsx by extracting focused workflow/section helpers while preserving report wording, ordering, filters, and layout behavior.
- [x] App controller assessment (July 2026): leave oversized `src/hooks/useAppController.ts` for now because it is a composition root over already-focused hooks; splitting this batch would mainly add pass-through prop plumbing rather than a clearer responsibility boundary.
- [x] Adjusted points export split batch 1 (July 2026): reduce oversized `src/engine/adjustedPointsExport.ts` by extracting focused export formatting/settings helpers while preserving adjusted point column ordering, transforms, and file output behavior.
- [x] Direct run pipeline split batch 1 (July 2026): reduce oversized `src/engine/directRunPipeline.ts` by extracting focused run, compare, and export helpers while preserving direct-run outputs, diagnostics, and exit behavior.
- [x] Adjustment workflow split batch 1 (July 2026): reduce oversized `src/hooks/useAdjustmentWorkflow.ts` by extracting focused workflow helpers while preserving run-session state, errors, profiles, and callback behavior.
- [x] Legacy corpus harness split batch 1 (July 2026): reduce oversized `src/legacyCorpusHarness.ts` by extracting focused comparison/report helpers while preserving corpus diff behavior and deterministic output.
- [x] Survey CAD parcel layout workflow split batch 2 (July 2026): reduce oversized `src/components/useSurveyCadParcelLayoutWorkflow.ts` by extracting focused layout derivation/action helpers while preserving parcel preview, commit, and warning behavior.
- [x] Import conflict review split batch 1 (July 2026): reduce oversized `src/engine/importConflictReview.ts` by extracting focused conflict analysis helpers while preserving review decisions, diagnostics, and deterministic ordering.
- [x] Industry listing relationship precision split batch 1 (July 2026): reduce oversized `src/engine/industryListingRelationshipPrecision.ts` by extracting focused rendering/model helpers while preserving relationship precision wording, ordering, and parity output.
- [x] Processing summary view split batch 1 (July 2026): reduce oversized `src/components/ProcessingSummaryView.tsx` by extracting focused summary sections while preserving import/processing summary wording and layout behavior.
- [x] Direction set workflow split batch 1 (July 2026): reduce oversized `src/engine/parseDirectionSetWorkflow.ts` by extracting focused normalization/diagnostic helpers while preserving direction-set observations, warnings, and deterministic ordering.
- [x] CAD parcel closed-boundary split batch 1 (July 2026): reduce oversized `src/engine/cad/cadCogoParcelClosedBoundary.ts` by extracting focused boundary validation/geometry helpers while preserving closed-boundary parcel results and diagnostics.
- [x] Other files project options tab split batch 1 (July 2026): reduce oversized `src/components/projectOptions/tabs/OtherFilesProjectOptionsTab.tsx` by extracting focused output/export/transform cards while preserving project file controls and wording.
- [x] Matrix helper split batch 1 (July 2026): reduce oversized `src/engine/matrix.ts` by extracting matrix types, sparse helpers, Cholesky helpers, and LDLT helpers while preserving public `./matrix` exports and numeric behavior.
- [x] Survey CAD typed submit split batch 1 (July 2026): reduce oversized `src/hooks/surveyCad/useSurveyCadTypedSubmit.ts` by extracting typed command submit handlers by command group while preserving dispatcher order and result wording.
- [x] CAD parcel geometry split batch 1 (July 2026): reduce oversized `src/engine/cad/cadCogoParcelGeometry.ts` by extracting parcel geometry types, polygon overlap helpers, source-draft assembly, and closure/report summaries while preserving public exports and CAD diagnostics behavior.
- [x] Adjustment reduction helper split batch 1 (July 2026): reduce oversized `src/engine/adjustReductionHelpers.ts` by extracting reduction types, raw traverse correction capture, distance scaling/modeling, and zenith reduction helpers while preserving public exports and parity-sensitive numeric behavior.
- [x] CAD frontage reference auto-layout split batch 1 (July 2026): reduce oversized `src/engine/cad/cadCogoParcelFrontageReferenceAutoLayout.ts` by extracting remainder-junction infill selection and segment trim helpers while preserving chained frontage auto-layout behavior.
- [x] Survey CAD preview split batch 1 (July 2026): reduce oversized `src/components/surveyCad/SurveyCadPreview.tsx` by extracting the SVG canvas/render layer while preserving interaction handlers, snap behavior, and overlay layout.
- [x] Survey CAD traverse draft panel split batch 1 (July 2026): reduce oversized `src/components/surveyCad/SurveyCadTraverseDraftPanel.tsx` by extracting props/classes plus adjustment and sideshot sections while preserving panel wording and controls.
- [x] CAD spatial index split batch 1 (July 2026): reduce oversized `src/engine/cad/cadSpatialIndex.ts` by extracting exact/apparent intersection candidate builders while preserving snap ranking and construction behavior.
- [x] Project workspace split batch 1 (July 2026): reduce oversized `src/engine/projectWorkspace.ts` by extracting workspace manifest types and clone helpers while preserving public project workspace exports.
- [x] CAD alignment split batch 1 (July 2026): reduce oversized `src/engine/cad/cadAlignment.ts` by extracting alignment types, shared element geometry, and alignment draft traversal while preserving public alignment exports.
- [x] Browser exports split batch 1 (July 2026): split oversized `src/engine/browserExports.ts` into residual CSV and network GeoJSON modules while preserving public browser export names, units, deterministic ordering, and export text.
- [x] QA workflow split batch 1 (July 2026): split oversized `src/engine/qaWorkflow.ts` into contracts, snapshot/fingerprint helpers, derived QA references, and run comparison builders while preserving public QA workflow exports and deterministic ordering.
- [x] Map view 2D helper split batch 1 (July 2026): split oversized `src/components/mapView/mapView2d.ts` into 2D map types, projection/base-geometry helpers, and derived visibility state while preserving existing map helper exports and interaction behavior.
- [x] CAD curve geometry split batch 1 (July 2026): split oversized `src/engine/cad/cadGeometryCurves.ts` into curve metrics/core helpers, arc primitives, arc construction helpers, and tangent-curve builders while preserving public CAD geometry exports.
- [x] GPS observation model split batch 1 (July 2026): split oversized `src/engine/adjustGpsObservationModel.ts` into weighting/covariance, observed-vector, modeled-vector, and AddHiHt diagnostics modules while preserving solver math and diagnostics text.
- [x] Synthetic observation generator split batch 1 (July 2026): split oversized `src/engine/generateSyntheticObservations.ts` into generator types, geometry helpers, template routing, and render/rename helpers while preserving deterministic synthetic inputs.
- [x] Report diagnostic panels split batch 1 (July 2026): split oversized `src/components/report/ReportDiagnosticPanels.tsx` into shared panel contracts/header plus focused auto-adjust, auto-sideshot, setup, and GPS rover offset panels while preserving report rendering and wording.
- [x] RW5 importer split batch 1 (July 2026): split oversized `src/engine/importers/rw5Importer.ts` by extracting RW5 line parsing, detection, field helpers, station conversion, and shot payload helpers while preserving Carlson/TDS import output and trace wording.
- [x] CAD linked entities split batch 1 (July 2026): split oversized `src/engine/cad/cadTransactionsLinkedEntities.ts` into point reference sync, arc support sync, and copied dependent entity helpers while preserving CAD transaction behavior.
- [x] CAD traverse transaction split batch 1 (July 2026): split oversized `src/engine/cad/cadTransactionsTraverseCommands.ts` into named point helpers plus focused PLINE, TRAVERSE, and BATCH_COGO command modules while preserving transaction labels, reports, and selection behavior.
- [x] CAD parcel layout transaction split batch 1 (July 2026): split oversized `src/engine/cad/cadTransactionsParcelLayoutCommands.ts` into frontage resolver plus focused split and auto-layout command modules while preserving parcel layout transaction behavior and reports.
- [x] Workspace recovery test split batch 1 (July 2026): split oversized `tests/workspace_recovery.test.tsx` into shared recovery test support plus bounded startup/import, draft persistence, and disabled/error spec chunks while preserving browser recovery, import-review, project-load, disable, quota, and size-limit coverage.
- [x] Industry output view test split batch 1 (July 2026): split oversized `tests/industry_output_view.test.tsx` into shared output-view test support plus bounded context-menu and scroll-restoration spec chunks while preserving source-line, menu, section, sort, scroll, transient reset, and heading coverage.
- [x] Project options modal controller spec split batch 1 (August 2026): split oversized `tests/project_options_modal_controller.test.tsx` into shared controller test support plus bounded solve-profile and transform-scope spec chunks while preserving draft update, filtered selection, and OK/Cancel coverage.
- [x] Browser large-project benchmark split batch 1 (August 2026): split oversized `tests/browser_large_project_benchmark.test.tsx` into shared benchmark fixture/input/render support while preserving solve, rerun cache, artifact, QA, report, and map guardrail coverage.
- [x] Export workflow state test split batch 1 (August 2026): split oversized `tests/export_workflow_state.test.tsx` into shared export harness support plus bounded picker and bundle spec chunks while preserving blocked transform, LandXML, CSV, GeoJSON, and QA bundle coverage.
- [x] Project storage spec split batch 1 (August 2026): split oversized `tests/project_storage.spec.ts` into shared IndexedDB/OPFS fake support plus bounded status and reopen spec chunks while preserving availability, persistence, OPFS, and recent-project recency coverage.
- [x] Import conflict review test split batch 1 (August 2026): split oversized `tests/import_conflict_review.test.ts` into shared parse-settings support plus bounded conflict summary and resolved text spec chunks while preserving collision, default, reconciliation, rename, keep-both, and multi-source coverage.
- [x] Adjusted points export spec split batch 1 (August 2026): split oversized `tests/adjusted_points_export.spec.ts` into shared result fixture plus bounded formatting and transform spec chunks while preserving preset, delimiter, lost-station, transform, chain-order, and validation coverage.
- [x] Map basemap browser perf split batch 1 (August 2026): split oversized `tests-browser/map-basemap-perf.spec.ts` into shared browser perf harness support while preserving OSM tile mocking, pan/zoom sweeps, action capture, and hotspot assertions.
- [x] Input pane project files test split batch 1 (August 2026): split oversized `tests/input_pane_project_files.test.tsx` into shared render/file fixture support while preserving project-files button, popover, tab, action, and editor windowing coverage.
- [x] CLI phase 2 output test split batch 1 (August 2026): split oversized `tests/cli_phase2_output.test.ts` into shared CLI runner support plus bounded output/options and include-resolution specs while preserving status codes, output modes, run modes, CRS/geoid flags, and include diagnostics coverage.
- [x] Camp design map profile test split batch 1 (August 2026): split oversized `tests/map_view_camp_design_profile.test.tsx` into shared map profiling harness support while preserving renderer sweeps, perf summaries, and hotspot assertions.
- [x] Adjust bootstrap helper split batch 1 (August 2026): split oversized `src/engine/adjustBootstrapHelpers.ts` by extracting bootstrap pair metric aggregation while preserving traverse bootstrap exports and station seeding behavior.
- [x] CAD fillet general helper split batch 1 (August 2026): split oversized `src/engine/cad/cadTransactionsFilletGeneralHelpers.ts` by extracting fillet types while preserving public helper exports and fillet transaction behavior.
- [x] CAD properties test split batch 1 (August 2026): split oversized `tests/cad_properties.test.ts` by extracting the base CAD project fixture while preserving property panel coverage.
- [x] App controller split batch 1 (August 2026): carefully split oversized `src/hooks/useAppController.ts` into focused controller state/ref wiring while preserving the public `useAppController` return shape and app workflow behavior.
- [x] Permanent example projects and lean export batch (August 2026): add Pre-analysis, Combined, and Combined (split files) as built-in project-file examples above Recent Local Projects, and keep exported project settings lean by excluding refetchable OSM obstacle/cache data plus moving Survey CAD state into a sidecar artifact.
- [x] Project export polish and split-example parity batch (August 2026): keep UI theme local during project import/export, name exported `.wnproj` and source files from visible project/file names, and rebuild Combined (split files) from the Combined source-of-truth input/settings.
- [x] Persistent Locate picker tab + drill/Recall touch-ups (2026-09-05, completed and validated): one persistent picker tab per 10-question sprint (ephemeral sprint id, `picker-ready`/`picker-context`/`picker-sprint-ended` control channel, 2s heartbeat/6s loss detection, fresh token per item with no second `window.open`, Library adopts context with banner/search-clear/sent-reset/waiting/ended states); drill Related chips open Learn via `openStudyUrlNewTab` preserving drill state; Recall Content V1 `recall:A-SURV-03:1` override pinned to the exact integrated-area wording (hash `9c07b1b1…`, `recall-content-v1-review.md` regenerated). No curriculum/DB/schema/Mock/Recognition changes. Focused suites green (persistent-picker 7, library-picker patch, recall-content-v1, locate picker/flow/channel, library picker actions, exam-prep UI incl. drill chip), lint + typecheck clean. Docs: CURRENT_BEHAVIOR, this file.
- [ ] Locate picker live-use follow-up: make repeated same-token picker context adoption idempotent and add a same-tab overlay picker while preserving the secure persistent new-tab fallback.
- [ ] Phase 13E linked-sync correctness hardening (bounded to sync/wiring/tests/docs; no math/tolerance/catalog redesign): (1) wire successful non-preanalysis adjustments to the active linked CAD drawing via onSuccessfulAdjustmentRun subscriber; (2) restrict rerun delta comparison to F2F points; (3) gate auto-sync on sourceKind==='adjustment' + real adjustment-link stamping via generation args; (4) split regeneration.ts rerun-sync into linkedRerunSync.ts with compat re-exports; (5) true set equality + dedupe; (6) settingsFingerprint through outcome callback; (7) stale status + preview-required banner in F2F UI; (8) link.sourceRecordIds from point records only; (9) MANUAL_CONFLICT recomputed (not cleared) on bit-identical reruns and confirmed regens while linked MANUAL_OVERRIDE entities remain (DETACHED silent), via shared hasLinkedManualOverrides helper; (10) reachable production adjustment-link flow (explicit panel opt-in commit overlaid with run coords, threaded adjustmentSource end-to-end) + catalog-edit staleness (edit-time CATALOG_CHANGED/FEATURE_METADATA_CHANGED stamps, sync preservation) with production integration tests; (11) shared authoritativeCoordinatesOf for commit (adjusted-wins/sideshots-fill, full-coverage gate refusing partial linked commits), unused-vi lint fix; workspace avoids linkedRerunSync import edge that tripped the cadCogoParcel* star-export cycle (leaf-only imports + comment).
- [x] Phase 1 standalone Study Tauri foundation (no adjustment numerical/C++/WASM behavior changes): relocated `src/study` to `study-desktop/src` (one source of truth, no duplication) with host `/study` transitional shim (`src/study/StudyApp.tsx`); Study no longer imports `src/engine/projectStorageOpfs` (generic OPFS helpers moved to `study-desktop/src/opfsStorage.ts`); minimal Tauri 2 Rust shell/config (`study-desktop/src-tauri/`), standalone Vite/TS/package boundary on Study runtime deps only, shared curriculum asset kept at same relative depth; Study tests/scripts imports rewired in place (host agent tier still runs `tests/study`; standalone `npm test` delegates there); classifier (`study-desktop/` safe-only) and `docs/STUDY_MODULE.md`/`docs/ARCHITECTURE.md` updated. No persistence/native migration, behavior preserved.
- [x] Phase 12H.2 enable multi-file static GNSS projects default ON (branch feat/static-gnss-multifile-default-on, baseline origin/main 0e97da2b = PR #48 merge; SMALL rollout/hardening — one-boolean flip gnssMultifileEnabled false->true, kill switch retained, no math/covariance/12D/R2B-eligibility/tolerance changes, no free-network/datum-transforms/epoch/RINEX/terrestrial-mixing). Scope: §§1-10 default flip + switch preserve + one-source parity + multi-source auto + frozen contracts; §11 real-browser A-K; §§12-14 UX polish; §15 portable cross-platform gate; §16 save/reopen golden; §17 commercial smoke (A0 2.100053/A 1.102511/B0 1.965038); §§18-20 R2B/12D/provenance; §21 perf; §22 stress; §23 legacy; §24 rollback; §25 docs; §26 no-math-diff; §§27-30 acceptance/decision/PR/report.
  - [x] Worker B (12H.2) real-browser E2E + smoke + perf + legacy + validation DONE: tests-browser/gnss-multifile-workflow.spec.ts (NEW, 11/11 real-Chromium PASS §11 A-K: READY+adjust, disable/recompose/re-enable, reorder invariance, frame-BLOCKED, conflict-BLOCKED, STRONG-BLOCKED, POSSIBLE/REVERSED warn+retain+solve, save/reload across real page.reload, R2B native-sparse-selected-qxx stub parity, kill-switch OFF typescript-dense parity, OFF fail-closed + Phase12G UI regression) + scripts/gnss/gnssMultifileSplitEvidence.ts (removed redundant explicit enable + stale DEFAULT-OFF comment; default-ON proof, pins A0 2.100053/A 1.102511/B0 1.965038 HOLD, report byte-identical regen) + perf (1k: 23ms total, 10k: 89ms, 50k: 668ms; per-baseline ratio <3x, linear) + legacy focused 382/382 (.INCLUDE/bundles/gnssBaseline/session) + validation (lint 0 errors/2 pre-existing warnings, typecheck clean, test:agent 3311 pass + 3 pre-existing study-desktop fails re-proven via stash, test:wasm 54/54, parity 25/25, build ok). No flag/composer math/contract changes; no UI redesign; no vendor files committed.
- [x] Phase 12J.5 raw-GNSS survey weighting + session-dependence audit (branch feat/gnss-raw-survey-weighting-audit, baseline origin/main f0307938 = PR #57 merge; EVIDENCE/ARCHITECTURE ONLY — no auto-ingest, no TBC-fitting, no adjustment/R2B/free-network/Phase12D math changes, no multi-GNSS production, no silent antenna substitution). STATUS: DONE 2026-09-14 — formal-vs-survey frozen; ranked modern corpus (IGS triangle fetch-first + field spec, nothing fetched/committed); ANTEX audited to source (exact-match engine support, REVIEW_ONLY verdict — 60MB atx vs 32MiB cap needs subset extraction); SP3 path audited (preflight is load-bearing guard); new gnssStochasticEvidence.ts (354 lines, 14/14: repeats, scale ratios, ENU, normalized-T χ²-3DOF, loop closure, F/S/ENU/CL candidates) + gnssRawSession.ts (277 lines, 13/13: dependency groups, spanning shortlist, review table, v1-compat pinned); all-pairs independence REJECTED, spanning-tree recommended; decisions MORE-FIELD-EVIDENCE + NO-GO-DIRECT-INGEST; reviewer APPROVE. Validation: 548/548 (49 files), lint 0 errors, typecheck clean except pre-existing phase9l dirt, build clean, src/ tracked-diff empty. Report reports/gnss/phase12j5-survey-weighting-audit.md, acceptance A–R all PASS.
- [x] Phase 12J.8 medium-baseline GNSS covariance confirmation (branch feat/gnss-raw-covariance-confirmation, baseline origin/main cd221b55 = PR #60 merge). STATUS: STARTED 2026-09-14 — pre-registered evidence plan reports/gnss/phase12j8-evidence-plan.md frozen before new data (models F/S/SD/SL/ENU/CL, median estimator, FIT DOY124-130 / VAL 131-134 / TEST 135-138, matched-pair + >=10-loop design, CL/SL identifiability gates). EVIDENCE ONLY. No direct ingest, no production covariance, no math/R2B/free-network changes. Next: processing + FIT/VAL/TEST + report reports/gnss/phase12j8-covariance-confirmation.md. STAGE 2b DONE 2026-09-14 (87456fd5): FIT/VAL/TEST via scripts/gnss/gnss12j8Analyze.py + gnss12j8Models.py + gnss12j8SessionGraph.ts (12J.7 files untouched); F rejected 100% everywhere; S=18.111 replicates 12J.7 17.2 within ~5% (FIT n=107); VAL selects S (med 3.77/exc95 0.265; wins H/V balance, loop consistency, simplicity over ENU/CL); TEST once med 3.77 exact generalization; 15/15 primary loops (S exc95 0.0); EIJS-WARE SSC oracle -29.6mm; 61mm-outlier cause found (WERB-WARE-124-h12-brdc, 8 sats, 9/121 fixed epochs); graph SPANNING_TREE_SUFFICIENT_INITIAL (5 sessions); CERTIFICATION=REVIEW_ONLY_FINAL (tails ~26%, daily s 10-25, TIT2 59km residual 8.6x); ANTEX REVIEW_ONLY; DIRECT_INGEST NO; report reports/gnss/phase12j8-covariance-confirmation.md. STAGE 2a DONE 2026-09-14: 685-job matrix via scripts/gnss/gnss12j8Process.sh (PAR=2 wall 4:12.6, RSS 104MB; probe 12-run PAR1 6.45s / PAR4 1.64s) + scripts/gnss/gnss12j8Parse.py -> work12j8/solutions.json (local): 643 FIXED (417 1h), all prec baselines >=30 (TGRN-WARE 135, VOER-WARE 133, WERB-WARE 83, EIJS-WARE 75, TIT2-VOER 71, TGRN-VOER 60); 24/25 NODATA are WERB DOY137-138 (partial files) + 130-h18; brdc 15/1/2; report reports/gnss/phase12j8-stage2a-processing.md. STAGE 1 DONE 2026-09-14: corpus ~/Downloads/webnet-gnss-medium/belgian-12j8 (WARE/TGRN/VOER/EIJS/TIT2 15/15, WERB 11/15 with DOY133-136 404-confirmed outage, 15 SP3 + 15 BRDC RINEX-4 + 6 sitelogs + EPN SSC oracle), hardware single-interval verified all 6, ANTEX subset c0eb7a8b + full-vs-subset .pos byte-identical parity (WARE-TGRN DOY124 h00 prec), manifest reports/gnss/MANIFEST-12j8.sha256 + inventory reports/gnss/corpus-inventory-12j8.json + scripts/gnss/gnss12j8CorpusInventory.mjs. STAGE 2c DONE 2026-09-14 (independent-review fixes, evidence only, no src/): CL ppm slope-unit fix (mm/km two-pass grid aH=0/aV=11mm bH=0.5/bV=0.3 FIT med 2.37; VAL med 2.93/exc95 0.163, per-length 4.4x, still rejected) + VAL-only loop selection (n=4, all exc 0.0; all-15 post-selection confirmation S 1.31/0.0) + EIJS-WARE-124-h00 full-vs-subset ANTEX parity byte-identical (pos 35d89452) with mission-F=GAP on single-leg shared-type coverage; selection S stands, REVIEW_ONLY_FINAL. MERGED PR #61 (374aff42).
- [x] Phase 12J.9 raw GNSS session review + ANTEX production hardening (branch feat/gnss-raw-session-review, baseline origin/main 374aff42 = PR #61 merge). STATUS: STARTED 2026-09-14 — multi-receiver session intake (2-20 obs + NAV + opt SP3/ANTEX), deterministic occupation identity, overlap/common-window session detection, duplicate detection, STAR/MST/manual spanning-tree (N-1, validated), per-edge common-window + AUTO interval, production ANTEX subset utility + full/subset parity, bounded PAR=2 session worker pool, failure isolation + tree repair, session review UI, webnet-raw-static-session/1 export + reopen, NO adjustment ingest, covariance contract REVIEW_ONLY_FINAL frozen. Baseline validation: lint 0 errors, typecheck clean. STATUS: IMPLEMENTATION COMPLETE 2026-09-14, review/PR pending
  - Independent review: APPROVE-WITH-NITS (2 REJECT rounds cleared, nits non-blocking)
  - PR #62 feat/gnss-raw-session-review -> main (no auto-merge). STATUS: MERGED 2026-09-14 as 58d7cbed. CI CLOSURE: b9baa605/e0271b49 red runs failed ONLY on branch-new tests/gnssRaw/gnssRawSessionPanel.test.tsx > stages fixtures into inventory with a deterministic STAR tree (AssertionError expected '' to contain 'SYNB', 1 failed/3515 passed/34 skipped, identical both runs); file absent on baseline 374aff42 => BRANCH_REGRESSION (async-intake timing, not Study Desktop — zero Study failures in CI logs). Fix 39376114 (2-line waitForText) => run 34893566186 classify/core/numerical all PASS.
- [x] Phase 12J.10 final raw-GNSS hardening (branch feat/gnss-raw-final-hardening, baseline origin/main 58d7cbed = PR #62 merge). STATUS: DONE 2026-09-14 — panel split zero-behavior (664→219+hook+4 comps) + ant1/ant2 rinexhead with -r clobber removed + explicit header anttype/antdel (receiver PCV now applies; SUB==12J.8 truth 0.2mm; old lineage fixed ~1m off here) + RINEX 4 intake + clean-clone E2E + ANTEX matrix 63/63 byte-identical (7 legs×3d×windows) + falsification live + reversals ≤0.1mm + 12/12 STAR sessions + MST/MANUAL/SIX production-path proofs + export/reopen determinism. Verdicts: ANTEX_PRODUCTION_READY_WITH_SUBSET, GO-RAW-SESSION-REVIEW-PRODUCTION, Phase 12J COMPLETE. Report reports/gnss/phase12j10-final-hardening.md. PR #63 MERGED 2026-09-14 as c1b60f2f.
- [x] Phase 13D review-blocker fixes (branch feat/field-to-finish-coding). STATUS: DONE 2026-09-15 — (1) regen no longer deletes valid linework: cadGeneration.ts re-upserts unchanged chains in place (new isSameFieldToFinishLinework helper ignores the generationRunId stamp; manual linework kept + warned; visible/locked preserved) and regeneration.ts diffs per-chain valid-set from the fresh payload (same-id geometry changes flagged, only vanished chains removed on confirmed regen); (2) splitImportedCodeDescription no longer invents feature codes from descriptions (feature only when a distinct code field present; rawCodeText/rawCode keep exact whitespace, canonical code trimmed) + FieldGenius shot DESC-fallback removed; (3) docs downgrade: updateFieldToFinishCoordinates documented as available-but-unwired (CURRENT_BEHAVIOR.md + regeneration.ts header), no speculative wiring. Tests: regen byte-identical-linework + affected-chain-only (cad_f2f_regen), description-only/whitespace/FG-DESC-only (cad_f2f_importers). Validation: focused 5 F2F files 40/40, lint 0 errors, typecheck clean, test:agent 3726 pass + 3 pre-existing study-desktop calibration failures (stash-proven). No math/tolerance changes.
- [x] Phase 13E ExportResult/no-silent-drop fixes (bounded to export modules/UI/tests/docs; no math/tolerance/F2F catalog changes). STATUS: DONE 2026-09-15 — (1) Export Center LandXML via production buildLandXmlProjectExportWithResult (exported XOR omitted, approximated⊆exported, ellipses NOT_APPLICABLE, warningsPending=false); (2) buildDxfLayoutTextWithResult surfaces model dispositions + mapped paper warnings (bare wrapper unchanged); (3) buildDxfModelSpaceTextWithResult merges model+serializer warnings with byte-identical payload; (4) finalizeExportResult fail-closed (omitted wins, stray approx→omitted, both warned); (5) standard ACI 1-255 table (10=255,0,0, 12=204,0,0); (6) DXF model validation omits+warns degenerate polylines/polygons/parcels/texts/arcs/alignments/labels (no 0-coercion); (7) DXF polygon/parcel + alignment APPROXIMATED per docs matrix (partial skips exported+approximated + entity warning); (8) workspace-owned active catalog shared by F2F panel + Export Center + wiring test. Validation: focused export suites 73/73, lint 0 errors, typecheck clean, test:agent 3801 pass + 3 pre-existing study-desktop calibration failures (stash-proven), build clean. Left uncommitted in working tree (shares tree with other in-progress Phase 13E dirt). Round-2 re-review fixes DONE 2026-09-15 — (a) exportCenter.mergeWarnings now carries exported ids so the hardened finalizer no longer converts valid scene approximations into omitted (+ SVG/PDF non-circle-symbol test); (b) ACI gray ramp corrected to standard 51,80,105,130,190,255 with nearest-black expectation updated (ACI 18); (c) LandXML duplicate-station safety: conflicting duplicate points omitted+warned, colliding line endpoints keep actual coords via collision-safe synthetic refs + warning (+ tests). Validation: focused 74/74, lint 0 errors, typecheck clean, test:agent 3806 pass + same 3 pre-existing study-desktop failures, build clean. — (1) Export Center LandXML per-entity ExportResult via production CAD→LandXML adapter (exported XOR omitted, approximated⊆exported, ellipses NOT_APPLICABLE omission, no warningsPending); (2) R2000 WithResult wrapper surfacing model warnings/dispositions; (3) R12 result-aware path merging model+serializer warnings with identical payload bytes; (4) finalizeExportResult fail-closed hardening + tests; (5) standard ACI 1-255 table + tests; (6) DXF model validation for polyline/polygon/parcel/text/alignment (omit+warn, no 0-coercion); (7) DXF polygon/parcel + alignment marked APPROXIMATED per docs matrix (partial alignment skips warn entity-attributed, still exported/approximated); (8) lift active F2F catalog to SurveyCadWorkspace and pass to ExportCenterPanel + production wiring test.
- [x] Phase 14F §§5,7,8,9,30,31 QC OVERVIEW + attention model + badges + cross-navigation (branch feat/statistical-qc-integration-audit). STATUS: DONE 2026-09-16 — new src/engine/qcOverviewModel.ts (pure counts + categorized attention, no scores/grades) + new src/components/report/QcOverviewSection.tsx (overview card order -208 below Adjustment Summary + shared QcCategoryTag) + onSelectObservation/jump threaded ReportView->TopSections + category tags on chi(Local strips)/Reliability/Stochastic/LOO (systematic already DESCRIPTIVE) + §31 equation/component local-flag counting (GPS per-component) + tests/reportQcOverview 10/10 + reportQcSectionOrder marker disambiguated (overview 'Reliability concerns' heading). §8 audit: no verdict wording on scale/CoordEff/LOO-shift/trends/face-balance (locked by test); LOO FAILED/OK is re-solve status, direction localPass is formal local — both kept. Validation: lint 0 errors (2 pre-existing warnings), typecheck clean, test:agent 4119 + same 3 pre-existing study-desktop calibration fails (stash-proven), parity 25/25, build clean. SCOPE: new src/engine/qcOverviewModel.ts (pure counts + categorized attention, no scores) + new src/components/report/QcOverviewSection.tsx (overview card below Adjustment Summary + shared QcCategoryTag) + thread onSelectObservation/jump into ReportViewTopSections + category tags on Local/Reliability/Stochastic/LOO strips + §31 equation/component local-flag counting + tests/reportQcOverview.test.tsx. OUT: no math/result-field/CSV/text/industry/tooltip-registry/settings changes; no docs/ or settings-component touches.

- [ ] Phase 15A post-QC covariance performance audit (branch feat/post-qc-covariance-performance-audit, baseline 173a4acb = PR #77 merge). STATUS: DISPATCHED 2026-09-16 — scout Qxx-producer/factorization inventory + scout native-boundary/route audit + scout Phase14-consumer/ellipse/allpairs audit + worker baseline validation (lint/typecheck/test:agent/parity/test:wasm/build) running in parallel. EVIDENCE ONLY — no production changes.
Phase 15A update 2026-09-16 — all 4 intakes DONE: scout-qxx (dual N^-1 on 2D/robust, R2/R3 duplicate Qvv, matrixFactorizationMs conflation, VERIF-vs-FCR separation), scout-native (native full-Qxx gates table, boundary copy sites A–I, all-pairs consumers, 2D-sparse vs 3D-dense asymmetry), scout-qc-consumer (consumer matrix 11x5, full-Qxx necessity per cohort, report-only couplings: suspectImpact auto disables native routes by default, robust comparison extra solve, all-pairs Option-B demand), worker-baseline (HEAD 173a4acb clean; lint 0err/2warn, typecheck PASS, agent 4141+3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build 9.11s; env Ryzen5800X3D/node26/npm11/emcc6.0.9; fixtures synthetic seeded chain-2d/gps-2d/gps-3d). NEXT: bench + memory/profiler workers.
Phase 15A bench-worker DONE 2026-09-16 (evidence-only, no src/ changes): tests/evidence/phase15a_covariance_operation_benchmark.test.ts + reports/performance/phase15a-covariance-operations.{md,json} (tier-registered, suite phase15a). Dual recovery measured on all 2D dense arms (FINAL 1 accum + 1 Qxx recovery, STATS 1 accum + 1 Qxx recovery, reason two-dimensional-legacy); 3D reuses (STATS 0+0). Biggest bottleneck: FINAL Qxx recovery dominates at scale (gps-3d-128: 92.4ms of 283.8ms wall; chain-2d-128: 28.1+26.8ms recoveries of 117.7ms). Marginals: force-legacy-vs-reuse +15.4ms (gps-3d-64), robust-huber +13.8ms (3D, flips to robust-mode-inadmissible), suspectImpact auto +19.7ms / 3 extra solves (chain-2d-32), tscorr/reliability ~noise (+1.1ms statistical on 3D). Sparse-selected chain-2d-128: 1 WASM transfer, 52.5ms vs 117.7ms dense, external reliability 514/514 sparse-route-unavailable (dense 100% available), relativePrecision 0 rows (255 connected relativeCovariances kept). All-pairs loop cheap (<=13ms, ~1-4us/pair upper bound). No redundancy fixed (recorded for 15B). NEXT: memory/profiler workers.
Phase 15A reviewer APPROVE 2026-09-16 (reviewer-15a, read-only): 2 evidence commits verified, zero src/ diff, lint 0err/2pre-existing-warn, typecheck clean, evidence 1/1 pass; 10 gates 9xYES+correlated-PARTIAL(non-blocking)+production-change NO; rerun wall-drift noted (counts stable). Minor notes closed: all-pairs per-pair labeled upper-bound, runEvidence usage lists phase15a. Decision: GO-15B-QXX-REUSE ranked first (measured), then NATIVE-BOUNDARY E+F, ROW-PRODUCT/sparse-selected, SELECTED/G defer, MORE-EVIDENCE correlated blocks, NO-LEAD/B last. Branch NOT merged per mission.

- [ ] Phase 15C native full-Qxx default-session reachability (branch perf/native-qxx-default-session-reachability). Planned scope: admit suspect-impact auto/off sessions (primary + up to 3 LOO alternates) to the native full-Qxx route under MODEL A (whole-session native attempt: every solve runs with the injected native capture, each captured system inline-verified before reaching the engine, finalizer aggregates cached evidence, ANY failure reruns the whole session clean TypeScript; no per-solve runtime switching, no native algorithm change); keep rejecting 'on'/unknown modes fail-closed with a mode-naming reason; keep every other gate, the 64-system bound, defaults (auto stays default), and all routing/thresholds byte-identical. STATUS: IMPLEMENTED 2026-09-16, uncommitted — production diff limited to deriveNativeFullQxxEligibility suspect gate + header/eligibility doc reword (src/workers/adjustmentNativeFullQxxAutoRoute.ts); tests/phase15c_native_default_session.test.ts (8/8 agent, <1 s: off+auto admit / on+unknown reject, kill-switch, 65-system overflow + truncated + empty rejects, auto 1-candidate native reachability with 2-system whole-session verification + clean parity, 2nd-system-throw whole-session clean rerun parity); tests/evidence/phase15c_baseline_reachability.test.ts reworked to off-vs-on (auto now admitted); tests/evidence/phase15c_default_session_parity.test.ts (4/4 real-WASM: auto 0/1/3-candidate native-vs-clean parity at maxAbs 2.6e-15/1.4e-13/7.5e-14 with 1/2/4 verified systems, LOO exactness 1/1 + 3/3 vs independent clean-TS manual-exclusion runs, on-mode typescript rejection with bundle untouched). Validation: lint 0 errors (2 pre-existing warnings), typecheck clean, manifest 11/11, build pending. M1+M2 DONE 2026-09-16 (no src/ changes, auto-route frozen) — benchmark verdicts hardened to HARD expect ≤1.05 on all LARGE shapes (S192/G192/S384 multi-solve + S192/B; soft-only H0 with 1ms timer-floor comment); rerun 4/4 PASS with median-of-5 (ratios 0.50–0.99, median-of-7/1.10 fallback NOT needed); report reports/performance/phase15c-default-session.md. No commit per instruction.

- [ ] Phase 15D native Qxx boundary/verification efficiency (branch perf/native-qxx-boundary-verification-efficiency, baseline origin/main aeea52a3 = PR #80 merge). Planned scope: §§1-43 — reduce native-result data movement + verification scaffolding cost with C1/C2/C3, coverage, tolerances, sampled columns, finite scans, whole-session fail-closed fallback all FROZEN; cohort (3D/worker-only/adjustment/auto-off/no-robust/no-tscorr/no-orient/no-staticGNSS/no-gpscov/<=768) UNCHANGED; 2D + correction routes UNTOUCHED; no tolerance changes; no row-product caching (15E decision at end). STATUS: DISPATCHED 2026-09-16 — worker-baseline (§1 validation + S192/S384 AUTO medians) + scout-trace (§§2-7 dataflow/copy map) running in parallel.

Phase 15D scout-trace DONE 2026-09-16 (read-only): full T1–T21 transition table. Key: nested Qxx materialized only AFTER querySelected returns (no TypedArray+flat+nested coexistence single-solve; multi-solve retains R6/R7 × 64 systems). Oracle already 1 factorization + ≤16 solves/system (no §17 win). Finalizer already cache-only zero numerics (§12 proven). All Qxx consumers read-only, writes only whole-ref assigns (§10 alias-safe). C2 rebuilds dense N from packed (packed-direct = frozen-mechanics risk). Prime candidates: R6 captured-cov copy + R7 captured-packed copies (verify in place), T7 flat number[] (Float64Array direct, ~0.76ms@192), T10 scaled-copy fusion, per-system probe-plan rebuild; copyMatrix HISTORICAL (probe-gated only). Risks: capture.systems shape test-asserted (COMPATIBILITY), C2 mechanics frozen.

Phase 15D baseline DONE 2026-09-16 (worker-15d-baseline): HEAD aeea52a3 (= PR #80), wasm md5 df94d377b3a569448c779e9a7e26fe22, node26.8.1/npm11.19.0, Ryzen5800X3D/31GiB, cap 768, auto/off-admit confirmed (:147-149). Validation: lint 0err/2warn, typecheck clean, agent 4166+3 pre-existing study-desktop trio, parity 25/25, wasm 74/74, build 8.63s. Medians (median-of-5): S192/C AUTO-3 155.8ms (TS 164.7, 0.946 GO); S384/C 585.0ms (TS 798.9, 0.732 GO). Tolerances pinned phase8a6SparseCovarianceSentinel.ts: C1 1e-6 rel / 1e-12 abs floor; C2 1e-6 inf-norm/col; C3 = C1 AND C2, sym 1e-9, CS slack (1+1e-6).
Phase 15D decision 2026-09-16: implement T6 (captured-cov copy) + T7 (flat number[]) removal, T10 fusion only if zero-math-change; C2 stays dense (frozen); oracle/finalizer already optimal; Qxx alias-safe. Capture.systems shape stays COMPATIBLE (tests assert it).
Phase 15D impl DONE 2026-09-16 (worker-15d-impl, uncommitted): T6+T7 removed in src/workers/adjustmentNativeFullQxxAutoRoute.ts (capture stores engine buffer by reference; verifier finite-scans Float64Array directly; legacy C3 fallback via cast, no copy); T10 SKIPPED (scaled-dense fusion would change choleskyDecomposeWithDamping inputs — not provably bitwise-identical). Tests tests/phase15d_native_verification_nocopy.test.ts 5/5 (old-copy/new-ref parity, A–H mutation corpus, NaN whole-session fallback with identical clean-TS result). Bench S192-3 172.5→167.8ms, S384-3 588.8→588.0ms; native bytes copied 2.36/9.44MB→0; retained native −50%. Validation: lint 0err, typecheck clean, agent 4171+3 pre-existing study-desktop fails, parity 25/25, wasm 74/74, build 8.62s. No commit per instruction.

Phase 15D DONE 2026-09-16 (uncommitted, NO PR per mission): T6+T7 removed (43-line diff, src/workers/adjustmentNativeFullQxxAutoRoute.ts only); T10 skipped (not bitwise-provable). Evidence: 15D parity/mutation/fault tests 5/5, 10L 16/16, 15C 8/8; lint 0err/2pre-existing-warn, typecheck clean, agent 4171+3 pre-existing trio, parity 25/25, wasm 74/74, build ok; tolerance diff zero. Bench (median-of-5 real-WASM): S384 AUTO-3 verify 120.9→108.6ms (−10.2%), session 588.8→588.0 (no regression), retained native −50%, copied →0. Reviewer APPROVE-WITH-NITS (2 nits closed: legacy-zero comment + reports/performance/phase15d-boundary-efficiency.md). Decision: GO-NATIVE-BOUNDARY-PRODUCTION (memory-copy elimination + modest verify win; oracle/probe/C2 dominate remainder). Recommend 15E next (row-product reuse) — verification scaffolding now at floor.
Phase 15E NO-MEANINGFUL-GAIN 2026-09-16 (branch perf/residual-covariance-row-product-reuse, NOT merged to main): ordered-pair memo of repeated scalar/cross row-product dots cut dot counts ~37–45% but wall deltas −14.78%…+0.77% inside session noise — dots are NOT the bottleneck. Production code NOT merged per mission.
- [x] Phase 15F selected-covariance production-contract audit (branch feat/selected-covariance-production-contract-audit, baseline origin/main cbceefbc = PR #81 merge). EVIDENCE/ARCHITECTURE ONLY — no production numerical changes, no selected-covariance rollout, no output removal. STATUS DONE 2026-09-16 — 4 subagents (3 scouts + 1 worker) + independent reviewer gate. Findings: full-Qxx recovery 13–33× larger than B construction in every case; dense P dominates memory above n≈256; 32 consumers traced (only ext-reliability dense-B + reuse-eligibility truly need full dense); station demand 3/6 entries; dense C(U,2) all-pairs vs selected zeroed; factor-solve alternative strictly worse; factorization survives nowhere; prototypes prove selected-column bit-identity (diff 0) + CoordEff rel err ~2e-15; ROUTE B saves ~8–11% wall, ROUTE C costs ~1.65× full-dense, memory relief non-structural. Evidence: tests/evidence/phase15f_selected_covariance_prototypes.test.ts + tests/evidence/phase15f_benchmark_matrix.test.ts (tier-registered) + reports/performance/phase15f-selected-covariance-evidence.{md,json}. Validation: evidence 2/2, lint 0err/2pre-existing-warn, typecheck clean. Reviewer APPROVE (15/15 gates). Decision: NO-MEANINGFUL-GAIN / KEEP-FULL-DENSE. NOT merged per mission.
- [x] Phase 16B-A structured-weight transfer foundation (branch perf/structured-weight-solve-transfer, baseline de6c0aa3). STATUS: WORKER A DONE 2026-09-16 — Route B Level 1 ONLY (transfer/storage; dense accumulators, iteration/robust/LOO/auto-adjust/native/sparse/Qxx/Qvv/reliability/exports/tolerances/settings untouched). New: src/engine/structuredWeightOracle.ts (round-trip compare + dense-identical structured A'PA accumulator) + src/engine/structuredWeightTelemetry.ts (dense-P bytes/builds/materializations/fallbacks counters) + tests/phase16b_weight_roundtrip.test.ts (agent-tier 26/26: 9-family bit-identical P+N, fail-closed triplets, 6-file candidate-vs-legacy exact full parity, preanalysis dead-P preserved) + tests/evidence/phase16b_weight_transfer_bench.test.ts (evidence-tier registered). Production: kill switch structuredWeightTransfer (false=forces legacy; threaded EngineOptions/AdjustmentRuntime/nested solves) + covariance-recovery structured path (eligible m>=128, fail-closed dense fallback, no log change) + preanalysis stats dead-P elimination (sparse+omitDenseP; P provably unread there; tsCorrelationDiagnostics preserved). Findings: chain-1000 assembly 38.4→18.7ms (2.06x, 32MB dense churn gone); dense shapes lose on finalize string-sort (setup-32 0.07→0.45, setup-256 ~36x) → dense fallback below m=128; setup-128+ single-group pathology documented for Worker B (builder finalize optimization). Validation: lint 0err/2pre-existing-warn, typecheck clean, agent 4197+same 3 stash-proven study-desktop fails, parity 25/25, wasm 74/74, build 8.65s; scratch before/after IDENTICAL (preanalysis incl. tsd + industry_phase2 incl. Qxx ellipses).
- [ ] Phase 17A end-to-end survey workflow readiness audit (branch feat/end-to-end-survey-workflow-readiness-audit, baseline origin/main 592524ca = PR #85 merge). AUDIT/EVIDENCE ONLY — no production feature work, no numerical-method/solver-routing changes. STATUS: evidence gathering DONE 2026-09-17 — 6 scouts + 2 workers + reviewer gate. Baseline: lint PASS (2 pre-existing warnings), typecheck PASS, build PASS, test:wasm 12/74 PASS, parity 25/25 PASS, test:agent 4237/4241 (3 known study-desktop calibration fails). Capability A-N PRODUCTION, O large-job PARTIAL. E2E terrestrial (industry_standard_reference_case.dat 16stn/162obs/dof134 converged) PASS end-to-end w/ linked-F2F caveat; mixed combined fixture (43stn/1617obs) non-convergent as-is. Top hazards: stale-result export unguarded (runFreshness dead), adj->draft SILENTLY-STALE parcels/annotations + destructive adjusted-points import, silent import units, reimport double-count, CRS-silent exports (GeoJSON/LandXML/CSV), CAD/saved-runs persistence gaps, F2F overwrite + no prod regen + catalog gaps, listing hard-coded 1: file column, 6-8 step deliverable no zip, hidden blocking reasons/Review Queue off by default. Recommendation: GO-17B-DATA-INTEGRITY. Reviewer gate GO 15/15 (0 fix rounds). Full report: reports/product/phase17a-end-to-end-readiness.md (cites refreshed, incl. projectWorkflowUtils src/hooks correction).
- [x] Phase 17B result-freshness/export-integrity (branch feat/result-freshness-export-integrity, baseline 9000db92 = PR #86 merge). STATUS: DONE 2026-09-17 — full brief §1-49 executed. WORKER (1 rate-limit resume): pure src/engine/resultIntegrity.ts + useResultIntegrity hook beside untouched legacy runFreshness; apply-time identity {input, math, exclusion}; central deliverable gate (8 formats BLOCK unless FRESH_SUCCESS, webnet text diagnostic-with-status line); drafting feeds gated (spike/import/F2F sync+commit, mode=adjustment+converged); toolbar chip CURRENT/STALE/FAILED/NO RESULT; save/reopen recompute; metadata test-only (no false EXPLICIT CRS); listing ordinal NOT fixed (no provenance, recorded). REVIEWER GO-RESULT-INTEGRITY-PRODUCTION 15/15, 0 fixes. FINALVAL b170996d: lint 0err/2 pre-existing warns, typecheck clean, agent 4272+3 pre-existing study-desktop (calibration/calibration_v5/preflight), parity 25/25, wasm 74/74, build 8.69s. PR opened, NOT merged.
- [x] Phase 18D survey point styles, labels, point groups, F2F styling (branch feat/cad-survey-point-styles, baseline origin/main 97a6d90d = PR #93 merge). STATUS: BASELINE DONE 2026-09-17 — origin/main fetched, branch created. Baseline validation: lint PASS, parity 25/25 PASS, wasm 74/74 PASS, build PASS, portable-paths PASS; typecheck 4 pre-existing errors in UNTRACKED leftover src/engine/runSessionAsync.ts (phase9l dirt, not on branch); test:agent 11 pre-existing fails all from untracked phase9l/study leftovers. Recon: scout-cad-points + scout-f2f done; point-display map docs/evidence/phase18d-point-style-map.md written (10 paths P1-P10). Reviewer APPROVE 15/15 2026-09-17. PR opened, NOT merged. Decision: GO-CAD-SURVEY-POINT-STYLES. Worker-label-styles DONE 2026-09-17 (HEAD 1b9e8c17): CadPointLabelStyle + binding + buildPointLabelContent/materializePointLabel/migrateLegacyLabelToBinding in new cadPointLabelStyles.ts, 8 defaults seeded on all create/clone/load paths, WNCAD additive at schema v2, tests/cad_point_label_styles_18d.test.ts 11/11; no renderer/export/F2F/UI changes. Worker-render-export DONE 2026-09-17: renderer resolves effective marker via resolveSurveyPointDisplay single call site (surveyPointMarker in cadRendererStyle.ts; No Display emits no primitive, entity persists/selectable; radius = symbol × markerScale drawing units pinned) + shape on CadDisplayPointPrimitive; shared geometry in new cadPointSymbolShape.ts (all outlines touch radius circle; unknown → circle) drawn honestly in SurveyCadPreviewPrimitive; text branch materializes bound labels (materializeBoundPointLabel in cadPointLabelStyles.ts; No Label hides; orphan/unknown-style → baked fallback, never crash); SVG/PDF export honest shapes (circle/dot = circle, rest = closed polyline/lines, zero POINT_SYMBOL_APPROXIMATED — writer represents all six) + No Display/No Label excluded from SKIPPED noise; DXF bound labels materialized, points stay POINT+TEXT + warning; LandXML unchanged (text NOT_APPLICABLE, CgPoint); WNCAD unchanged. Tests: new tests/cad_survey_style_render_18d.test.ts 7/7; updated cad_export_color (pt-P1 honest polyline, no warning) + exportCenter.01 (honest shapes, no approximation). Worker-f2f-styles DONE 2026-09-17 (Phase 18D F2F point/label style integration): FeatureDefinition.pointStyleId additive + catalogIo round-trip (schema v1 kept) + validateCatalogStyleReferences warnings; generation sets BASE pointStyleId/pointLabelStyleId (chain: pointStyleId → symbol-compat → default; label: labelStyleId → F2F Full → default) + bound derived labels via single buildPointLabelContent (buildLabelText kept as deprecated legacy reference); regen updates BASE refs, preserves manual overrides/manual label text+offset, migrates legacy labels provenance-gated; editor dropdowns with drawing context (text+warning fallback); new tests/cad_f2f_styles_18d.test.ts 10/10; no golden changes; lint clean, typecheck 4 pre-existing only.
- [x] Phase 18D survey style UI (HEAD d54ccb02 + this batch): SURVEY_POINT_OVERRIDE/SURVEY_STYLE_TABLE/SURVEY_GROUP_TABLE undoable commands (cadTransactionsSurveyDisplay.ts + cadSurveyDisplayRefs.ts), shell survey snapshot (cadSurveySnapshot.ts), Properties SURVEY POINT + POINT DISPLAY sections with COMMON/VARIES batch editors, Toolspace Survey points/groups tree + capped point table + Settings style nodes, three manager dialogs, ribbon SURVEY tab. Validation: lint clean; typecheck clean (4 pre-existing runSessionAsync untracked errors excluded); new tests/cad_survey_display_commands_18d.test.ts 10/10 + cad_shell_panels 19/19 (5 new UI); test:agent 11 pre-existing fails (phase9l/study dirt, unchanged). Manually-QA-only: manager dialog flows, Toolspace context menus, ribbon focus paths (jsdom covers render/dispatch only).
- [x] Phase 18F ENGINE surface TIN core (branch feat/cad-surface-tin-foundation). STATUS: DONE 2026-09-18 — engine only (no UI/persistence/worker-file by this worker): cadTypes CadSurface* model + trailing CadProject.surfaces?/surfaceStyles? (no schema bump; UI worker converged in-tree: layerId/buildDiagnostic/showPoints/showBoundary); cadPersistence clone covers new tables; new src/engine/cad/tin/ (tinTypes/tinPredicates/tinDedupe/tinBase/tinConstraintRecovery/tinLegalize/tinDomainFilter/tinBuild, all <250 lines) on delaunator@5.1.0 (ISC) + robust-predicates@3.0.3 (Unlicense), tarballs vendored third-party/npm via relative file: refs; Sloan-style flip recovery written from literature with mandatory Steiner fallback (unit-pinned); cadSurfaces.ts revision/build/status/interpolation (missing-Z skip-with-warning never Z=0, exact XY-dedupe with differing-Z BLOCK, breakline Z via survey-point refs only, centroid outer/void clip, uniform-grid queries); tests/cad_surface_tin.test.ts 19/19 (plane/ridge/translation/shuffle oracles, degenerate corpus, dup-conflict, boundary/void, F2F chain, Steiner, revision/status, gotcha pins); docs/evidence/phase18f-triangulation-decision.md (12-section; rejects d3-delaunay/earcut/cdt2d/poly2tri/@three-roads/cdt/cdt-js/triangle-wasm) + phase18f-surface-architecture-audit.md (no prior surface model). Validation: new 19/19 + cad_point_groups_18d (1 trailing-key expectation updated to surfaceStyles — surfaces repoint owned by UI worker) + cad_drawing_file + cad_persistence_and_entities green; eslint clean; tsc clean for touched files (4 pre-existing errors: SURFACE_* command defs + runSessionAsync, UI-worker in-flight). No solver/parity changes; no triangle entities.
- [ ] Phase 18L LandXML civil interchange for TIN surfaces and alignments (branch feat/cad-landxml-civil-interchange, baseline origin/main 43033433794de964543e458521709670d3b5816f = PR #101 merge, exact match to mission creation SHA). STATUS: BASELINE DONE 2026-09-19 — fetch confirms origin/main at mission SHA (no advance); lint 0err/2 pre-existing warns, typecheck clean. Scout audit (landxml.ts/landxmlCad.ts/landxmlImport.ts arch, Export Center, CRS 17D, alignments, surfaces, profiles/sections, commit seam) + researcher schema evidence (XSD element structure, N-E order, 1-based face refs, rot cw/ccw, staAhead/staInternal, ProfSurf station/elev, section right-positive offsets, NOT_VERIFIED flags) delivered. Evidence docs: docs/evidence/phase18l-landxml-support-matrix.md + phase18l-landxml-schema-audit.md. EXPORT SIDE DONE 2026-09-19 (worker-18l-export, export files only): landxmlCad split into types/serialize/project barrel (public API unchanged); Surfaces(surfType=TIN, deterministic 1..N P/F, elev min/max + 2D/3D area, stale/mesh-missing blocked); native Line/Curve(rot,radius) + Alignment staStart + StaEquation (auto rawStation resolution); bounded Profile(ProfSurf/PntList2D raw chainage, multi-segment gaps, sub-2-sample block) + CrossSect(sta, offsets NEGATED left→right positive, gap rules) via landxmlCivilSource.ts consuming optional session caches; Export Center per-class summary (Points/Parcels/Alignments/TIN Surfaces/Surface Profiles/Cross Sections incl. blocked reasons, no silent omission) + optional caches arg (UI wiring deferred); CRS unchanged. GO decision: profile + section export both GO/bounded (lossless via multiple PntList2D; unrepresentable gaps block with reason). Tests 15/15 new (tests/landxml_civil_surface_18l + landxml_civil_profile_section_18l + exportCenter.18l; independent re-parser); regression: landxml_export/golden, cad_export_coverage/18c/dxf, f2f export matrix/golden, cad_surface_stats_wncad, exportCenter.01, 17C/17D, 18F–18K profile/section/surface suites green (211+56). NOTE (2026-09-19 import worker): the parallel import-side files are now lint/typecheck clean (unused arg + stale interface fixed during import implementation) — repo-wide lint 0err/2 pre-existing warns, typecheck clean. IMPORT SIDE DONE 2026-09-19 (worker-18l-import, import files only, export serializers untouched): shared N/E parser (landxmlCoords.ts) + plan/parcel split (landxmlPlanFeatures.ts) + surface parse/validate (landxmlSurfaceImport.ts: TIN-only, arbitrary P-id map, CCW canon, same-Z dedupe vs conflicting-Z block, duplicate-face + non-manifold blocks) + alignment upgrade (landxmlAlignmentImport.ts: native elements no flattening, staStart, StaEquations raw/ahead/back, cw/ccw travel deltas, spiral/unknown blocks whole alignment, profiles/cross-sects UNSUPPORTED) + native imported-TIN model (cadTypes sourceKind/importedTin additive, engine wiring cadSurfaceRevision/cadSurfaces/cadImportedTin, clone/persist round-trip, reopen topology-exact) + atomic LANDXML_IMPORT commit (cadTransactionsLandxmlImport.ts/cadLandxmlCommit.ts: one undoable tx, stable content IDs, Name (2), exact-duplicate skips, meshes via worker seam) + Surface Manager source label with imported edit lock. Tests 21/21 new (landxml_import_surfaces_18l 11 + landxml_import_alignments_18l 10, hand-authored external-style); interchange spiral test updated to whole-alignment block. VALIDATION: new + interchange/export-golden/17C/17D/surface/alignment/sibling-18L-export green, lint 0err (2 pre-existing warns), typecheck clean, agent 5005 + 3 pre-existing study-desktop fails, portable-paths 4144/0, build 9.54s, files <=400 lines. NEXT: reviewer gate, PR (do NOT merge). STATUS: IMPLEMENTATION DONE 2026-09-19 — import 325a0507 + export 6fa3b5f6; lint 0err/2 pre-existing warns, typecheck clean, agent 5005 + 3 pre-existing study-desktop fails, wasm 74/74, parity 25/25, build 9.59s, portable 4155/0. REVIEWER GO-CAD-LANDXML-CIVIL-INTERCHANGE 15/15. PR #102 opened, NOT merged.

- Phase 18M LandXML production import — async build wiring (branch feat/cad-landxml-production-import, workspace + `SurfaceBuildService` glue only; no numerical change). Commit: staged review selection → `commitLandXmlImport(..., { deferMeshBuild: true })` → ONE `LANDXML_IMPORT` transaction through the workspace history seam (`runLandXmlImport`) → dirty via the normal drawing-change path; all-duplicate payloads commit nothing (no undo entry, no dirty) and report "Nothing new to import.". Builds: each committed surface id is appended to the existing single-worker serial queue (`SurfaceBuildService.scheduleSurfaces`), BUILDING immediately (queued + in-flight), CURRENT on cache hit / FAILED + diagnostic on failure; second imports coexist without cancelling in-flight builds. Ownership: late results from a switched drawing or an undone/deleted surface are discarded (no resurrection, no ghost cache) via the existing 18G drawing/revision guards. Failure: authoritative definition stays, workspace shows "Imported, but surface materialization failed" once (diagnostic retained; Rebuild through the worker route preserves explicit faces). Item 7: sync-fallback budget for imported TINs counts imported vertices, and the worker request omits the whole-project survey-point snapshot on the imported path. Tests: `tests/landxml_import_build_glue_18m.test.ts`, `tests/cad_surface_build_service.test.ts` (imported scheduling/ownership/undo/drawing-switch), `tests/landxml_commit_production_18m.test.ts` (deferred contract). Docs: `docs/CURRENT_BEHAVIOR.md`. NOT merged (PR pending per phase workflow). Undo-seam fix 2026-09-19: the external-adopt history effect compared plain `JSON.stringify` project signatures, so the drawing-sync clone (`cloneCadSurfaceDefinition` normalizes imported-TIN definition key order) looked like an external update and wiped the just-committed `LANDXML_IMPORT` transaction (SHELL_UNDO disabled). The adopt effect now compares order-insensitive `buildStableCadProjectSignature` values (`src/engine/cad/cadProjectState.ts`, used only in `useSurveyCadWorkspaceHistory.ts`; engine commits, service ownership, and persistence guards untouched). Contract: one Undo removes all imported objects (derived mesh cleared with the definition), Redo restores definitions as honest UNBUILT until rebuilt (same as save/reopen; auto-rebuild on redo is out of scope). Regression: `tests/landxml_import_undo_history_18m.test.tsx` (fails without the fix); browser 18M-I/18M-J un-fixmed and green, full 18M spec 15 passed + 18M-F skip.

- [ ] Phase 18P annotation production hardening (branch perf/cad-annotation-production-hardening, uncommitted; baseline fef23556). STATUS: IMPLEMENTATION + QA EVIDENCE COMPLETE 2026-09-20 — anchor factory + creation wiring (`cadAnnotationAnchorFromCommandPoint`: survey-point/line-endpoint/arc-point/block-insertion associative, all other snaps + polyline vertices fixed); O(1) `CadProjectLookup` threaded through renderer/bounds/export (18O doc export-scene 10k 2414.6→54.1 ms; fresh same-machine baseline probe 74.1×→7.5×/decade, 1519→54 ms; snap 63.2→2.25 ms, 28× faster; bounds 19.1→12.8 ms; primitives identical 370/3700/37000); prepared-bounds spatial index + broad-phase cursor cull with lifecycle audit (decision doc docs/evidence/phase18p-spatial-index-decision.md — dirty cache + uniform grid DEFERRED with explicit revisit thresholds); placement closure (leader 9-point attachment, curve mid-sweep/tangent rotation CW+CCW, dimension `textPoint` option A absolute-model-coords). Validation: vitest annotation probe 2/2 (no time gate), Playwright cad-annotation-18p 9/9 (A–G associative + 18o 2/2) zero page/console errors, lint/typecheck clean on touched files. QA evidence: docs/evidence/phase18p-browser-qa.md + PNGs under docs/evidence/phase18p/ (3 resolutions + large drawing); print-scale 1:250/1:500/1:1000 verified numerically (paper text constant 2.5 mm; model/legacy text scales with plot scale). KNOWN FLAGS: production MOVE duplicates block references (18Q owns transforms — do NOT fix here); sheet SVG/PDF text rotation and DXF rotated-text alignment remain APPROXIMATED; whole-scene re-derive per edit (1687× affected-label cost) and ~115 ms/move OSNAP hover re-render at 1.25k entities remain follow-ups. NOT committed.
- [ ] Phase 18P worker D annotation placement closure (branch perf/cad-annotation-production-hardening, uncommitted). STATUS: DONE 2026-09-20 — shared `src/engine/cad/annotation/cadAnnotationPlacement.ts` (9-point leader attachment, curve mid-sweep/tangent placement, normalized default `middle-left`); renderer honors leader `textAttachment` (rows, landing end + gap) and rotates curve labels via `uprightRotation`; `deriveCurveLabel` uses signed sweep so CW arcs label instead of BROKEN; DXF gained leader landing segment + corrected arrow bearing + curve style offset + TEXT group 50 rotation (new `DxfExportModel.texts.rotationDeg`); dimension `textPoint` OPTION A honored by renderer/DXF/bounds with manual-side suppression of auto inside/outside fit; Properties gained leader attachment select + dimension Text E/N + Auto text placement (ops `leader-update.textAttachment`, `dimension-update.textPoint`, null clears); bounds include arrowheads, leader landing/text reference and manual textPoint. Documented SVG/PDF rotation APPROXIMATED divergence (cadExportScene untouched, Worker B) in `docs/evidence/phase18p-annotation-hardening-map.md`. Tests: renderer attachment×9 + CW/CCW curve + arc-edit update + manual textPoint + bounds inclusion; DXF attachment rows + curve rotation; ops set/clear; WNCAD round-trip + absent-field preservation. Validation: focused suites green, lint/typecheck clean. NOT committed.
- [ ] Phase 18R project coordinate transformation TESTS + EVIDENCE (branch feat/cad-project-coordinate-transformation; engine cadProjectTransform.ts/cadProjectTransformRequest.ts + PROJECTTRANSFORM/SURVEYTRANSFORM UI already landed — kernel/UI untouched). STATUS: TESTS+EVIDENCE DONE 2026-09-20 — tests/cad_project_transform_18r.test.ts 15/15 (station-equation single/multi §§79-80, sample-line §81, rigid bit-identity §82, Helmert-vs-independent §83, Grid/Ground CSF §84, imported-TIN topology §37, area/perimeter/profile/section scale laws §§14/41-43, ownership detach §85, reimport-block/undo §86, F2F manual-override §87, worker late-reject + undo/redo determinism §§88/45/62-63, WNCAD round-trip §§89-90, LandXML export→reimport §92); tests-browser/cad-project-transform-18r.spec.ts flows A–O + fixture tests-browser/fixtures/cad-project-transform-18r.wncad; perf evidence docs/evidence/phase18r-project-transform-performance.md (50k entities ~21ms, 100k TIN verts ~2ms, O(n), no worker needed). No Adjustment-solve changes. NEXT: validation battery, reviewer gate, PR (do NOT merge).
- [ ] Phase 18R.1 project-transform integrity (branch fix/cad-project-transform-integrity, baseline 041f7b1d = PR #108 merge). STATUS: SCOUT AUDIT DONE 2026-09-20 — findings A (production PROJECTTRANSFORM never passes options.draft; transformDraftDocument unreachable), B (buildCadBounds entity-only, ignores imported-TIN vertices), C (malformed TIN warn-and-continue, validator unused on transform path) all CONFIRMED with file:line evidence. WORKERS DONE 2026-09-20 (draft-atomic + bounds + tin-preflight). VALIDATION 2026-09-20: lint 0 errors, typecheck clean, test:agent 5364 pass + 3 pre-existing study-desktop fails, test:wasm 74/74, parity 25/25, build PASS, portable-paths 0 violations. BROWSER QA DONE 2026-09-20 (18r1 spec 5/5 flows A-H, 0 page errors; 18R spec 3/3). INDEPENDENT REVIEW PENDING — reviewer provider 429 on all attempts (3 resumes + fresh + 3 resumes); orchestrator verified 10 review questions against diffs (all PASS, not independent). COMMITTED 6c7dfe1a, pushed, PR #109 opened NOT merged.

- [ ] Phase 18S persistent TIN surface edit stack (branch feat/cad-surface-tin-edit-stack, baseline origin/main cef381780ef2a657b2e1ace3d5456746dde3038 = PR #109 merge, fetch-verified no advance). STATUS: BASELINE DONE 2026-09-20 — fetch confirms origin/main at mission SHA; branch created. Scouts dispatched (surface-core + surface-consumers). NEXT: baseline validation battery, architecture doc, engine edit model + applicator + commands + UI + persistence + integration + oracles + browser QA + reviewer gate, PR (do NOT merge). ENGINE SLICE DONE 2026-09-20 (457265c1) — edit model + fail-closed applicator + single replay chokepoint in buildCadSurface + revision coverage + 25 oracles. UI + DOWNSTREAM SLICE DONE 2026-09-20 — Toolspace Definition→Edits child rows (readable refs, no UUIDs; imported shows Imported LandXML TIN), manager EDITS table (#/Type/Vertices-Edge/Enabled/Status/Description + Move Up/Down/Enable-Disable/Delete), Properties edit counts only, borderless ribbon Surface→Edit group (Swap Edge/Add TIN Line/Delete TIN Line/Edit History), snapshot edit summaries with derived status + fail-closed broken-reference reason; new tests/cad_surface_edit_downstream_18s.test.ts (3/3: contour retire+hole stop, profile/section gap, volume SOURCE_NOT_CURRENT→NEEDS_RECALC + delete-boundary overlap), tests/cad_surface_edit_persist_18s.test.ts (7/7: native/imported WNCAD round-trip exact + same topology digest, LandXML final-mesh flatten, PROJECTTRANSFORM verbatim refs + uniform scale, snapshot labels); browser tests-browser/cad-surface-edits-18s.spec.ts (3 flows, seeded imported/native edit stacks); perf evidence docs/evidence/phase18s-performance.md (honest finding: replay is O(edits×triangles) at this revision because refreshEditEdges rebuilds the full edge map per edit — 1k/10k/50k/100k verts × 10/100/1000 edits numbers; incremental edge-map upgrade documented, no CI gate).
  ENGINE SLICE DONE 2026-09-20 (branch feat/cad-surface-tin-edit-stack) — cadTypes edit model, cadSurfaceEdits applicator (+ cadSurfaceEditMesh kernel + cadSurfaceEditAddLine retriangulation), tinBuild.constrained exposure, clone/revision/imported-revision coverage, dual-leg replaySurfaceEdits chokepoint in buildCadSurface, tests/cad_surface_edits_18s.test.ts 25/25; validation: lint 0 errors, typecheck clean, test:agent 5389 pass + 3 pre-existing study-desktop fails, test:wasm 74/74, parity 25/25. No UI/manager/toolspace/commands.

Phase 18S STATUS 2026-09-20 — engine 457265c1 (edit model + applicator + replay + 25 oracles) + arch 455fd188 + UI/downstream 38ef2bee (EDITS table, Toolspace rows, ribbon group, downstream/persist oracles 10/10, browser 3/3, perf doc with HONEST O(edits×triangles) flag) LANDED. PENDING: TX slice (transactions/commands/picking/sessions, resume attempt 3 running); then incremental edge-map perf fix (§31), full validation, reviewer gate, PR (do NOT merge).

Phase 18S STATUS 2026-09-20 — COMPLETE, PR #110 OPENED (NOT merged). Baseline cef381780ef2a657b2e1ace3d5456746dde3038 (fetch-verified, no advance). Commits: 455fd188 arch + 457265c1 engine + eb3bfda0/38ef2bee UI/downstream + 55e671c4 TX/commands/picking + 7c5f34c1 perf (225-280x) + 8d1b5c0a tracking + 96ab64cb label fix. Validation: lint 0err, typecheck clean, agent 5422 + 3 pre-existing study-desktop, wasm 74/74, parity 25/25, build + portable clean, browser 3/3 zero errors. Parent verified 15/15 gates PASS against diff; INDEPENDENT REVIEWER provider-throttled (6x429, rung-4 fallback) — re-review recommended before merge. Restrictions: browser flows subset of A-O, manual 3-resolution visual QA outstanding. Decision: GO-WITH-TIN-EDIT-RESTRICTIONS. Do NOT merge without explicit instruction.

Phase 18T UI STATUS 2026-09-25 (branch feat/cad-surface-point-elevation-edits, UI-ONLY on landed engine core) — DONE, unpushed. Five session commands (SURFADDPOINT/SURFDELETEPOINT/SURFMOVEPOINT/SURFSETELEV/SURFRAISELOWER) via registry + ribbon Surface→Edit + typed entry form + dock values; each commit one SURFACE_ADD_EDIT vs fresh revision (stale → SURFACE_EDIT_STALE_REVISION re-pick); transactions draft/validation extended for the 5 kinds (replay/revision untouched). Manager table extracted to CadSurfaceEditTable.tsx (dense #/Type/Target/Value/Enabled/Status/Description, E-numbers, inspector warnings, consumer-before-producer reorder block, add-point delete/disable confirm); summaries split to cadSurfaceEditSummaries.ts (Target/Value, group counts); Properties Topology/Point/Elevation counts. Fixed latent session-rebuild hang (post-commit rebuild deferred until history publishes — also applied to 18S hook). Validation: lint 0err, typecheck clean, browser 18T 5/5 + 18S 3/3 zero errors, unit 18S/18T green, build + portable clean; test:agent 5450 pass + 4 pre-existing study-desktop real-data failures (no surface linkage).

Phase 18T INTEGRATION PINS STATUS 2026-09-25 (branch feat/cad-surface-point-elevation-edits) — DONE. New 18T integration tests only (no src changes): downstream 5/5 (contour peak loop + undo→CURRENT, slope/aspect on edited triangles, profile/section SOURCE_NOT_CURRENT→NEEDS_REBUILD with new elevations, volume comparison raise → NEEDS_RECALC + analytic fill += area×Δ), LandXML 3/3 (edited Pnts/Faces export, independent re-parse + real 18L/M reimport mesh-equivalent, payload unmutated, history flattened), display/inquiry 3/3 (showPoints from final mesh, interpolated-Z add query-neutral, raise +2 on every probe), agent perf pin 1/1 (10k all stacks + WNCAD delta, measured not gated), manual evidence campaign tests/evidence/phase18t_point_edit_perf.test.ts (10k/50k/100k, 109 s) + docs/evidence/phase18t-performance.md. Finding (measured): set/delete/add scale linearly; move-point is O(moves×triangles) because applyMovePoint's crossing guard full-scans state.tris. Validation: lint 0 err, typecheck clean, new 18T 12/12 + existing 18T 26/26 + 18S/LandXML/18R 105/105, tier manifest green, portable-paths clean.

Phase 18T STATUS 2026-09-25 — EVIDENCE + FULL VALIDATION DONE (branch feat/cad-surface-point-elevation-edits). Evidence doc docs/evidence/phase18t-point-edit-architecture.md (18S re-audit: all 9 invariants TRUE; 18T model: 5 kinds, edit: identity, mutable-point replay, compaction, transform gate, perf finding). Visual QA 059f24a4 (35 files x3 resolutions, 18T spec 5/5, 0 page errors; UI gate 7/8 — viewport dominance FAILS, Properties panel floats over manager, docking fix recommended; contour shots lack contour lines). Full validation: lint/typecheck/wasm/parity/build/portable PASS; test:agent 5462 pass + 3 pre-existing study-desktop fails + 1 load-flake (cad_app_adjustment_isolation, passes solo); focused 45 files/422 tests green. REVIEWER DONE: GO-CAD-SURFACE-POINT-EDITING 15/15 + UI 8/8, 0 fix rounds. PR #111 OPENED 2026-09-25 (NOT merged). Decision: GO-CAD-SURFACE-POINT-EDITING. Do NOT merge without explicit instruction.

Phase 18U UI SLICE E STATUS 2026-09-25 (branch feat/cad-surface-analysis-bands-legends) — DONE, unpushed. Analysis manager section (surface Elevation/Slope + volume Depth) with CRUD via new undoable ANALYSIS_* commands, dense range editor (Generate Equal 5/64 cap, overlap/non-finite rejection, UNCLASSIFIED gaps, Near Balance depth label), Toolspace Analyses nodes under surfaces AND volume surfaces with legends, ribbon ANALYSIS/VOLUME-Depth/Analysis Legend groups + 6 registry commands (menu/ribbon/dock share one registry), Properties analysis+legend blocks, CURRENT-only Analysis Summary CSV (explicit units), direct-mesh point inquiry, session control plane (worker-backed SurfaceAnalysisService with synchronous engine fallback into the same cache) and scene display (analysis fills under surface/contour passes, legend geometry). Tests: tests/cad_analysis_ui_18u.test.ts 16/16 (manager CRUD, validation pins, color-only no-recalc, threshold→NEEDS_RECALC, delete-blocked-by-legend, CSV CURRENT gate, 18T source lifecycle, raise/lower slope invariance, transform invalidation). Validation: lint 0 errors, typecheck clean, shell suites green.

Phase 18U QA/RECONCILIATION STATUS 2026-09-25 (branch feat/cad-surface-analysis-bands-legends) — DONE, unpushed. Twin modules unified by documentation (cadAnalysisDisplayView is the thin wired facade; cadAnalysisView.service + cadAnalysisLegendView.service are the canonical wired builders — stale "reference-only/not-landed" docstrings corrected; resolveCurrentAnalysisRevision has one definition). Service-slice inquiry call sites verified against real signatures (tsc clean, no guessed shapes). Export seam closed: new src/cad-app/shell/cadAnalysisExportInput.ts builds CadAnalysisExportInput from the session cache (CURRENT regions per metric + real-status non-current layers + legend rows with explicit units), threaded ExportCenterPanel→buildExportCenterPreview (production SVG/PDF/DXF previously dropped all analysis geometry silently). Real bugs found+fixed: (1) elevation/depth double-counted constant-valued polygons sitting exactly on a shared band edge (closed clip vs [lower,upper) contract) — half-open skip in both band loops, engine+session pins; (2) analysis rows/display/export memos missed surfaceMeshSessions, so post-rebuild status stuck at SOURCE_NOT_CURRENT instead of NEEDS_RECALC. Pins added: tests/cad_analysis_export_input_18u.test.ts 5/5, tests/cad_analysis_reconciliation_18u.test.ts 7/7 (session Σ vs independent mesh measures, depth Σ vs computeVolumeQuantities, bit-identical 18I oracle, on-edge single ownership, LEGEND_MOVE revision-neutral). Browser: tests-browser/cad-surface-analysis-18u.spec.ts 4/4 zero page errors (flows A–M + 3 viewports, 10 shots under docs/evidence/phase18u/ local-only). Validation: lint 0err/2 pre-existing warns, typecheck clean, test:agent 5552 pass + 4 pre-existing fails (3 study-desktop real-data + cad_app_controller Send-to-CAD gate, worktree-proven on unmodified HEAD), test:wasm 74/74, parity 25/25, build clean, portable-paths 4412/0, browser 18G/H/I/S/T + 18U green. Restrictions: worker supersession pinned deterministically in the service suite (browser is smoke only); legend MOVE is command-level (legends are pointer-events-none, no canvas drag); post-reopen analyses read SOURCE_NOT_CURRENT (not UNBUILT like volumes — no result AND unbuilt source, source precedence is fail-closed) then UNBUILT after rebuild.
Phase 18U REVIEWER 2026-09-25 — GO-CAD-SURFACE-ANALYSIS-MAPS (reviewer-18u 15/15, 0 fix rounds; validation lint 0err/typecheck clean/test:agent 735-file pass w/ 3 pre-existing study-desktop fails/18U 90 tests + wasm 74/74 + build + portable 4416/0; parity not re-run, 18I diff empty). PR OPENED, NOT merged. Do NOT merge without explicit instruction.
- [ ] Phase 18V bulk and region surface editing with local move validation (branch feat/cad-surface-bulk-region-editing, baseline origin/main a34ec79b = PR #112 merge, fetch-verified no advance). STATUS: BASELINE STARTED 2026-09-25 — branch created. Scouts dispatched (edits-core + UI/persist + test-inventory). NEXT: baseline validation battery, architecture doc docs/evidence/phase18v-bulk-surface-edit-architecture.md, engine bulk edits + spatial index + UI + persistence + oracles + browser QA + reviewer gate, PR (do NOT merge). BASELINE DONE 2026-09-25 — origin/main fetch-verified at a34ec79b (no advance); branch cut. lint 0err/2 pre-existing warns, typecheck clean, portable 4416/0; focused 18S 93/93 + 18U/model 100/100 + civil/LandXML/xform 155/155; test:agent 5554 pass + 3 pre-existing study-desktop real-data fails (calibration, calibration_v5, preflight — no surface linkage); test:wasm 74/74; parity 25/25; build 10.34s clean; browser 18s/18t/18u 12/12. ARCH DOC DONE 2026-09-25 — docs/evidence/phase18v-bulk-surface-edit-architecture.md (single-point model, identity, picking, O(moves×tris) root cause, selection ownership, bulk representation, simultaneous move, spatial-index design, worker/WNCAD/downstream). ENGINE WORKERS DISPATCHED 2026-09-25 (worker-bulk-kinds + worker-move-index, parallel, file-ownership split). BULK-KINDS DONE — cadTypes 3 kinds + cadSurfaceEditBulk.ts (~230 lines, canonicalize/Z/raise/simultaneous-move brute-force) + replay/clone/describe/transform(vector)/transactions seams + cadSurfaceEditSummaries labels + tests/cad_surface_bulk_edits_18v 10/10; lint/typecheck clean, 18s+18t 51/51. DEBT: bulk file mirrors 3 private helpers from cadSurfaceEditPointModify.ts (sibling owns that file) — reconcile to shared exports after index lands. NOTE: worker reports test:agent 4th fail cad_app_adjustment_isolation (18T-noted load-flake, passes solo) — verify at validation. INDEX DONE — cadEditEdgeSpatialIndex.ts (329 lines, uniform hash grid, fail-closed superset, lazy, refcount-aware hooks) + applyMovePoint indexed (+22/-8, predicate/gates identical) + tests/cad_surface_move_index_parity_18v 13/13 (2000-proposal corpus 100% parity, large-coords, lifecycle pins); 60×60 grid 69.4 candidates/move (152×), 100×100 69.8/move (424×); wasm 74/74, build clean. OPEN: bulk kernel must adopt invalidate/updateVertexEdges seam (mixed stacks) + bulk validation via index (§30) + de-mirror 3 private helpers — RESUMED worker-bulk-kinds. INTEGRATION DONE — bulk via index (old-index + local proposed set, §30), §28 union update + invalidate fallback, 3 predicates exported from canonical home, +2 tests (12/12 bulk, 76/76 across 4 suites); 21,299→92 predicate tests (~231×). UI SLICE DISPATCHED (worker-ui-selection: selection state, SURFSELECTPOINTS, 3 bulk commands, manager/ribbon). UI DONE 2026-09-25 — session SurfaceVertexSelection {surfaceId,revision,refs[]} + SURFSELECTPOINTS window/polygon/All/Clear/Invert (inclusive-rect; exact winding point-in-polygon, boundary included, self-intersection blocks; synthetic excluded + reported) + SURFSETELEVMULTI/SURFRAISELOWERSELECTED/SURFMOVEPOINTS (one SURFACE_ADD_EDIT each, stale ⇒ reselect, zero refs ⇒ no undo entry; move ghost preview validates proposed coords read-only via replaySurfaceEdits so blocking class is authoritative/all-or-nothing); new hooks ≤400 lines (surfaceBulkSelectionUtils 210, surfaceBulkEditSessionUtils 213, useSurveyCadSurfaceBulkSelection ~240, useSurveyCadSurfaceBulkEditSessions ~235); manager POINT SELECTION section + expandable edit-row detail + bulk-ref dependency projection (shell-only); ribbon Surface Select Points/Set Selected Z/Raise-Lower Selected/Move Selected; tests/cad_surface_bulk_selection_18v 11/11 + bulk edits 12/12; lint 0err/2 pre-existing warns, typecheck clean, build clean. DEFERRED: bulk delete/add/smooth/grade/paste (§§50-51/114-117). DEP-CLEANUP DONE — engine cadSurfaceEditDeps.consumedEditKeysOf now covers bulk vertices[] refs (pinned by 3 tests); shell workaround cadSurfaceEditDependencyView.ts deleted and CadSurfaceEditTable routes through the engine inspector directly. Reverted stray 18T PNG overwrites (browser side-effect, evidence must stay byte-identical). QA DISPATCHED 2026-09-25: resumed worker-bulk-kinds for engine consumedEditKeysOf bulk fix; worker-qa-pins (downstream/persist/transform oracles + 10k/50k/100k perf matrix, evidence-tier); worker-qa-browser (§107 A-N spec + 3-resolution visual QA + §109 UI gate). DEPS FIX DONE — consumedEditKeysOf covers bulk vertices[] (+4 lines) + 3 agreement tests (bulk suite 15/15); resumed worker-ui-selection to delete the now-duplicate shell workaround. WORKAROUND DELETED — CadSurfaceEditTable calls engine inspectSurfaceEditDependencies directly; bulk tests assert engine report; 62/62 across 5 suites. QA PINS DONE — downstream 5/5 (contours/profile/section/volume/slope/18U analysis), persist/transform 6/6 (native+imported round-trip, WNCAD ~80 B/ref, §105 commutativity, Grid/Ground), evidence perf 3/3 in 72.9s: 100k single100 replay 817ms (vs ~8s pre-18V for 100 moves), bulkMove1000 replay 3.3s, candidate reduction up to 29,972× (§98 proven), index mem 88 MB at 100k; 18V suites 50/50, 18S/T/U+manifest 247/247. MICRO-FIX RESUMED (bulk-kinds): O(selected×points) coincidence guard → single-pass exact Set sweep. COINCIDENCE DONE — ~30% off bulk-move replay, bit-identical, bulk suite 16/16. BROWSER QA DONE — tests-browser/cad-surface-bulk-edits-18v.spec.ts (7 tests A-N + visual sweep) + 18s/18t/18u = 19/19 zero page errors; 32 PNGs local-only docs/evidence/phase18v/; UI gate 8/9 (viewport-dominance FAIL = known docking, same class as 18T 7/8, no redesign per mission). 18T PNGs verified untouched. FIXES RESUMED (ui-selection): overlay pointer-events-none + bulk highlight key namespacing (showTriangles default + docking explicitly out of scope). FIXES DONE + spec hardened to fail on any console error. FULL VALIDATION 2026-09-25 — lint 0err/2 pre-existing warns, typecheck clean, portable 4416/0, 18V suites 51/51, test:agent 5605 pass + only the 3 pre-existing study-desktop fails (baseline-identical), wasm 74/74, parity 25/25, build 10.44s, browser 18v/18s/18t/18u 19/19 zero page+console errors (18T PNGs re-reverted). REVIEWER DISPATCHED (reviewer-18v, 15 gates). REVIEWER VERDICT: NO-GO — gate 8 (bulk moved-vs-moved brute-force parity unproven: only 3 hand cases, no randomized oracle) + gate 10 (single-move/add coincidence still O(points) per op). 13/15 PASS. FIXES DISPATCHED: resumed worker-bulk-kinds (test-only legacyApplyMovePoints oracle + randomized corpus + old-index-alone-misses discriminating test); worker-point-index (exact lazy point-coincidence map, O(1) lookup, same halt reasons). Then re-validate + re-review gates 8/10. GATE 8 DONE — tests/cad_surface_bulk_bruteforce_oracle_18v.test.ts (487 lines): legacyApplyMovePoints global-scan twin + index-only twin + 151-case corpus (81/70) at 100% verdict+reason agreement + bit-identical coords; honest negative §30 result (~13k proposals, zero constructible old-index-alone-miss cases; local set retained zero-cost). GATE 10 DONE — cadEditPointLocationIndex.ts (114 lines, exact keys, lazy, shared-seam maintenance) serving move+add coincidence: 35.1µs→0.126µs/lookup (~279×), break-even ~287 checks; parity 13→19; agent 5612 + 3 pre-existing, wasm 74/74. RE-REVIEW RESUMED (reviewer-18v, gates 8+10). RE-REVIEW VERDICT 2026-09-25 — GO-CAD-BULK-SURFACE-EDITING (gate 8 PASS: genuine twin, families covered, §30 negative accepted empirical+safe-sided with local set retained; gate 10 PASS: identical BLOCK answers, maintenance complete, no tolerance; 1 non-blocking advisory: duplicate-baseline-XY invariant already documented in file header). CURRENT_BEHAVIOR 18V bullet added. COMMITTED e022d1b9 + pushed; PR #113 OPENED, NOT merged. Tracked tree clean (5 untracked dirs intentionally local-only: phase18c/i/s/u pre-existing + phase18v visual PNGs). FULL VALIDATION: lint 0err/2 pre-existing warns, typecheck clean, portable 4416/0, 18V suites 51+oracle, test:agent 5612 + 3 pre-existing study-desktop fails, wasm 74/74, parity 25/25, build 10.44s, browser 19/19 zero errors. Decision: GO-CAD-BULK-SURFACE-EDITING. Do NOT merge without explicit instruction. FIXES DONE 2026-09-25 — [data-survey-cad-file-status]/[data-survey-cad-dependency-status] now pointer-events-none (canvas picks no longer eaten); committed highlight vs bulk-session highlight keys namespaced (surface-bulk-selected vs surface-bulk-session-selected); browser spec's duplicate-key filter removed so 18V suite asserts zero console errors (7/7 green).
- [ ] Phase 18Y exact TIN surface composition and paste workflow (branch feat/cad-surface-exact-composition, baseline 603bc59a0b867a43b0127714bef85da758a6a7f7 = PR #115 merge). STATUS: BASELINE DONE 2026-09-26 — lint PASS (0 err), typecheck PASS, portable-paths PASS (4468 paths), test:agent 5734 pass + 3 pre-existing study_ai calibration/preflight fails, wasm 74/74, parity 25/25, build PASS. ARCH AUDIT DONE (docs/evidence/phase18y-surface-composition-architecture.md). SEAM POLICY AGREE (reviewer pre-implementation sign-off). IMPLEMENTATION DONE 2026-09-26 (engine + worker/txn + UI/QA parallel batches): engine surfaceCompose.ts + compose/coverage.ts + pslg.ts, provenance kind webnet-compose, worker compose op + service, SURFCOMPOSE/SURFCOMPOSEPASTE transactions, SURFCOMPOSECOPY/SURFPASTE openers, ribbon/manager/toolspace, 45 compose tests green, browser 18Y 10/10 zero errors, wasm 74/74, parity 25/25, lint 0 err, typecheck clean. FINAL REVIEWER: 15/15 PASS, GO-WITH-COMPOSITION-RESTRICTIONS (measured envelope ≤10k, worker path above 25k dry-run limit, 50k/100k extrapolation-only, visual sweep deferred to 18V docking fix). SCOPE: snapshot overlay-wins composition (COMPOSE COPY + PASTE INTO), explicit-tin output, composite provenance, worker-backed, CURRENT/revision gates, fail-closed SEAM_Z_MISMATCH, union domain, void show-through, downstream integration, perf evidence, browser QA, 3-resolution visual QA. Do NOT merge the PR.
Phase 18Y UI + INTEGRATION + QA STATUS 2026-09-26 — UI SLICE DONE, unpushed (sibling engine + worker/txn slices in flight). UI: registry SURFCOMPOSE / SURFCOMPOSECOPY / SURFPASTE (availability gate = two CURRENT surfaces, following SURFBAKE pattern; commands open the manager Compose dialog via consumeSurfaceComposeFocus); ribbon Surface > Definition Tools Compose + Paste (separate from TIN EDIT); manager `Compose Surface…` dialog (Base/Target + Overlay/Source pickers, Create Composite Copy vs Paste Into Target radio, static `Overlay Coverage Wins` policy label, paste confirm naming source/target + coverage-wins + explicit-composite flattening + source-unchanged + undo restore, exact summary Base/Overlay/Overlap/Result areas + vertices + triangles + seam length + max mismatch + Exact/Blocked); snapshot `summarizeExplicitTinSource` webnet-compose branch ("Composite Explicit TIN — N vertices, M faces (Base X + Overlay Y)") + display-only `composed` provenance block (storage stays `explicit-tin`); Toolspace composite Base/Overlay/policy row; shell action `previewSurfaceCompose` = deterministic engine dry-run on session cache meshes (returns diagnostics + payload); commit dispatches payload-carrying SURFCOMPOSE / SURFCOMPOSEPASTE. NOTE: txn slice renamed the keys from the mission's SURFCOMPOSECOPY/SURFPASTE to SURFCOMPOSE (copy) / SURFCOMPOSEPASTE (paste) and made both payload-carrying (worker computes, command persists) — UI adapter cadSurfaceCompose.ts is the single mapping site. OPEN: wire the worker `surfaceComposeService` path for large meshes (UI preview currently sync main-thread, `ponytail:` noted); browser spec + perf doc in flight; do NOT commit; do NOT merge the PR.
Phase 18Y UI/QA FINAL 2026-09-26 — browser spec tests-browser/cad-surface-compose-18y.spec.ts 10/10 zero page/console errors (copy + overlay-wins priority probe, paste identity + undo/redo, seam-mismatch blocked + zero history, void show-through + base-void fill, disjoint union, post-compose Set Elevation, Volume+Analysis dependents stale, save/reopen honest UNBUILT + exact rebuild, PROJECTTRANSFORM XY-once, LandXML export counts, two-CURRENT-surface ribbon/manager gates). Perf evidence docs/evidence/phase18y-surface-composition-perf.md + tests/evidence/phase18y_compose_perf.test.ts (evidence tier): 1k/10k measured, 10k→ ~90-100× for 10× input (≈O(n²)); 50k/100k NOT MEASURED (extrapolated 3-65 min/case) and flagged as the engine retriangulation bottleneck. Validation: lint 0 errors (2 pre-existing warnings), typecheck clean, build clean, test:agent 5779 pass + only the 3 pre-existing study-desktop real-data fails, test:wasm 74/74, parity 25/25, portable-paths 4468/0, browser 18Y 10/10 + 18X 7/7 + 18W 7/7 green. NOTE: 18V functional 7/7 green, but 18V-VISUAL (3-resolution evidence sweep) now fails on a floating properties-panel-header pointer interception over the ribbon — same known docking class the 18V TODO already documented (viewport-dominance, no redesign per mission); flagged for reviewer, not a functional composition regression. WORKER NOTE: stale-worker/supersede/cross-drawing discard is pinned deterministically in tests/surface_compose_service_18y.test.ts (browser timing of that race is inherently flaky, same precedent as 18U). Do NOT commit (orchestrator owns merge order: engine → worker/txn → UI). Do NOT merge the PR.
